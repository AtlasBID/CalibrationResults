Analysis(negative tags,light,JetFitterCOMBNNc,  0.45, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4658,0.0602)
    usys(MC statistics, 4.53%)
    sys(jet energy scale,-6.50%)
    sys(jet energy resolution,-8.95%)
    sys(b-fractions, 0.76%)
    sys(c-fractions, 1.46%)
    sys(track multiplicity, 3.69%)
    sys(long lived particles, 6.22%)
    sys(fake tracks,-0.38%)
    sys(photon conversions, 4.49%)
    sys(trigger bias, 8.22%)
    sys(negative tag epsilon b, 2.24%)
    sys(negative tag epsilon c, 3.91%)
    sys(negative tag run period dependence, 8.46%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.5602,0.0465)
    usys(MC statistics, 3.25%)
    sys(jet energy scale,-1.22%)
    sys(jet energy resolution,-6.26%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 2.27%)
    sys(track multiplicity, 4.13%)
    sys(long lived particles,-1.21%)
    sys(fake tracks,-0.72%)
    sys(photon conversions, 6.77%)
    sys(trigger bias, 8.22%)
    sys(negative tag epsilon b, 2.16%)
    sys(negative tag epsilon c, 4.92%)
    sys(negative tag run period dependence, 8.00%)
    sys(d0 and z0 smearing,-0.52%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3555,0.0268)
    usys(MC statistics, 2.66%)
    sys(jet energy scale,-0.81%)
    sys(jet energy resolution,-2.38%)
    sys(b-fractions, 2.99%)
    sys(c-fractions, 4.81%)
    sys(track multiplicity, 7.46%)
    sys(long lived particles, 1.77%)
    sys(fake tracks,-0.93%)
    sys(photon conversions,13.86%)
    sys(trigger bias,14.41%)
    sys(negative tag epsilon b, 6.68%)
    sys(negative tag epsilon c, 8.06%)
    sys(negative tag run period dependence,-1.18%)
    sys(d0 and z0 smearing,19.40%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3291,0.0201)
    usys(MC statistics, 5.90%)
    sys(jet energy scale, 4.18%)
    sys(jet energy resolution, 3.35%)
    sys(b-fractions, 5.70%)
    sys(c-fractions, 4.99%)
    sys(track multiplicity, 8.37%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-0.27%)
    sys(photon conversions,16.78%)
    sys(trigger bias,11.88%)
    sys(negative tag epsilon b,11.93%)
    sys(negative tag epsilon c, 7.53%)
    sys(negative tag run period dependence,-8.57%)
    sys(d0 and z0 smearing, 0.63%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4985,0.0206)
    usys(MC statistics, 3.33%)
    sys(jet energy scale,-0.35%)
    sys(jet energy resolution, 1.80%)
    sys(b-fractions, 5.98%)
    sys(c-fractions, 4.85%)
    sys(track multiplicity, 8.84%)
    sys(long lived particles,-0.91%)
    sys(fake tracks, 0.98%)
    sys(photon conversions,15.90%)
    sys(trigger bias, 9.33%)
    sys(negative tag epsilon b,12.14%)
    sys(negative tag epsilon c, 7.27%)
    sys(negative tag run period dependence,-17.93%)
    sys(d0 and z0 smearing, 4.86%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2607,0.0131)
    usys(MC statistics, 3.71%)
    sys(jet energy scale, 1.95%)
    sys(jet energy resolution, 2.82%)
    sys(b-fractions, 5.22%)
    sys(c-fractions, 4.66%)
    sys(track multiplicity,14.30%)
    sys(long lived particles,-1.18%)
    sys(fake tracks, 2.90%)
    sys(photon conversions,14.43%)
    sys(trigger bias, 7.79%)
    sys(negative tag epsilon b,12.25%)
    sys(negative tag epsilon c, 7.72%)
    sys(negative tag run period dependence, 2.26%)
    sys(d0 and z0 smearing, 5.23%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2875,0.0490)
    usys(MC statistics, 4.48%)
    sys(jet energy scale,-5.13%)
    sys(jet energy resolution, 4.23%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.32%)
    sys(track multiplicity,-0.41%)
    sys(long lived particles, 3.98%)
    sys(fake tracks,-0.58%)
    sys(photon conversions, 2.52%)
    sys(trigger bias, 7.39%)
    sys(negative tag epsilon b, 0.86%)
    sys(negative tag epsilon c, 2.51%)
    sys(negative tag run period dependence,14.93%)
    sys(d0 and z0 smearing,-1.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4768,0.0397)
    usys(MC statistics, 2.55%)
    sys(jet energy scale,-2.80%)
    sys(jet energy resolution,-0.91%)
    sys(b-fractions, 0.22%)
    sys(c-fractions, 0.81%)
    sys(track multiplicity, 4.43%)
    sys(long lived particles,-0.82%)
    sys(fake tracks,-0.58%)
    sys(photon conversions, 3.59%)
    sys(trigger bias, 7.39%)
    sys(negative tag epsilon b, 0.89%)
    sys(negative tag epsilon c, 3.16%)
    sys(negative tag run period dependence, 7.15%)
    sys(d0 and z0 smearing,-9.04%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2514,0.0296)
    usys(MC statistics, 3.30%)
    sys(jet energy scale,-2.25%)
    sys(jet energy resolution, 0.37%)
    sys(b-fractions, 1.28%)
    sys(c-fractions, 2.68%)
    sys(track multiplicity, 9.41%)
    sys(long lived particles, 1.09%)
    sys(fake tracks,-0.78%)
    sys(photon conversions, 7.06%)
    sys(trigger bias,17.73%)
    sys(negative tag epsilon b, 3.17%)
    sys(negative tag epsilon c, 5.52%)
    sys(negative tag run period dependence,-2.95%)
    sys(d0 and z0 smearing,-7.30%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.6030,0.0435)
    usys(MC statistics, 4.94%)
    sys(jet energy scale,-4.92%)
    sys(jet energy resolution,-0.53%)
    sys(b-fractions, 3.49%)
    sys(c-fractions, 2.81%)
    sys(track multiplicity, 6.99%)
    sys(long lived particles, 1.31%)
    sys(fake tracks,-0.21%)
    sys(photon conversions, 9.70%)
    sys(trigger bias,17.73%)
    sys(negative tag epsilon b, 7.49%)
    sys(negative tag epsilon c, 5.16%)
    sys(negative tag run period dependence, 6.13%)
    sys(d0 and z0 smearing,-7.17%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6380,0.0537)
    usys(MC statistics, 7.47%)
    sys(jet energy scale, 2.14%)
    sys(jet energy resolution,-4.66%)
    sys(b-fractions, 3.49%)
    sys(c-fractions, 3.97%)
    sys(track multiplicity, 9.66%)
    sys(long lived particles,-0.91%)
    sys(fake tracks, 0.57%)
    sys(photon conversions, 8.85%)
    sys(trigger bias,16.86%)
    sys(negative tag epsilon b, 7.32%)
    sys(negative tag epsilon c, 6.53%)
    sys(negative tag run period dependence,-16.31%)
    sys(d0 and z0 smearing,-26.99%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5610,0.0409)
    usys(MC statistics, 8.89%)
    sys(jet energy scale,-3.22%)
    sys(jet energy resolution,-7.26%)
    sys(b-fractions, 3.39%)
    sys(c-fractions, 2.48%)
    sys(track multiplicity,13.07%)
    sys(long lived particles,-0.83%)
    sys(fake tracks, 1.52%)
    sys(photon conversions, 8.31%)
    sys(trigger bias,16.86%)
    sys(negative tag epsilon b, 7.99%)
    sys(negative tag epsilon c, 4.97%)
    sys(negative tag run period dependence,-16.31%)
    sys(d0 and z0 smearing,-27.67%)
  }
}
