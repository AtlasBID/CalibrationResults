Analysis(negative tags,light,MV1, 0.980, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3575,0.1979)
    usys(MC statistics,25.98%)
    sys(jet energy scale,-8.20%)
    sys(jet energy resolution,10.69%)
    sys(b-fractions, 4.23%)
    sys(c-fractions, 1.72%)
    sys(track multiplicity, 1.52%)
    sys(long lived particles,25.34%)
    sys(fake tracks,-19.54%)
    sys(photon conversions,24.03%)
    sys(trigger bias,-1.46%)
    sys(negative tag epsilon b,10.31%)
    sys(negative tag epsilon c, 3.45%)
    sys(negative tag run period dependence,-14.28%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.5103,0.0995)
    usys(MC statistics,14.59%)
    sys(jet energy scale,-8.20%)
    sys(jet energy resolution, 4.07%)
    sys(b-fractions, 0.83%)
    sys(c-fractions, 2.94%)
    sys(track multiplicity, 1.33%)
    sys(long lived particles,25.34%)
    sys(fake tracks,-3.68%)
    sys(photon conversions,24.03%)
    sys(trigger bias,-1.46%)
    sys(negative tag epsilon b, 2.06%)
    sys(negative tag epsilon c, 5.64%)
    sys(negative tag run period dependence, 8.00%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3750,0.0429)
    usys(MC statistics, 4.62%)
    sys(jet energy scale, 2.62%)
    sys(jet energy resolution, 0.98%)
    sys(b-fractions, 2.93%)
    sys(c-fractions, 4.35%)
    sys(track multiplicity, 4.79%)
    sys(long lived particles, 4.67%)
    sys(fake tracks, 4.36%)
    sys(photon conversions, 9.33%)
    sys(trigger bias,16.63%)
    sys(negative tag epsilon b, 6.41%)
    sys(negative tag epsilon c, 7.47%)
    sys(negative tag run period dependence,-6.38%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4516,0.0297)
    usys(MC statistics, 3.70%)
    sys(jet energy scale, 1.31%)
    sys(jet energy resolution,-8.12%)
    sys(b-fractions, 4.37%)
    sys(c-fractions, 6.44%)
    sys(track multiplicity,10.38%)
    sys(long lived particles, 4.24%)
    sys(fake tracks, 6.51%)
    sys(photon conversions, 8.40%)
    sys(trigger bias,16.63%)
    sys(negative tag epsilon b, 9.48%)
    sys(negative tag epsilon c, 9.84%)
    sys(negative tag run period dependence,-2.27%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4922,0.0301)
    usys(MC statistics, 4.28%)
    sys(jet energy scale,-1.13%)
    sys(jet energy resolution,-4.20%)
    sys(b-fractions, 4.45%)
    sys(c-fractions, 6.68%)
    sys(track multiplicity,14.34%)
    sys(long lived particles, 1.25%)
    sys(fake tracks, 0.99%)
    sys(photon conversions, 4.14%)
    sys(trigger bias, 5.64%)
    sys(negative tag epsilon b, 9.40%)
    sys(negative tag epsilon c,10.05%)
    sys(negative tag run period dependence,-6.24%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3680,0.0229)
    usys(MC statistics, 4.23%)
    sys(jet energy scale,-0.54%)
    sys(jet energy resolution, 0.47%)
    sys(b-fractions, 4.19%)
    sys(c-fractions, 5.82%)
    sys(track multiplicity,18.68%)
    sys(long lived particles,-3.96%)
    sys(fake tracks, 5.04%)
    sys(photon conversions, 3.31%)
    sys(trigger bias,15.74%)
    sys(negative tag epsilon b,10.11%)
    sys(negative tag epsilon c, 9.50%)
    sys(negative tag run period dependence, 5.31%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(0.8574,0.1827)
    usys(MC statistics,25.00%)
    sys(jet energy scale,18.57%)
    sys(jet energy resolution,19.39%)
    sys(b-fractions, 0.38%)
    sys(c-fractions,-0.58%)
    sys(track multiplicity,-0.13%)
    sys(long lived particles, 6.13%)
    sys(fake tracks,11.97%)
    sys(photon conversions,21.66%)
    sys(trigger bias, 8.19%)
    sys(negative tag epsilon b, 1.29%)
    sys(negative tag epsilon c, 1.37%)
    sys(negative tag run period dependence,27.99%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3702,0.1183)
    usys(MC statistics,15.90%)
    sys(jet energy scale, 6.89%)
    sys(jet energy resolution,19.39%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 0.88%)
    sys(track multiplicity, 1.74%)
    sys(long lived particles, 6.13%)
    sys(fake tracks,11.97%)
    sys(photon conversions,21.66%)
    sys(trigger bias, 8.19%)
    sys(negative tag epsilon b, 0.91%)
    sys(negative tag epsilon c, 3.19%)
    sys(negative tag run period dependence,27.99%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.4301,0.0694)
    usys(MC statistics, 7.76%)
    sys(jet energy scale,-0.70%)
    sys(jet energy resolution, 0.11%)
    sys(b-fractions, 1.24%)
    sys(c-fractions, 2.65%)
    sys(track multiplicity, 4.87%)
    sys(long lived particles, 7.98%)
    sys(fake tracks, 2.22%)
    sys(photon conversions, 1.78%)
    sys(trigger bias,14.13%)
    sys(negative tag epsilon b, 3.09%)
    sys(negative tag epsilon c, 5.45%)
    sys(negative tag run period dependence,17.43%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5951,0.0648)
    usys(MC statistics, 8.34%)
    sys(jet energy scale, 2.27%)
    sys(jet energy resolution, 3.77%)
    sys(b-fractions, 2.51%)
    sys(c-fractions, 3.11%)
    sys(track multiplicity, 8.54%)
    sys(long lived particles, 7.27%)
    sys(fake tracks,-0.05%)
    sys(photon conversions,-0.53%)
    sys(trigger bias,12.48%)
    sys(negative tag epsilon b, 5.74%)
    sys(negative tag epsilon c, 5.76%)
    sys(negative tag run period dependence,-1.16%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.7478,0.0871)
    usys(MC statistics,10.32%)
    sys(jet energy scale, 0.77%)
    sys(jet energy resolution,-4.03%)
    sys(b-fractions, 2.41%)
    sys(c-fractions, 3.81%)
    sys(track multiplicity,15.04%)
    sys(long lived particles,-0.11%)
    sys(fake tracks,-1.24%)
    sys(photon conversions,-0.94%)
    sys(trigger bias,16.55%)
    sys(negative tag epsilon b, 5.16%)
    sys(negative tag epsilon c, 6.64%)
    sys(negative tag run period dependence,-11.32%)
    sys(d0 and z0 smearing,-21.88%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.6497,0.0794)
    usys(MC statistics,14.38%)
    sys(jet energy scale,-6.07%)
    sys(jet energy resolution,-1.66%)
    sys(b-fractions, 1.47%)
    sys(c-fractions, 2.55%)
    sys(track multiplicity,10.28%)
    sys(long lived particles, 4.33%)
    sys(fake tracks, 2.34%)
    sys(photon conversions, 0.30%)
    sys(trigger bias,16.55%)
    sys(negative tag epsilon b, 3.86%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence,-19.27%)
    sys(d0 and z0 smearing,-21.88%)
  }
}
