Analysis(negative tags,light,JetFitterCOMBNN, -0.95, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.5041,0.0591)
    usys(MC statistics, 8.66%)
    sys(jet energy scale,-16.60%)
    sys(jet energy resolution,-17.99%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 0.71%)
    sys(track multiplicity,-0.87%)
    sys(long lived particles,10.44%)
    sys(fake tracks, 3.93%)
    sys(photon conversions,15.56%)
    sys(trigger bias,20.54%)
    sys(negative tag epsilon b, 1.68%)
    sys(negative tag epsilon c, 2.99%)
    sys(negative tag run period dependence,-0.86%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.6937,0.0430)
    usys(MC statistics, 5.12%)
    sys(jet energy scale,-3.42%)
    sys(jet energy resolution,-17.99%)
    sys(b-fractions, 0.74%)
    sys(c-fractions, 1.29%)
    sys(track multiplicity, 1.95%)
    sys(long lived particles,10.44%)
    sys(fake tracks,-1.14%)
    sys(photon conversions,-2.45%)
    sys(trigger bias,20.54%)
    sys(negative tag epsilon b, 1.86%)
    sys(negative tag epsilon c, 3.71%)
    sys(negative tag run period dependence,-2.91%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3506,0.0185)
    usys(MC statistics, 2.25%)
    sys(jet energy scale,-0.35%)
    sys(jet energy resolution, 0.11%)
    sys(b-fractions, 1.86%)
    sys(c-fractions, 4.31%)
    sys(track multiplicity, 3.09%)
    sys(long lived particles, 9.07%)
    sys(fake tracks, 3.80%)
    sys(photon conversions,10.35%)
    sys(trigger bias, 6.51%)
    sys(negative tag epsilon b, 4.27%)
    sys(negative tag epsilon c, 7.66%)
    sys(negative tag run period dependence,-7.18%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3908,0.0143)
    usys(MC statistics, 2.80%)
    sys(jet energy scale, 2.01%)
    sys(jet energy resolution,-4.06%)
    sys(b-fractions, 3.32%)
    sys(c-fractions, 5.26%)
    sys(track multiplicity, 9.73%)
    sys(long lived particles, 3.06%)
    sys(fake tracks, 2.66%)
    sys(photon conversions, 4.22%)
    sys(trigger bias,14.58%)
    sys(negative tag epsilon b, 7.49%)
    sys(negative tag epsilon c, 8.70%)
    sys(negative tag run period dependence,-2.13%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3962,0.0156)
    usys(MC statistics, 1.98%)
    sys(jet energy scale, 0.08%)
    sys(jet energy resolution,-1.06%)
    sys(b-fractions, 3.43%)
    sys(c-fractions, 5.50%)
    sys(track multiplicity,14.40%)
    sys(long lived particles,-0.69%)
    sys(fake tracks, 5.06%)
    sys(photon conversions, 0.72%)
    sys(trigger bias, 3.78%)
    sys(negative tag epsilon b, 7.52%)
    sys(negative tag epsilon c, 9.02%)
    sys(negative tag run period dependence,-9.54%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3853,0.0150)
    usys(MC statistics, 2.29%)
    sys(jet energy scale,-0.56%)
    sys(jet energy resolution,-1.60%)
    sys(b-fractions, 2.87%)
    sys(c-fractions, 5.62%)
    sys(track multiplicity,16.92%)
    sys(long lived particles,-2.03%)
    sys(fake tracks, 4.91%)
    sys(photon conversions, 1.21%)
    sys(trigger bias,12.77%)
    sys(negative tag epsilon b, 7.40%)
    sys(negative tag epsilon c, 9.93%)
    sys(negative tag run period dependence, 2.33%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3782,0.0637)
    usys(MC statistics, 9.65%)
    sys(jet energy scale,-11.13%)
    sys(jet energy resolution, 1.47%)
    sys(b-fractions,-0.06%)
    sys(c-fractions,-0.90%)
    sys(track multiplicity, 1.43%)
    sys(long lived particles, 2.40%)
    sys(fake tracks,11.67%)
    sys(photon conversions, 0.17%)
    sys(trigger bias,-0.46%)
    sys(negative tag epsilon b, 0.33%)
    sys(negative tag epsilon c, 1.06%)
    sys(negative tag run period dependence,-2.47%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4730,0.0459)
    usys(MC statistics, 7.11%)
    sys(jet energy scale,-9.66%)
    sys(jet energy resolution,-7.04%)
    sys(b-fractions, 0.13%)
    sys(c-fractions, 0.80%)
    sys(track multiplicity, 0.91%)
    sys(long lived particles, 2.58%)
    sys(fake tracks, 0.66%)
    sys(photon conversions,-0.48%)
    sys(trigger bias,-0.46%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 3.09%)
    sys(negative tag run period dependence, 4.23%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3555,0.0275)
    usys(MC statistics, 3.13%)
    sys(jet energy scale,-0.91%)
    sys(jet energy resolution,-1.71%)
    sys(b-fractions, 1.03%)
    sys(c-fractions, 2.39%)
    sys(track multiplicity, 3.73%)
    sys(long lived particles,-0.57%)
    sys(fake tracks, 1.61%)
    sys(photon conversions, 2.33%)
    sys(trigger bias, 2.87%)
    sys(negative tag epsilon b, 2.66%)
    sys(negative tag epsilon c, 5.17%)
    sys(negative tag run period dependence, 3.71%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4351,0.0268)
    usys(MC statistics, 5.00%)
    sys(jet energy scale, 3.24%)
    sys(jet energy resolution,-8.68%)
    sys(b-fractions, 1.85%)
    sys(c-fractions, 3.19%)
    sys(track multiplicity, 8.36%)
    sys(long lived particles,-1.24%)
    sys(fake tracks, 0.30%)
    sys(photon conversions, 2.28%)
    sys(trigger bias,10.48%)
    sys(negative tag epsilon b, 4.24%)
    sys(negative tag epsilon c, 6.09%)
    sys(negative tag run period dependence,-2.65%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4362,0.0366)
    usys(MC statistics, 4.24%)
    sys(jet energy scale,-1.24%)
    sys(jet energy resolution, 2.99%)
    sys(b-fractions, 2.57%)
    sys(c-fractions, 4.60%)
    sys(track multiplicity,15.55%)
    sys(long lived particles,-0.22%)
    sys(fake tracks, 0.61%)
    sys(photon conversions, 0.69%)
    sys(trigger bias,11.02%)
    sys(negative tag epsilon b, 5.68%)
    sys(negative tag epsilon c, 7.82%)
    sys(negative tag run period dependence,-12.74%)
    sys(d0 and z0 smearing,-3.14%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3618,0.0381)
    usys(MC statistics, 5.73%)
    sys(jet energy scale,-1.35%)
    sys(jet energy resolution,-5.28%)
    sys(b-fractions, 1.61%)
    sys(c-fractions, 3.32%)
    sys(track multiplicity,15.61%)
    sys(long lived particles,-2.63%)
    sys(fake tracks, 1.44%)
    sys(photon conversions, 0.92%)
    sys(trigger bias,11.02%)
    sys(negative tag epsilon b, 4.29%)
    sys(negative tag epsilon c, 6.72%)
    sys(negative tag run period dependence,-0.23%)
    sys(d0 and z0 smearing,-3.14%)
  }
}
