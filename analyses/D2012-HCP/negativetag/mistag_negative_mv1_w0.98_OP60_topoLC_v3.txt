Analysis(negative tags,light,MV1, 0.980, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4252,0.1554)
    usys(MC statistics,11.41%)
    sys(jet energy scale, 3.76%)
    sys(jet energy resolution,-3.65%)
    sys(b-fractions, 0.87%)
    sys(c-fractions, 0.52%)
    sys(track multiplicity, 1.52%)
    sys(long lived particles, 4.22%)
    sys(fake tracks,-0.36%)
    sys(photon conversions, 7.70%)
    sys(trigger bias, 6.37%)
    sys(negative tag epsilon b, 2.50%)
    sys(negative tag epsilon c, 2.73%)
    sys(negative tag run period dependence,16.31%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2804,0.0645)
    usys(MC statistics, 6.90%)
    sys(jet energy scale, 5.87%)
    sys(jet energy resolution,11.91%)
    sys(b-fractions, 0.56%)
    sys(c-fractions, 2.49%)
    sys(track multiplicity, 1.28%)
    sys(long lived particles,-1.29%)
    sys(fake tracks,-0.77%)
    sys(photon conversions, 7.04%)
    sys(trigger bias, 6.37%)
    sys(negative tag epsilon b, 1.55%)
    sys(negative tag epsilon c, 5.25%)
    sys(negative tag run period dependence, 6.04%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3191,0.0302)
    usys(MC statistics, 3.14%)
    sys(jet energy scale, 1.55%)
    sys(jet energy resolution,-0.26%)
    sys(b-fractions, 2.18%)
    sys(c-fractions, 5.05%)
    sys(track multiplicity, 3.71%)
    sys(long lived particles, 1.69%)
    sys(fake tracks,-1.05%)
    sys(photon conversions,14.21%)
    sys(trigger bias,11.17%)
    sys(negative tag epsilon b, 5.02%)
    sys(negative tag epsilon c, 8.61%)
    sys(negative tag run period dependence,-2.71%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3933,0.0190)
    usys(MC statistics, 2.96%)
    sys(jet energy scale,-2.35%)
    sys(jet energy resolution, 1.79%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 5.85%)
    sys(track multiplicity, 5.75%)
    sys(long lived particles, 2.42%)
    sys(fake tracks,-0.34%)
    sys(photon conversions,18.14%)
    sys(trigger bias, 7.56%)
    sys(negative tag epsilon b, 7.96%)
    sys(negative tag epsilon c, 9.23%)
    sys(negative tag run period dependence,-5.94%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4598,0.0176)
    usys(MC statistics, 3.10%)
    sys(jet energy scale,-0.41%)
    sys(jet energy resolution,-4.91%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 4.82%)
    sys(track multiplicity, 8.39%)
    sys(long lived particles,-1.69%)
    sys(fake tracks, 1.16%)
    sys(photon conversions,17.50%)
    sys(trigger bias, 7.54%)
    sys(negative tag epsilon b, 7.76%)
    sys(negative tag epsilon c, 7.98%)
    sys(negative tag run period dependence,-11.11%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2178,0.0109)
    usys(MC statistics, 3.31%)
    sys(jet energy scale, 1.70%)
    sys(jet energy resolution, 0.78%)
    sys(b-fractions, 2.90%)
    sys(c-fractions, 4.60%)
    sys(track multiplicity,11.53%)
    sys(long lived particles,-1.53%)
    sys(fake tracks, 3.44%)
    sys(photon conversions,15.47%)
    sys(trigger bias,15.98%)
    sys(negative tag epsilon b, 7.06%)
    sys(negative tag epsilon c, 8.53%)
    sys(negative tag run period dependence, 8.44%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2378,0.1699)
    usys(MC statistics,13.94%)
    sys(jet energy scale,-4.25%)
    sys(jet energy resolution, 3.25%)
    sys(b-fractions,-0.06%)
    sys(c-fractions, 2.20%)
    sys(track multiplicity, 0.08%)
    sys(long lived particles, 4.88%)
    sys(fake tracks,-0.51%)
    sys(photon conversions, 5.55%)
    sys(trigger bias, 1.34%)
    sys(negative tag epsilon b, 0.30%)
    sys(negative tag epsilon c, 4.82%)
    sys(negative tag run period dependence,-2.48%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.6802,0.1031)
    usys(MC statistics, 6.24%)
    sys(jet energy scale, 4.42%)
    sys(jet energy resolution, 3.89%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 2.16%)
    sys(track multiplicity, 0.31%)
    sys(long lived particles,-1.08%)
    sys(fake tracks,-0.78%)
    sys(photon conversions, 5.72%)
    sys(trigger bias, 1.34%)
    sys(negative tag epsilon b, 1.13%)
    sys(negative tag epsilon c, 4.73%)
    sys(negative tag run period dependence,12.85%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.4739,0.0483)
    usys(MC statistics, 4.45%)
    sys(jet energy scale, 1.97%)
    sys(jet energy resolution, 5.33%)
    sys(b-fractions, 1.37%)
    sys(c-fractions, 1.79%)
    sys(track multiplicity, 3.45%)
    sys(long lived particles, 1.44%)
    sys(fake tracks,-0.86%)
    sys(photon conversions, 9.40%)
    sys(trigger bias,17.49%)
    sys(negative tag epsilon b, 3.38%)
    sys(negative tag epsilon c, 4.36%)
    sys(negative tag run period dependence,11.41%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5311,0.0358)
    usys(MC statistics, 4.42%)
    sys(jet energy scale,-1.37%)
    sys(jet energy resolution,-0.55%)
    sys(b-fractions, 1.59%)
    sys(c-fractions, 2.72%)
    sys(track multiplicity, 4.64%)
    sys(long lived particles, 1.84%)
    sys(fake tracks,-0.23%)
    sys(photon conversions,10.70%)
    sys(trigger bias,17.49%)
    sys(negative tag epsilon b, 3.64%)
    sys(negative tag epsilon c, 5.51%)
    sys(negative tag run period dependence,-8.26%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4451,0.0358)
    usys(MC statistics, 5.94%)
    sys(jet energy scale, 1.41%)
    sys(jet energy resolution, 1.42%)
    sys(b-fractions, 1.33%)
    sys(c-fractions, 1.64%)
    sys(track multiplicity, 6.84%)
    sys(long lived particles,-1.30%)
    sys(fake tracks, 0.71%)
    sys(photon conversions,10.47%)
    sys(trigger bias, 9.78%)
    sys(negative tag epsilon b, 3.05%)
    sys(negative tag epsilon c, 4.13%)
    sys(negative tag run period dependence,-5.80%)
    sys(d0 and z0 smearing,-21.88%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5300,0.0306)
    usys(MC statistics, 6.66%)
    sys(jet energy scale,-7.48%)
    sys(jet energy resolution,-10.52%)
    sys(b-fractions, 1.18%)
    sys(c-fractions, 1.86%)
    sys(track multiplicity, 9.82%)
    sys(long lived particles,-1.25%)
    sys(fake tracks, 1.87%)
    sys(photon conversions,10.07%)
    sys(trigger bias, 9.78%)
    sys(negative tag epsilon b, 3.03%)
    sys(negative tag epsilon c, 4.71%)
    sys(negative tag run period dependence,-0.38%)
    sys(d0 and z0 smearing,-21.88%)
  }
}
