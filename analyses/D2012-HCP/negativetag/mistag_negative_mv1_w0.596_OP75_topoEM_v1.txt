Analysis(negative tags,light,MV1, 0.596, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4178,0.0485)
    usys(MC statistics, 7.10%)
    sys(jet energy scale,-8.74%)
    sys(jet energy resolution, 1.92%)
    sys(b-fractions, 0.39%)
    sys(c-fractions, 1.11%)
    sys(track multiplicity,-0.58%)
    sys(long lived particles, 8.55%)
    sys(fake tracks, 0.24%)
    sys(photon conversions, 1.09%)
    sys(trigger bias,10.56%)
    sys(negative tag epsilon b, 1.40%)
    sys(negative tag epsilon c, 3.51%)
    sys(negative tag run period dependence,-3.42%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.4673,0.0286)
    usys(MC statistics, 4.91%)
    sys(jet energy scale, 3.10%)
    sys(jet energy resolution,-8.74%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 1.70%)
    sys(track multiplicity, 1.46%)
    sys(long lived particles,10.94%)
    sys(fake tracks, 2.58%)
    sys(photon conversions, 8.05%)
    sys(trigger bias,10.56%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 4.22%)
    sys(negative tag run period dependence, 3.82%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2645,0.0125)
    usys(MC statistics, 1.55%)
    sys(jet energy scale, 0.60%)
    sys(jet energy resolution,-1.26%)
    sys(b-fractions, 1.32%)
    sys(c-fractions, 3.83%)
    sys(track multiplicity, 2.69%)
    sys(long lived particles, 5.94%)
    sys(fake tracks, 3.98%)
    sys(photon conversions, 8.26%)
    sys(trigger bias, 9.68%)
    sys(negative tag epsilon b, 3.21%)
    sys(negative tag epsilon c, 7.23%)
    sys(negative tag run period dependence,-2.95%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2923,0.0090)
    usys(MC statistics, 1.79%)
    sys(jet energy scale, 2.08%)
    sys(jet energy resolution,-0.28%)
    sys(b-fractions, 2.15%)
    sys(c-fractions, 5.14%)
    sys(track multiplicity, 6.23%)
    sys(long lived particles, 3.25%)
    sys(fake tracks, 4.66%)
    sys(photon conversions, 4.59%)
    sys(trigger bias,11.04%)
    sys(negative tag epsilon b, 5.07%)
    sys(negative tag epsilon c, 8.94%)
    sys(negative tag run period dependence, 0.92%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3082,0.0098)
    usys(MC statistics, 1.27%)
    sys(jet energy scale,-0.03%)
    sys(jet energy resolution, 0.56%)
    sys(b-fractions, 2.15%)
    sys(c-fractions, 4.99%)
    sys(track multiplicity,10.34%)
    sys(long lived particles, 0.25%)
    sys(fake tracks, 4.55%)
    sys(photon conversions, 1.24%)
    sys(trigger bias, 4.94%)
    sys(negative tag epsilon b, 4.94%)
    sys(negative tag epsilon c, 8.83%)
    sys(negative tag run period dependence,-8.86%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2849,0.0093)
    usys(MC statistics, 1.56%)
    sys(jet energy scale, 0.44%)
    sys(jet energy resolution,-1.26%)
    sys(b-fractions, 1.79%)
    sys(c-fractions, 4.95%)
    sys(track multiplicity,12.17%)
    sys(long lived particles,-1.29%)
    sys(fake tracks, 5.17%)
    sys(photon conversions, 2.31%)
    sys(trigger bias,15.37%)
    sys(negative tag epsilon b, 4.86%)
    sys(negative tag epsilon c, 9.44%)
    sys(negative tag run period dependence, 0.63%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2766,0.0591)
    usys(MC statistics, 9.44%)
    sys(jet energy scale,-2.08%)
    sys(jet energy resolution, 3.40%)
    sys(b-fractions,-0.08%)
    sys(c-fractions,-0.78%)
    sys(track multiplicity, 1.58%)
    sys(long lived particles, 4.47%)
    sys(fake tracks,10.53%)
    sys(photon conversions,-4.50%)
    sys(trigger bias, 3.39%)
    sys(negative tag epsilon b, 0.29%)
    sys(negative tag epsilon c, 1.22%)
    sys(negative tag run period dependence,-0.34%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4812,0.0376)
    usys(MC statistics, 5.45%)
    sys(jet energy scale,-3.49%)
    sys(jet energy resolution,-8.14%)
    sys(b-fractions, 0.12%)
    sys(c-fractions, 0.67%)
    sys(track multiplicity, 1.61%)
    sys(long lived particles, 4.94%)
    sys(fake tracks,-0.21%)
    sys(photon conversions, 1.62%)
    sys(trigger bias, 3.39%)
    sys(negative tag epsilon b, 0.69%)
    sys(negative tag epsilon c, 2.95%)
    sys(negative tag run period dependence, 7.94%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.4167,0.0202)
    usys(MC statistics, 2.32%)
    sys(jet energy scale,-1.80%)
    sys(jet energy resolution,-1.45%)
    sys(b-fractions, 0.66%)
    sys(c-fractions, 2.02%)
    sys(track multiplicity, 3.90%)
    sys(long lived particles, 1.01%)
    sys(fake tracks, 2.46%)
    sys(photon conversions, 2.71%)
    sys(trigger bias, 5.99%)
    sys(negative tag epsilon b, 1.90%)
    sys(negative tag epsilon c, 4.79%)
    sys(negative tag run period dependence, 5.91%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5196,0.0176)
    usys(MC statistics, 2.70%)
    sys(jet energy scale,-1.39%)
    sys(jet energy resolution,-4.09%)
    sys(b-fractions, 1.20%)
    sys(c-fractions, 3.23%)
    sys(track multiplicity, 8.14%)
    sys(long lived particles,-1.10%)
    sys(fake tracks,-0.69%)
    sys(photon conversions, 1.90%)
    sys(trigger bias,18.38%)
    sys(negative tag epsilon b, 2.90%)
    sys(negative tag epsilon c, 6.30%)
    sys(negative tag run period dependence,-0.86%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5123,0.0232)
    usys(MC statistics, 2.64%)
    sys(jet energy scale, 0.23%)
    sys(jet energy resolution, 1.90%)
    sys(b-fractions, 1.26%)
    sys(c-fractions, 3.56%)
    sys(track multiplicity,12.68%)
    sys(long lived particles,-1.28%)
    sys(fake tracks,-0.50%)
    sys(photon conversions, 1.06%)
    sys(trigger bias, 8.88%)
    sys(negative tag epsilon b, 2.99%)
    sys(negative tag epsilon c, 6.81%)
    sys(negative tag run period dependence,-11.49%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.4841,0.0257)
    usys(MC statistics, 3.79%)
    sys(jet energy scale,-1.77%)
    sys(jet energy resolution,-2.26%)
    sys(b-fractions, 1.02%)
    sys(c-fractions, 2.35%)
    sys(track multiplicity,11.21%)
    sys(long lived particles,-2.72%)
    sys(fake tracks, 0.62%)
    sys(photon conversions, 1.17%)
    sys(trigger bias, 8.88%)
    sys(negative tag epsilon b, 2.86%)
    sys(negative tag epsilon c, 5.51%)
    sys(negative tag run period dependence, 2.33%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
