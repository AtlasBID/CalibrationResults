Analysis(negative tags,light,JetFitterCOMBNN, -0.95, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3147,0.0405)
    usys(MC statistics, 3.52%)
    sys(jet energy scale,-8.68%)
    sys(jet energy resolution,-1.21%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 0.76%)
    sys(track multiplicity, 1.67%)
    sys(long lived particles, 4.40%)
    sys(fake tracks,-0.43%)
    sys(photon conversions, 3.86%)
    sys(trigger bias, 5.63%)
    sys(negative tag epsilon b, 1.30%)
    sys(negative tag epsilon c, 3.14%)
    sys(negative tag run period dependence, 0.22%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.4154,0.0306)
    usys(MC statistics, 2.55%)
    sys(jet energy scale,-0.64%)
    sys(jet energy resolution,-5.06%)
    sys(b-fractions, 0.58%)
    sys(c-fractions, 2.46%)
    sys(track multiplicity, 4.77%)
    sys(long lived particles,-1.03%)
    sys(fake tracks,-0.76%)
    sys(photon conversions, 6.45%)
    sys(trigger bias, 5.63%)
    sys(negative tag epsilon b, 1.60%)
    sys(negative tag epsilon c, 5.22%)
    sys(negative tag run period dependence, 0.48%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3252,0.0160)
    usys(MC statistics, 1.66%)
    sys(jet energy scale, 0.26%)
    sys(jet energy resolution,-0.43%)
    sys(b-fractions, 1.93%)
    sys(c-fractions, 4.88%)
    sys(track multiplicity, 7.15%)
    sys(long lived particles, 1.69%)
    sys(fake tracks,-1.18%)
    sys(photon conversions,11.77%)
    sys(trigger bias, 6.19%)
    sys(negative tag epsilon b, 4.52%)
    sys(negative tag epsilon c, 8.46%)
    sys(negative tag run period dependence,-2.73%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3982,0.0107)
    usys(MC statistics, 2.09%)
    sys(jet energy scale, 0.25%)
    sys(jet energy resolution, 0.68%)
    sys(b-fractions, 3.55%)
    sys(c-fractions, 6.35%)
    sys(track multiplicity, 8.84%)
    sys(long lived particles, 2.46%)
    sys(fake tracks,-0.41%)
    sys(photon conversions,14.85%)
    sys(trigger bias, 9.55%)
    sys(negative tag epsilon b, 7.60%)
    sys(negative tag epsilon c, 9.90%)
    sys(negative tag run period dependence,-8.61%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4778,0.0097)
    usys(MC statistics, 1.67%)
    sys(jet energy scale,-0.34%)
    sys(jet energy resolution,-0.39%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 6.03%)
    sys(track multiplicity, 9.49%)
    sys(long lived particles,-1.72%)
    sys(fake tracks, 1.38%)
    sys(photon conversions,14.98%)
    sys(trigger bias, 5.68%)
    sys(negative tag epsilon b, 7.74%)
    sys(negative tag epsilon c, 9.46%)
    sys(negative tag run period dependence,-14.92%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2979,0.0061)
    usys(MC statistics, 2.96%)
    sys(jet energy scale, 2.38%)
    sys(jet energy resolution, 1.34%)
    sys(b-fractions, 3.04%)
    sys(c-fractions, 5.69%)
    sys(track multiplicity,15.23%)
    sys(long lived particles,-1.37%)
    sys(fake tracks, 3.77%)
    sys(photon conversions,13.24%)
    sys(trigger bias, 5.89%)
    sys(negative tag epsilon b, 7.34%)
    sys(negative tag epsilon c, 9.95%)
    sys(negative tag run period dependence, 0.03%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2704,0.0444)
    usys(MC statistics, 3.62%)
    sys(jet energy scale,-7.39%)
    sys(jet energy resolution, 0.08%)
    sys(b-fractions, 0.14%)
    sys(c-fractions, 0.01%)
    sys(track multiplicity, 2.78%)
    sys(long lived particles, 3.75%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 2.27%)
    sys(trigger bias, 0.81%)
    sys(negative tag epsilon b, 0.80%)
    sys(negative tag epsilon c, 2.14%)
    sys(negative tag run period dependence, 9.61%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3774,0.0362)
    usys(MC statistics, 2.61%)
    sys(jet energy scale,-2.71%)
    sys(jet energy resolution, 0.96%)
    sys(b-fractions, 0.25%)
    sys(c-fractions, 1.29%)
    sys(track multiplicity, 4.70%)
    sys(long lived particles,-0.80%)
    sys(fake tracks,-0.59%)
    sys(photon conversions, 3.72%)
    sys(trigger bias, 0.81%)
    sys(negative tag epsilon b, 0.94%)
    sys(negative tag epsilon c, 3.72%)
    sys(negative tag run period dependence, 6.86%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3551,0.0243)
    usys(MC statistics, 2.46%)
    sys(jet energy scale, 0.70%)
    sys(jet energy resolution, 0.63%)
    sys(b-fractions, 1.27%)
    sys(c-fractions, 2.74%)
    sys(track multiplicity, 9.03%)
    sys(long lived particles, 1.20%)
    sys(fake tracks,-0.92%)
    sys(photon conversions, 7.13%)
    sys(trigger bias, 7.47%)
    sys(negative tag epsilon b, 3.14%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence,-2.00%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5334,0.0227)
    usys(MC statistics, 2.85%)
    sys(jet energy scale,-2.66%)
    sys(jet energy resolution,-1.57%)
    sys(b-fractions, 2.45%)
    sys(c-fractions, 3.81%)
    sys(track multiplicity, 8.84%)
    sys(long lived particles, 1.65%)
    sys(fake tracks,-0.28%)
    sys(photon conversions, 9.25%)
    sys(trigger bias,19.70%)
    sys(negative tag epsilon b, 5.37%)
    sys(negative tag epsilon c, 6.71%)
    sys(negative tag run period dependence,-2.73%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6376,0.0261)
    usys(MC statistics, 3.80%)
    sys(jet energy scale, 0.90%)
    sys(jet energy resolution,-1.50%)
    sys(b-fractions, 2.64%)
    sys(c-fractions, 4.23%)
    sys(track multiplicity, 9.98%)
    sys(long lived particles,-1.03%)
    sys(fake tracks, 0.77%)
    sys(photon conversions, 9.16%)
    sys(trigger bias, 8.72%)
    sys(negative tag epsilon b, 5.61%)
    sys(negative tag epsilon c, 7.06%)
    sys(negative tag run period dependence,-15.95%)
    sys(d0 and z0 smearing,-3.14%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5067,0.0184)
    usys(MC statistics, 4.62%)
    sys(jet energy scale,-2.38%)
    sys(jet energy resolution,-5.99%)
    sys(b-fractions, 2.24%)
    sys(c-fractions, 3.81%)
    sys(track multiplicity,13.33%)
    sys(long lived particles,-1.11%)
    sys(fake tracks, 1.96%)
    sys(photon conversions, 8.67%)
    sys(trigger bias,15.13%)
    sys(negative tag epsilon b, 5.38%)
    sys(negative tag epsilon c, 7.15%)
    sys(negative tag run period dependence,-0.97%)
    sys(d0 and z0 smearing,-3.14%)
  }
}
