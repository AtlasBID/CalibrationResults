Analysis(negative tags,light,JetFitterCOMBNN, -2.55, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2300,0.0169)
    usys(MC statistics, 1.44%)
    sys(jet energy scale,-5.97%)
    sys(jet energy resolution,-2.20%)
    sys(b-fractions, 0.25%)
    sys(c-fractions, 0.58%)
    sys(track multiplicity, 6.20%)
    sys(long lived particles, 3.74%)
    sys(fake tracks,-0.39%)
    sys(photon conversions, 3.56%)
    sys(trigger bias, 1.76%)
    sys(negative tag epsilon b, 1.08%)
    sys(negative tag epsilon c, 2.95%)
    sys(negative tag run period dependence,-1.17%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2464,0.0115)
    usys(MC statistics, 1.00%)
    sys(jet energy scale,-0.57%)
    sys(jet energy resolution,-1.94%)
    sys(b-fractions, 0.29%)
    sys(c-fractions, 1.27%)
    sys(track multiplicity, 4.52%)
    sys(long lived particles,-0.88%)
    sys(fake tracks,-0.65%)
    sys(photon conversions, 5.24%)
    sys(trigger bias, 1.76%)
    sys(negative tag epsilon b, 1.07%)
    sys(negative tag epsilon c, 3.89%)
    sys(negative tag run period dependence, 2.52%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2296,0.0062)
    usys(MC statistics, 0.67%)
    sys(jet energy scale, 0.13%)
    sys(jet energy resolution, 0.23%)
    sys(b-fractions, 0.82%)
    sys(c-fractions, 2.92%)
    sys(track multiplicity, 7.33%)
    sys(long lived particles, 1.16%)
    sys(fake tracks,-0.99%)
    sys(photon conversions, 8.24%)
    sys(trigger bias, 5.23%)
    sys(negative tag epsilon b, 2.31%)
    sys(negative tag epsilon c, 6.35%)
    sys(negative tag run period dependence,-3.01%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2853,0.0040)
    usys(MC statistics, 0.94%)
    sys(jet energy scale, 0.99%)
    sys(jet energy resolution,-0.21%)
    sys(b-fractions, 1.35%)
    sys(c-fractions, 4.29%)
    sys(track multiplicity, 5.80%)
    sys(long lived particles, 1.67%)
    sys(fake tracks,-0.34%)
    sys(photon conversions,10.84%)
    sys(trigger bias, 6.10%)
    sys(negative tag epsilon b, 3.28%)
    sys(negative tag epsilon c, 8.06%)
    sys(negative tag run period dependence,-6.84%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3660,0.0036)
    usys(MC statistics, 0.66%)
    sys(jet energy scale, 0.86%)
    sys(jet energy resolution,-0.37%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 3.99%)
    sys(track multiplicity, 6.48%)
    sys(long lived particles,-1.22%)
    sys(fake tracks, 1.07%)
    sys(photon conversions,11.01%)
    sys(trigger bias, 3.68%)
    sys(negative tag epsilon b, 3.10%)
    sys(negative tag epsilon c, 7.76%)
    sys(negative tag run period dependence,-8.48%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2524,0.0023)
    usys(MC statistics, 0.96%)
    sys(jet energy scale, 1.74%)
    sys(jet energy resolution, 3.20%)
    sys(b-fractions, 1.07%)
    sys(c-fractions, 3.55%)
    sys(track multiplicity,10.98%)
    sys(long lived particles,-0.95%)
    sys(fake tracks, 2.77%)
    sys(photon conversions, 9.74%)
    sys(trigger bias, 0.94%)
    sys(negative tag epsilon b, 3.01%)
    sys(negative tag epsilon c, 7.84%)
    sys(negative tag run period dependence,-1.47%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1943,0.0162)
    usys(MC statistics, 1.43%)
    sys(jet energy scale,-8.40%)
    sys(jet energy resolution,-1.82%)
    sys(b-fractions, 0.06%)
    sys(c-fractions, 0.25%)
    sys(track multiplicity, 5.19%)
    sys(long lived particles, 2.60%)
    sys(fake tracks,-0.34%)
    sys(photon conversions, 1.94%)
    sys(trigger bias, 0.83%)
    sys(negative tag epsilon b, 0.60%)
    sys(negative tag epsilon c, 2.44%)
    sys(negative tag run period dependence, 1.73%)
    sys(d0 and z0 smearing, 0.03%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2298,0.0126)
    usys(MC statistics, 1.01%)
    sys(jet energy scale,-1.50%)
    sys(jet energy resolution,-3.23%)
    sys(b-fractions, 0.12%)
    sys(c-fractions, 0.59%)
    sys(track multiplicity, 4.95%)
    sys(long lived particles,-0.66%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 2.94%)
    sys(trigger bias, 0.83%)
    sys(negative tag epsilon b, 0.69%)
    sys(negative tag epsilon c, 2.91%)
    sys(negative tag run period dependence,-0.29%)
    sys(d0 and z0 smearing,-0.89%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2298,0.0082)
    usys(MC statistics, 0.88%)
    sys(jet energy scale,-0.20%)
    sys(jet energy resolution,-0.06%)
    sys(b-fractions, 0.45%)
    sys(c-fractions, 1.62%)
    sys(track multiplicity, 7.82%)
    sys(long lived particles, 0.77%)
    sys(fake tracks,-0.69%)
    sys(photon conversions, 4.97%)
    sys(trigger bias, 6.93%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 4.37%)
    sys(negative tag run period dependence,-2.73%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2990,0.0066)
    usys(MC statistics, 1.38%)
    sys(jet energy scale,-0.81%)
    sys(jet energy resolution, 1.28%)
    sys(b-fractions, 0.75%)
    sys(c-fractions, 2.51%)
    sys(track multiplicity, 5.54%)
    sys(long lived particles, 1.15%)
    sys(fake tracks,-0.22%)
    sys(photon conversions, 6.92%)
    sys(trigger bias, 7.08%)
    sys(negative tag epsilon b, 1.95%)
    sys(negative tag epsilon c, 5.44%)
    sys(negative tag run period dependence,-7.92%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3341,0.0071)
    usys(MC statistics, 1.19%)
    sys(jet energy scale, 0.67%)
    sys(jet energy resolution, 0.31%)
    sys(b-fractions, 0.77%)
    sys(c-fractions, 2.72%)
    sys(track multiplicity, 6.59%)
    sys(long lived particles,-0.89%)
    sys(fake tracks, 0.64%)
    sys(photon conversions, 7.09%)
    sys(trigger bias, 2.52%)
    sys(negative tag epsilon b, 1.93%)
    sys(negative tag epsilon c, 5.61%)
    sys(negative tag run period dependence,-9.84%)
    sys(d0 and z0 smearing, 1.89%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3848,0.0056)
    usys(MC statistics, 1.52%)
    sys(jet energy scale,-2.02%)
    sys(jet energy resolution,-5.47%)
    sys(b-fractions, 0.60%)
    sys(c-fractions, 2.22%)
    sys(track multiplicity, 9.28%)
    sys(long lived particles,-0.83%)
    sys(fake tracks, 1.61%)
    sys(photon conversions, 6.70%)
    sys(trigger bias, 5.12%)
    sys(negative tag epsilon b, 1.74%)
    sys(negative tag epsilon c, 5.37%)
    sys(negative tag run period dependence,-3.84%)
    sys(d0 and z0 smearing, 1.89%)
  }
}
