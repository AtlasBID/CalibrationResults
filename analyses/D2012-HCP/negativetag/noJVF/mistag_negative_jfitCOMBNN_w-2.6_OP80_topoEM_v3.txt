Analysis(negative tags,light,JetFitterCOMBNN, -2.60, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1895,0.0167)
    usys(MC statistics, 1.55%)
    sys(jet vertex fraction, 6.52%)
    sys(jet energy scale,-2.67%)
    sys(jet energy resolution,-1.77%)
    sys(b-fractions, 0.26%)
    sys(c-fractions, 0.83%)
    sys(track multiplicity, 6.37%)
    sys(long lived particles, 4.42%)
    sys(fake tracks,-0.52%)
    sys(photon conversions, 4.17%)
    sys(trigger bias, 4.66%)
    sys(negative tag epsilon b, 1.10%)
    sys(negative tag epsilon c, 3.22%)
    sys(negative tag run period dependence,-0.18%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2247,0.0101)
    usys(MC statistics, 0.96%)
    sys(jet vertex fraction, 0.92%)
    sys(jet energy scale,-1.12%)
    sys(jet energy resolution,-1.64%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity, 4.29%)
    sys(long lived particles,-0.95%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 5.53%)
    sys(trigger bias, 4.66%)
    sys(negative tag epsilon b, 1.06%)
    sys(negative tag epsilon c, 3.60%)
    sys(negative tag run period dependence, 3.20%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2037,0.0057)
    usys(MC statistics, 0.62%)
    sys(jet vertex fraction, 0.72%)
    sys(jet energy scale, 0.17%)
    sys(jet energy resolution, 0.56%)
    sys(b-fractions, 0.75%)
    sys(c-fractions, 2.69%)
    sys(track multiplicity, 7.90%)
    sys(long lived particles, 1.10%)
    sys(fake tracks,-0.97%)
    sys(photon conversions, 8.31%)
    sys(trigger bias, 7.66%)
    sys(negative tag epsilon b, 2.17%)
    sys(negative tag epsilon c, 6.05%)
    sys(negative tag run period dependence,-2.31%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3168,0.0040)
    usys(MC statistics, 0.86%)
    sys(jet vertex fraction, 0.07%)
    sys(jet energy scale, 1.61%)
    sys(jet energy resolution,-0.48%)
    sys(b-fractions, 1.26%)
    sys(c-fractions, 4.25%)
    sys(track multiplicity, 5.61%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-0.34%)
    sys(photon conversions,11.31%)
    sys(trigger bias, 7.89%)
    sys(negative tag epsilon b, 3.07%)
    sys(negative tag epsilon c, 7.98%)
    sys(negative tag run period dependence,-5.46%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3628,0.0036)
    usys(MC statistics, 0.75%)
    sys(jet vertex fraction, 0.09%)
    sys(jet energy scale, 0.86%)
    sys(jet energy resolution, 0.93%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 4.04%)
    sys(track multiplicity, 5.73%)
    sys(long lived particles,-1.12%)
    sys(fake tracks, 1.06%)
    sys(photon conversions,10.87%)
    sys(trigger bias, 3.44%)
    sys(negative tag epsilon b, 3.10%)
    sys(negative tag epsilon c, 7.83%)
    sys(negative tag run period dependence,-9.39%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2743,0.0024)
    usys(MC statistics, 0.89%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale,-0.50%)
    sys(jet energy resolution, 1.12%)
    sys(b-fractions, 1.02%)
    sys(c-fractions, 3.61%)
    sys(track multiplicity,10.60%)
    sys(long lived particles,-0.87%)
    sys(fake tracks, 2.84%)
    sys(photon conversions,10.16%)
    sys(trigger bias, 9.55%)
    sys(negative tag epsilon b, 3.02%)
    sys(negative tag epsilon c, 8.13%)
    sys(negative tag run period dependence,-0.80%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1805,0.0173)
    usys(MC statistics, 1.54%)
    sys(jet vertex fraction, 1.36%)
    sys(jet energy scale,-6.24%)
    sys(jet energy resolution,-1.01%)
    sys(b-fractions, 0.07%)
    sys(c-fractions, 0.12%)
    sys(track multiplicity, 7.54%)
    sys(long lived particles, 3.00%)
    sys(fake tracks,-0.44%)
    sys(photon conversions, 2.27%)
    sys(trigger bias, 4.20%)
    sys(negative tag epsilon b, 0.62%)
    sys(negative tag epsilon c, 2.27%)
    sys(negative tag run period dependence, 0.57%)
    sys(d0 and z0 smearing, 0.03%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2274,0.0113)
    usys(MC statistics, 0.95%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale,-1.35%)
    sys(jet energy resolution,-0.31%)
    sys(b-fractions, 0.10%)
    sys(c-fractions, 0.49%)
    sys(track multiplicity, 5.24%)
    sys(long lived particles,-0.67%)
    sys(fake tracks,-0.43%)
    sys(photon conversions, 3.22%)
    sys(trigger bias, 4.20%)
    sys(negative tag epsilon b, 0.66%)
    sys(negative tag epsilon c, 2.79%)
    sys(negative tag run period dependence,-2.24%)
    sys(d0 and z0 smearing,-0.89%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2006,0.0071)
    usys(MC statistics, 0.75%)
    sys(jet vertex fraction, 0.76%)
    sys(jet energy scale,-0.20%)
    sys(jet energy resolution, 0.29%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 1.40%)
    sys(track multiplicity, 8.57%)
    sys(long lived particles, 0.71%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 4.89%)
    sys(trigger bias, 6.75%)
    sys(negative tag epsilon b, 1.25%)
    sys(negative tag epsilon c, 4.13%)
    sys(negative tag run period dependence,-0.48%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2831,0.0060)
    usys(MC statistics, 1.39%)
    sys(jet vertex fraction, 0.20%)
    sys(jet energy scale, 1.06%)
    sys(jet energy resolution, 1.84%)
    sys(b-fractions, 0.67%)
    sys(c-fractions, 2.18%)
    sys(track multiplicity, 5.74%)
    sys(long lived particles, 1.04%)
    sys(fake tracks,-0.22%)
    sys(photon conversions, 6.68%)
    sys(trigger bias, 5.91%)
    sys(negative tag epsilon b, 1.76%)
    sys(negative tag epsilon c, 4.99%)
    sys(negative tag run period dependence,-5.35%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3735,0.0070)
    usys(MC statistics, 1.30%)
    sys(jet vertex fraction, 0.13%)
    sys(jet energy scale, 1.43%)
    sys(jet energy resolution, 0.31%)
    sys(b-fractions, 0.71%)
    sys(c-fractions, 2.64%)
    sys(track multiplicity, 5.70%)
    sys(long lived particles,-0.86%)
    sys(fake tracks, 0.63%)
    sys(photon conversions, 7.21%)
    sys(trigger bias, 3.23%)
    sys(negative tag epsilon b, 1.79%)
    sys(negative tag epsilon c, 5.57%)
    sys(negative tag run period dependence,-9.81%)
    sys(d0 and z0 smearing, 1.89%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3180,0.0054)
    usys(MC statistics, 2.31%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale, 3.57%)
    sys(jet energy resolution, 3.30%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 2.02%)
    sys(track multiplicity, 9.67%)
    sys(long lived particles,-0.71%)
    sys(fake tracks, 1.64%)
    sys(photon conversions, 6.54%)
    sys(trigger bias,-1.20%)
    sys(negative tag epsilon b, 1.62%)
    sys(negative tag epsilon c, 5.23%)
    sys(negative tag run period dependence,-4.34%)
    sys(d0 and z0 smearing, 1.89%)
  }
}
