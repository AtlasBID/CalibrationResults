Analysis(negative tags,light,SV0,  2.85, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2917,0.0827)
    usys(MC statistics, 5.78%)
    sys(jet vertex fraction,-1.78%)
    sys(jet energy scale, 7.31%)
    sys(jet energy resolution,-12.29%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.16%)
    sys(track multiplicity, 4.00%)
    sys(long lived particles, 0.78%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.52%)
    sys(trigger bias, 7.94%)
    sys(negative tag epsilon b, 0.70%)
    sys(negative tag epsilon c, 2.44%)
    sys(negative tag run period dependence,10.46%)
    sys(d0 and z0 smearing,-3.59%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1974,0.0289)
    usys(MC statistics, 2.61%)
    sys(jet vertex fraction, 0.12%)
    sys(jet energy scale, 3.29%)
    sys(jet energy resolution, 3.76%)
    sys(b-fractions, 0.14%)
    sys(c-fractions, 0.68%)
    sys(track multiplicity,-0.15%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.73%)
    sys(trigger bias, 7.94%)
    sys(negative tag epsilon b, 0.80%)
    sys(negative tag epsilon c, 3.20%)
    sys(negative tag run period dependence,10.46%)
    sys(d0 and z0 smearing,-3.59%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0415,0.0121)
    usys(MC statistics, 1.39%)
    sys(jet vertex fraction, 0.35%)
    sys(jet energy scale, 0.33%)
    sys(jet energy resolution,-0.08%)
    sys(b-fractions, 0.31%)
    sys(c-fractions, 1.84%)
    sys(track multiplicity,-0.98%)
    sys(long lived particles, 0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.84%)
    sys(trigger bias,12.90%)
    sys(negative tag epsilon b, 1.30%)
    sys(negative tag epsilon c, 5.12%)
    sys(negative tag run period dependence, 1.30%)
    sys(d0 and z0 smearing,-0.35%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1350,0.0077)
    usys(MC statistics, 1.83%)
    sys(jet vertex fraction, 0.03%)
    sys(jet energy scale, 2.52%)
    sys(jet energy resolution, 2.67%)
    sys(b-fractions, 0.64%)
    sys(c-fractions, 2.95%)
    sys(track multiplicity, 0.25%)
    sys(long lived particles, 0.18%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.91%)
    sys(trigger bias, 8.22%)
    sys(negative tag epsilon b, 1.89%)
    sys(negative tag epsilon c, 6.58%)
    sys(negative tag run period dependence,-3.53%)
    sys(d0 and z0 smearing,-0.35%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1642,0.0067)
    usys(MC statistics, 1.44%)
    sys(jet vertex fraction, 0.57%)
    sys(jet energy scale, 0.74%)
    sys(jet energy resolution, 2.07%)
    sys(b-fractions, 0.55%)
    sys(c-fractions, 3.13%)
    sys(track multiplicity, 0.99%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 0.72%)
    sys(trigger bias, 3.41%)
    sys(negative tag epsilon b, 1.68%)
    sys(negative tag epsilon c, 6.97%)
    sys(negative tag run period dependence,-13.67%)
    sys(d0 and z0 smearing,-3.63%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.0900,0.0047)
    usys(MC statistics, 1.46%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale,-3.16%)
    sys(jet energy resolution, 0.13%)
    sys(b-fractions, 0.40%)
    sys(c-fractions, 2.70%)
    sys(track multiplicity, 2.80%)
    sys(long lived particles,-0.11%)
    sys(fake tracks, 0.08%)
    sys(photon conversions, 0.65%)
    sys(trigger bias,17.86%)
    sys(negative tag epsilon b, 1.60%)
    sys(negative tag epsilon c, 7.11%)
    sys(negative tag run period dependence,-0.40%)
    sys(d0 and z0 smearing,-3.63%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.4702,0.1116)
    usys(MC statistics, 6.79%)
    sys(jet vertex fraction,-3.44%)
    sys(jet energy scale,10.79%)
    sys(jet energy resolution,-0.10%)
    sys(b-fractions,-0.03%)
    sys(c-fractions, 0.55%)
    sys(track multiplicity, 2.40%)
    sys(long lived particles, 0.79%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.46%)
    sys(trigger bias, 9.09%)
    sys(negative tag epsilon b, 0.40%)
    sys(negative tag epsilon c, 2.82%)
    sys(negative tag run period dependence,19.69%)
    sys(d0 and z0 smearing,-8.71%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3303,0.0402)
    usys(MC statistics, 3.09%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale, 2.56%)
    sys(jet energy resolution, 2.04%)
    sys(b-fractions, 0.02%)
    sys(c-fractions, 0.51%)
    sys(track multiplicity,-0.63%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.51%)
    sys(trigger bias, 9.09%)
    sys(negative tag epsilon b, 0.50%)
    sys(negative tag epsilon c, 2.83%)
    sys(negative tag run period dependence,19.69%)
    sys(d0 and z0 smearing,-8.71%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1751,0.0177)
    usys(MC statistics, 1.87%)
    sys(jet vertex fraction, 0.94%)
    sys(jet energy scale, 0.48%)
    sys(jet energy resolution, 1.20%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 1.23%)
    sys(track multiplicity,-0.22%)
    sys(long lived particles, 0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.53%)
    sys(trigger bias,11.69%)
    sys(negative tag epsilon b, 1.00%)
    sys(negative tag epsilon c, 3.94%)
    sys(negative tag run period dependence, 6.31%)
    sys(d0 and z0 smearing,-6.37%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2596,0.0128)
    usys(MC statistics, 3.19%)
    sys(jet vertex fraction,-0.07%)
    sys(jet energy scale, 0.91%)
    sys(jet energy resolution,-1.77%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.45%)
    sys(track multiplicity, 0.74%)
    sys(long lived particles, 0.17%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.55%)
    sys(trigger bias, 9.79%)
    sys(negative tag epsilon b, 1.10%)
    sys(negative tag epsilon c, 4.13%)
    sys(negative tag run period dependence,-3.25%)
    sys(d0 and z0 smearing,-6.37%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3628,0.0144)
    usys(MC statistics, 2.71%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale, 1.40%)
    sys(jet energy resolution, 3.93%)
    sys(b-fractions, 0.41%)
    sys(c-fractions, 1.38%)
    sys(track multiplicity, 1.63%)
    sys(long lived particles,-0.12%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.46%)
    sys(trigger bias, 3.90%)
    sys(negative tag epsilon b, 1.22%)
    sys(negative tag epsilon c, 4.03%)
    sys(negative tag run period dependence,-7.05%)
    sys(d0 and z0 smearing,-6.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2998,0.0117)
    usys(MC statistics, 4.20%)
    sys(jet vertex fraction, 0.27%)
    sys(jet energy scale,-7.00%)
    sys(jet energy resolution,-2.68%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.47%)
    sys(track multiplicity, 5.42%)
    sys(long lived particles,-0.09%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 0.42%)
    sys(trigger bias, 9.02%)
    sys(negative tag epsilon b, 0.97%)
    sys(negative tag epsilon c, 4.51%)
    sys(negative tag run period dependence,-0.17%)
    sys(d0 and z0 smearing,-6.19%)
  }
}
