Analysis(negative tags,light,MV2, 0.010, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4610,0.0966)
    usys(MC statistics, 7.24%)
    sys(jet vertex fraction, 7.00%)
    sys(jet energy scale,-19.43%)
    sys(jet energy resolution,-1.39%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 1.20%)
    sys(track multiplicity, 3.97%)
    sys(long lived particles, 5.42%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 5.21%)
    sys(trigger bias, 5.23%)
    sys(negative tag epsilon b, 1.59%)
    sys(negative tag epsilon c, 3.63%)
    sys(negative tag run period dependence,11.78%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2695,0.0133)
    usys(MC statistics, 1.25%)
    sys(jet vertex fraction, 0.38%)
    sys(jet energy scale, 6.04%)
    sys(jet energy resolution,-1.39%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.42%)
    sys(track multiplicity, 2.46%)
    sys(long lived particles,-1.03%)
    sys(fake tracks,-0.81%)
    sys(photon conversions, 6.75%)
    sys(trigger bias, 5.23%)
    sys(negative tag epsilon b, 1.15%)
    sys(negative tag epsilon c, 4.00%)
    sys(negative tag run period dependence, 3.84%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1667,0.0051)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction, 0.92%)
    sys(jet energy scale,-1.91%)
    sys(jet energy resolution, 2.46%)
    sys(b-fractions, 0.72%)
    sys(c-fractions, 2.48%)
    sys(track multiplicity, 4.36%)
    sys(long lived particles, 1.02%)
    sys(fake tracks,-0.84%)
    sys(photon conversions, 8.84%)
    sys(trigger bias, 8.38%)
    sys(negative tag epsilon b, 2.11%)
    sys(negative tag epsilon c, 5.80%)
    sys(negative tag run period dependence,-0.01%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3798,0.0114)
    usys(MC statistics, 1.66%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale,-9.12%)
    sys(jet energy resolution,-9.83%)
    sys(b-fractions, 2.32%)
    sys(c-fractions, 5.20%)
    sys(track multiplicity, 4.33%)
    sys(long lived particles, 2.31%)
    sys(fake tracks,-0.31%)
    sys(photon conversions,15.12%)
    sys(trigger bias,15.49%)
    sys(negative tag epsilon b, 5.13%)
    sys(negative tag epsilon c, 8.81%)
    sys(negative tag run period dependence,10.66%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4953,0.0182)
    usys(MC statistics, 3.41%)
    sys(jet vertex fraction, 0.56%)
    sys(jet energy scale,-0.95%)
    sys(jet energy resolution, 4.05%)
    sys(b-fractions, 3.92%)
    sys(c-fractions, 5.08%)
    sys(track multiplicity, 7.50%)
    sys(long lived particles,-1.53%)
    sys(fake tracks, 1.33%)
    sys(photon conversions,17.27%)
    sys(trigger bias, 6.76%)
    sys(negative tag epsilon b, 8.12%)
    sys(negative tag epsilon c, 8.24%)
    sys(negative tag run period dependence,-15.64%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2161,0.0112)
    usys(MC statistics, 4.83%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale, 5.91%)
    sys(jet energy resolution, 6.65%)
    sys(b-fractions, 3.04%)
    sys(c-fractions, 6.23%)
    sys(track multiplicity,14.65%)
    sys(long lived particles,-1.51%)
    sys(fake tracks, 3.66%)
    sys(photon conversions,14.96%)
    sys(trigger bias, 1.18%)
    sys(negative tag epsilon b, 7.67%)
    sys(negative tag epsilon c,10.96%)
    sys(negative tag run period dependence, 5.31%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.4115,0.1062)
    usys(MC statistics,11.26%)
    sys(jet vertex fraction,-1.79%)
    sys(jet energy scale,-16.41%)
    sys(jet energy resolution, 3.19%)
    sys(b-fractions, 0.14%)
    sys(c-fractions, 0.29%)
    sys(track multiplicity, 1.51%)
    sys(long lived particles, 4.84%)
    sys(fake tracks,-0.79%)
    sys(photon conversions, 3.17%)
    sys(trigger bias, 6.34%)
    sys(negative tag epsilon b, 0.78%)
    sys(negative tag epsilon c, 2.47%)
    sys(negative tag run period dependence, 2.72%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3048,0.0176)
    usys(MC statistics, 1.57%)
    sys(jet vertex fraction, 0.23%)
    sys(jet energy scale, 6.39%)
    sys(jet energy resolution, 3.19%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.88%)
    sys(track multiplicity, 4.22%)
    sys(long lived particles,-0.82%)
    sys(fake tracks,-0.64%)
    sys(photon conversions, 4.61%)
    sys(trigger bias, 6.34%)
    sys(negative tag epsilon b, 0.80%)
    sys(negative tag epsilon c, 3.23%)
    sys(negative tag run period dependence, 2.72%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2185,0.0085)
    usys(MC statistics, 0.93%)
    sys(jet vertex fraction, 1.00%)
    sys(jet energy scale,-4.19%)
    sys(jet energy resolution, 1.89%)
    sys(b-fractions, 0.46%)
    sys(c-fractions, 1.56%)
    sys(track multiplicity, 5.56%)
    sys(long lived particles, 0.90%)
    sys(fake tracks,-0.67%)
    sys(photon conversions, 5.65%)
    sys(trigger bias, 9.92%)
    sys(negative tag epsilon b, 1.49%)
    sys(negative tag epsilon c, 4.30%)
    sys(negative tag run period dependence, 5.32%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5455,0.0232)
    usys(MC statistics, 4.80%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale,-11.03%)
    sys(jet energy resolution,-9.14%)
    sys(b-fractions, 1.51%)
    sys(c-fractions, 3.73%)
    sys(track multiplicity, 5.85%)
    sys(long lived particles, 2.07%)
    sys(fake tracks,-0.23%)
    sys(photon conversions, 8.68%)
    sys(trigger bias,12.26%)
    sys(negative tag epsilon b, 3.39%)
    sys(negative tag epsilon c, 6.77%)
    sys(negative tag run period dependence, 4.78%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.7902,0.0461)
    usys(MC statistics, 7.64%)
    sys(jet vertex fraction,-0.39%)
    sys(jet energy scale,-3.06%)
    sys(jet energy resolution,-25.66%)
    sys(b-fractions, 1.80%)
    sys(c-fractions, 2.60%)
    sys(track multiplicity, 5.91%)
    sys(long lived particles,-1.30%)
    sys(fake tracks, 0.74%)
    sys(photon conversions, 9.70%)
    sys(trigger bias, 5.00%)
    sys(negative tag epsilon b, 3.97%)
    sys(negative tag epsilon c, 5.26%)
    sys(negative tag run period dependence,-11.94%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.8276,0.0366)
    usys(MC statistics, 7.76%)
    sys(jet vertex fraction, 0.84%)
    sys(jet energy scale,-9.80%)
    sys(jet energy resolution,-25.66%)
    sys(b-fractions, 1.19%)
    sys(c-fractions, 3.59%)
    sys(track multiplicity,15.45%)
    sys(long lived particles,-1.21%)
    sys(fake tracks, 1.94%)
    sys(photon conversions, 9.30%)
    sys(trigger bias,13.47%)
    sys(negative tag epsilon b, 3.22%)
    sys(negative tag epsilon c, 7.33%)
    sys(negative tag run period dependence,-7.32%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
