Analysis(negative tags,light,MV2, 0.169, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.8271,0.6460)
    usys(MC statistics,22.20%)
    sys(jet vertex fraction,-13.17%)
    sys(jet energy scale, 8.33%)
    sys(jet energy resolution,-1.25%)
    sys(b-fractions, 2.25%)
    sys(c-fractions, 2.63%)
    sys(track multiplicity, 3.77%)
    sys(long lived particles, 6.83%)
    sys(fake tracks, 0.14%)
    sys(photon conversions, 8.48%)
    sys(trigger bias, 9.53%)
    sys(negative tag epsilon b, 5.66%)
    sys(negative tag epsilon c, 5.04%)
    sys(negative tag run period dependence,14.61%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.4336,0.0518)
    usys(MC statistics, 4.88%)
    sys(jet vertex fraction, 0.73%)
    sys(jet energy scale, 8.33%)
    sys(jet energy resolution,-1.25%)
    sys(b-fractions, 0.62%)
    sys(c-fractions, 2.71%)
    sys(track multiplicity, 2.61%)
    sys(long lived particles,-1.20%)
    sys(fake tracks,-0.87%)
    sys(photon conversions, 7.98%)
    sys(trigger bias, 9.53%)
    sys(negative tag epsilon b, 1.68%)
    sys(negative tag epsilon c, 5.49%)
    sys(negative tag run period dependence,14.61%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2911,0.0164)
    usys(MC statistics, 1.73%)
    sys(jet vertex fraction, 0.65%)
    sys(jet energy scale,-2.01%)
    sys(jet energy resolution, 1.71%)
    sys(b-fractions, 1.62%)
    sys(c-fractions, 4.35%)
    sys(track multiplicity, 4.81%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-1.00%)
    sys(photon conversions,11.28%)
    sys(trigger bias, 9.00%)
    sys(negative tag epsilon b, 3.91%)
    sys(negative tag epsilon c, 7.90%)
    sys(negative tag run period dependence, 0.57%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2458,0.0405)
    usys(MC statistics, 7.95%)
    sys(jet vertex fraction,-0.65%)
    sys(jet energy scale,-13.14%)
    sys(jet energy resolution,-18.36%)
    sys(b-fractions, 6.55%)
    sys(c-fractions, 5.06%)
    sys(track multiplicity, 7.72%)
    sys(long lived particles, 3.04%)
    sys(fake tracks,-0.28%)
    sys(photon conversions,19.92%)
    sys(trigger bias,10.81%)
    sys(negative tag epsilon b,13.59%)
    sys(negative tag epsilon c, 7.33%)
    sys(negative tag run period dependence,-15.23%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4882,0.0616)
    usys(MC statistics,11.39%)
    sys(jet vertex fraction,-0.21%)
    sys(jet energy scale, 4.35%)
    sys(jet energy resolution, 2.42%)
    sys(b-fractions, 8.09%)
    sys(c-fractions, 3.03%)
    sys(track multiplicity,17.90%)
    sys(long lived particles,-1.43%)
    sys(fake tracks, 1.40%)
    sys(photon conversions,17.66%)
    sys(trigger bias,10.69%)
    sys(negative tag epsilon b,16.26%)
    sys(negative tag epsilon c, 4.34%)
    sys(negative tag run period dependence,16.65%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2058,0.0447)
    usys(MC statistics,14.74%)
    sys(jet vertex fraction,-0.25%)
    sys(jet energy scale,11.32%)
    sys(jet energy resolution,-15.31%)
    sys(b-fractions, 6.97%)
    sys(c-fractions, 2.69%)
    sys(track multiplicity,17.90%)
    sys(long lived particles,-2.29%)
    sys(fake tracks, 3.24%)
    sys(photon conversions,20.07%)
    sys(trigger bias,-12.47%)
    sys(negative tag epsilon b,16.21%)
    sys(negative tag epsilon c, 4.40%)
    sys(negative tag run period dependence,16.65%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(3.5782,1.6001)
    usys(MC statistics,38.32%)
    sys(jet vertex fraction, 1.42%)
    sys(jet energy scale, 7.53%)
    sys(jet energy resolution, 9.63%)
    sys(b-fractions, 1.75%)
    sys(c-fractions,-0.36%)
    sys(track multiplicity, 3.65%)
    sys(long lived particles, 8.78%)
    sys(fake tracks,-0.16%)
    sys(photon conversions, 1.64%)
    sys(trigger bias, 3.40%)
    sys(negative tag epsilon b, 4.33%)
    sys(negative tag epsilon c, 1.31%)
    sys(negative tag run period dependence, 9.10%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.5036,0.0676)
    usys(MC statistics, 6.06%)
    sys(jet vertex fraction, 1.42%)
    sys(jet energy scale, 7.53%)
    sys(jet energy resolution, 9.63%)
    sys(b-fractions, 0.32%)
    sys(c-fractions, 1.72%)
    sys(track multiplicity, 3.33%)
    sys(long lived particles,-0.99%)
    sys(fake tracks,-0.73%)
    sys(photon conversions, 5.04%)
    sys(trigger bias, 3.40%)
    sys(negative tag epsilon b, 1.07%)
    sys(negative tag epsilon c, 4.22%)
    sys(negative tag run period dependence, 9.10%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.4548,0.0353)
    usys(MC statistics, 3.38%)
    sys(jet vertex fraction, 1.13%)
    sys(jet energy scale,-3.29%)
    sys(jet energy resolution, 4.60%)
    sys(b-fractions, 1.14%)
    sys(c-fractions, 2.57%)
    sys(track multiplicity, 6.34%)
    sys(long lived particles, 1.53%)
    sys(fake tracks,-0.76%)
    sys(photon conversions, 7.73%)
    sys(trigger bias,14.26%)
    sys(negative tag epsilon b, 2.89%)
    sys(negative tag epsilon c, 5.39%)
    sys(negative tag run period dependence,30.07%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.9256,0.1689)
    usys(MC statistics,18.07%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale,-14.91%)
    sys(jet energy resolution,-10.50%)
    sys(b-fractions, 6.02%)
    sys(c-fractions, 2.91%)
    sys(track multiplicity,10.49%)
    sys(long lived particles, 2.84%)
    sys(fake tracks,-0.23%)
    sys(photon conversions,12.24%)
    sys(trigger bias,14.26%)
    sys(negative tag epsilon b,12.59%)
    sys(negative tag epsilon c, 4.67%)
    sys(negative tag run period dependence,30.07%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4108,0.2456)
    usys(MC statistics,33.35%)
    sys(jet vertex fraction,-3.11%)
    sys(jet energy scale, 3.07%)
    sys(jet energy resolution,-14.15%)
    sys(b-fractions, 7.29%)
    sys(c-fractions, 0.90%)
    sys(track multiplicity,14.43%)
    sys(long lived particles,-2.88%)
    sys(fake tracks, 0.61%)
    sys(photon conversions,12.64%)
    sys(trigger bias,70.28%)
    sys(negative tag epsilon b,14.93%)
    sys(negative tag epsilon c, 1.77%)
    sys(negative tag run period dependence,-18.53%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(2.5259,0.4045)
    usys(MC statistics,52.30%)
    sys(jet vertex fraction, 0.75%)
    sys(jet energy scale,-12.18%)
    sys(jet energy resolution,-14.87%)
    sys(b-fractions, 2.91%)
    sys(c-fractions,-0.44%)
    sys(track multiplicity, 9.82%)
    sys(long lived particles,-3.14%)
    sys(fake tracks, 1.48%)
    sys(photon conversions,12.20%)
    sys(trigger bias,70.28%)
    sys(negative tag epsilon b, 6.95%)
    sys(negative tag epsilon c, 1.05%)
    sys(negative tag run period dependence,-18.53%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
