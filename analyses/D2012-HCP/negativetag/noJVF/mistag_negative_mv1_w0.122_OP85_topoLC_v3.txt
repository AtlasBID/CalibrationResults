Analysis(negative tags,light,MV1, 0.122, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.0947,0.0112)
    usys(MC statistics, 1.21%)
    sys(jet vertex fraction, 9.48%)
    sys(jet energy scale,-4.41%)
    sys(jet energy resolution,-1.45%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.60%)
    sys(track multiplicity, 8.92%)
    sys(long lived particles, 3.47%)
    sys(fake tracks,-0.43%)
    sys(photon conversions, 3.61%)
    sys(trigger bias, 3.80%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 2.96%)
    sys(negative tag run period dependence, 0.75%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1764,0.0073)
    usys(MC statistics, 0.66%)
    sys(jet vertex fraction, 0.89%)
    sys(jet energy scale,-0.33%)
    sys(jet energy resolution,-1.00%)
    sys(b-fractions, 0.30%)
    sys(c-fractions, 1.30%)
    sys(track multiplicity, 4.47%)
    sys(long lived particles,-0.86%)
    sys(fake tracks,-0.63%)
    sys(photon conversions, 5.90%)
    sys(trigger bias, 3.80%)
    sys(negative tag epsilon b, 1.10%)
    sys(negative tag epsilon c, 3.92%)
    sys(negative tag run period dependence, 2.13%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1664,0.0040)
    usys(MC statistics, 0.45%)
    sys(jet vertex fraction, 0.55%)
    sys(jet energy scale, 0.23%)
    sys(jet energy resolution, 0.16%)
    sys(b-fractions, 0.66%)
    sys(c-fractions, 2.39%)
    sys(track multiplicity, 4.14%)
    sys(long lived particles, 1.00%)
    sys(fake tracks,-0.83%)
    sys(photon conversions, 8.43%)
    sys(trigger bias, 4.46%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 5.73%)
    sys(negative tag run period dependence,-2.05%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2194,0.0025)
    usys(MC statistics, 0.58%)
    sys(jet vertex fraction, 0.10%)
    sys(jet energy scale, 0.60%)
    sys(jet energy resolution, 0.27%)
    sys(b-fractions, 0.89%)
    sys(c-fractions, 3.15%)
    sys(track multiplicity, 2.74%)
    sys(long lived particles, 1.28%)
    sys(fake tracks,-0.25%)
    sys(photon conversions,10.03%)
    sys(trigger bias, 4.02%)
    sys(negative tag epsilon b, 2.41%)
    sys(negative tag epsilon c, 6.78%)
    sys(negative tag run period dependence,-4.79%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.2729,0.0023)
    usys(MC statistics, 0.45%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale, 0.65%)
    sys(jet energy resolution, 0.06%)
    sys(b-fractions, 0.82%)
    sys(c-fractions, 2.90%)
    sys(track multiplicity, 3.36%)
    sys(long lived particles,-0.98%)
    sys(fake tracks, 0.84%)
    sys(photon conversions, 9.38%)
    sys(trigger bias, 2.38%)
    sys(negative tag epsilon b, 2.21%)
    sys(negative tag epsilon c, 6.58%)
    sys(negative tag run period dependence,-5.67%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2054,0.0015)
    usys(MC statistics, 0.66%)
    sys(jet vertex fraction, 0.07%)
    sys(jet energy scale, 1.45%)
    sys(jet energy resolution, 1.87%)
    sys(b-fractions, 0.67%)
    sys(c-fractions, 2.64%)
    sys(track multiplicity, 6.21%)
    sys(long lived particles,-0.76%)
    sys(fake tracks, 2.25%)
    sys(photon conversions, 8.14%)
    sys(trigger bias, 0.69%)
    sys(negative tag epsilon b, 2.15%)
    sys(negative tag epsilon c, 6.76%)
    sys(negative tag run period dependence,-0.77%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1206,0.0130)
    usys(MC statistics, 1.26%)
    sys(jet vertex fraction, 3.92%)
    sys(jet energy scale,-5.98%)
    sys(jet energy resolution,-0.99%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.27%)
    sys(track multiplicity, 6.88%)
    sys(long lived particles, 2.72%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 2.29%)
    sys(trigger bias, 3.57%)
    sys(negative tag epsilon b, 0.64%)
    sys(negative tag epsilon c, 2.45%)
    sys(negative tag run period dependence, 2.94%)
    sys(d0 and z0 smearing, 0.54%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2135,0.0091)
    usys(MC statistics, 0.75%)
    sys(jet vertex fraction,-0.43%)
    sys(jet energy scale,-0.94%)
    sys(jet energy resolution,-2.74%)
    sys(b-fractions, 0.15%)
    sys(c-fractions, 0.68%)
    sys(track multiplicity, 4.33%)
    sys(long lived particles,-0.67%)
    sys(fake tracks,-0.50%)
    sys(photon conversions, 3.77%)
    sys(trigger bias, 3.57%)
    sys(negative tag epsilon b, 0.77%)
    sys(negative tag epsilon c, 3.01%)
    sys(negative tag run period dependence, 3.42%)
    sys(d0 and z0 smearing,-0.08%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1756,0.0054)
    usys(MC statistics, 0.60%)
    sys(jet vertex fraction, 0.38%)
    sys(jet energy scale, 0.33%)
    sys(jet energy resolution, 0.03%)
    sys(b-fractions, 0.39%)
    sys(c-fractions, 1.42%)
    sys(track multiplicity, 4.96%)
    sys(long lived particles, 0.74%)
    sys(fake tracks,-0.67%)
    sys(photon conversions, 5.48%)
    sys(trigger bias, 6.03%)
    sys(negative tag epsilon b, 1.33%)
    sys(negative tag epsilon c, 4.12%)
    sys(negative tag run period dependence,-2.62%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2576,0.0042)
    usys(MC statistics, 0.85%)
    sys(jet vertex fraction,-0.07%)
    sys(jet energy scale,-0.49%)
    sys(jet energy resolution, 0.82%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 1.89%)
    sys(track multiplicity, 3.40%)
    sys(long lived particles, 1.02%)
    sys(fake tracks,-0.20%)
    sys(photon conversions, 6.64%)
    sys(trigger bias, 5.73%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 4.71%)
    sys(negative tag run period dependence,-4.99%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3143,0.0048)
    usys(MC statistics, 0.82%)
    sys(jet vertex fraction, 0.06%)
    sys(jet energy scale, 0.77%)
    sys(jet energy resolution,-0.18%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 2.05%)
    sys(track multiplicity, 4.24%)
    sys(long lived particles,-0.79%)
    sys(fake tracks, 0.58%)
    sys(photon conversions, 6.53%)
    sys(trigger bias, 2.25%)
    sys(negative tag epsilon b, 1.38%)
    sys(negative tag epsilon c, 4.83%)
    sys(negative tag run period dependence,-4.84%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3372,0.0038)
    usys(MC statistics, 1.07%)
    sys(jet vertex fraction, 0.34%)
    sys(jet energy scale,-2.17%)
    sys(jet energy resolution,-4.53%)
    sys(b-fractions, 0.38%)
    sys(c-fractions, 1.63%)
    sys(track multiplicity, 6.68%)
    sys(long lived particles,-0.68%)
    sys(fake tracks, 1.49%)
    sys(photon conversions, 6.12%)
    sys(trigger bias, 5.71%)
    sys(negative tag epsilon b, 1.26%)
    sys(negative tag epsilon c, 4.62%)
    sys(negative tag run period dependence,-4.53%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
