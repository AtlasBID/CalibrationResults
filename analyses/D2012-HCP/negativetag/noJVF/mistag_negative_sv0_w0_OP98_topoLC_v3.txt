Analysis(negative tags,light,SV0,  0.00, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(0.9996,0.0006)
    usys(MC statistics, 0.06%)
    sys(jet vertex fraction,-0.30%)
    sys(jet energy scale,-0.10%)
    sys(jet energy resolution,-0.01%)
    sys(b-fractions,-0.09%)
    sys(c-fractions,-0.18%)
    sys(track multiplicity,-0.34%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.00%)
    sys(trigger bias,-0.43%)
    sys(negative tag epsilon b, 0.31%)
    sys(negative tag epsilon c, 2.04%)
    sys(negative tag run period dependence, 0.07%)
    sys(d0 and z0 smearing, 0.01%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(0.9958,0.0004)
    usys(MC statistics, 0.04%)
    sys(jet vertex fraction,-0.15%)
    sys(jet energy scale,-0.07%)
    sys(jet energy resolution,-0.15%)
    sys(b-fractions,-0.16%)
    sys(c-fractions,-0.32%)
    sys(track multiplicity,-0.28%)
    sys(long lived particles, 0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.00%)
    sys(trigger bias,-0.43%)
    sys(negative tag epsilon b, 0.26%)
    sys(negative tag epsilon c, 2.13%)
    sys(negative tag run period dependence,-0.28%)
    sys(d0 and z0 smearing,-0.01%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(0.9903,0.0003)
    usys(MC statistics, 0.04%)
    sys(jet vertex fraction,-0.05%)
    sys(jet energy scale,-0.02%)
    sys(jet energy resolution,-0.03%)
    sys(b-fractions,-0.24%)
    sys(c-fractions,-0.51%)
    sys(track multiplicity,-0.21%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.59%)
    sys(negative tag epsilon b, 0.24%)
    sys(negative tag epsilon c, 2.38%)
    sys(negative tag run period dependence, 0.04%)
    sys(d0 and z0 smearing,-0.06%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(0.9860,0.0002)
    usys(MC statistics, 0.05%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale,-0.09%)
    sys(jet energy resolution,-0.04%)
    sys(b-fractions,-0.23%)
    sys(c-fractions,-0.58%)
    sys(track multiplicity,-0.14%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.81%)
    sys(negative tag epsilon b, 0.27%)
    sys(negative tag epsilon c, 2.52%)
    sys(negative tag run period dependence, 0.40%)
    sys(d0 and z0 smearing, 0.11%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(0.9848,0.0003)
    usys(MC statistics, 0.05%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale, 0.01%)
    sys(jet energy resolution, 0.02%)
    sys(b-fractions,-0.19%)
    sys(c-fractions,-0.57%)
    sys(track multiplicity,-0.16%)
    sys(long lived particles, 0.00%)
    sys(fake tracks,-0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.44%)
    sys(negative tag epsilon b, 0.33%)
    sys(negative tag epsilon c, 2.67%)
    sys(negative tag run period dependence, 0.87%)
    sys(d0 and z0 smearing, 0.02%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(0.9955,0.0002)
    usys(MC statistics, 0.07%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale,-0.11%)
    sys(jet energy resolution, 0.00%)
    sys(b-fractions,-0.16%)
    sys(c-fractions,-0.51%)
    sys(track multiplicity,-0.34%)
    sys(long lived particles, 0.00%)
    sys(fake tracks,-0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.48%)
    sys(negative tag epsilon b, 0.39%)
    sys(negative tag epsilon c, 2.86%)
    sys(negative tag run period dependence, 0.03%)
    sys(d0 and z0 smearing,-0.20%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(0.9984,0.0006)
    usys(MC statistics, 0.05%)
    sys(jet vertex fraction,-0.12%)
    sys(jet energy scale,-0.09%)
    sys(jet energy resolution,-0.10%)
    sys(b-fractions,-0.06%)
    sys(c-fractions,-0.11%)
    sys(track multiplicity,-0.28%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.00%)
    sys(trigger bias,-0.31%)
    sys(negative tag epsilon b, 0.34%)
    sys(negative tag epsilon c, 2.01%)
    sys(negative tag run period dependence,-0.10%)
    sys(d0 and z0 smearing,-0.04%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(0.9949,0.0004)
    usys(MC statistics, 0.04%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale,-0.05%)
    sys(jet energy resolution,-0.06%)
    sys(b-fractions,-0.11%)
    sys(c-fractions,-0.18%)
    sys(track multiplicity,-0.26%)
    sys(long lived particles, 0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.00%)
    sys(trigger bias,-0.31%)
    sys(negative tag epsilon b, 0.26%)
    sys(negative tag epsilon c, 2.03%)
    sys(negative tag run period dependence,-0.29%)
    sys(d0 and z0 smearing,-0.04%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(0.9920,0.0003)
    usys(MC statistics, 0.04%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale,-0.00%)
    sys(jet energy resolution,-0.02%)
    sys(b-fractions,-0.15%)
    sys(c-fractions,-0.31%)
    sys(track multiplicity,-0.30%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.57%)
    sys(negative tag epsilon b, 0.23%)
    sys(negative tag epsilon c, 2.07%)
    sys(negative tag run period dependence, 0.00%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(0.9900,0.0003)
    usys(MC statistics, 0.05%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale, 0.11%)
    sys(jet energy resolution,-0.01%)
    sys(b-fractions,-0.13%)
    sys(c-fractions,-0.33%)
    sys(track multiplicity,-0.18%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.75%)
    sys(negative tag epsilon b, 0.20%)
    sys(negative tag epsilon c, 1.97%)
    sys(negative tag run period dependence, 0.04%)
    sys(d0 and z0 smearing, 0.17%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(0.9872,0.0004)
    usys(MC statistics, 0.07%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale, 0.03%)
    sys(jet energy resolution, 0.04%)
    sys(b-fractions,-0.10%)
    sys(c-fractions,-0.36%)
    sys(track multiplicity,-0.19%)
    sys(long lived particles, 0.00%)
    sys(fake tracks,-0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.44%)
    sys(negative tag epsilon b, 0.23%)
    sys(negative tag epsilon c, 1.95%)
    sys(negative tag run period dependence, 0.49%)
    sys(d0 and z0 smearing,-0.02%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(0.9924,0.0003)
    usys(MC statistics, 0.10%)
    sys(jet vertex fraction,-0.03%)
    sys(jet energy scale, 0.17%)
    sys(jet energy resolution, 0.04%)
    sys(b-fractions,-0.08%)
    sys(c-fractions,-0.32%)
    sys(track multiplicity,-0.27%)
    sys(long lived particles, 0.00%)
    sys(fake tracks,-0.00%)
    sys(photon conversions,-0.01%)
    sys(trigger bias,-0.77%)
    sys(negative tag epsilon b, 0.26%)
    sys(negative tag epsilon c, 2.02%)
    sys(negative tag run period dependence, 0.09%)
    sys(d0 and z0 smearing, 0.02%)
  }
}
