Analysis(negative tags,light,SV0,  5.70, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4761,0.1789)
    usys(MC statistics,10.79%)
    sys(jet vertex fraction,12.39%)
    sys(jet energy scale, 7.30%)
    sys(jet energy resolution,-19.56%)
    sys(b-fractions, 0.10%)
    sys(c-fractions,-0.60%)
    sys(track multiplicity, 4.86%)
    sys(long lived particles, 1.11%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.81%)
    sys(trigger bias, 5.55%)
    sys(negative tag epsilon b, 0.77%)
    sys(negative tag epsilon c, 1.50%)
    sys(negative tag run period dependence,18.59%)
    sys(d0 and z0 smearing, 0.77%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1827,0.0552)
    usys(MC statistics, 5.19%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale, 4.25%)
    sys(jet energy resolution, 6.24%)
    sys(b-fractions, 0.22%)
    sys(c-fractions, 0.02%)
    sys(track multiplicity, 0.94%)
    sys(long lived particles,-0.22%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.91%)
    sys(trigger bias, 5.55%)
    sys(negative tag epsilon b, 0.97%)
    sys(negative tag epsilon c, 2.42%)
    sys(negative tag run period dependence,18.59%)
    sys(d0 and z0 smearing, 0.77%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0132,0.0220)
    usys(MC statistics, 2.61%)
    sys(jet vertex fraction, 0.66%)
    sys(jet energy scale, 0.06%)
    sys(jet energy resolution, 1.95%)
    sys(b-fractions, 0.67%)
    sys(c-fractions, 1.88%)
    sys(track multiplicity,-1.78%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 1.11%)
    sys(trigger bias,15.88%)
    sys(negative tag epsilon b, 2.02%)
    sys(negative tag epsilon c, 5.06%)
    sys(negative tag run period dependence, 3.51%)
    sys(d0 and z0 smearing,-0.38%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1091,0.0131)
    usys(MC statistics, 3.91%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale, 5.94%)
    sys(jet energy resolution, 5.57%)
    sys(b-fractions, 1.38%)
    sys(c-fractions, 3.55%)
    sys(track multiplicity, 0.49%)
    sys(long lived particles, 0.27%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 1.21%)
    sys(trigger bias, 8.82%)
    sys(negative tag epsilon b, 3.33%)
    sys(negative tag epsilon c, 7.08%)
    sys(negative tag run period dependence,-7.60%)
    sys(d0 and z0 smearing,-0.38%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1524,0.0112)
    usys(MC statistics, 2.39%)
    sys(jet vertex fraction, 0.99%)
    sys(jet energy scale, 0.86%)
    sys(jet energy resolution, 0.75%)
    sys(b-fractions, 1.09%)
    sys(c-fractions, 3.80%)
    sys(track multiplicity, 2.12%)
    sys(long lived particles,-0.20%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 1.02%)
    sys(trigger bias,15.20%)
    sys(negative tag epsilon b, 2.70%)
    sys(negative tag epsilon c, 7.62%)
    sys(negative tag run period dependence,-13.50%)
    sys(d0 and z0 smearing,-5.21%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.0274,0.0077)
    usys(MC statistics, 2.16%)
    sys(jet vertex fraction, 0.20%)
    sys(jet energy scale,-5.31%)
    sys(jet energy resolution, 0.27%)
    sys(b-fractions, 0.94%)
    sys(c-fractions, 4.10%)
    sys(track multiplicity, 4.89%)
    sys(long lived particles,-0.16%)
    sys(fake tracks, 0.09%)
    sys(photon conversions, 0.92%)
    sys(trigger bias,15.20%)
    sys(negative tag epsilon b, 2.82%)
    sys(negative tag epsilon c, 8.83%)
    sys(negative tag run period dependence, 0.14%)
    sys(d0 and z0 smearing,-5.21%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.6400,0.2695)
    usys(MC statistics,14.65%)
    sys(jet vertex fraction,-2.84%)
    sys(jet energy scale, 8.97%)
    sys(jet energy resolution,-12.08%)
    sys(b-fractions,-0.10%)
    sys(c-fractions, 0.07%)
    sys(track multiplicity, 5.57%)
    sys(long lived particles, 1.36%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.55%)
    sys(trigger bias,-1.55%)
    sys(negative tag epsilon b, 0.25%)
    sys(negative tag epsilon c, 2.24%)
    sys(negative tag run period dependence,19.92%)
    sys(d0 and z0 smearing, 2.20%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3495,0.0920)
    usys(MC statistics, 6.70%)
    sys(jet vertex fraction,-1.97%)
    sys(jet energy scale,-1.52%)
    sys(jet energy resolution,-7.05%)
    sys(b-fractions, 0.01%)
    sys(c-fractions, 0.59%)
    sys(track multiplicity, 0.01%)
    sys(long lived particles,-0.26%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.64%)
    sys(trigger bias,-1.55%)
    sys(negative tag epsilon b, 0.49%)
    sys(negative tag epsilon c, 2.93%)
    sys(negative tag run period dependence,19.92%)
    sys(d0 and z0 smearing, 2.20%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1394,0.0358)
    usys(MC statistics, 3.66%)
    sys(jet vertex fraction, 0.25%)
    sys(jet energy scale, 1.06%)
    sys(jet energy resolution, 4.66%)
    sys(b-fractions, 0.42%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity, 0.96%)
    sys(long lived particles, 0.20%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 0.70%)
    sys(trigger bias,15.63%)
    sys(negative tag epsilon b, 1.40%)
    sys(negative tag epsilon c, 3.67%)
    sys(negative tag run period dependence,15.35%)
    sys(d0 and z0 smearing,-8.44%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.1603,0.0229)
    usys(MC statistics, 3.29%)
    sys(jet vertex fraction,-0.69%)
    sys(jet energy scale,-4.99%)
    sys(jet energy resolution,-3.26%)
    sys(b-fractions, 0.71%)
    sys(c-fractions, 2.05%)
    sys(track multiplicity, 1.76%)
    sys(long lived particles, 0.23%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.68%)
    sys(trigger bias,15.63%)
    sys(negative tag epsilon b, 1.84%)
    sys(negative tag epsilon c, 4.80%)
    sys(negative tag run period dependence,-5.99%)
    sys(d0 and z0 smearing,-8.44%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.1622,0.0232)
    usys(MC statistics, 4.84%)
    sys(jet vertex fraction,-0.04%)
    sys(jet energy scale, 2.81%)
    sys(jet energy resolution, 8.02%)
    sys(b-fractions, 0.77%)
    sys(c-fractions, 1.84%)
    sys(track multiplicity, 2.95%)
    sys(long lived particles,-0.16%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.59%)
    sys(trigger bias, 2.79%)
    sys(negative tag epsilon b, 1.94%)
    sys(negative tag epsilon c, 4.53%)
    sys(negative tag run period dependence,-18.89%)
    sys(d0 and z0 smearing,-5.18%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.0934,0.0189)
    usys(MC statistics,10.20%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale,-6.84%)
    sys(jet energy resolution,-3.03%)
    sys(b-fractions, 0.42%)
    sys(c-fractions, 1.65%)
    sys(track multiplicity, 8.59%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 0.53%)
    sys(trigger bias,-2.09%)
    sys(negative tag epsilon b, 1.39%)
    sys(negative tag epsilon c, 4.72%)
    sys(negative tag run period dependence, 3.26%)
    sys(d0 and z0 smearing,-5.18%)
  }
}
