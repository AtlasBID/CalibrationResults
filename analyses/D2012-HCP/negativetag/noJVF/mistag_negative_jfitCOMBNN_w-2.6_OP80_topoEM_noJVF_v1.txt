Analysis(negative tags,light,JetFitterCOMBNN, -2.60, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1285,0.0191)
    usys(MC statistics, 3.93%)
    sys(jet energy scale,-9.98%)
    sys(jet energy resolution, 5.64%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 0.07%)
    sys(track multiplicity, 0.70%)
    sys(long lived particles, 6.98%)
    sys(fake tracks, 6.67%)
    sys(photon conversions, 9.10%)
    sys(trigger bias, 6.32%)
    sys(negative tag epsilon b, 1.12%)
    sys(negative tag epsilon c, 2.25%)
    sys(negative tag run period dependence,-2.31%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2871,0.0127)
    usys(MC statistics, 2.20%)
    sys(jet energy scale,-2.32%)
    sys(jet energy resolution,-4.61%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.56%)
    sys(track multiplicity, 1.57%)
    sys(long lived particles, 6.83%)
    sys(fake tracks,-1.76%)
    sys(photon conversions,-1.73%)
    sys(trigger bias, 6.32%)
    sys(negative tag epsilon b, 0.87%)
    sys(negative tag epsilon c, 3.02%)
    sys(negative tag run period dependence, 4.55%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2143,0.0069)
    usys(MC statistics, 0.91%)
    sys(jet energy scale,-0.34%)
    sys(jet energy resolution, 0.24%)
    sys(b-fractions, 0.71%)
    sys(c-fractions, 2.65%)
    sys(track multiplicity, 3.18%)
    sys(long lived particles, 4.87%)
    sys(fake tracks, 2.96%)
    sys(photon conversions, 5.33%)
    sys(trigger bias, 7.05%)
    sys(negative tag epsilon b, 2.04%)
    sys(negative tag epsilon c, 5.95%)
    sys(negative tag run period dependence,-2.93%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2887,0.0052)
    usys(MC statistics, 1.18%)
    sys(jet energy scale, 1.67%)
    sys(jet energy resolution, 0.30%)
    sys(b-fractions, 1.27%)
    sys(c-fractions, 4.17%)
    sys(track multiplicity, 6.34%)
    sys(long lived particles, 2.59%)
    sys(fake tracks, 3.07%)
    sys(photon conversions, 2.09%)
    sys(trigger bias, 7.52%)
    sys(negative tag epsilon b, 3.26%)
    sys(negative tag epsilon c, 8.03%)
    sys(negative tag run period dependence,-2.29%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3097,0.0056)
    usys(MC statistics, 0.74%)
    sys(jet energy scale, 0.28%)
    sys(jet energy resolution, 0.12%)
    sys(b-fractions, 1.26%)
    sys(c-fractions, 3.92%)
    sys(track multiplicity, 9.74%)
    sys(long lived particles,-0.05%)
    sys(fake tracks, 4.13%)
    sys(photon conversions, 0.52%)
    sys(trigger bias, 4.15%)
    sys(negative tag epsilon b, 3.16%)
    sys(negative tag epsilon c, 7.80%)
    sys(negative tag run period dependence,-7.06%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2507,0.0050)
    usys(MC statistics, 1.21%)
    sys(jet energy scale, 1.07%)
    sys(jet energy resolution, 0.37%)
    sys(b-fractions, 1.00%)
    sys(c-fractions, 3.64%)
    sys(track multiplicity,10.20%)
    sys(long lived particles,-0.05%)
    sys(fake tracks, 4.47%)
    sys(photon conversions, 1.87%)
    sys(trigger bias, 4.36%)
    sys(negative tag epsilon b, 3.02%)
    sys(negative tag epsilon c, 8.00%)
    sys(negative tag run period dependence,-2.54%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2194,0.0212)
    usys(MC statistics, 3.64%)
    sys(jet energy scale,-10.95%)
    sys(jet energy resolution,-1.36%)
    sys(b-fractions, 0.15%)
    sys(c-fractions,-0.03%)
    sys(track multiplicity, 1.07%)
    sys(long lived particles, 1.53%)
    sys(fake tracks, 7.40%)
    sys(photon conversions, 2.66%)
    sys(trigger bias, 2.50%)
    sys(negative tag epsilon b, 0.77%)
    sys(negative tag epsilon c, 2.09%)
    sys(negative tag run period dependence, 0.85%)
    sys(d0 and z0 smearing, 0.03%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2231,0.0135)
    usys(MC statistics, 2.64%)
    sys(jet energy scale,-3.39%)
    sys(jet energy resolution, 0.64%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.49%)
    sys(track multiplicity, 1.51%)
    sys(long lived particles, 5.48%)
    sys(fake tracks,-1.31%)
    sys(photon conversions, 2.64%)
    sys(trigger bias, 2.50%)
    sys(negative tag epsilon b, 0.63%)
    sys(negative tag epsilon c, 2.76%)
    sys(negative tag run period dependence,-2.02%)
    sys(d0 and z0 smearing,-0.89%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1959,0.0085)
    usys(MC statistics, 1.14%)
    sys(jet energy scale,-0.84%)
    sys(jet energy resolution, 1.02%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 1.23%)
    sys(track multiplicity, 3.17%)
    sys(long lived particles, 0.46%)
    sys(fake tracks, 0.23%)
    sys(photon conversions, 1.31%)
    sys(trigger bias, 6.33%)
    sys(negative tag epsilon b, 1.25%)
    sys(negative tag epsilon c, 3.87%)
    sys(negative tag run period dependence, 0.03%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2864,0.0078)
    usys(MC statistics, 1.73%)
    sys(jet energy scale, 1.77%)
    sys(jet energy resolution,-1.00%)
    sys(b-fractions, 0.61%)
    sys(c-fractions, 2.16%)
    sys(track multiplicity, 5.53%)
    sys(long lived particles,-0.56%)
    sys(fake tracks, 0.57%)
    sys(photon conversions, 1.32%)
    sys(trigger bias, 7.94%)
    sys(negative tag epsilon b, 1.72%)
    sys(negative tag epsilon c, 5.08%)
    sys(negative tag run period dependence,-3.23%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3188,0.0105)
    usys(MC statistics, 1.32%)
    sys(jet energy scale, 0.56%)
    sys(jet energy resolution, 1.45%)
    sys(b-fractions, 0.68%)
    sys(c-fractions, 2.46%)
    sys(track multiplicity, 8.39%)
    sys(long lived particles,-1.94%)
    sys(fake tracks, 0.61%)
    sys(photon conversions, 0.82%)
    sys(trigger bias, 4.12%)
    sys(negative tag epsilon b, 1.81%)
    sys(negative tag epsilon c, 5.50%)
    sys(negative tag run period dependence,-6.68%)
    sys(d0 and z0 smearing, 1.89%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3243,0.0116)
    usys(MC statistics, 1.84%)
    sys(jet energy scale,-0.96%)
    sys(jet energy resolution,-0.43%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 1.52%)
    sys(track multiplicity, 8.69%)
    sys(long lived particles,-3.20%)
    sys(fake tracks, 0.56%)
    sys(photon conversions, 0.62%)
    sys(trigger bias,18.02%)
    sys(negative tag epsilon b, 1.64%)
    sys(negative tag epsilon c, 4.48%)
    sys(negative tag run period dependence,-5.83%)
    sys(d0 and z0 smearing, 1.89%)
  }
}
