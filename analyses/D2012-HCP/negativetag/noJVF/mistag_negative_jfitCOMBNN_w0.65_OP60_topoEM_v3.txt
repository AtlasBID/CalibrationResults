Analysis(negative tags,light,JetFitterCOMBNN,  0.65, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.7301,0.1584)
    usys(MC statistics,10.85%)
    sys(jet vertex fraction,-1.67%)
    sys(jet energy scale,-7.49%)
    sys(jet energy resolution,-6.33%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 1.46%)
    sys(track multiplicity, 6.39%)
    sys(long lived particles, 6.53%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 4.51%)
    sys(trigger bias,15.93%)
    sys(negative tag epsilon b, 2.44%)
    sys(negative tag epsilon c, 3.86%)
    sys(negative tag run period dependence, 2.92%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.5202,0.0782)
    usys(MC statistics, 6.76%)
    sys(jet vertex fraction, 0.21%)
    sys(jet energy scale, 6.17%)
    sys(jet energy resolution,-0.50%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 3.29%)
    sys(track multiplicity, 2.31%)
    sys(long lived particles,-1.50%)
    sys(fake tracks,-0.74%)
    sys(photon conversions, 7.50%)
    sys(trigger bias,15.93%)
    sys(negative tag epsilon b, 2.92%)
    sys(negative tag epsilon c, 5.92%)
    sys(negative tag run period dependence, 2.92%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3972,0.0363)
    usys(MC statistics, 3.55%)
    sys(jet vertex fraction, 1.48%)
    sys(jet energy scale, 1.21%)
    sys(jet energy resolution, 0.22%)
    sys(b-fractions, 3.36%)
    sys(c-fractions, 5.71%)
    sys(track multiplicity,10.60%)
    sys(long lived particles, 1.87%)
    sys(fake tracks,-1.36%)
    sys(photon conversions,13.54%)
    sys(trigger bias,10.45%)
    sys(negative tag epsilon b, 7.36%)
    sys(negative tag epsilon c, 9.03%)
    sys(negative tag run period dependence, 3.79%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4830,0.0261)
    usys(MC statistics, 3.57%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale,-3.15%)
    sys(jet energy resolution, 3.67%)
    sys(b-fractions, 5.55%)
    sys(c-fractions, 5.38%)
    sys(track multiplicity, 8.08%)
    sys(long lived particles, 2.74%)
    sys(fake tracks,-0.51%)
    sys(photon conversions,17.72%)
    sys(trigger bias,15.37%)
    sys(negative tag epsilon b,11.47%)
    sys(negative tag epsilon c, 8.00%)
    sys(negative tag run period dependence,-13.48%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.6412,0.0258)
    usys(MC statistics, 4.63%)
    sys(jet vertex fraction, 0.34%)
    sys(jet energy scale, 3.50%)
    sys(jet energy resolution, 9.78%)
    sys(b-fractions, 6.08%)
    sys(c-fractions, 4.81%)
    sys(track multiplicity,17.82%)
    sys(long lived particles,-1.64%)
    sys(fake tracks, 1.55%)
    sys(photon conversions,17.47%)
    sys(trigger bias,21.25%)
    sys(negative tag epsilon b,12.31%)
    sys(negative tag epsilon c, 7.19%)
    sys(negative tag run period dependence,-4.29%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2173,0.0159)
    usys(MC statistics, 5.24%)
    sys(jet vertex fraction, 0.07%)
    sys(jet energy scale, 3.25%)
    sys(jet energy resolution, 5.29%)
    sys(b-fractions, 5.36%)
    sys(c-fractions, 5.16%)
    sys(track multiplicity,17.82%)
    sys(long lived particles,-1.58%)
    sys(fake tracks, 3.90%)
    sys(photon conversions,15.74%)
    sys(trigger bias,21.25%)
    sys(negative tag epsilon b,13.15%)
    sys(negative tag epsilon c, 8.54%)
    sys(negative tag run period dependence,-4.29%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1311,0.1296)
    usys(MC statistics,19.48%)
    sys(jet vertex fraction, 3.90%)
    sys(jet energy scale,-3.44%)
    sys(jet energy resolution,11.78%)
    sys(b-fractions, 0.17%)
    sys(c-fractions,-0.37%)
    sys(track multiplicity,-1.78%)
    sys(long lived particles, 4.32%)
    sys(fake tracks,-0.92%)
    sys(photon conversions, 3.20%)
    sys(trigger bias,-1.37%)
    sys(negative tag epsilon b, 0.86%)
    sys(negative tag epsilon c, 1.63%)
    sys(negative tag run period dependence,21.35%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4539,0.1008)
    usys(MC statistics, 7.37%)
    sys(jet vertex fraction, 3.90%)
    sys(jet energy scale,-1.65%)
    sys(jet energy resolution,11.78%)
    sys(b-fractions, 0.59%)
    sys(c-fractions, 1.03%)
    sys(track multiplicity, 6.72%)
    sys(long lived particles,-0.96%)
    sys(fake tracks,-0.49%)
    sys(photon conversions, 3.97%)
    sys(trigger bias,-1.37%)
    sys(negative tag epsilon b, 1.61%)
    sys(negative tag epsilon c, 3.33%)
    sys(negative tag run period dependence,21.35%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.5101,0.0654)
    usys(MC statistics, 6.16%)
    sys(jet vertex fraction, 2.33%)
    sys(jet energy scale, 0.91%)
    sys(jet energy resolution, 0.17%)
    sys(b-fractions, 1.93%)
    sys(c-fractions, 3.73%)
    sys(track multiplicity,10.14%)
    sys(long lived particles, 1.68%)
    sys(fake tracks,-1.11%)
    sys(photon conversions, 8.34%)
    sys(trigger bias,21.88%)
    sys(negative tag epsilon b, 4.54%)
    sys(negative tag epsilon c, 6.70%)
    sys(negative tag run period dependence, 1.74%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4535,0.0582)
    usys(MC statistics, 8.72%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale,-7.79%)
    sys(jet energy resolution,16.09%)
    sys(b-fractions, 3.90%)
    sys(c-fractions, 4.43%)
    sys(track multiplicity,10.23%)
    sys(long lived particles, 2.04%)
    sys(fake tracks,-0.32%)
    sys(photon conversions,10.49%)
    sys(trigger bias,21.88%)
    sys(negative tag epsilon b, 8.12%)
    sys(negative tag epsilon c, 7.09%)
    sys(negative tag run period dependence,-15.93%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5446,0.0736)
    usys(MC statistics,16.28%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale,-5.64%)
    sys(jet energy resolution, 7.37%)
    sys(b-fractions, 3.96%)
    sys(c-fractions, 3.28%)
    sys(track multiplicity,12.83%)
    sys(long lived particles,-1.38%)
    sys(fake tracks, 0.83%)
    sys(photon conversions,10.61%)
    sys(trigger bias, 2.39%)
    sys(negative tag epsilon b, 8.28%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence,-15.31%)
    sys(d0 and z0 smearing,-9.06%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5096,0.0600)
    usys(MC statistics,15.07%)
    sys(jet vertex fraction,-0.39%)
    sys(jet energy scale,-15.55%)
    sys(jet energy resolution,-11.00%)
    sys(b-fractions, 2.23%)
    sys(c-fractions, 4.06%)
    sys(track multiplicity,17.84%)
    sys(long lived particles,-1.57%)
    sys(fake tracks, 2.25%)
    sys(photon conversions,10.41%)
    sys(trigger bias,-4.24%)
    sys(negative tag epsilon b, 5.72%)
    sys(negative tag epsilon c, 7.71%)
    sys(negative tag run period dependence,-6.46%)
    sys(d0 and z0 smearing,-9.06%)
  }
}
