Analysis(negative tags,light,SV0,  5.65, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3073,0.1731)
    usys(MC statistics,12.22%)
    sys(jet vertex fraction, 4.71%)
    sys(jet energy scale,-3.90%)
    sys(jet energy resolution,-11.32%)
    sys(b-fractions, 0.13%)
    sys(c-fractions, 0.34%)
    sys(track multiplicity, 6.24%)
    sys(long lived particles, 0.90%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.93%)
    sys(trigger bias, 4.64%)
    sys(negative tag epsilon b, 0.80%)
    sys(negative tag epsilon c, 2.64%)
    sys(negative tag run period dependence,22.21%)
    sys(d0 and z0 smearing, 0.77%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1475,0.0555)
    usys(MC statistics, 4.94%)
    sys(jet vertex fraction, 4.71%)
    sys(jet energy scale, 5.73%)
    sys(jet energy resolution,-0.11%)
    sys(b-fractions, 0.30%)
    sys(c-fractions, 0.26%)
    sys(track multiplicity, 1.21%)
    sys(long lived particles,-0.23%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 1.02%)
    sys(trigger bias, 4.64%)
    sys(negative tag epsilon b, 1.12%)
    sys(negative tag epsilon c, 2.70%)
    sys(negative tag run period dependence,22.21%)
    sys(d0 and z0 smearing, 0.77%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0150,0.0229)
    usys(MC statistics, 2.84%)
    sys(jet vertex fraction, 0.83%)
    sys(jet energy scale,-1.10%)
    sys(jet energy resolution, 0.09%)
    sys(b-fractions, 0.61%)
    sys(c-fractions, 1.91%)
    sys(track multiplicity,-0.75%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 1.09%)
    sys(trigger bias,10.62%)
    sys(negative tag epsilon b, 1.90%)
    sys(negative tag epsilon c, 5.14%)
    sys(negative tag run period dependence, 2.19%)
    sys(d0 and z0 smearing,-0.38%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1536,0.0141)
    usys(MC statistics, 2.16%)
    sys(jet vertex fraction,-0.14%)
    sys(jet energy scale,-1.81%)
    sys(jet energy resolution, 0.94%)
    sys(b-fractions, 1.08%)
    sys(c-fractions, 3.48%)
    sys(track multiplicity, 0.83%)
    sys(long lived particles, 0.28%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 1.17%)
    sys(trigger bias, 9.92%)
    sys(negative tag epsilon b, 2.77%)
    sys(negative tag epsilon c, 7.12%)
    sys(negative tag run period dependence,-5.43%)
    sys(d0 and z0 smearing,-0.38%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1308,0.0115)
    usys(MC statistics, 2.27%)
    sys(jet vertex fraction, 0.52%)
    sys(jet energy scale,-1.36%)
    sys(jet energy resolution,-0.06%)
    sys(b-fractions, 1.11%)
    sys(c-fractions, 4.01%)
    sys(track multiplicity, 3.06%)
    sys(long lived particles,-0.20%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 1.01%)
    sys(trigger bias, 5.30%)
    sys(negative tag epsilon b, 2.74%)
    sys(negative tag epsilon c, 7.85%)
    sys(negative tag run period dependence,-9.32%)
    sys(d0 and z0 smearing,-5.21%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.0014,0.0074)
    usys(MC statistics, 3.44%)
    sys(jet vertex fraction, 0.15%)
    sys(jet energy scale,-4.09%)
    sys(jet energy resolution, 2.31%)
    sys(b-fractions, 0.86%)
    sys(c-fractions, 3.87%)
    sys(track multiplicity, 5.00%)
    sys(long lived particles,-0.15%)
    sys(fake tracks, 0.08%)
    sys(photon conversions, 0.87%)
    sys(trigger bias, 7.51%)
    sys(negative tag epsilon b, 2.54%)
    sys(negative tag epsilon c, 8.34%)
    sys(negative tag run period dependence, 2.07%)
    sys(d0 and z0 smearing,-5.21%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3721,0.2318)
    usys(MC statistics,13.39%)
    sys(jet vertex fraction, 3.28%)
    sys(jet energy scale, 7.13%)
    sys(jet energy resolution,18.13%)
    sys(b-fractions,-0.10%)
    sys(c-fractions,-0.00%)
    sys(track multiplicity, 3.68%)
    sys(long lived particles, 1.41%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 0.51%)
    sys(trigger bias,-7.50%)
    sys(negative tag epsilon b, 0.24%)
    sys(negative tag epsilon c, 2.15%)
    sys(negative tag run period dependence,-3.59%)
    sys(d0 and z0 smearing, 2.20%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2049,0.0858)
    usys(MC statistics, 6.98%)
    sys(jet vertex fraction,-3.22%)
    sys(jet energy scale, 2.61%)
    sys(jet energy resolution,-0.80%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 1.65%)
    sys(track multiplicity, 1.31%)
    sys(long lived particles,-0.21%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.57%)
    sys(trigger bias,-7.50%)
    sys(negative tag epsilon b, 0.60%)
    sys(negative tag epsilon c, 4.19%)
    sys(negative tag run period dependence,18.59%)
    sys(d0 and z0 smearing, 2.20%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1315,0.0369)
    usys(MC statistics, 3.95%)
    sys(jet vertex fraction,-0.47%)
    sys(jet energy scale, 0.27%)
    sys(jet energy resolution, 3.21%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.05%)
    sys(track multiplicity, 2.33%)
    sys(long lived particles, 0.19%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.70%)
    sys(trigger bias, 3.31%)
    sys(negative tag epsilon b, 1.21%)
    sys(negative tag epsilon c, 3.67%)
    sys(negative tag run period dependence,14.31%)
    sys(d0 and z0 smearing,-8.44%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.0914,0.0226)
    usys(MC statistics, 3.49%)
    sys(jet vertex fraction,-0.55%)
    sys(jet energy scale,-3.10%)
    sys(jet energy resolution, 2.70%)
    sys(b-fractions, 0.70%)
    sys(c-fractions, 1.99%)
    sys(track multiplicity, 2.94%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.64%)
    sys(trigger bias,13.98%)
    sys(negative tag epsilon b, 1.84%)
    sys(negative tag epsilon c, 4.78%)
    sys(negative tag run period dependence,-3.65%)
    sys(d0 and z0 smearing,-8.44%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.1393,0.0238)
    usys(MC statistics, 4.35%)
    sys(jet vertex fraction,-0.06%)
    sys(jet energy scale, 2.79%)
    sys(jet energy resolution, 1.97%)
    sys(b-fractions, 0.80%)
    sys(c-fractions, 2.41%)
    sys(track multiplicity, 3.69%)
    sys(long lived particles,-0.17%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.59%)
    sys(trigger bias, 4.89%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 5.22%)
    sys(negative tag run period dependence,-9.88%)
    sys(d0 and z0 smearing,-5.18%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.1486,0.0194)
    usys(MC statistics, 6.01%)
    sys(jet vertex fraction,-0.07%)
    sys(jet energy scale,-7.06%)
    sys(jet energy resolution,-2.35%)
    sys(b-fractions, 0.69%)
    sys(c-fractions, 1.99%)
    sys(track multiplicity, 7.98%)
    sys(long lived particles,-0.12%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 0.51%)
    sys(trigger bias, 0.20%)
    sys(negative tag epsilon b, 1.93%)
    sys(negative tag epsilon c, 5.03%)
    sys(negative tag run period dependence,-1.19%)
    sys(d0 and z0 smearing,-5.18%)
  }
}
