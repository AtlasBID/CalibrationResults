Analysis(negative tags,light,MV2, 0.067, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1667,0.1384)
    usys(MC statistics,13.03%)
    sys(jet vertex fraction, 0.72%)
    sys(jet energy scale,-17.35%)
    sys(jet energy resolution, 0.58%)
    sys(b-fractions, 0.62%)
    sys(c-fractions, 0.75%)
    sys(track multiplicity, 1.32%)
    sys(long lived particles, 4.29%)
    sys(fake tracks,-0.11%)
    sys(photon conversions, 6.32%)
    sys(trigger bias, 8.27%)
    sys(negative tag epsilon b, 1.89%)
    sys(negative tag epsilon c, 3.05%)
    sys(negative tag run period dependence,11.26%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3138,0.0210)
    usys(MC statistics, 1.96%)
    sys(jet vertex fraction, 0.72%)
    sys(jet energy scale, 6.62%)
    sys(jet energy resolution, 0.58%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 1.98%)
    sys(track multiplicity, 2.16%)
    sys(long lived particles,-1.21%)
    sys(fake tracks,-0.79%)
    sys(photon conversions, 7.53%)
    sys(trigger bias, 8.27%)
    sys(negative tag epsilon b, 1.46%)
    sys(negative tag epsilon c, 4.61%)
    sys(negative tag run period dependence, 8.08%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2028,0.0078)
    usys(MC statistics, 0.85%)
    sys(jet vertex fraction, 0.73%)
    sys(jet energy scale,-2.39%)
    sys(jet energy resolution, 2.57%)
    sys(b-fractions, 0.97%)
    sys(c-fractions, 3.03%)
    sys(track multiplicity, 4.79%)
    sys(long lived particles, 1.26%)
    sys(fake tracks,-0.96%)
    sys(photon conversions,10.01%)
    sys(trigger bias,10.61%)
    sys(negative tag epsilon b, 2.59%)
    sys(negative tag epsilon c, 6.41%)
    sys(negative tag run period dependence, 0.21%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4691,0.0195)
    usys(MC statistics, 2.63%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale,-9.54%)
    sys(jet energy resolution,-10.46%)
    sys(b-fractions, 3.22%)
    sys(c-fractions, 5.75%)
    sys(track multiplicity, 4.52%)
    sys(long lived particles, 2.53%)
    sys(fake tracks,-0.33%)
    sys(photon conversions,17.05%)
    sys(trigger bias,18.41%)
    sys(negative tag epsilon b, 6.88%)
    sys(negative tag epsilon c, 9.20%)
    sys(negative tag run period dependence,11.14%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.7043,0.0405)
    usys(MC statistics, 6.83%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale,-3.63%)
    sys(jet energy resolution, 0.06%)
    sys(b-fractions, 5.68%)
    sys(c-fractions, 5.34%)
    sys(track multiplicity, 7.99%)
    sys(long lived particles,-1.55%)
    sys(fake tracks, 1.32%)
    sys(photon conversions,19.31%)
    sys(trigger bias, 9.05%)
    sys(negative tag epsilon b,11.51%)
    sys(negative tag epsilon c, 7.98%)
    sys(negative tag run period dependence,-7.13%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.1581,0.0211)
    usys(MC statistics, 7.72%)
    sys(jet vertex fraction,-0.04%)
    sys(jet energy scale, 6.91%)
    sys(jet energy resolution, 9.41%)
    sys(b-fractions, 4.69%)
    sys(c-fractions, 5.87%)
    sys(track multiplicity,15.25%)
    sys(long lived particles,-1.85%)
    sys(fake tracks, 4.04%)
    sys(photon conversions,18.29%)
    sys(trigger bias,18.58%)
    sys(negative tag epsilon b,11.56%)
    sys(negative tag epsilon c, 9.79%)
    sys(negative tag run period dependence,-7.13%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2856,0.1765)
    usys(MC statistics,14.46%)
    sys(jet vertex fraction, 6.82%)
    sys(jet energy scale, 6.26%)
    sys(jet energy resolution, 6.49%)
    sys(b-fractions, 0.08%)
    sys(c-fractions,-0.00%)
    sys(track multiplicity, 1.75%)
    sys(long lived particles, 5.07%)
    sys(fake tracks,-0.96%)
    sys(photon conversions, 3.67%)
    sys(trigger bias, 3.93%)
    sys(negative tag epsilon b, 0.65%)
    sys(negative tag epsilon c, 2.11%)
    sys(negative tag run period dependence, 4.07%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3192,0.0283)
    usys(MC statistics, 2.64%)
    sys(jet vertex fraction, 0.90%)
    sys(jet energy scale, 6.26%)
    sys(jet energy resolution, 6.49%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.36%)
    sys(track multiplicity, 3.64%)
    sys(long lived particles,-0.91%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 5.12%)
    sys(trigger bias, 3.93%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 3.79%)
    sys(negative tag run period dependence, 4.07%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2666,0.0144)
    usys(MC statistics, 1.52%)
    sys(jet vertex fraction, 1.18%)
    sys(jet energy scale,-3.16%)
    sys(jet energy resolution, 1.82%)
    sys(b-fractions, 0.68%)
    sys(c-fractions, 1.94%)
    sys(track multiplicity, 5.54%)
    sys(long lived particles, 1.15%)
    sys(fake tracks,-0.76%)
    sys(photon conversions, 6.55%)
    sys(trigger bias,12.95%)
    sys(negative tag epsilon b, 1.94%)
    sys(negative tag epsilon c, 4.73%)
    sys(negative tag run period dependence, 7.61%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.6041,0.0409)
    usys(MC statistics, 5.47%)
    sys(jet vertex fraction,-0.91%)
    sys(jet energy scale,-14.50%)
    sys(jet energy resolution,-5.37%)
    sys(b-fractions, 2.04%)
    sys(c-fractions, 4.18%)
    sys(track multiplicity, 5.23%)
    sys(long lived particles, 2.29%)
    sys(fake tracks,-0.24%)
    sys(photon conversions, 9.74%)
    sys(trigger bias,12.49%)
    sys(negative tag epsilon b, 4.44%)
    sys(negative tag epsilon c, 7.21%)
    sys(negative tag run period dependence, 6.89%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(2.2078,0.1197)
    usys(MC statistics,21.68%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale,-0.93%)
    sys(jet energy resolution,19.67%)
    sys(b-fractions, 2.19%)
    sys(c-fractions, 2.93%)
    sys(track multiplicity, 6.61%)
    sys(long lived particles,-2.29%)
    sys(fake tracks, 0.72%)
    sys(photon conversions,11.08%)
    sys(trigger bias, 0.93%)
    sys(negative tag epsilon b, 4.76%)
    sys(negative tag epsilon c, 5.58%)
    sys(negative tag run period dependence,-25.24%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.6525,0.0688)
    usys(MC statistics,13.84%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale,-11.68%)
    sys(jet energy resolution,-11.21%)
    sys(b-fractions, 1.22%)
    sys(c-fractions, 3.41%)
    sys(track multiplicity, 9.44%)
    sys(long lived particles,-1.90%)
    sys(fake tracks, 1.85%)
    sys(photon conversions,10.86%)
    sys(trigger bias, 6.96%)
    sys(negative tag epsilon b, 3.30%)
    sys(negative tag epsilon c, 7.06%)
    sys(negative tag run period dependence,-25.24%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
