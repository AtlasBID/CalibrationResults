Analysis(negative tags,light,IP3DSV1, -0.544, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2055,0.0201)
    usys(MC statistics, 1.83%)
    sys(jet vertex fraction, 6.81%)
    sys(jet energy scale, 2.33%)
    sys(jet energy resolution,-0.41%)
    sys(b-fractions, 0.44%)
    sys(c-fractions, 1.09%)
    sys(track multiplicity, 8.82%)
    sys(long lived particles, 5.00%)
    sys(fake tracks,-0.39%)
    sys(photon conversions, 3.92%)
    sys(trigger bias, 4.55%)
    sys(negative tag epsilon b, 1.49%)
    sys(negative tag epsilon c, 3.51%)
    sys(negative tag run period dependence, 6.76%)
    sys(d0 and z0 smearing, 1.22%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2208,0.0090)
    usys(MC statistics, 0.88%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale, 0.73%)
    sys(jet energy resolution, 1.28%)
    sys(b-fractions, 0.54%)
    sys(c-fractions, 1.77%)
    sys(track multiplicity, 5.65%)
    sys(long lived particles,-1.04%)
    sys(fake tracks,-0.58%)
    sys(photon conversions, 5.39%)
    sys(trigger bias, 4.55%)
    sys(negative tag epsilon b, 1.52%)
    sys(negative tag epsilon c, 4.36%)
    sys(negative tag run period dependence, 2.08%)
    sys(d0 and z0 smearing, 1.22%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2000,0.0048)
    usys(MC statistics, 0.52%)
    sys(jet vertex fraction, 0.52%)
    sys(jet energy scale, 0.40%)
    sys(jet energy resolution, 0.17%)
    sys(b-fractions, 0.91%)
    sys(c-fractions, 2.91%)
    sys(track multiplicity, 7.50%)
    sys(long lived particles, 1.26%)
    sys(fake tracks,-0.77%)
    sys(photon conversions, 7.60%)
    sys(trigger bias, 4.85%)
    sys(negative tag epsilon b, 2.48%)
    sys(negative tag epsilon c, 6.27%)
    sys(negative tag run period dependence,-2.56%)
    sys(d0 and z0 smearing, 5.14%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2506,0.0031)
    usys(MC statistics, 0.68%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale, 1.11%)
    sys(jet energy resolution,-0.24%)
    sys(b-fractions, 1.21%)
    sys(c-fractions, 3.88%)
    sys(track multiplicity, 5.03%)
    sys(long lived particles, 1.69%)
    sys(fake tracks,-0.26%)
    sys(photon conversions, 9.02%)
    sys(trigger bias, 5.96%)
    sys(negative tag epsilon b, 2.99%)
    sys(negative tag epsilon c, 7.54%)
    sys(negative tag run period dependence,-4.30%)
    sys(d0 and z0 smearing, 5.14%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3068,0.0030)
    usys(MC statistics, 0.64%)
    sys(jet vertex fraction, 0.13%)
    sys(jet energy scale, 0.54%)
    sys(jet energy resolution, 0.04%)
    sys(b-fractions, 1.24%)
    sys(c-fractions, 3.70%)
    sys(track multiplicity, 4.75%)
    sys(long lived particles,-1.23%)
    sys(fake tracks, 0.81%)
    sys(photon conversions, 8.43%)
    sys(trigger bias, 2.75%)
    sys(negative tag epsilon b, 3.00%)
    sys(negative tag epsilon c, 7.44%)
    sys(negative tag run period dependence,-6.44%)
    sys(d0 and z0 smearing, 2.98%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2410,0.0021)
    usys(MC statistics, 0.81%)
    sys(jet vertex fraction, 0.16%)
    sys(jet energy scale, 0.14%)
    sys(jet energy resolution, 1.32%)
    sys(b-fractions, 0.93%)
    sys(c-fractions, 3.04%)
    sys(track multiplicity, 8.69%)
    sys(long lived particles,-0.95%)
    sys(fake tracks, 2.24%)
    sys(photon conversions, 7.84%)
    sys(trigger bias, 5.76%)
    sys(negative tag epsilon b, 2.82%)
    sys(negative tag epsilon c, 7.37%)
    sys(negative tag run period dependence, 0.08%)
    sys(d0 and z0 smearing, 2.98%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1621,0.0202)
    usys(MC statistics, 1.74%)
    sys(jet vertex fraction, 1.33%)
    sys(jet energy scale, 1.28%)
    sys(jet energy resolution,-0.87%)
    sys(b-fractions, 0.20%)
    sys(c-fractions, 0.51%)
    sys(track multiplicity, 8.64%)
    sys(long lived particles, 3.26%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 2.17%)
    sys(trigger bias, 5.06%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 2.73%)
    sys(negative tag run period dependence,-3.78%)
    sys(d0 and z0 smearing, 3.01%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1990,0.0094)
    usys(MC statistics, 0.81%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale,-0.62%)
    sys(jet energy resolution, 1.37%)
    sys(b-fractions, 0.22%)
    sys(c-fractions, 0.81%)
    sys(track multiplicity, 6.27%)
    sys(long lived particles,-0.68%)
    sys(fake tracks,-0.48%)
    sys(photon conversions, 3.18%)
    sys(trigger bias, 5.06%)
    sys(negative tag epsilon b, 0.89%)
    sys(negative tag epsilon c, 3.14%)
    sys(negative tag run period dependence, 1.76%)
    sys(d0 and z0 smearing, 3.01%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1420,0.0051)
    usys(MC statistics, 0.55%)
    sys(jet vertex fraction, 0.44%)
    sys(jet energy scale, 0.51%)
    sys(jet energy resolution, 0.45%)
    sys(b-fractions, 0.38%)
    sys(c-fractions, 1.38%)
    sys(track multiplicity, 8.31%)
    sys(long lived particles, 0.75%)
    sys(fake tracks,-0.61%)
    sys(photon conversions, 4.32%)
    sys(trigger bias, 5.03%)
    sys(negative tag epsilon b, 1.33%)
    sys(negative tag epsilon c, 4.10%)
    sys(negative tag run period dependence,-2.63%)
    sys(d0 and z0 smearing, 2.48%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.1891,0.0038)
    usys(MC statistics, 0.86%)
    sys(jet vertex fraction, 0.04%)
    sys(jet energy scale, 0.93%)
    sys(jet energy resolution, 0.40%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 1.58%)
    sys(track multiplicity, 4.80%)
    sys(long lived particles, 0.98%)
    sys(fake tracks,-0.17%)
    sys(photon conversions, 5.19%)
    sys(trigger bias, 3.92%)
    sys(negative tag epsilon b, 1.41%)
    sys(negative tag epsilon c, 4.26%)
    sys(negative tag run period dependence,-3.48%)
    sys(d0 and z0 smearing, 2.48%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.2806,0.0047)
    usys(MC statistics, 0.90%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale, 0.56%)
    sys(jet energy resolution,-0.21%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 1.80%)
    sys(track multiplicity, 4.13%)
    sys(long lived particles,-0.81%)
    sys(fake tracks, 0.48%)
    sys(photon conversions, 5.30%)
    sys(trigger bias, 1.66%)
    sys(negative tag epsilon b, 1.37%)
    sys(negative tag epsilon c, 4.55%)
    sys(negative tag run period dependence,-5.50%)
    sys(d0 and z0 smearing,-1.03%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3061,0.0039)
    usys(MC statistics, 1.44%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale, 0.21%)
    sys(jet energy resolution,-1.00%)
    sys(b-fractions, 0.34%)
    sys(c-fractions, 1.31%)
    sys(track multiplicity, 7.02%)
    sys(long lived particles,-0.71%)
    sys(fake tracks, 1.25%)
    sys(photon conversions, 4.94%)
    sys(trigger bias, 1.52%)
    sys(negative tag epsilon b, 1.22%)
    sys(negative tag epsilon c, 4.24%)
    sys(negative tag run period dependence,-2.93%)
    sys(d0 and z0 smearing,-1.03%)
  }
}
