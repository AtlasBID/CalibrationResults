Analysis(negative tags,light,MV2, -0.098, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2446,0.0322)
    usys(MC statistics, 2.80%)
    sys(jet vertex fraction, 5.15%)
    sys(jet energy scale,-17.64%)
    sys(jet energy resolution,-10.66%)
    sys(b-fractions, 0.25%)
    sys(c-fractions, 1.10%)
    sys(track multiplicity, 3.29%)
    sys(long lived particles, 5.25%)
    sys(fake tracks,-0.54%)
    sys(photon conversions, 4.71%)
    sys(trigger bias, 2.72%)
    sys(negative tag epsilon b, 1.06%)
    sys(negative tag epsilon c, 3.56%)
    sys(negative tag run period dependence, 0.97%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1381,0.0049)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale, 7.63%)
    sys(jet energy resolution,-10.66%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.72%)
    sys(track multiplicity, 4.71%)
    sys(long lived particles,-0.60%)
    sys(fake tracks,-0.48%)
    sys(photon conversions, 4.39%)
    sys(trigger bias, 2.72%)
    sys(negative tag epsilon b, 0.87%)
    sys(negative tag epsilon c, 3.24%)
    sys(negative tag run period dependence, 1.50%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(0.9685,0.0013)
    usys(MC statistics, 0.16%)
    sys(jet vertex fraction, 0.78%)
    sys(jet energy scale,-3.19%)
    sys(jet energy resolution, 2.44%)
    sys(b-fractions, 0.01%)
    sys(c-fractions, 0.12%)
    sys(track multiplicity,11.18%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.12%)
    sys(photon conversions, 1.83%)
    sys(trigger bias, 4.41%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 3.08%)
    sys(negative tag run period dependence, 4.65%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2855,0.0047)
    usys(MC statistics, 0.93%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale,-7.02%)
    sys(jet energy resolution,-8.59%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 4.12%)
    sys(track multiplicity, 3.61%)
    sys(long lived particles, 1.63%)
    sys(fake tracks,-0.28%)
    sys(photon conversions,11.84%)
    sys(trigger bias, 9.29%)
    sys(negative tag epsilon b, 3.16%)
    sys(negative tag epsilon c, 7.80%)
    sys(negative tag run period dependence, 7.98%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4439,0.0075)
    usys(MC statistics, 1.47%)
    sys(jet vertex fraction, 0.12%)
    sys(jet energy scale, 0.78%)
    sys(jet energy resolution, 1.86%)
    sys(b-fractions, 2.36%)
    sys(c-fractions, 5.49%)
    sys(track multiplicity, 6.66%)
    sys(long lived particles,-1.56%)
    sys(fake tracks, 1.13%)
    sys(photon conversions,14.33%)
    sys(trigger bias, 6.08%)
    sys(negative tag epsilon b, 5.11%)
    sys(negative tag epsilon c, 9.27%)
    sys(negative tag run period dependence,-11.67%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2850,0.0047)
    usys(MC statistics, 1.56%)
    sys(jet vertex fraction, 0.18%)
    sys(jet energy scale, 1.44%)
    sys(jet energy resolution, 1.28%)
    sys(b-fractions, 1.83%)
    sys(c-fractions, 4.77%)
    sys(track multiplicity,12.82%)
    sys(long lived particles,-1.12%)
    sys(fake tracks, 3.35%)
    sys(photon conversions,12.90%)
    sys(trigger bias,15.80%)
    sys(negative tag epsilon b, 4.90%)
    sys(negative tag epsilon c, 9.42%)
    sys(negative tag run period dependence,-0.18%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3005,0.0390)
    usys(MC statistics, 3.28%)
    sys(jet vertex fraction, 1.53%)
    sys(jet energy scale, 7.15%)
    sys(jet energy resolution,-1.70%)
    sys(b-fractions, 0.15%)
    sys(c-fractions, 0.18%)
    sys(track multiplicity, 4.71%)
    sys(long lived particles, 4.09%)
    sys(fake tracks,-0.47%)
    sys(photon conversions, 2.97%)
    sys(trigger bias, 3.15%)
    sys(negative tag epsilon b, 0.81%)
    sys(negative tag epsilon c, 2.32%)
    sys(negative tag run period dependence,-0.99%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1620,0.0055)
    usys(MC statistics, 0.61%)
    sys(jet vertex fraction, 0.42%)
    sys(jet energy scale, 7.15%)
    sys(jet energy resolution,-1.70%)
    sys(b-fractions, 0.07%)
    sys(c-fractions, 0.32%)
    sys(track multiplicity, 4.56%)
    sys(long lived particles,-0.46%)
    sys(fake tracks,-0.43%)
    sys(photon conversions, 2.97%)
    sys(trigger bias, 3.15%)
    sys(negative tag epsilon b, 0.61%)
    sys(negative tag epsilon c, 2.60%)
    sys(negative tag run period dependence, 1.45%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.0817,0.0027)
    usys(MC statistics, 0.32%)
    sys(jet vertex fraction, 0.65%)
    sys(jet energy scale,-4.28%)
    sys(jet energy resolution, 1.83%)
    sys(b-fractions, 0.11%)
    sys(c-fractions, 0.53%)
    sys(track multiplicity, 7.64%)
    sys(long lived particles, 0.41%)
    sys(fake tracks,-0.36%)
    sys(photon conversions, 2.94%)
    sys(trigger bias, 3.45%)
    sys(negative tag epsilon b, 0.76%)
    sys(negative tag epsilon c, 3.08%)
    sys(negative tag run period dependence, 3.51%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3714,0.0085)
    usys(MC statistics, 2.20%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale,-10.48%)
    sys(jet energy resolution,-6.87%)
    sys(b-fractions, 0.86%)
    sys(c-fractions, 2.66%)
    sys(track multiplicity, 3.99%)
    sys(long lived particles, 1.33%)
    sys(fake tracks,-0.19%)
    sys(photon conversions, 7.10%)
    sys(trigger bias, 6.20%)
    sys(negative tag epsilon b, 2.13%)
    sys(negative tag epsilon c, 5.55%)
    sys(negative tag run period dependence, 6.33%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6816,0.0169)
    usys(MC statistics, 2.78%)
    sys(jet vertex fraction, 0.07%)
    sys(jet energy scale, 0.62%)
    sys(jet energy resolution, 0.92%)
    sys(b-fractions, 1.35%)
    sys(c-fractions, 3.46%)
    sys(track multiplicity, 6.48%)
    sys(long lived particles,-1.11%)
    sys(fake tracks, 0.71%)
    sys(photon conversions, 8.90%)
    sys(trigger bias, 5.87%)
    sys(negative tag epsilon b, 3.07%)
    sys(negative tag epsilon c, 6.47%)
    sys(negative tag run period dependence,-16.20%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5017,0.0116)
    usys(MC statistics, 3.89%)
    sys(jet vertex fraction, 0.55%)
    sys(jet energy scale, 1.20%)
    sys(jet energy resolution,-2.77%)
    sys(b-fractions, 1.01%)
    sys(c-fractions, 2.40%)
    sys(track multiplicity,13.25%)
    sys(long lived particles,-1.04%)
    sys(fake tracks, 1.80%)
    sys(photon conversions, 7.98%)
    sys(trigger bias,-1.97%)
    sys(negative tag epsilon b, 2.82%)
    sys(negative tag epsilon c, 5.64%)
    sys(negative tag run period dependence,-3.43%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
