Analysis(negative tags,light,SV0,  9.50, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.7337,0.4334)
    usys(MC statistics,13.94%)
    sys(jet vertex fraction, 2.67%)
    sys(jet energy scale, 9.27%)
    sys(jet energy resolution,-5.94%)
    sys(b-fractions, 1.12%)
    sys(c-fractions, 4.18%)
    sys(track multiplicity, 7.17%)
    sys(long lived particles, 0.91%)
    sys(fake tracks,-0.02%)
    sys(photon conversions, 1.01%)
    sys(trigger bias, 8.23%)
    sys(negative tag epsilon b, 3.01%)
    sys(negative tag epsilon c, 7.24%)
    sys(negative tag run period dependence,42.59%)
    sys(d0 and z0 smearing,-12.66%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2222,0.1033)
    usys(MC statistics,10.05%)
    sys(jet vertex fraction, 2.67%)
    sys(jet energy scale, 7.31%)
    sys(jet energy resolution,-5.94%)
    sys(b-fractions, 0.38%)
    sys(c-fractions, 0.90%)
    sys(track multiplicity, 2.98%)
    sys(long lived particles,-0.30%)
    sys(fake tracks,-0.02%)
    sys(photon conversions, 1.01%)
    sys(trigger bias, 8.23%)
    sys(negative tag epsilon b, 1.25%)
    sys(negative tag epsilon c, 3.43%)
    sys(negative tag run period dependence,42.59%)
    sys(d0 and z0 smearing,-12.66%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0221,0.0382)
    usys(MC statistics, 4.94%)
    sys(jet vertex fraction, 2.15%)
    sys(jet energy scale,-0.32%)
    sys(jet energy resolution, 0.52%)
    sys(b-fractions, 0.72%)
    sys(c-fractions, 1.80%)
    sys(track multiplicity,-0.55%)
    sys(long lived particles, 0.30%)
    sys(fake tracks,-0.02%)
    sys(photon conversions, 1.26%)
    sys(trigger bias,12.83%)
    sys(negative tag epsilon b, 2.14%)
    sys(negative tag epsilon c, 4.97%)
    sys(negative tag run period dependence, 0.83%)
    sys(d0 and z0 smearing,-4.01%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1402,0.0223)
    usys(MC statistics, 3.39%)
    sys(jet vertex fraction,-0.21%)
    sys(jet energy scale, 1.28%)
    sys(jet energy resolution, 0.80%)
    sys(b-fractions, 1.52%)
    sys(c-fractions, 3.87%)
    sys(track multiplicity, 1.93%)
    sys(long lived particles, 0.33%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 1.37%)
    sys(trigger bias,12.04%)
    sys(negative tag epsilon b, 3.64%)
    sys(negative tag epsilon c, 7.47%)
    sys(negative tag run period dependence,-4.50%)
    sys(d0 and z0 smearing,-4.01%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1217,0.0180)
    usys(MC statistics, 3.56%)
    sys(jet vertex fraction, 0.26%)
    sys(jet energy scale,-1.15%)
    sys(jet energy resolution,-1.51%)
    sys(b-fractions, 1.54%)
    sys(c-fractions, 4.47%)
    sys(track multiplicity, 4.18%)
    sys(long lived particles,-0.24%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 1.25%)
    sys(trigger bias,18.57%)
    sys(negative tag epsilon b, 3.56%)
    sys(negative tag epsilon c, 8.26%)
    sys(negative tag run period dependence,-12.19%)
    sys(d0 and z0 smearing,-6.55%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.0024,0.0119)
    usys(MC statistics, 3.37%)
    sys(jet vertex fraction, 0.28%)
    sys(jet energy scale,-1.68%)
    sys(jet energy resolution, 2.00%)
    sys(b-fractions, 1.33%)
    sys(c-fractions, 4.38%)
    sys(track multiplicity, 7.84%)
    sys(long lived particles,-0.19%)
    sys(fake tracks, 0.08%)
    sys(photon conversions, 1.10%)
    sys(trigger bias,18.57%)
    sys(negative tag epsilon b, 3.56%)
    sys(negative tag epsilon c, 8.84%)
    sys(negative tag run period dependence, 1.75%)
    sys(d0 and z0 smearing,-6.55%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.4663,0.5184)
    usys(MC statistics,22.22%)
    sys(jet vertex fraction, 8.13%)
    sys(jet energy scale, 8.68%)
    sys(jet energy resolution,-17.55%)
    sys(b-fractions,-0.01%)
    sys(c-fractions, 0.05%)
    sys(track multiplicity,10.07%)
    sys(long lived particles, 2.00%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.50%)
    sys(trigger bias,-6.48%)
    sys(negative tag epsilon b, 0.45%)
    sys(negative tag epsilon c, 2.20%)
    sys(negative tag run period dependence,21.68%)
    sys(d0 and z0 smearing,-2.44%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.0678,0.1557)
    usys(MC statistics,16.71%)
    sys(jet vertex fraction,-2.86%)
    sys(jet energy scale,12.75%)
    sys(jet energy resolution,-17.55%)
    sys(b-fractions, 0.04%)
    sys(c-fractions, 4.83%)
    sys(track multiplicity, 1.67%)
    sys(long lived particles,-0.25%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.52%)
    sys(trigger bias,-6.48%)
    sys(negative tag epsilon b, 0.47%)
    sys(negative tag epsilon c, 8.00%)
    sys(negative tag run period dependence,21.68%)
    sys(d0 and z0 smearing,-2.44%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1068,0.0677)
    usys(MC statistics, 6.84%)
    sys(jet vertex fraction,-0.66%)
    sys(jet energy scale, 3.66%)
    sys(jet energy resolution, 0.38%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 1.15%)
    sys(track multiplicity, 3.58%)
    sys(long lived particles, 0.23%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.81%)
    sys(trigger bias, 0.48%)
    sys(negative tag epsilon b, 1.56%)
    sys(negative tag epsilon c, 3.75%)
    sys(negative tag run period dependence, 7.87%)
    sys(d0 and z0 smearing,-13.05%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.0381,0.0371)
    usys(MC statistics, 6.45%)
    sys(jet vertex fraction, 0.04%)
    sys(jet energy scale,-3.94%)
    sys(jet energy resolution, 5.28%)
    sys(b-fractions, 0.89%)
    sys(c-fractions, 1.97%)
    sys(track multiplicity, 3.62%)
    sys(long lived particles, 0.24%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.77%)
    sys(trigger bias,14.47%)
    sys(negative tag epsilon b, 2.22%)
    sys(negative tag epsilon c, 4.71%)
    sys(negative tag run period dependence,-7.68%)
    sys(d0 and z0 smearing,-13.05%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.0552,0.0387)
    usys(MC statistics, 7.68%)
    sys(jet vertex fraction, 0.39%)
    sys(jet energy scale, 4.98%)
    sys(jet energy resolution, 5.42%)
    sys(b-fractions, 0.91%)
    sys(c-fractions, 2.62%)
    sys(track multiplicity, 6.34%)
    sys(long lived particles,-0.21%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.70%)
    sys(trigger bias, 6.89%)
    sys(negative tag epsilon b, 2.20%)
    sys(negative tag epsilon c, 5.45%)
    sys(negative tag run period dependence,-9.23%)
    sys(d0 and z0 smearing,-1.18%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.1999,0.0371)
    usys(MC statistics, 9.13%)
    sys(jet vertex fraction, 0.53%)
    sys(jet energy scale,-4.91%)
    sys(jet energy resolution,-7.87%)
    sys(b-fractions, 1.26%)
    sys(c-fractions, 2.43%)
    sys(track multiplicity,12.68%)
    sys(long lived particles,-0.15%)
    sys(fake tracks, 0.04%)
    sys(photon conversions, 0.64%)
    sys(trigger bias, 6.89%)
    sys(negative tag epsilon b, 3.21%)
    sys(negative tag epsilon c, 5.49%)
    sys(negative tag run period dependence,-9.68%)
    sys(d0 and z0 smearing,-1.18%)
  }
}
