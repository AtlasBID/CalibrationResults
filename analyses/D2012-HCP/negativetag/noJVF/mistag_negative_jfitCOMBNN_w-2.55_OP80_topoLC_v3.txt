Analysis(negative tags,light,JetFitterCOMBNN, -2.55, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1082,0.0161)
    usys(MC statistics, 1.71%)
    sys(jet vertex fraction,11.05%)
    sys(jet energy scale,-5.46%)
    sys(jet energy resolution,-1.65%)
    sys(b-fractions, 0.25%)
    sys(c-fractions, 0.72%)
    sys(track multiplicity, 6.65%)
    sys(long lived particles, 4.70%)
    sys(fake tracks,-0.46%)
    sys(photon conversions, 4.21%)
    sys(trigger bias, 3.77%)
    sys(negative tag epsilon b, 1.08%)
    sys(negative tag epsilon c, 3.09%)
    sys(negative tag run period dependence,-4.74%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2345,0.0114)
    usys(MC statistics, 0.99%)
    sys(jet vertex fraction, 1.49%)
    sys(jet energy scale,-0.11%)
    sys(jet energy resolution,-1.62%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.26%)
    sys(track multiplicity, 4.49%)
    sys(long lived particles,-0.92%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 5.52%)
    sys(trigger bias, 3.77%)
    sys(negative tag epsilon b, 1.08%)
    sys(negative tag epsilon c, 3.87%)
    sys(negative tag run period dependence, 3.05%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2272,0.0063)
    usys(MC statistics, 0.69%)
    sys(jet vertex fraction, 0.58%)
    sys(jet energy scale, 0.07%)
    sys(jet energy resolution,-0.03%)
    sys(b-fractions, 0.84%)
    sys(c-fractions, 3.00%)
    sys(track multiplicity, 7.40%)
    sys(long lived particles, 1.17%)
    sys(fake tracks,-1.01%)
    sys(photon conversions, 8.37%)
    sys(trigger bias, 5.30%)
    sys(negative tag epsilon b, 2.34%)
    sys(negative tag epsilon c, 6.43%)
    sys(negative tag run period dependence,-3.26%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2926,0.0041)
    usys(MC statistics, 0.93%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale, 1.00%)
    sys(jet energy resolution,-0.45%)
    sys(b-fractions, 1.40%)
    sys(c-fractions, 4.42%)
    sys(track multiplicity, 5.85%)
    sys(long lived particles, 1.71%)
    sys(fake tracks,-0.34%)
    sys(photon conversions,11.04%)
    sys(trigger bias, 6.32%)
    sys(negative tag epsilon b, 3.39%)
    sys(negative tag epsilon c, 8.18%)
    sys(negative tag run period dependence,-7.30%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3758,0.0037)
    usys(MC statistics, 0.68%)
    sys(jet vertex fraction, 0.10%)
    sys(jet energy scale, 0.85%)
    sys(jet energy resolution,-0.37%)
    sys(b-fractions, 1.36%)
    sys(c-fractions, 4.10%)
    sys(track multiplicity, 6.58%)
    sys(long lived particles,-1.24%)
    sys(fake tracks, 1.09%)
    sys(photon conversions,11.17%)
    sys(trigger bias, 3.88%)
    sys(negative tag epsilon b, 3.22%)
    sys(negative tag epsilon c, 7.87%)
    sys(negative tag run period dependence,-8.63%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2542,0.0023)
    usys(MC statistics, 1.01%)
    sys(jet vertex fraction, 0.04%)
    sys(jet energy scale, 2.14%)
    sys(jet energy resolution, 3.92%)
    sys(b-fractions, 1.12%)
    sys(c-fractions, 3.66%)
    sys(track multiplicity,11.08%)
    sys(long lived particles,-0.96%)
    sys(fake tracks, 2.78%)
    sys(photon conversions, 9.83%)
    sys(trigger bias, 0.55%)
    sys(negative tag epsilon b, 3.11%)
    sys(negative tag epsilon c, 7.97%)
    sys(negative tag run period dependence,-1.41%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1505,0.0166)
    usys(MC statistics, 1.58%)
    sys(jet vertex fraction, 3.39%)
    sys(jet energy scale,-8.23%)
    sys(jet energy resolution,-2.34%)
    sys(b-fractions, 0.07%)
    sys(c-fractions, 0.12%)
    sys(track multiplicity, 5.47%)
    sys(long lived particles, 3.11%)
    sys(fake tracks,-0.46%)
    sys(photon conversions, 2.30%)
    sys(trigger bias, 2.35%)
    sys(negative tag epsilon b, 0.63%)
    sys(negative tag epsilon c, 2.26%)
    sys(negative tag run period dependence, 4.29%)
    sys(d0 and z0 smearing, 0.03%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2427,0.0127)
    usys(MC statistics, 1.00%)
    sys(jet vertex fraction,-1.11%)
    sys(jet energy scale,-0.90%)
    sys(jet energy resolution,-3.48%)
    sys(b-fractions, 0.11%)
    sys(c-fractions, 0.60%)
    sys(track multiplicity, 5.34%)
    sys(long lived particles,-0.70%)
    sys(fake tracks,-0.44%)
    sys(photon conversions, 3.16%)
    sys(trigger bias, 2.35%)
    sys(negative tag epsilon b, 0.69%)
    sys(negative tag epsilon c, 2.92%)
    sys(negative tag run period dependence, 1.95%)
    sys(d0 and z0 smearing,-0.89%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2309,0.0083)
    usys(MC statistics, 0.90%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale,-0.16%)
    sys(jet energy resolution,-0.12%)
    sys(b-fractions, 0.47%)
    sys(c-fractions, 1.68%)
    sys(track multiplicity, 8.13%)
    sys(long lived particles, 0.79%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 5.05%)
    sys(trigger bias, 6.82%)
    sys(negative tag epsilon b, 1.49%)
    sys(negative tag epsilon c, 4.43%)
    sys(negative tag run period dependence,-2.83%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3102,0.0068)
    usys(MC statistics, 1.01%)
    sys(jet vertex fraction, 0.03%)
    sys(jet energy scale,-1.91%)
    sys(jet energy resolution, 0.06%)
    sys(b-fractions, 0.80%)
    sys(c-fractions, 2.61%)
    sys(track multiplicity, 5.99%)
    sys(long lived particles, 1.17%)
    sys(fake tracks,-0.23%)
    sys(photon conversions, 7.04%)
    sys(trigger bias, 8.56%)
    sys(negative tag epsilon b, 2.03%)
    sys(negative tag epsilon c, 5.56%)
    sys(negative tag run period dependence,-7.95%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3437,0.0074)
    usys(MC statistics, 1.24%)
    sys(jet vertex fraction, 0.04%)
    sys(jet energy scale, 1.23%)
    sys(jet energy resolution, 0.02%)
    sys(b-fractions, 0.81%)
    sys(c-fractions, 2.77%)
    sys(track multiplicity, 6.64%)
    sys(long lived particles,-0.91%)
    sys(fake tracks, 0.64%)
    sys(photon conversions, 7.17%)
    sys(trigger bias, 2.88%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 5.67%)
    sys(negative tag run period dependence,-9.92%)
    sys(d0 and z0 smearing, 1.89%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3835,0.0058)
    usys(MC statistics, 1.57%)
    sys(jet vertex fraction, 0.37%)
    sys(jet energy scale,-2.05%)
    sys(jet energy resolution,-4.91%)
    sys(b-fractions, 0.63%)
    sys(c-fractions, 2.31%)
    sys(track multiplicity, 9.47%)
    sys(long lived particles,-0.86%)
    sys(fake tracks, 1.62%)
    sys(photon conversions, 6.78%)
    sys(trigger bias, 5.74%)
    sys(negative tag epsilon b, 1.79%)
    sys(negative tag epsilon c, 5.50%)
    sys(negative tag run period dependence,-4.34%)
    sys(d0 and z0 smearing, 1.89%)
  }
}
