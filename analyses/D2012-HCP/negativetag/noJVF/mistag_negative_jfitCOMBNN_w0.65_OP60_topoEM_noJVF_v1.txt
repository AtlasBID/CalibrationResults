Analysis(negative tags,light,JetFitterCOMBNN,  0.65, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2448,0.1276)
    usys(MC statistics,44.93%)
    sys(jet energy scale,-7.88%)
    sys(jet energy resolution,-5.76%)
    sys(b-fractions, 0.46%)
    sys(c-fractions,-0.32%)
    sys(track multiplicity, 2.79%)
    sys(long lived particles,22.80%)
    sys(fake tracks,-19.91%)
    sys(photon conversions,-2.79%)
    sys(trigger bias,20.03%)
    sys(negative tag epsilon b, 1.52%)
    sys(negative tag epsilon c, 1.73%)
    sys(negative tag run period dependence,10.45%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.8084,0.1106)
    usys(MC statistics,12.03%)
    sys(jet energy scale,-7.88%)
    sys(jet energy resolution,11.11%)
    sys(b-fractions, 1.76%)
    sys(c-fractions, 3.69%)
    sys(track multiplicity, 0.94%)
    sys(long lived particles,22.80%)
    sys(fake tracks,-8.20%)
    sys(photon conversions,-6.24%)
    sys(trigger bias,20.03%)
    sys(negative tag epsilon b, 3.76%)
    sys(negative tag epsilon c, 6.16%)
    sys(negative tag run period dependence,-6.47%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.4789,0.0461)
    usys(MC statistics, 5.19%)
    sys(jet energy scale,-1.79%)
    sys(jet energy resolution, 0.67%)
    sys(b-fractions, 3.50%)
    sys(c-fractions, 6.32%)
    sys(track multiplicity, 5.37%)
    sys(long lived particles,10.81%)
    sys(fake tracks, 9.10%)
    sys(photon conversions,10.06%)
    sys(trigger bias,14.54%)
    sys(negative tag epsilon b, 7.50%)
    sys(negative tag epsilon c, 9.68%)
    sys(negative tag run period dependence,-0.94%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4673,0.0348)
    usys(MC statistics, 5.01%)
    sys(jet energy scale, 0.87%)
    sys(jet energy resolution,-7.03%)
    sys(b-fractions, 5.84%)
    sys(c-fractions, 4.44%)
    sys(track multiplicity,12.20%)
    sys(long lived particles, 5.58%)
    sys(fake tracks, 2.07%)
    sys(photon conversions, 4.01%)
    sys(trigger bias,14.54%)
    sys(negative tag epsilon b,12.72%)
    sys(negative tag epsilon c, 6.84%)
    sys(negative tag run period dependence,-7.26%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4149,0.0377)
    usys(MC statistics, 4.69%)
    sys(jet energy scale,-0.39%)
    sys(jet energy resolution, 0.93%)
    sys(b-fractions, 6.35%)
    sys(c-fractions, 4.73%)
    sys(track multiplicity,21.65%)
    sys(long lived particles,-0.21%)
    sys(fake tracks, 6.07%)
    sys(photon conversions, 1.60%)
    sys(trigger bias,16.05%)
    sys(negative tag epsilon b,13.44%)
    sys(negative tag epsilon c, 7.07%)
    sys(negative tag run period dependence,-16.58%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3026,0.0358)
    usys(MC statistics, 4.64%)
    sys(jet energy scale,-1.94%)
    sys(jet energy resolution,-4.90%)
    sys(b-fractions, 5.25%)
    sys(c-fractions, 5.22%)
    sys(track multiplicity,21.65%)
    sys(long lived particles,-2.44%)
    sys(fake tracks, 5.25%)
    sys(photon conversions,-2.75%)
    sys(trigger bias,16.05%)
    sys(negative tag epsilon b,13.07%)
    sys(negative tag epsilon c, 8.47%)
    sys(negative tag run period dependence, 4.18%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.8502,0.2427)
    usys(MC statistics,23.66%)
    sys(jet energy scale,-9.60%)
    sys(jet energy resolution,-4.50%)
    sys(b-fractions,-0.20%)
    sys(c-fractions,-1.29%)
    sys(track multiplicity, 3.05%)
    sys(long lived particles, 5.80%)
    sys(fake tracks,-2.74%)
    sys(photon conversions,-1.58%)
    sys(trigger bias,-5.62%)
    sys(negative tag epsilon b, 0.01%)
    sys(negative tag epsilon c, 0.67%)
    sys(negative tag run period dependence,18.42%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.5546,0.1304)
    usys(MC statistics,20.14%)
    sys(jet energy scale, 0.76%)
    sys(jet energy resolution,-4.50%)
    sys(b-fractions, 0.19%)
    sys(c-fractions,-0.99%)
    sys(track multiplicity, 1.42%)
    sys(long lived particles, 5.80%)
    sys(fake tracks, 9.51%)
    sys(photon conversions,-11.38%)
    sys(trigger bias,-5.62%)
    sys(negative tag epsilon b, 0.88%)
    sys(negative tag epsilon c, 1.01%)
    sys(negative tag run period dependence,18.42%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3353,0.0709)
    usys(MC statistics, 8.90%)
    sys(jet energy scale,-1.21%)
    sys(jet energy resolution,-1.03%)
    sys(b-fractions, 1.88%)
    sys(c-fractions, 3.14%)
    sys(track multiplicity, 2.21%)
    sys(long lived particles,-1.29%)
    sys(fake tracks,-1.83%)
    sys(photon conversions, 3.72%)
    sys(trigger bias,11.08%)
    sys(negative tag epsilon b, 4.44%)
    sys(negative tag epsilon c, 5.91%)
    sys(negative tag run period dependence,10.23%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.7711,0.0946)
    usys(MC statistics, 9.64%)
    sys(jet energy scale,-5.49%)
    sys(jet energy resolution,-3.22%)
    sys(b-fractions, 4.86%)
    sys(c-fractions, 2.18%)
    sys(track multiplicity,10.53%)
    sys(long lived particles,-6.13%)
    sys(fake tracks,-1.73%)
    sys(photon conversions, 4.79%)
    sys(trigger bias,11.08%)
    sys(negative tag epsilon b,10.45%)
    sys(negative tag epsilon c, 4.03%)
    sys(negative tag run period dependence,-11.49%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4135,0.1059)
    usys(MC statistics,13.67%)
    sys(jet energy scale, 5.42%)
    sys(jet energy resolution,23.67%)
    sys(b-fractions, 3.54%)
    sys(c-fractions, 7.74%)
    sys(track multiplicity,15.29%)
    sys(long lived particles,-2.40%)
    sys(fake tracks, 2.30%)
    sys(photon conversions,-0.40%)
    sys(trigger bias,24.97%)
    sys(negative tag epsilon b, 7.65%)
    sys(negative tag epsilon c,11.68%)
    sys(negative tag run period dependence,-3.25%)
    sys(d0 and z0 smearing,-9.06%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.1968,0.1070)
    usys(MC statistics,18.57%)
    sys(jet energy scale,-8.47%)
    sys(jet energy resolution,23.67%)
    sys(b-fractions, 2.32%)
    sys(c-fractions, 5.76%)
    sys(track multiplicity,15.52%)
    sys(long lived particles, 9.12%)
    sys(fake tracks,-1.37%)
    sys(photon conversions,-0.36%)
    sys(trigger bias,24.97%)
    sys(negative tag epsilon b, 5.94%)
    sys(negative tag epsilon c, 9.99%)
    sys(negative tag run period dependence,-9.69%)
    sys(d0 and z0 smearing,-9.06%)
  }
}
