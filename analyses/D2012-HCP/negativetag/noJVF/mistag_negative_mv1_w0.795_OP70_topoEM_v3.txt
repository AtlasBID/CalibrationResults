Analysis(negative tags,light,MV1, 0.795, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4495,0.0732)
    usys(MC statistics, 5.58%)
    sys(jet vertex fraction,-0.49%)
    sys(jet energy scale, 1.71%)
    sys(jet energy resolution,-3.81%)
    sys(b-fractions, 0.48%)
    sys(c-fractions, 0.63%)
    sys(track multiplicity, 6.19%)
    sys(long lived particles, 6.01%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 5.22%)
    sys(trigger bias,11.25%)
    sys(negative tag epsilon b, 1.57%)
    sys(negative tag epsilon c, 2.94%)
    sys(negative tag run period dependence, 7.36%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3489,0.0336)
    usys(MC statistics, 2.78%)
    sys(jet vertex fraction, 0.03%)
    sys(jet energy scale, 1.95%)
    sys(jet energy resolution, 0.59%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 1.96%)
    sys(track multiplicity, 1.73%)
    sys(long lived particles,-1.29%)
    sys(fake tracks,-0.88%)
    sys(photon conversions, 7.96%)
    sys(trigger bias,11.25%)
    sys(negative tag epsilon b, 1.47%)
    sys(negative tag epsilon c, 4.58%)
    sys(negative tag run period dependence, 7.36%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2532,0.0151)
    usys(MC statistics, 1.59%)
    sys(jet vertex fraction, 0.80%)
    sys(jet energy scale, 0.76%)
    sys(jet energy resolution, 1.78%)
    sys(b-fractions, 1.62%)
    sys(c-fractions, 4.28%)
    sys(track multiplicity, 4.07%)
    sys(long lived particles, 1.52%)
    sys(fake tracks,-1.09%)
    sys(photon conversions,12.20%)
    sys(trigger bias,11.36%)
    sys(negative tag epsilon b, 3.88%)
    sys(negative tag epsilon c, 7.77%)
    sys(negative tag run period dependence,-2.56%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3826,0.0105)
    usys(MC statistics, 1.79%)
    sys(jet vertex fraction, 0.25%)
    sys(jet energy scale,-0.26%)
    sys(jet energy resolution, 0.87%)
    sys(b-fractions, 2.72%)
    sys(c-fractions, 5.75%)
    sys(track multiplicity, 4.14%)
    sys(long lived particles, 2.21%)
    sys(fake tracks,-0.39%)
    sys(photon conversions,15.76%)
    sys(trigger bias,13.09%)
    sys(negative tag epsilon b, 5.91%)
    sys(negative tag epsilon c, 9.35%)
    sys(negative tag run period dependence,-5.00%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4505,0.0098)
    usys(MC statistics, 1.95%)
    sys(jet vertex fraction, 0.15%)
    sys(jet energy scale, 1.27%)
    sys(jet energy resolution, 4.23%)
    sys(b-fractions, 2.91%)
    sys(c-fractions, 5.40%)
    sys(track multiplicity, 6.18%)
    sys(long lived particles,-1.56%)
    sys(fake tracks, 1.15%)
    sys(photon conversions,14.86%)
    sys(trigger bias, 5.38%)
    sys(negative tag epsilon b, 6.18%)
    sys(negative tag epsilon c, 8.97%)
    sys(negative tag run period dependence,-11.70%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2951,0.0067)
    usys(MC statistics, 1.91%)
    sys(jet vertex fraction, 0.15%)
    sys(jet energy scale,-2.61%)
    sys(jet energy resolution, 1.68%)
    sys(b-fractions, 2.26%)
    sys(c-fractions, 4.93%)
    sys(track multiplicity,11.22%)
    sys(long lived particles,-1.21%)
    sys(fake tracks, 3.38%)
    sys(photon conversions,13.86%)
    sys(trigger bias,18.88%)
    sys(negative tag epsilon b, 5.89%)
    sys(negative tag epsilon c, 9.47%)
    sys(negative tag run period dependence, 2.06%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3478,0.0928)
    usys(MC statistics, 9.40%)
    sys(jet vertex fraction, 0.62%)
    sys(jet energy scale, 1.76%)
    sys(jet energy resolution, 9.22%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.60%)
    sys(track multiplicity,-0.18%)
    sys(long lived particles, 5.02%)
    sys(fake tracks,-0.75%)
    sys(photon conversions, 3.99%)
    sys(trigger bias, 5.48%)
    sys(negative tag epsilon b, 0.64%)
    sys(negative tag epsilon c, 2.87%)
    sys(negative tag run period dependence,12.43%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.5351,0.0485)
    usys(MC statistics, 4.16%)
    sys(jet vertex fraction, 1.10%)
    sys(jet energy scale, 2.25%)
    sys(jet energy resolution, 3.86%)
    sys(b-fractions, 0.16%)
    sys(c-fractions, 1.03%)
    sys(track multiplicity, 2.29%)
    sys(long lived particles,-1.15%)
    sys(fake tracks,-0.77%)
    sys(photon conversions, 5.68%)
    sys(trigger bias, 5.48%)
    sys(negative tag epsilon b, 0.77%)
    sys(negative tag epsilon c, 3.42%)
    sys(negative tag run period dependence, 7.97%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.4283,0.0248)
    usys(MC statistics, 2.34%)
    sys(jet vertex fraction, 1.79%)
    sys(jet energy scale, 0.71%)
    sys(jet energy resolution, 2.69%)
    sys(b-fractions, 0.91%)
    sys(c-fractions, 2.79%)
    sys(track multiplicity, 4.90%)
    sys(long lived particles, 1.26%)
    sys(fake tracks,-0.86%)
    sys(photon conversions, 8.23%)
    sys(trigger bias,14.16%)
    sys(negative tag epsilon b, 2.41%)
    sys(negative tag epsilon c, 5.76%)
    sys(negative tag run period dependence, 5.05%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5422,0.0200)
    usys(MC statistics, 6.23%)
    sys(jet vertex fraction, 0.35%)
    sys(jet energy scale, 4.41%)
    sys(jet energy resolution, 6.30%)
    sys(b-fractions, 1.29%)
    sys(c-fractions, 3.68%)
    sys(track multiplicity, 3.88%)
    sys(long lived particles, 1.56%)
    sys(fake tracks,-0.25%)
    sys(photon conversions, 9.67%)
    sys(trigger bias,10.52%)
    sys(negative tag epsilon b, 2.96%)
    sys(negative tag epsilon c, 6.75%)
    sys(negative tag run period dependence,-5.25%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6580,0.0239)
    usys(MC statistics, 3.99%)
    sys(jet vertex fraction, 0.27%)
    sys(jet energy scale,-2.42%)
    sys(jet energy resolution, 2.46%)
    sys(b-fractions, 1.43%)
    sys(c-fractions, 3.26%)
    sys(track multiplicity, 4.71%)
    sys(long lived particles,-1.11%)
    sys(fake tracks, 0.75%)
    sys(photon conversions, 9.70%)
    sys(trigger bias, 3.07%)
    sys(negative tag epsilon b, 3.23%)
    sys(negative tag epsilon c, 6.19%)
    sys(negative tag run period dependence,-13.17%)
    sys(d0 and z0 smearing,-22.13%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5754,0.0190)
    usys(MC statistics, 5.54%)
    sys(jet vertex fraction, 0.87%)
    sys(jet energy scale, 3.87%)
    sys(jet energy resolution, 5.92%)
    sys(b-fractions, 1.02%)
    sys(c-fractions, 2.82%)
    sys(track multiplicity,11.21%)
    sys(long lived particles,-1.17%)
    sys(fake tracks, 1.96%)
    sys(photon conversions, 9.17%)
    sys(trigger bias,-12.37%)
    sys(negative tag epsilon b, 2.83%)
    sys(negative tag epsilon c, 6.25%)
    sys(negative tag run period dependence, 2.07%)
    sys(d0 and z0 smearing,-22.13%)
  }
}
