Analysis(negative tags,light,JetFitterCOMBNN, -0.95, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2012,0.0384)
    usys(MC statistics, 3.85%)
    sys(jet vertex fraction, 9.45%)
    sys(jet energy scale,-8.45%)
    sys(jet energy resolution,-1.68%)
    sys(b-fractions, 0.51%)
    sys(c-fractions, 0.83%)
    sys(track multiplicity, 1.13%)
    sys(long lived particles, 5.02%)
    sys(fake tracks,-0.49%)
    sys(photon conversions, 4.57%)
    sys(trigger bias, 8.74%)
    sys(negative tag epsilon b, 1.67%)
    sys(negative tag epsilon c, 3.18%)
    sys(negative tag run period dependence,-3.82%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3738,0.0290)
    usys(MC statistics, 2.47%)
    sys(jet vertex fraction, 3.03%)
    sys(jet energy scale, 0.27%)
    sys(jet energy resolution,-4.25%)
    sys(b-fractions, 0.61%)
    sys(c-fractions, 2.44%)
    sys(track multiplicity, 4.84%)
    sys(long lived particles,-1.09%)
    sys(fake tracks,-0.83%)
    sys(photon conversions, 6.88%)
    sys(trigger bias, 8.74%)
    sys(negative tag epsilon b, 1.68%)
    sys(negative tag epsilon c, 5.18%)
    sys(negative tag run period dependence, 3.28%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3225,0.0158)
    usys(MC statistics, 1.65%)
    sys(jet vertex fraction, 0.21%)
    sys(jet energy scale, 0.38%)
    sys(jet energy resolution,-0.32%)
    sys(b-fractions, 1.93%)
    sys(c-fractions, 4.87%)
    sys(track multiplicity, 7.24%)
    sys(long lived particles, 1.68%)
    sys(fake tracks,-1.19%)
    sys(photon conversions,11.73%)
    sys(trigger bias, 6.32%)
    sys(negative tag epsilon b, 4.52%)
    sys(negative tag epsilon c, 8.45%)
    sys(negative tag run period dependence,-2.71%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3964,0.0106)
    usys(MC statistics, 2.08%)
    sys(jet vertex fraction, 0.13%)
    sys(jet energy scale, 0.39%)
    sys(jet energy resolution, 0.73%)
    sys(b-fractions, 3.54%)
    sys(c-fractions, 6.36%)
    sys(track multiplicity, 8.91%)
    sys(long lived particles, 2.48%)
    sys(fake tracks,-0.41%)
    sys(photon conversions,14.90%)
    sys(trigger bias, 9.60%)
    sys(negative tag epsilon b, 7.58%)
    sys(negative tag epsilon c, 9.90%)
    sys(negative tag run period dependence,-8.50%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4741,0.0097)
    usys(MC statistics, 1.67%)
    sys(jet vertex fraction, 0.25%)
    sys(jet energy scale,-0.21%)
    sys(jet energy resolution,-0.28%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 6.01%)
    sys(track multiplicity, 9.49%)
    sys(long lived particles,-1.72%)
    sys(fake tracks, 1.39%)
    sys(photon conversions,15.02%)
    sys(trigger bias, 5.61%)
    sys(negative tag epsilon b, 7.74%)
    sys(negative tag epsilon c, 9.44%)
    sys(negative tag run period dependence,-14.93%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2979,0.0061)
    usys(MC statistics, 2.95%)
    sys(jet vertex fraction, 0.00%)
    sys(jet energy scale, 2.31%)
    sys(jet energy resolution, 1.32%)
    sys(b-fractions, 3.04%)
    sys(c-fractions, 5.68%)
    sys(track multiplicity,15.10%)
    sys(long lived particles,-1.37%)
    sys(fake tracks, 3.77%)
    sys(photon conversions,13.25%)
    sys(trigger bias, 5.93%)
    sys(negative tag epsilon b, 7.35%)
    sys(negative tag epsilon c, 9.93%)
    sys(negative tag run period dependence, 0.21%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2728,0.0455)
    usys(MC statistics, 3.37%)
    sys(jet vertex fraction,-0.19%)
    sys(jet energy scale,-7.33%)
    sys(jet energy resolution, 4.55%)
    sys(b-fractions, 0.16%)
    sys(c-fractions,-0.12%)
    sys(track multiplicity, 3.50%)
    sys(long lived particles, 4.72%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 2.52%)
    sys(trigger bias, 2.72%)
    sys(negative tag epsilon b, 0.83%)
    sys(negative tag epsilon c, 1.94%)
    sys(negative tag run period dependence,10.80%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3580,0.0350)
    usys(MC statistics, 2.49%)
    sys(jet vertex fraction, 1.42%)
    sys(jet energy scale,-2.07%)
    sys(jet energy resolution, 1.45%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 1.29%)
    sys(track multiplicity, 4.36%)
    sys(long lived particles,-0.87%)
    sys(fake tracks,-0.59%)
    sys(photon conversions, 3.86%)
    sys(trigger bias, 2.72%)
    sys(negative tag epsilon b, 0.86%)
    sys(negative tag epsilon c, 3.72%)
    sys(negative tag run period dependence, 7.59%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3477,0.0240)
    usys(MC statistics, 2.45%)
    sys(jet vertex fraction, 0.55%)
    sys(jet energy scale, 0.72%)
    sys(jet energy resolution, 0.63%)
    sys(b-fractions, 1.29%)
    sys(c-fractions, 2.73%)
    sys(track multiplicity, 9.65%)
    sys(long lived particles, 1.22%)
    sys(fake tracks,-0.95%)
    sys(photon conversions, 7.13%)
    sys(trigger bias, 7.68%)
    sys(negative tag epsilon b, 3.19%)
    sys(negative tag epsilon c, 5.56%)
    sys(negative tag run period dependence,-0.96%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5412,0.0228)
    usys(MC statistics, 2.85%)
    sys(jet vertex fraction,-0.50%)
    sys(jet energy scale,-1.85%)
    sys(jet energy resolution,-1.57%)
    sys(b-fractions, 2.45%)
    sys(c-fractions, 3.79%)
    sys(track multiplicity, 8.84%)
    sys(long lived particles, 1.67%)
    sys(fake tracks,-0.28%)
    sys(photon conversions, 9.25%)
    sys(trigger bias,19.80%)
    sys(negative tag epsilon b, 5.35%)
    sys(negative tag epsilon c, 6.69%)
    sys(negative tag run period dependence,-2.77%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6397,0.0260)
    usys(MC statistics, 3.78%)
    sys(jet vertex fraction,-0.13%)
    sys(jet energy scale, 1.64%)
    sys(jet energy resolution,-1.46%)
    sys(b-fractions, 2.63%)
    sys(c-fractions, 4.26%)
    sys(track multiplicity, 9.95%)
    sys(long lived particles,-1.04%)
    sys(fake tracks, 0.77%)
    sys(photon conversions, 9.15%)
    sys(trigger bias, 9.06%)
    sys(negative tag epsilon b, 5.59%)
    sys(negative tag epsilon c, 7.09%)
    sys(negative tag run period dependence,-15.94%)
    sys(d0 and z0 smearing,-3.14%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.4793,0.0180)
    usys(MC statistics, 4.64%)
    sys(jet vertex fraction, 1.85%)
    sys(jet energy scale,-5.13%)
    sys(jet energy resolution,-4.85%)
    sys(b-fractions, 2.25%)
    sys(c-fractions, 3.76%)
    sys(track multiplicity,13.35%)
    sys(long lived particles,-1.13%)
    sys(fake tracks, 1.97%)
    sys(photon conversions, 8.65%)
    sys(trigger bias,11.86%)
    sys(negative tag epsilon b, 5.40%)
    sys(negative tag epsilon c, 7.07%)
    sys(negative tag run period dependence,-1.26%)
    sys(d0 and z0 smearing,-3.14%)
  }
}
