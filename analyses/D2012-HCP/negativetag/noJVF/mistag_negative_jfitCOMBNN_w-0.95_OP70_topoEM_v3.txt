Analysis(negative tags,light,JetFitterCOMBNN, -0.95, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2951,0.0439)
    usys(MC statistics, 4.05%)
    sys(jet vertex fraction, 6.79%)
    sys(jet energy scale,-5.19%)
    sys(jet energy resolution,-1.37%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.12%)
    sys(track multiplicity, 4.14%)
    sys(long lived particles, 4.58%)
    sys(fake tracks,-0.64%)
    sys(photon conversions, 4.34%)
    sys(trigger bias,13.26%)
    sys(negative tag epsilon b, 1.13%)
    sys(negative tag epsilon c, 3.58%)
    sys(negative tag run period dependence, 9.61%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3930,0.0290)
    usys(MC statistics, 2.34%)
    sys(jet vertex fraction, 1.51%)
    sys(jet energy scale,-1.30%)
    sys(jet energy resolution,-1.55%)
    sys(b-fractions, 0.60%)
    sys(c-fractions, 2.10%)
    sys(track multiplicity, 3.74%)
    sys(long lived particles,-1.24%)
    sys(fake tracks,-0.86%)
    sys(photon conversions, 6.77%)
    sys(trigger bias,13.26%)
    sys(negative tag epsilon b, 1.64%)
    sys(negative tag epsilon c, 4.72%)
    sys(negative tag run period dependence, 0.04%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3176,0.0149)
    usys(MC statistics, 1.57%)
    sys(jet vertex fraction, 0.63%)
    sys(jet energy scale, 1.22%)
    sys(jet energy resolution, 0.22%)
    sys(b-fractions, 1.80%)
    sys(c-fractions, 4.60%)
    sys(track multiplicity, 8.30%)
    sys(long lived particles, 1.65%)
    sys(fake tracks,-1.26%)
    sys(photon conversions,11.09%)
    sys(trigger bias,10.74%)
    sys(negative tag epsilon b, 4.24%)
    sys(negative tag epsilon c, 8.12%)
    sys(negative tag run period dependence,-4.12%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3883,0.0106)
    usys(MC statistics, 2.18%)
    sys(jet vertex fraction, 0.31%)
    sys(jet energy scale, 1.70%)
    sys(jet energy resolution, 3.32%)
    sys(b-fractions, 3.17%)
    sys(c-fractions, 6.29%)
    sys(track multiplicity, 6.65%)
    sys(long lived particles, 2.32%)
    sys(fake tracks,-0.41%)
    sys(photon conversions,15.61%)
    sys(trigger bias,11.10%)
    sys(negative tag epsilon b, 6.78%)
    sys(negative tag epsilon c, 9.87%)
    sys(negative tag run period dependence,-6.59%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4925,0.0101)
    usys(MC statistics, 1.96%)
    sys(jet vertex fraction, 0.15%)
    sys(jet energy scale, 1.22%)
    sys(jet energy resolution, 3.54%)
    sys(b-fractions, 3.54%)
    sys(c-fractions, 6.14%)
    sys(track multiplicity, 8.22%)
    sys(long lived particles,-1.50%)
    sys(fake tracks, 1.37%)
    sys(photon conversions,15.23%)
    sys(trigger bias, 6.80%)
    sys(negative tag epsilon b, 7.38%)
    sys(negative tag epsilon c, 9.67%)
    sys(negative tag run period dependence,-13.60%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3479,0.0069)
    usys(MC statistics, 2.22%)
    sys(jet vertex fraction, 0.01%)
    sys(jet energy scale,-1.13%)
    sys(jet energy resolution, 1.92%)
    sys(b-fractions, 2.88%)
    sys(c-fractions, 5.48%)
    sys(track multiplicity,16.09%)
    sys(long lived particles,-1.19%)
    sys(fake tracks, 3.70%)
    sys(photon conversions,13.72%)
    sys(trigger bias,12.42%)
    sys(negative tag epsilon b, 7.32%)
    sys(negative tag epsilon c, 9.98%)
    sys(negative tag run period dependence, 3.49%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3805,0.0540)
    usys(MC statistics, 4.39%)
    sys(jet vertex fraction,-1.06%)
    sys(jet energy scale,-5.90%)
    sys(jet energy resolution, 0.83%)
    sys(b-fractions, 0.10%)
    sys(c-fractions, 0.37%)
    sys(track multiplicity, 3.99%)
    sys(long lived particles, 4.01%)
    sys(fake tracks,-0.51%)
    sys(photon conversions, 2.32%)
    sys(trigger bias, 2.60%)
    sys(negative tag epsilon b, 0.70%)
    sys(negative tag epsilon c, 2.57%)
    sys(negative tag run period dependence,10.26%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3151,0.0334)
    usys(MC statistics, 2.67%)
    sys(jet vertex fraction,-0.84%)
    sys(jet energy scale,-1.46%)
    sys(jet energy resolution, 2.66%)
    sys(b-fractions, 0.26%)
    sys(c-fractions, 1.12%)
    sys(track multiplicity, 4.32%)
    sys(long lived particles,-0.85%)
    sys(fake tracks,-0.56%)
    sys(photon conversions, 4.10%)
    sys(trigger bias, 2.60%)
    sys(negative tag epsilon b, 0.96%)
    sys(negative tag epsilon c, 3.50%)
    sys(negative tag run period dependence, 1.86%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3139,0.0219)
    usys(MC statistics, 2.15%)
    sys(jet vertex fraction, 1.67%)
    sys(jet energy scale, 0.54%)
    sys(jet energy resolution,-0.41%)
    sys(b-fractions, 1.11%)
    sys(c-fractions, 2.76%)
    sys(track multiplicity, 8.86%)
    sys(long lived particles, 1.23%)
    sys(fake tracks,-0.97%)
    sys(photon conversions, 7.13%)
    sys(trigger bias,11.22%)
    sys(negative tag epsilon b, 2.83%)
    sys(negative tag epsilon c, 5.67%)
    sys(negative tag run period dependence, 3.12%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5445,0.0220)
    usys(MC statistics, 4.19%)
    sys(jet vertex fraction,-0.03%)
    sys(jet energy scale,-2.34%)
    sys(jet energy resolution,-5.34%)
    sys(b-fractions, 1.91%)
    sys(c-fractions, 3.86%)
    sys(track multiplicity, 8.66%)
    sys(long lived particles, 1.65%)
    sys(fake tracks,-0.28%)
    sys(photon conversions, 9.17%)
    sys(trigger bias,17.60%)
    sys(negative tag epsilon b, 4.19%)
    sys(negative tag epsilon c, 6.84%)
    sys(negative tag run period dependence,-6.46%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6502,0.0263)
    usys(MC statistics, 4.50%)
    sys(jet vertex fraction, 0.14%)
    sys(jet energy scale, 0.62%)
    sys(jet energy resolution,-1.11%)
    sys(b-fractions, 2.35%)
    sys(c-fractions, 3.93%)
    sys(track multiplicity, 8.58%)
    sys(long lived particles,-1.00%)
    sys(fake tracks, 0.77%)
    sys(photon conversions, 9.56%)
    sys(trigger bias, 8.13%)
    sys(negative tag epsilon b, 5.06%)
    sys(negative tag epsilon c, 6.83%)
    sys(negative tag run period dependence,-18.61%)
    sys(d0 and z0 smearing,-3.14%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5475,0.0201)
    usys(MC statistics, 5.79%)
    sys(jet vertex fraction, 1.10%)
    sys(jet energy scale,-3.07%)
    sys(jet energy resolution,-1.89%)
    sys(b-fractions, 2.05%)
    sys(c-fractions, 2.85%)
    sys(track multiplicity,17.33%)
    sys(long lived particles,-1.00%)
    sys(fake tracks, 1.96%)
    sys(photon conversions, 8.28%)
    sys(trigger bias,-0.63%)
    sys(negative tag epsilon b, 5.31%)
    sys(negative tag epsilon c, 6.00%)
    sys(negative tag run period dependence,-2.98%)
    sys(d0 and z0 smearing,-3.14%)
  }
}
