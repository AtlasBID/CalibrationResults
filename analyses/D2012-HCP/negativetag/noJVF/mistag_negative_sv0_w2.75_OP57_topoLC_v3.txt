Analysis(negative tags,light,SV0,  2.75, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2487,0.0817)
    usys(MC statistics, 5.40%)
    sys(jet vertex fraction, 4.07%)
    sys(jet energy scale, 3.59%)
    sys(jet energy resolution,-4.56%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.48%)
    sys(track multiplicity, 3.63%)
    sys(long lived particles, 0.71%)
    sys(fake tracks,-0.02%)
    sys(photon conversions, 0.62%)
    sys(trigger bias,10.52%)
    sys(negative tag epsilon b, 0.90%)
    sys(negative tag epsilon c, 2.81%)
    sys(negative tag run period dependence,12.92%)
    sys(d0 and z0 smearing,-3.59%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2046,0.0296)
    usys(MC statistics, 2.49%)
    sys(jet vertex fraction, 1.12%)
    sys(jet energy scale, 2.94%)
    sys(jet energy resolution, 1.54%)
    sys(b-fractions, 0.12%)
    sys(c-fractions, 0.89%)
    sys(track multiplicity,-0.28%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.75%)
    sys(trigger bias,10.52%)
    sys(negative tag epsilon b, 0.78%)
    sys(negative tag epsilon c, 3.47%)
    sys(negative tag run period dependence, 9.82%)
    sys(d0 and z0 smearing,-3.59%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0749,0.0128)
    usys(MC statistics, 1.48%)
    sys(jet vertex fraction, 0.71%)
    sys(jet energy scale,-0.36%)
    sys(jet energy resolution,-0.23%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.86%)
    sys(track multiplicity,-0.68%)
    sys(long lived particles, 0.15%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.83%)
    sys(trigger bias,10.82%)
    sys(negative tag epsilon b, 1.34%)
    sys(negative tag epsilon c, 5.17%)
    sys(negative tag run period dependence, 1.97%)
    sys(d0 and z0 smearing,-0.35%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1745,0.0082)
    usys(MC statistics, 1.28%)
    sys(jet vertex fraction,-0.06%)
    sys(jet energy scale,-1.21%)
    sys(jet energy resolution,-0.95%)
    sys(b-fractions, 0.58%)
    sys(c-fractions, 2.86%)
    sys(track multiplicity, 0.44%)
    sys(long lived particles, 0.18%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.86%)
    sys(trigger bias, 9.15%)
    sys(negative tag epsilon b, 1.79%)
    sys(negative tag epsilon c, 6.52%)
    sys(negative tag run period dependence,-1.33%)
    sys(d0 and z0 smearing,-0.35%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1455,0.0069)
    usys(MC statistics, 1.36%)
    sys(jet vertex fraction, 0.49%)
    sys(jet energy scale,-0.76%)
    sys(jet energy resolution, 1.34%)
    sys(b-fractions, 0.57%)
    sys(c-fractions, 2.92%)
    sys(track multiplicity, 1.64%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 0.72%)
    sys(trigger bias, 3.38%)
    sys(negative tag epsilon b, 1.71%)
    sys(negative tag epsilon c, 6.69%)
    sys(negative tag run period dependence,-8.39%)
    sys(d0 and z0 smearing,-3.63%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.0923,0.0046)
    usys(MC statistics, 1.63%)
    sys(jet vertex fraction, 0.09%)
    sys(jet energy scale,-3.06%)
    sys(jet energy resolution, 0.04%)
    sys(b-fractions, 0.44%)
    sys(c-fractions, 2.80%)
    sys(track multiplicity, 2.87%)
    sys(long lived particles,-0.10%)
    sys(fake tracks, 0.06%)
    sys(photon conversions, 0.60%)
    sys(trigger bias, 8.70%)
    sys(negative tag epsilon b, 1.62%)
    sys(negative tag epsilon c, 7.07%)
    sys(negative tag run period dependence, 0.79%)
    sys(d0 and z0 smearing,-3.63%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3369,0.1083)
    usys(MC statistics, 6.51%)
    sys(jet vertex fraction,-1.93%)
    sys(jet energy scale, 4.67%)
    sys(jet energy resolution,12.95%)
    sys(b-fractions,-0.01%)
    sys(c-fractions, 0.99%)
    sys(track multiplicity,-0.45%)
    sys(long lived particles, 0.75%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.45%)
    sys(trigger bias, 3.64%)
    sys(negative tag epsilon b, 0.44%)
    sys(negative tag epsilon c, 3.36%)
    sys(negative tag run period dependence,20.53%)
    sys(d0 and z0 smearing,-8.71%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3626,0.0420)
    usys(MC statistics, 2.95%)
    sys(jet vertex fraction,-2.16%)
    sys(jet energy scale, 2.56%)
    sys(jet energy resolution, 5.29%)
    sys(b-fractions, 0.05%)
    sys(c-fractions, 0.88%)
    sys(track multiplicity,-0.94%)
    sys(long lived particles,-0.12%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 0.50%)
    sys(trigger bias, 3.64%)
    sys(negative tag epsilon b, 0.55%)
    sys(negative tag epsilon c, 3.27%)
    sys(negative tag run period dependence,20.53%)
    sys(d0 and z0 smearing,-8.71%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1928,0.0186)
    usys(MC statistics, 1.97%)
    sys(jet vertex fraction, 0.58%)
    sys(jet energy scale,-0.81%)
    sys(jet energy resolution, 2.18%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 1.04%)
    sys(track multiplicity, 0.54%)
    sys(long lived particles, 0.14%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.53%)
    sys(trigger bias,11.23%)
    sys(negative tag epsilon b, 0.96%)
    sys(negative tag epsilon c, 3.69%)
    sys(negative tag run period dependence, 3.96%)
    sys(d0 and z0 smearing,-6.37%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2530,0.0131)
    usys(MC statistics, 2.09%)
    sys(jet vertex fraction,-0.19%)
    sys(jet energy scale,-1.34%)
    sys(jet energy resolution, 3.49%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.76%)
    sys(track multiplicity, 1.13%)
    sys(long lived particles, 0.16%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.50%)
    sys(trigger bias, 7.34%)
    sys(negative tag epsilon b, 1.11%)
    sys(negative tag epsilon c, 4.57%)
    sys(negative tag run period dependence,-0.93%)
    sys(d0 and z0 smearing,-6.37%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3153,0.0145)
    usys(MC statistics, 2.40%)
    sys(jet vertex fraction,-0.17%)
    sys(jet energy scale, 1.11%)
    sys(jet energy resolution, 0.90%)
    sys(b-fractions, 0.41%)
    sys(c-fractions, 1.41%)
    sys(track multiplicity, 1.87%)
    sys(long lived particles,-0.12%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.45%)
    sys(trigger bias, 2.99%)
    sys(negative tag epsilon b, 1.21%)
    sys(negative tag epsilon c, 4.05%)
    sys(negative tag run period dependence,-0.09%)
    sys(d0 and z0 smearing,-6.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3120,0.0116)
    usys(MC statistics, 3.30%)
    sys(jet vertex fraction, 0.11%)
    sys(jet energy scale,-3.79%)
    sys(jet energy resolution,-2.77%)
    sys(b-fractions, 0.32%)
    sys(c-fractions, 1.80%)
    sys(track multiplicity, 4.93%)
    sys(long lived particles,-0.08%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 0.40%)
    sys(trigger bias, 1.99%)
    sys(negative tag epsilon b, 1.11%)
    sys(negative tag epsilon c, 4.88%)
    sys(negative tag run period dependence,-0.13%)
    sys(d0 and z0 smearing,-6.19%)
  }
}
