Analysis(negative tags,light,MV1, 0.148, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1597,0.0123)
    usys(MC statistics, 1.21%)
    sys(jet vertex fraction, 5.77%)
    sys(jet energy scale,-0.94%)
    sys(jet energy resolution,-0.16%)
    sys(b-fractions, 0.20%)
    sys(c-fractions, 0.65%)
    sys(track multiplicity, 7.12%)
    sys(long lived particles, 3.54%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 3.71%)
    sys(trigger bias, 3.64%)
    sys(negative tag epsilon b, 0.96%)
    sys(negative tag epsilon c, 3.01%)
    sys(negative tag run period dependence, 2.24%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1906,0.0069)
    usys(MC statistics, 0.68%)
    sys(jet vertex fraction, 0.63%)
    sys(jet energy scale,-0.46%)
    sys(jet energy resolution,-0.55%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.16%)
    sys(track multiplicity, 3.96%)
    sys(long lived particles,-0.86%)
    sys(fake tracks,-0.63%)
    sys(photon conversions, 5.97%)
    sys(trigger bias, 3.64%)
    sys(negative tag epsilon b, 1.06%)
    sys(negative tag epsilon c, 3.72%)
    sys(negative tag run period dependence, 1.71%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1612,0.0039)
    usys(MC statistics, 0.43%)
    sys(jet vertex fraction, 0.74%)
    sys(jet energy scale, 0.27%)
    sys(jet energy resolution,-0.02%)
    sys(b-fractions, 0.64%)
    sys(c-fractions, 2.34%)
    sys(track multiplicity, 4.81%)
    sys(long lived particles, 1.00%)
    sys(fake tracks,-0.86%)
    sys(photon conversions, 8.66%)
    sys(trigger bias, 6.03%)
    sys(negative tag epsilon b, 1.95%)
    sys(negative tag epsilon c, 5.64%)
    sys(negative tag run period dependence,-2.36%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2336,0.0026)
    usys(MC statistics, 0.59%)
    sys(jet vertex fraction, 0.14%)
    sys(jet energy scale, 1.01%)
    sys(jet energy resolution, 0.10%)
    sys(b-fractions, 0.90%)
    sys(c-fractions, 3.35%)
    sys(track multiplicity, 2.93%)
    sys(long lived particles, 1.31%)
    sys(fake tracks,-0.28%)
    sys(photon conversions,10.48%)
    sys(trigger bias, 6.16%)
    sys(negative tag epsilon b, 2.39%)
    sys(negative tag epsilon c, 6.99%)
    sys(negative tag run period dependence,-4.61%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.2796,0.0024)
    usys(MC statistics, 0.53%)
    sys(jet vertex fraction, 0.16%)
    sys(jet energy scale, 0.73%)
    sys(jet energy resolution, 0.34%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 3.14%)
    sys(track multiplicity, 3.04%)
    sys(long lived particles,-0.97%)
    sys(fake tracks, 0.86%)
    sys(photon conversions, 9.55%)
    sys(trigger bias, 2.70%)
    sys(negative tag epsilon b, 2.31%)
    sys(negative tag epsilon c, 6.88%)
    sys(negative tag run period dependence,-7.40%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2107,0.0017)
    usys(MC statistics, 0.65%)
    sys(jet vertex fraction, 0.22%)
    sys(jet energy scale,-0.20%)
    sys(jet energy resolution, 0.72%)
    sys(b-fractions, 0.69%)
    sys(c-fractions, 2.70%)
    sys(track multiplicity, 6.55%)
    sys(long lived particles,-0.77%)
    sys(fake tracks, 2.42%)
    sys(photon conversions, 8.95%)
    sys(trigger bias, 6.79%)
    sys(negative tag epsilon b, 2.28%)
    sys(negative tag epsilon c, 7.00%)
    sys(negative tag run period dependence,-0.97%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1551,0.0139)
    usys(MC statistics, 1.30%)
    sys(jet vertex fraction, 1.06%)
    sys(jet energy scale,-4.35%)
    sys(jet energy resolution,-1.42%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.19%)
    sys(track multiplicity, 6.78%)
    sys(long lived particles, 2.68%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 2.39%)
    sys(trigger bias, 4.85%)
    sys(negative tag epsilon b, 0.64%)
    sys(negative tag epsilon c, 2.35%)
    sys(negative tag run period dependence,-2.44%)
    sys(d0 and z0 smearing, 0.54%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2037,0.0085)
    usys(MC statistics, 0.74%)
    sys(jet vertex fraction, 0.41%)
    sys(jet energy scale,-1.00%)
    sys(jet energy resolution,-1.27%)
    sys(b-fractions, 0.13%)
    sys(c-fractions, 0.54%)
    sys(track multiplicity, 4.37%)
    sys(long lived particles,-0.67%)
    sys(fake tracks,-0.52%)
    sys(photon conversions, 3.86%)
    sys(trigger bias, 4.85%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 2.84%)
    sys(negative tag run period dependence, 1.05%)
    sys(d0 and z0 smearing,-0.08%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1642,0.0050)
    usys(MC statistics, 0.55%)
    sys(jet vertex fraction, 0.70%)
    sys(jet energy scale, 0.08%)
    sys(jet energy resolution, 0.55%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.30%)
    sys(track multiplicity, 5.75%)
    sys(long lived particles, 0.71%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 5.44%)
    sys(trigger bias, 6.66%)
    sys(negative tag epsilon b, 1.21%)
    sys(negative tag epsilon c, 4.00%)
    sys(negative tag run period dependence,-1.78%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2446,0.0041)
    usys(MC statistics, 0.92%)
    sys(jet vertex fraction, 0.12%)
    sys(jet energy scale, 0.79%)
    sys(jet energy resolution, 0.26%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 1.81%)
    sys(track multiplicity, 3.50%)
    sys(long lived particles, 0.99%)
    sys(fake tracks,-0.20%)
    sys(photon conversions, 6.63%)
    sys(trigger bias, 5.98%)
    sys(negative tag epsilon b, 1.41%)
    sys(negative tag epsilon c, 4.55%)
    sys(negative tag run period dependence,-4.29%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3415,0.0049)
    usys(MC statistics, 0.92%)
    sys(jet vertex fraction, 0.20%)
    sys(jet energy scale, 0.94%)
    sys(jet energy resolution, 0.22%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 2.00%)
    sys(track multiplicity, 3.84%)
    sys(long lived particles,-0.77%)
    sys(fake tracks, 0.58%)
    sys(photon conversions, 6.68%)
    sys(trigger bias, 2.46%)
    sys(negative tag epsilon b, 1.38%)
    sys(negative tag epsilon c, 4.80%)
    sys(negative tag run period dependence,-6.55%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2906,0.0038)
    usys(MC statistics, 1.48%)
    sys(jet vertex fraction, 0.37%)
    sys(jet energy scale, 2.19%)
    sys(jet energy resolution, 0.22%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.58%)
    sys(track multiplicity, 7.17%)
    sys(long lived particles,-0.67%)
    sys(fake tracks, 1.55%)
    sys(photon conversions, 6.18%)
    sys(trigger bias, 1.89%)
    sys(negative tag epsilon b, 1.20%)
    sys(negative tag epsilon c, 4.64%)
    sys(negative tag run period dependence,-3.41%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
