Analysis(negative tags,light,MV2, 0.067, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2046,0.1795)
    usys(MC statistics,13.66%)
    sys(jet vertex fraction,-3.95%)
    sys(jet energy scale, 5.57%)
    sys(jet energy resolution,-2.90%)
    sys(b-fractions, 1.21%)
    sys(c-fractions, 0.25%)
    sys(track multiplicity, 2.71%)
    sys(long lived particles, 4.66%)
    sys(fake tracks,-0.13%)
    sys(photon conversions, 6.69%)
    sys(trigger bias, 8.70%)
    sys(negative tag epsilon b, 3.31%)
    sys(negative tag epsilon c, 2.28%)
    sys(negative tag run period dependence, 7.37%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3078,0.0204)
    usys(MC statistics, 1.80%)
    sys(jet vertex fraction, 0.53%)
    sys(jet energy scale, 5.57%)
    sys(jet energy resolution,-2.90%)
    sys(b-fractions, 0.43%)
    sys(c-fractions, 1.89%)
    sys(track multiplicity, 3.29%)
    sys(long lived particles,-1.04%)
    sys(fake tracks,-0.83%)
    sys(photon conversions, 7.18%)
    sys(trigger bias, 8.70%)
    sys(negative tag epsilon b, 1.34%)
    sys(negative tag epsilon c, 4.58%)
    sys(negative tag run period dependence, 7.37%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1946,0.0074)
    usys(MC statistics, 0.83%)
    sys(jet vertex fraction, 0.75%)
    sys(jet energy scale,-2.53%)
    sys(jet energy resolution, 2.49%)
    sys(b-fractions, 0.95%)
    sys(c-fractions, 3.06%)
    sys(track multiplicity, 3.84%)
    sys(long lived particles, 1.22%)
    sys(fake tracks,-0.89%)
    sys(photon conversions, 9.63%)
    sys(trigger bias, 7.85%)
    sys(negative tag epsilon b, 2.57%)
    sys(negative tag epsilon c, 6.48%)
    sys(negative tag run period dependence, 0.89%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4699,0.0208)
    usys(MC statistics, 3.04%)
    sys(jet vertex fraction,-0.08%)
    sys(jet energy scale,-13.77%)
    sys(jet energy resolution,-16.67%)
    sys(b-fractions, 3.91%)
    sys(c-fractions, 6.00%)
    sys(track multiplicity, 7.15%)
    sys(long lived particles, 2.48%)
    sys(fake tracks,-0.31%)
    sys(photon conversions,16.75%)
    sys(trigger bias,15.02%)
    sys(negative tag epsilon b, 8.34%)
    sys(negative tag epsilon c, 9.33%)
    sys(negative tag run period dependence, 5.10%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.6564,0.0334)
    usys(MC statistics, 5.16%)
    sys(jet vertex fraction, 0.26%)
    sys(jet energy scale,-1.56%)
    sys(jet energy resolution,-4.76%)
    sys(b-fractions, 6.04%)
    sys(c-fractions, 4.59%)
    sys(track multiplicity, 9.95%)
    sys(long lived particles,-1.69%)
    sys(fake tracks, 1.20%)
    sys(photon conversions,18.94%)
    sys(trigger bias,25.32%)
    sys(negative tag epsilon b,12.27%)
    sys(negative tag epsilon c, 6.92%)
    sys(negative tag run period dependence,-12.52%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2216,0.0196)
    usys(MC statistics, 6.58%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale, 8.33%)
    sys(jet energy resolution,10.91%)
    sys(b-fractions, 5.55%)
    sys(c-fractions, 5.19%)
    sys(track multiplicity,17.19%)
    sys(long lived particles,-2.01%)
    sys(fake tracks, 3.61%)
    sys(photon conversions,17.39%)
    sys(trigger bias,25.32%)
    sys(negative tag epsilon b,12.96%)
    sys(negative tag epsilon c, 8.31%)
    sys(negative tag run period dependence, 9.94%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1288,0.2353)
    usys(MC statistics,16.39%)
    sys(jet vertex fraction, 0.34%)
    sys(jet energy scale, 6.58%)
    sys(jet energy resolution, 4.91%)
    sys(b-fractions, 0.09%)
    sys(c-fractions,-1.21%)
    sys(track multiplicity,-4.37%)
    sys(long lived particles, 4.96%)
    sys(fake tracks,-0.50%)
    sys(photon conversions, 3.37%)
    sys(trigger bias, 1.51%)
    sys(negative tag epsilon b, 0.68%)
    sys(negative tag epsilon c, 0.61%)
    sys(negative tag run period dependence,13.66%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3253,0.0252)
    usys(MC statistics, 2.17%)
    sys(jet vertex fraction, 0.34%)
    sys(jet energy scale, 6.58%)
    sys(jet energy resolution, 4.91%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 1.19%)
    sys(track multiplicity, 4.62%)
    sys(long lived particles,-0.86%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 4.74%)
    sys(trigger bias, 1.51%)
    sys(negative tag epsilon b, 0.88%)
    sys(negative tag epsilon c, 3.60%)
    sys(negative tag run period dependence,13.66%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3018,0.0138)
    usys(MC statistics, 1.48%)
    sys(jet vertex fraction, 0.95%)
    sys(jet energy scale,-3.84%)
    sys(jet energy resolution, 0.24%)
    sys(b-fractions, 0.71%)
    sys(c-fractions, 2.08%)
    sys(track multiplicity, 5.27%)
    sys(long lived particles, 1.03%)
    sys(fake tracks,-0.73%)
    sys(photon conversions, 6.41%)
    sys(trigger bias, 9.75%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 4.89%)
    sys(negative tag run period dependence, 2.36%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.7358,0.0593)
    usys(MC statistics, 6.38%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale,-19.07%)
    sys(jet energy resolution,-17.86%)
    sys(b-fractions, 3.03%)
    sys(c-fractions, 4.54%)
    sys(track multiplicity, 7.85%)
    sys(long lived particles, 1.97%)
    sys(fake tracks,-0.26%)
    sys(photon conversions,10.16%)
    sys(trigger bias, 9.75%)
    sys(negative tag epsilon b, 6.52%)
    sys(negative tag epsilon c, 7.53%)
    sys(negative tag run period dependence,17.09%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.9923,0.1120)
    usys(MC statistics,16.75%)
    sys(jet vertex fraction,-0.59%)
    sys(jet energy scale, 1.35%)
    sys(jet energy resolution, 0.45%)
    sys(b-fractions, 4.16%)
    sys(c-fractions, 2.67%)
    sys(track multiplicity,10.92%)
    sys(long lived particles,-1.67%)
    sys(fake tracks, 0.73%)
    sys(photon conversions,10.95%)
    sys(trigger bias,25.80%)
    sys(negative tag epsilon b, 8.66%)
    sys(negative tag epsilon c, 4.75%)
    sys(negative tag run period dependence,-14.03%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(2.1444,0.1002)
    usys(MC statistics,14.88%)
    sys(jet vertex fraction,-0.16%)
    sys(jet energy scale,-13.76%)
    sys(jet energy resolution,-10.62%)
    sys(b-fractions, 2.29%)
    sys(c-fractions, 3.46%)
    sys(track multiplicity,10.92%)
    sys(long lived particles,-2.19%)
    sys(fake tracks, 1.78%)
    sys(photon conversions,10.67%)
    sys(trigger bias,25.80%)
    sys(negative tag epsilon b, 5.50%)
    sys(negative tag epsilon c, 6.65%)
    sys(negative tag run period dependence,-14.03%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
