Analysis(negative tags,light,MV1, 0.772, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1360,0.0591)
    usys(MC statistics, 7.35%)
    sys(jet vertex fraction,12.95%)
    sys(jet energy scale,-2.28%)
    sys(jet energy resolution, 0.47%)
    sys(b-fractions, 0.81%)
    sys(c-fractions, 0.97%)
    sys(track multiplicity, 0.34%)
    sys(long lived particles, 6.36%)
    sys(fake tracks,-0.43%)
    sys(photon conversions, 6.72%)
    sys(trigger bias, 7.02%)
    sys(negative tag epsilon b, 2.36%)
    sys(negative tag epsilon c, 3.29%)
    sys(negative tag run period dependence,-1.80%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3059,0.0325)
    usys(MC statistics, 2.72%)
    sys(jet vertex fraction,-0.39%)
    sys(jet energy scale, 1.37%)
    sys(jet energy resolution, 2.21%)
    sys(b-fractions, 0.54%)
    sys(c-fractions, 1.97%)
    sys(track multiplicity, 2.86%)
    sys(long lived particles,-1.29%)
    sys(fake tracks,-0.78%)
    sys(photon conversions, 7.82%)
    sys(trigger bias, 7.02%)
    sys(negative tag epsilon b, 1.55%)
    sys(negative tag epsilon c, 4.64%)
    sys(negative tag run period dependence, 2.49%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2642,0.0153)
    usys(MC statistics, 1.62%)
    sys(jet vertex fraction, 1.05%)
    sys(jet energy scale, 0.65%)
    sys(jet energy resolution,-0.59%)
    sys(b-fractions, 1.63%)
    sys(c-fractions, 4.30%)
    sys(track multiplicity, 3.58%)
    sys(long lived particles, 1.56%)
    sys(fake tracks,-1.08%)
    sys(photon conversions,12.29%)
    sys(trigger bias, 8.44%)
    sys(negative tag epsilon b, 3.91%)
    sys(negative tag epsilon c, 7.82%)
    sys(negative tag run period dependence,-2.99%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3654,0.0100)
    usys(MC statistics, 1.93%)
    sys(jet vertex fraction,-0.16%)
    sys(jet energy scale,-0.69%)
    sys(jet energy resolution, 2.66%)
    sys(b-fractions, 2.67%)
    sys(c-fractions, 5.88%)
    sys(track multiplicity, 5.42%)
    sys(long lived particles, 2.10%)
    sys(fake tracks,-0.33%)
    sys(photon conversions,14.93%)
    sys(trigger bias, 7.78%)
    sys(negative tag epsilon b, 5.87%)
    sys(negative tag epsilon c, 9.58%)
    sys(negative tag run period dependence,-5.01%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4541,0.0094)
    usys(MC statistics, 1.64%)
    sys(jet vertex fraction, 0.06%)
    sys(jet energy scale, 0.11%)
    sys(jet energy resolution,-2.49%)
    sys(b-fractions, 2.88%)
    sys(c-fractions, 5.25%)
    sys(track multiplicity, 7.12%)
    sys(long lived particles,-1.60%)
    sys(fake tracks, 1.14%)
    sys(photon conversions,14.76%)
    sys(trigger bias, 6.32%)
    sys(negative tag epsilon b, 6.12%)
    sys(negative tag epsilon c, 8.78%)
    sys(negative tag run period dependence,-10.61%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2531,0.0059)
    usys(MC statistics, 2.89%)
    sys(jet vertex fraction, 0.07%)
    sys(jet energy scale, 0.69%)
    sys(jet energy resolution, 3.38%)
    sys(b-fractions, 2.33%)
    sys(c-fractions, 4.86%)
    sys(track multiplicity,11.97%)
    sys(long lived particles,-1.19%)
    sys(fake tracks, 3.18%)
    sys(photon conversions,12.70%)
    sys(trigger bias, 6.67%)
    sys(negative tag epsilon b, 5.78%)
    sys(negative tag epsilon c, 9.11%)
    sys(negative tag run period dependence, 0.57%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2792,0.0885)
    usys(MC statistics, 5.82%)
    sys(jet vertex fraction,-4.05%)
    sys(jet energy scale,-1.02%)
    sys(jet energy resolution,11.77%)
    sys(b-fractions, 0.32%)
    sys(c-fractions, 0.08%)
    sys(track multiplicity,-0.71%)
    sys(long lived particles, 7.20%)
    sys(fake tracks,-0.59%)
    sys(photon conversions, 4.07%)
    sys(trigger bias, 4.96%)
    sys(negative tag epsilon b, 1.17%)
    sys(negative tag epsilon c, 2.16%)
    sys(negative tag run period dependence,16.90%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4562,0.0468)
    usys(MC statistics, 3.68%)
    sys(jet vertex fraction, 2.31%)
    sys(jet energy scale,-1.63%)
    sys(jet energy resolution, 4.11%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 1.14%)
    sys(track multiplicity, 2.30%)
    sys(long lived particles,-1.03%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 5.51%)
    sys(trigger bias, 4.96%)
    sys(negative tag epsilon b, 0.91%)
    sys(negative tag epsilon c, 3.54%)
    sys(negative tag run period dependence,16.90%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3846,0.0242)
    usys(MC statistics, 2.39%)
    sys(jet vertex fraction, 0.51%)
    sys(jet energy scale, 1.58%)
    sys(jet energy resolution, 2.76%)
    sys(b-fractions, 1.08%)
    sys(c-fractions, 2.45%)
    sys(track multiplicity, 4.74%)
    sys(long lived particles, 1.25%)
    sys(fake tracks,-0.85%)
    sys(photon conversions, 8.14%)
    sys(trigger bias,10.56%)
    sys(negative tag epsilon b, 2.75%)
    sys(negative tag epsilon c, 5.26%)
    sys(negative tag run period dependence, 8.46%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5948,0.0199)
    usys(MC statistics, 2.86%)
    sys(jet vertex fraction,-0.03%)
    sys(jet energy scale,-1.29%)
    sys(jet energy resolution, 0.29%)
    sys(b-fractions, 1.37%)
    sys(c-fractions, 3.61%)
    sys(track multiplicity, 5.01%)
    sys(long lived particles, 1.63%)
    sys(fake tracks,-0.25%)
    sys(photon conversions, 9.41%)
    sys(trigger bias,13.62%)
    sys(negative tag epsilon b, 3.17%)
    sys(negative tag epsilon c, 6.72%)
    sys(negative tag run period dependence,-7.09%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5846,0.0220)
    usys(MC statistics, 3.40%)
    sys(jet vertex fraction, 0.10%)
    sys(jet energy scale, 2.59%)
    sys(jet energy resolution, 1.85%)
    sys(b-fractions, 1.47%)
    sys(c-fractions, 2.78%)
    sys(track multiplicity, 6.39%)
    sys(long lived particles,-1.01%)
    sys(fake tracks, 0.74%)
    sys(photon conversions, 9.40%)
    sys(trigger bias, 3.05%)
    sys(negative tag epsilon b, 3.31%)
    sys(negative tag epsilon c, 5.52%)
    sys(negative tag run period dependence,-10.95%)
    sys(d0 and z0 smearing,-22.13%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5595,0.0173)
    usys(MC statistics, 5.28%)
    sys(jet vertex fraction, 0.41%)
    sys(jet energy scale,-2.41%)
    sys(jet energy resolution,-10.44%)
    sys(b-fractions, 1.14%)
    sys(c-fractions, 2.22%)
    sys(track multiplicity, 9.30%)
    sys(long lived particles,-1.27%)
    sys(fake tracks, 1.82%)
    sys(photon conversions, 8.87%)
    sys(trigger bias, 2.37%)
    sys(negative tag epsilon b, 2.94%)
    sys(negative tag epsilon c, 5.23%)
    sys(negative tag run period dependence,-1.30%)
    sys(d0 and z0 smearing,-22.13%)
  }
}
