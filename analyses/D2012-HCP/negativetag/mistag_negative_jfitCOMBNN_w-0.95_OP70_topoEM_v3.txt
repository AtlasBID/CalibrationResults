Analysis(negative tags,light,JetFitterCOMBNN, -0.95, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3830,0.0458)
    usys(MC statistics, 3.75%)
    sys(jet energy scale,-5.57%)
    sys(jet energy resolution,-5.17%)
    sys(b-fractions, 0.27%)
    sys(c-fractions, 1.12%)
    sys(track multiplicity, 4.83%)
    sys(long lived particles, 4.14%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 3.61%)
    sys(trigger bias,10.47%)
    sys(negative tag epsilon b, 1.13%)
    sys(negative tag epsilon c, 3.57%)
    sys(negative tag run period dependence, 5.80%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.4140,0.0299)
    usys(MC statistics, 2.45%)
    sys(jet energy scale,-0.69%)
    sys(jet energy resolution,-0.48%)
    sys(b-fractions, 0.63%)
    sys(c-fractions, 2.18%)
    sys(track multiplicity, 3.33%)
    sys(long lived particles,-1.20%)
    sys(fake tracks,-0.81%)
    sys(photon conversions, 6.48%)
    sys(trigger bias,10.47%)
    sys(negative tag epsilon b, 1.68%)
    sys(negative tag epsilon c, 4.81%)
    sys(negative tag run period dependence,-1.09%)
    sys(d0 and z0 smearing, 1.36%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3259,0.0151)
    usys(MC statistics, 1.57%)
    sys(jet energy scale, 1.07%)
    sys(jet energy resolution,-0.06%)
    sys(b-fractions, 1.81%)
    sys(c-fractions, 4.62%)
    sys(track multiplicity, 8.14%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-1.25%)
    sys(photon conversions,11.04%)
    sys(trigger bias,10.11%)
    sys(negative tag epsilon b, 4.27%)
    sys(negative tag epsilon c, 8.13%)
    sys(negative tag run period dependence,-4.35%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3925,0.0107)
    usys(MC statistics, 2.18%)
    sys(jet energy scale, 1.69%)
    sys(jet energy resolution, 3.23%)
    sys(b-fractions, 3.18%)
    sys(c-fractions, 6.29%)
    sys(track multiplicity, 6.65%)
    sys(long lived particles, 2.28%)
    sys(fake tracks,-0.41%)
    sys(photon conversions,15.55%)
    sys(trigger bias,11.04%)
    sys(negative tag epsilon b, 6.79%)
    sys(negative tag epsilon c, 9.86%)
    sys(negative tag run period dependence,-6.78%)
    sys(d0 and z0 smearing, 9.64%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4947,0.0101)
    usys(MC statistics, 1.96%)
    sys(jet energy scale, 1.27%)
    sys(jet energy resolution, 3.48%)
    sys(b-fractions, 3.53%)
    sys(c-fractions, 6.15%)
    sys(track multiplicity, 8.24%)
    sys(long lived particles,-1.49%)
    sys(fake tracks, 1.37%)
    sys(photon conversions,15.18%)
    sys(trigger bias, 6.83%)
    sys(negative tag epsilon b, 7.35%)
    sys(negative tag epsilon c, 9.68%)
    sys(negative tag run period dependence,-13.54%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3481,0.0069)
    usys(MC statistics, 2.22%)
    sys(jet energy scale,-1.12%)
    sys(jet energy resolution, 1.95%)
    sys(b-fractions, 2.88%)
    sys(c-fractions, 5.48%)
    sys(track multiplicity,16.14%)
    sys(long lived particles,-1.19%)
    sys(fake tracks, 3.70%)
    sys(photon conversions,13.69%)
    sys(trigger bias,12.31%)
    sys(negative tag epsilon b, 7.31%)
    sys(negative tag epsilon c, 9.98%)
    sys(negative tag run period dependence, 3.38%)
    sys(d0 and z0 smearing, 0.53%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3659,0.0528)
    usys(MC statistics, 4.37%)
    sys(jet energy scale,-7.69%)
    sys(jet energy resolution,-1.60%)
    sys(b-fractions, 0.10%)
    sys(c-fractions, 0.13%)
    sys(track multiplicity, 2.79%)
    sys(long lived particles, 4.03%)
    sys(fake tracks,-0.36%)
    sys(photon conversions, 2.12%)
    sys(trigger bias, 0.26%)
    sys(negative tag epsilon b, 0.70%)
    sys(negative tag epsilon c, 2.29%)
    sys(negative tag run period dependence, 0.14%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3041,0.0339)
    usys(MC statistics, 2.82%)
    sys(jet energy scale,-2.25%)
    sys(jet energy resolution, 2.05%)
    sys(b-fractions, 0.29%)
    sys(c-fractions, 1.09%)
    sys(track multiplicity, 3.82%)
    sys(long lived particles,-0.82%)
    sys(fake tracks,-0.55%)
    sys(photon conversions, 4.05%)
    sys(trigger bias, 0.26%)
    sys(negative tag epsilon b, 1.02%)
    sys(negative tag epsilon c, 3.46%)
    sys(negative tag run period dependence, 3.76%)
    sys(d0 and z0 smearing,-2.60%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3359,0.0224)
    usys(MC statistics, 2.17%)
    sys(jet energy scale, 0.49%)
    sys(jet energy resolution,-0.55%)
    sys(b-fractions, 1.11%)
    sys(c-fractions, 2.82%)
    sys(track multiplicity, 8.12%)
    sys(long lived particles, 1.22%)
    sys(fake tracks,-0.95%)
    sys(photon conversions, 7.16%)
    sys(trigger bias,10.66%)
    sys(negative tag epsilon b, 2.83%)
    sys(negative tag epsilon c, 5.75%)
    sys(negative tag run period dependence, 2.51%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5440,0.0220)
    usys(MC statistics, 4.18%)
    sys(jet energy scale,-2.26%)
    sys(jet energy resolution,-5.40%)
    sys(b-fractions, 1.92%)
    sys(c-fractions, 3.88%)
    sys(track multiplicity, 8.74%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-0.28%)
    sys(photon conversions, 9.16%)
    sys(trigger bias,17.77%)
    sys(negative tag epsilon b, 4.22%)
    sys(negative tag epsilon c, 6.86%)
    sys(negative tag run period dependence,-6.56%)
    sys(d0 and z0 smearing, 1.62%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6525,0.0264)
    usys(MC statistics, 4.52%)
    sys(jet energy scale, 0.56%)
    sys(jet energy resolution,-1.47%)
    sys(b-fractions, 2.37%)
    sys(c-fractions, 3.85%)
    sys(track multiplicity, 8.67%)
    sys(long lived particles,-1.01%)
    sys(fake tracks, 0.77%)
    sys(photon conversions, 9.53%)
    sys(trigger bias, 7.93%)
    sys(negative tag epsilon b, 5.09%)
    sys(negative tag epsilon c, 6.72%)
    sys(negative tag run period dependence,-18.39%)
    sys(d0 and z0 smearing,-3.14%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5645,0.0203)
    usys(MC statistics, 5.82%)
    sys(jet energy scale,-3.17%)
    sys(jet energy resolution,-2.48%)
    sys(b-fractions, 2.02%)
    sys(c-fractions, 2.89%)
    sys(track multiplicity,17.36%)
    sys(long lived particles,-1.00%)
    sys(fake tracks, 1.95%)
    sys(photon conversions, 8.32%)
    sys(trigger bias, 0.13%)
    sys(negative tag epsilon b, 5.24%)
    sys(negative tag epsilon c, 6.08%)
    sys(negative tag run period dependence,-2.89%)
    sys(d0 and z0 smearing,-3.14%)
  }
}
