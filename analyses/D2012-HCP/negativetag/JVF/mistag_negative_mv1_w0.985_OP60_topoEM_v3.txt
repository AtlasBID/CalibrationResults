Analysis(negative tags,light,MV1, 0.985, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3786,0.1875)
    usys(MC statistics,12.05%)
    sys(jet energy scale,-6.20%)
    sys(jet energy resolution,-7.67%)
    sys(b-fractions, 0.59%)
    sys(c-fractions, 1.40%)
    sys(track multiplicity, 1.06%)
    sys(long lived particles, 5.19%)
    sys(fake tracks,-0.30%)
    sys(photon conversions, 5.37%)
    sys(trigger bias,18.54%)
    sys(negative tag epsilon b, 1.82%)
    sys(negative tag epsilon c, 3.84%)
    sys(negative tag run period dependence,20.99%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.5495,0.0905)
    usys(MC statistics, 7.72%)
    sys(jet energy scale, 4.46%)
    sys(jet energy resolution,-4.24%)
    sys(b-fractions, 0.58%)
    sys(c-fractions, 2.35%)
    sys(track multiplicity,-0.05%)
    sys(long lived particles,-1.30%)
    sys(fake tracks,-1.15%)
    sys(photon conversions, 7.68%)
    sys(trigger bias,18.54%)
    sys(negative tag epsilon b, 1.58%)
    sys(negative tag epsilon c, 5.01%)
    sys(negative tag run period dependence,20.99%)
    sys(d0 and z0 smearing, 6.57%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2939,0.0343)
    usys(MC statistics, 3.55%)
    sys(jet energy scale, 1.04%)
    sys(jet energy resolution, 1.29%)
    sys(b-fractions, 2.36%)
    sys(c-fractions, 5.66%)
    sys(track multiplicity, 4.68%)
    sys(long lived particles, 1.66%)
    sys(fake tracks,-0.98%)
    sys(photon conversions,13.20%)
    sys(trigger bias,15.39%)
    sys(negative tag epsilon b, 5.35%)
    sys(negative tag epsilon c, 9.26%)
    sys(negative tag run period dependence,-8.03%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4877,0.0249)
    usys(MC statistics, 3.57%)
    sys(jet energy scale, 1.75%)
    sys(jet energy resolution, 3.28%)
    sys(b-fractions, 4.24%)
    sys(c-fractions, 6.86%)
    sys(track multiplicity, 3.83%)
    sys(long lived particles, 2.21%)
    sys(fake tracks,-0.38%)
    sys(photon conversions,19.77%)
    sys(trigger bias,14.34%)
    sys(negative tag epsilon b, 8.85%)
    sys(negative tag epsilon c,10.22%)
    sys(negative tag run period dependence,-5.11%)
    sys(d0 and z0 smearing, 9.77%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4828,0.0230)
    usys(MC statistics, 4.47%)
    sys(jet energy scale, 2.56%)
    sys(jet energy resolution, 2.80%)
    sys(b-fractions, 4.29%)
    sys(c-fractions, 5.60%)
    sys(track multiplicity, 6.43%)
    sys(long lived particles,-1.79%)
    sys(fake tracks, 1.19%)
    sys(photon conversions,18.13%)
    sys(trigger bias, 5.83%)
    sys(negative tag epsilon b, 8.83%)
    sys(negative tag epsilon c, 8.76%)
    sys(negative tag run period dependence,-13.22%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3017,0.0168)
    usys(MC statistics, 5.45%)
    sys(jet energy scale,-4.72%)
    sys(jet energy resolution, 4.58%)
    sys(b-fractions, 4.10%)
    sys(c-fractions, 5.45%)
    sys(track multiplicity,12.41%)
    sys(long lived particles,-1.74%)
    sys(fake tracks, 3.52%)
    sys(photon conversions,17.37%)
    sys(trigger bias,17.59%)
    sys(negative tag epsilon b,10.18%)
    sys(negative tag epsilon c, 9.45%)
    sys(negative tag run period dependence, 4.41%)
    sys(d0 and z0 smearing,-4.83%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(0.8816,0.1799)
    usys(MC statistics,18.55%)
    sys(jet energy scale,-3.52%)
    sys(jet energy resolution, 6.87%)
    sys(b-fractions, 0.08%)
    sys(c-fractions,-0.34%)
    sys(track multiplicity,-0.06%)
    sys(long lived particles, 7.14%)
    sys(fake tracks,-0.82%)
    sys(photon conversions, 4.00%)
    sys(trigger bias,14.36%)
    sys(negative tag epsilon b, 0.66%)
    sys(negative tag epsilon c, 1.73%)
    sys(negative tag run period dependence,24.62%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.6419,0.1282)
    usys(MC statistics, 7.62%)
    sys(jet energy scale,-3.16%)
    sys(jet energy resolution,11.64%)
    sys(b-fractions, 0.46%)
    sys(c-fractions, 1.22%)
    sys(track multiplicity, 0.36%)
    sys(long lived particles,-0.90%)
    sys(fake tracks,-0.51%)
    sys(photon conversions, 6.02%)
    sys(trigger bias,14.36%)
    sys(negative tag epsilon b, 1.36%)
    sys(negative tag epsilon c, 3.58%)
    sys(negative tag run period dependence,24.62%)
    sys(d0 and z0 smearing,-13.50%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.6175,0.0663)
    usys(MC statistics, 5.91%)
    sys(jet energy scale,-1.27%)
    sys(jet energy resolution,-4.20%)
    sys(b-fractions, 1.29%)
    sys(c-fractions, 3.74%)
    sys(track multiplicity, 3.49%)
    sys(long lived particles, 1.54%)
    sys(fake tracks,-0.87%)
    sys(photon conversions, 9.66%)
    sys(trigger bias,21.49%)
    sys(negative tag epsilon b, 3.19%)
    sys(negative tag epsilon c, 6.88%)
    sys(negative tag run period dependence, 9.82%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.7789,0.0592)
    usys(MC statistics, 6.61%)
    sys(jet energy scale,-5.61%)
    sys(jet energy resolution, 1.89%)
    sys(b-fractions, 2.67%)
    sys(c-fractions, 4.11%)
    sys(track multiplicity, 4.31%)
    sys(long lived particles, 2.37%)
    sys(fake tracks,-0.25%)
    sys(photon conversions,11.03%)
    sys(trigger bias,21.49%)
    sys(negative tag epsilon b, 5.71%)
    sys(negative tag epsilon c, 6.98%)
    sys(negative tag run period dependence, 0.25%)
    sys(d0 and z0 smearing,-2.28%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(2.1549,0.0857)
    usys(MC statistics,13.51%)
    sys(jet energy scale,-4.79%)
    sys(jet energy resolution,-5.72%)
    sys(b-fractions, 2.84%)
    sys(c-fractions, 2.70%)
    sys(track multiplicity, 5.71%)
    sys(long lived particles,-1.71%)
    sys(fake tracks, 0.71%)
    sys(photon conversions,11.06%)
    sys(trigger bias,13.69%)
    sys(negative tag epsilon b, 6.05%)
    sys(negative tag epsilon c, 5.13%)
    sys(negative tag run period dependence,-15.57%)
    sys(d0 and z0 smearing,-21.88%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(2.3800,0.0833)
    usys(MC statistics,14.05%)
    sys(jet energy scale,-12.67%)
    sys(jet energy resolution,-8.03%)
    sys(b-fractions, 1.13%)
    sys(c-fractions, 7.17%)
    sys(track multiplicity,10.56%)
    sys(long lived particles,-1.34%)
    sys(fake tracks, 1.47%)
    sys(photon conversions,11.02%)
    sys(trigger bias, 3.63%)
    sys(negative tag epsilon b, 3.01%)
    sys(negative tag epsilon c,12.56%)
    sys(negative tag run period dependence,-15.57%)
    sys(d0 and z0 smearing,-21.88%)
  }
}
