Analysis(negative tags,light,MV1, 0.122, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1958,0.0119)
    usys(MC statistics, 1.05%)
    sys(jet energy scale,-4.72%)
    sys(jet energy resolution,-1.80%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.57%)
    sys(track multiplicity, 7.53%)
    sys(long lived particles, 2.94%)
    sys(fake tracks,-0.34%)
    sys(photon conversions, 3.18%)
    sys(trigger bias, 1.76%)
    sys(negative tag epsilon b, 0.94%)
    sys(negative tag epsilon c, 2.95%)
    sys(negative tag run period dependence, 2.32%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1865,0.0075)
    usys(MC statistics, 0.67%)
    sys(jet energy scale,-0.58%)
    sys(jet energy resolution,-1.13%)
    sys(b-fractions, 0.30%)
    sys(c-fractions, 1.32%)
    sys(track multiplicity, 4.06%)
    sys(long lived particles,-0.82%)
    sys(fake tracks,-0.59%)
    sys(photon conversions, 5.62%)
    sys(trigger bias, 1.76%)
    sys(negative tag epsilon b, 1.09%)
    sys(negative tag epsilon c, 3.95%)
    sys(negative tag run period dependence, 1.65%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1725,0.0040)
    usys(MC statistics, 0.45%)
    sys(jet energy scale, 0.16%)
    sys(jet energy resolution, 0.11%)
    sys(b-fractions, 0.66%)
    sys(c-fractions, 2.39%)
    sys(track multiplicity, 4.10%)
    sys(long lived particles, 0.99%)
    sys(fake tracks,-0.82%)
    sys(photon conversions, 8.39%)
    sys(trigger bias, 4.23%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 5.72%)
    sys(negative tag run period dependence,-2.10%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2206,0.0025)
    usys(MC statistics, 0.58%)
    sys(jet energy scale, 0.48%)
    sys(jet energy resolution, 0.14%)
    sys(b-fractions, 0.89%)
    sys(c-fractions, 3.13%)
    sys(track multiplicity, 2.76%)
    sys(long lived particles, 1.27%)
    sys(fake tracks,-0.25%)
    sys(photon conversions,10.03%)
    sys(trigger bias, 4.00%)
    sys(negative tag epsilon b, 2.40%)
    sys(negative tag epsilon c, 6.76%)
    sys(negative tag run period dependence,-4.71%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.2742,0.0023)
    usys(MC statistics, 0.45%)
    sys(jet energy scale, 0.60%)
    sys(jet energy resolution,-0.00%)
    sys(b-fractions, 0.82%)
    sys(c-fractions, 2.88%)
    sys(track multiplicity, 3.39%)
    sys(long lived particles,-0.97%)
    sys(fake tracks, 0.84%)
    sys(photon conversions, 9.35%)
    sys(trigger bias, 2.41%)
    sys(negative tag epsilon b, 2.20%)
    sys(negative tag epsilon c, 6.57%)
    sys(negative tag run period dependence,-5.70%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2064,0.0015)
    usys(MC statistics, 0.66%)
    sys(jet energy scale, 1.12%)
    sys(jet energy resolution, 1.89%)
    sys(b-fractions, 0.67%)
    sys(c-fractions, 2.62%)
    sys(track multiplicity, 6.23%)
    sys(long lived particles,-0.76%)
    sys(fake tracks, 2.24%)
    sys(photon conversions, 8.12%)
    sys(trigger bias, 0.66%)
    sys(negative tag epsilon b, 2.14%)
    sys(negative tag epsilon c, 6.75%)
    sys(negative tag run period dependence,-0.83%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1658,0.0130)
    usys(MC statistics, 1.13%)
    sys(jet energy scale,-6.32%)
    sys(jet energy resolution,-1.49%)
    sys(b-fractions, 0.07%)
    sys(c-fractions, 0.25%)
    sys(track multiplicity, 6.27%)
    sys(long lived particles, 2.37%)
    sys(fake tracks,-0.31%)
    sys(photon conversions, 2.00%)
    sys(trigger bias, 2.26%)
    sys(negative tag epsilon b, 0.63%)
    sys(negative tag epsilon c, 2.44%)
    sys(negative tag run period dependence, 3.51%)
    sys(d0 and z0 smearing, 0.54%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2058,0.0092)
    usys(MC statistics, 0.78%)
    sys(jet energy scale,-1.49%)
    sys(jet energy resolution,-2.64%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.71%)
    sys(track multiplicity, 4.12%)
    sys(long lived particles,-0.64%)
    sys(fake tracks,-0.47%)
    sys(photon conversions, 3.56%)
    sys(trigger bias, 2.26%)
    sys(negative tag epsilon b, 0.79%)
    sys(negative tag epsilon c, 3.04%)
    sys(negative tag run period dependence, 2.32%)
    sys(d0 and z0 smearing,-0.08%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1779,0.0055)
    usys(MC statistics, 0.60%)
    sys(jet energy scale,-0.21%)
    sys(jet energy resolution, 0.07%)
    sys(b-fractions, 0.39%)
    sys(c-fractions, 1.42%)
    sys(track multiplicity, 4.83%)
    sys(long lived particles, 0.74%)
    sys(fake tracks,-0.66%)
    sys(photon conversions, 5.43%)
    sys(trigger bias, 5.88%)
    sys(negative tag epsilon b, 1.33%)
    sys(negative tag epsilon c, 4.12%)
    sys(negative tag run period dependence,-2.55%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2576,0.0042)
    usys(MC statistics, 0.85%)
    sys(jet energy scale,-0.32%)
    sys(jet energy resolution, 1.04%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 1.89%)
    sys(track multiplicity, 3.38%)
    sys(long lived particles, 1.02%)
    sys(fake tracks,-0.20%)
    sys(photon conversions, 6.60%)
    sys(trigger bias, 5.52%)
    sys(negative tag epsilon b, 1.44%)
    sys(negative tag epsilon c, 4.70%)
    sys(negative tag run period dependence,-4.97%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3121,0.0048)
    usys(MC statistics, 0.82%)
    sys(jet energy scale, 0.37%)
    sys(jet energy resolution,-0.13%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 2.03%)
    sys(track multiplicity, 4.27%)
    sys(long lived particles,-0.78%)
    sys(fake tracks, 0.57%)
    sys(photon conversions, 6.50%)
    sys(trigger bias, 2.13%)
    sys(negative tag epsilon b, 1.38%)
    sys(negative tag epsilon c, 4.82%)
    sys(negative tag run period dependence,-4.67%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3395,0.0038)
    usys(MC statistics, 1.07%)
    sys(jet energy scale,-1.98%)
    sys(jet energy resolution,-4.37%)
    sys(b-fractions, 0.37%)
    sys(c-fractions, 1.61%)
    sys(track multiplicity, 6.66%)
    sys(long lived particles,-0.67%)
    sys(fake tracks, 1.49%)
    sys(photon conversions, 6.10%)
    sys(trigger bias, 6.02%)
    sys(negative tag epsilon b, 1.24%)
    sys(negative tag epsilon c, 4.59%)
    sys(negative tag run period dependence,-4.24%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
