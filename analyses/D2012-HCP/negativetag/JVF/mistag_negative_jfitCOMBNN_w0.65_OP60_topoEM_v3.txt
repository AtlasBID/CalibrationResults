Analysis(negative tags,light,JetFitterCOMBNN,  0.65, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.7012,0.1532)
    usys(MC statistics,10.54%)
    sys(jet energy scale,-6.35%)
    sys(jet energy resolution,-15.33%)
    sys(b-fractions, 0.86%)
    sys(c-fractions, 1.34%)
    sys(track multiplicity, 4.13%)
    sys(long lived particles, 5.79%)
    sys(fake tracks,-0.38%)
    sys(photon conversions, 3.82%)
    sys(trigger bias,14.50%)
    sys(negative tag epsilon b, 2.44%)
    sys(negative tag epsilon c, 3.71%)
    sys(negative tag run period dependence,14.54%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.5234,0.0793)
    usys(MC statistics, 7.00%)
    sys(jet energy scale, 3.54%)
    sys(jet energy resolution,-0.92%)
    sys(b-fractions, 1.33%)
    sys(c-fractions, 3.29%)
    sys(track multiplicity, 2.35%)
    sys(long lived particles,-1.48%)
    sys(fake tracks,-0.76%)
    sys(photon conversions, 7.02%)
    sys(trigger bias,14.50%)
    sys(negative tag epsilon b, 2.96%)
    sys(negative tag epsilon c, 5.92%)
    sys(negative tag run period dependence,-5.93%)
    sys(d0 and z0 smearing,-0.81%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.4178,0.0370)
    usys(MC statistics, 3.55%)
    sys(jet energy scale, 1.34%)
    sys(jet energy resolution,-0.08%)
    sys(b-fractions, 3.38%)
    sys(c-fractions, 5.61%)
    sys(track multiplicity,10.52%)
    sys(long lived particles, 1.87%)
    sys(fake tracks,-1.29%)
    sys(photon conversions,13.40%)
    sys(trigger bias, 9.65%)
    sys(negative tag epsilon b, 7.42%)
    sys(negative tag epsilon c, 8.90%)
    sys(negative tag run period dependence, 5.51%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.4847,0.0262)
    usys(MC statistics, 3.59%)
    sys(jet energy scale,-3.17%)
    sys(jet energy resolution, 3.48%)
    sys(b-fractions, 5.55%)
    sys(c-fractions, 5.38%)
    sys(track multiplicity, 8.07%)
    sys(long lived particles, 2.73%)
    sys(fake tracks,-0.50%)
    sys(photon conversions,17.67%)
    sys(trigger bias,15.05%)
    sys(negative tag epsilon b,11.45%)
    sys(negative tag epsilon c, 8.00%)
    sys(negative tag run period dependence,-13.66%)
    sys(d0 and z0 smearing,10.74%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.6468,0.0259)
    usys(MC statistics, 4.64%)
    sys(jet energy scale, 3.61%)
    sys(jet energy resolution,10.08%)
    sys(b-fractions, 6.06%)
    sys(c-fractions, 4.84%)
    sys(track multiplicity,17.88%)
    sys(long lived particles,-1.65%)
    sys(fake tracks, 1.53%)
    sys(photon conversions,17.40%)
    sys(trigger bias,21.44%)
    sys(negative tag epsilon b,12.26%)
    sys(negative tag epsilon c, 7.24%)
    sys(negative tag run period dependence,-4.34%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2181,0.0159)
    usys(MC statistics, 5.25%)
    sys(jet energy scale, 3.19%)
    sys(jet energy resolution, 5.14%)
    sys(b-fractions, 5.36%)
    sys(c-fractions, 5.18%)
    sys(track multiplicity,17.88%)
    sys(long lived particles,-1.57%)
    sys(fake tracks, 3.90%)
    sys(photon conversions,15.70%)
    sys(trigger bias,21.44%)
    sys(negative tag epsilon b,13.15%)
    sys(negative tag epsilon c, 8.56%)
    sys(negative tag run period dependence,-4.34%)
    sys(d0 and z0 smearing, 1.33%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3746,0.1517)
    usys(MC statistics,21.48%)
    sys(jet energy scale,-6.00%)
    sys(jet energy resolution,10.54%)
    sys(b-fractions, 0.27%)
    sys(c-fractions,-0.12%)
    sys(track multiplicity,-2.21%)
    sys(long lived particles, 4.48%)
    sys(fake tracks,-0.61%)
    sys(photon conversions, 3.00%)
    sys(trigger bias,-0.59%)
    sys(negative tag epsilon b, 1.09%)
    sys(negative tag epsilon c, 1.96%)
    sys(negative tag run period dependence,22.16%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4158,0.1008)
    usys(MC statistics, 7.63%)
    sys(jet energy scale, 1.16%)
    sys(jet energy resolution,10.54%)
    sys(b-fractions, 0.62%)
    sys(c-fractions, 0.99%)
    sys(track multiplicity, 5.57%)
    sys(long lived particles,-0.93%)
    sys(fake tracks,-0.53%)
    sys(photon conversions, 4.05%)
    sys(trigger bias,-0.59%)
    sys(negative tag epsilon b, 1.66%)
    sys(negative tag epsilon c, 3.28%)
    sys(negative tag run period dependence,22.16%)
    sys(d0 and z0 smearing,-9.55%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.5454,0.0675)
    usys(MC statistics, 6.21%)
    sys(jet energy scale, 1.05%)
    sys(jet energy resolution, 0.01%)
    sys(b-fractions, 1.96%)
    sys(c-fractions, 3.54%)
    sys(track multiplicity, 9.36%)
    sys(long lived particles, 1.64%)
    sys(fake tracks,-1.08%)
    sys(photon conversions, 8.46%)
    sys(trigger bias,21.69%)
    sys(negative tag epsilon b, 4.60%)
    sys(negative tag epsilon c, 6.46%)
    sys(negative tag run period dependence,-0.15%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4521,0.0583)
    usys(MC statistics, 8.85%)
    sys(jet energy scale,-7.58%)
    sys(jet energy resolution,15.76%)
    sys(b-fractions, 3.95%)
    sys(c-fractions, 4.48%)
    sys(track multiplicity,10.49%)
    sys(long lived particles, 2.03%)
    sys(fake tracks,-0.32%)
    sys(photon conversions,10.50%)
    sys(trigger bias,21.69%)
    sys(negative tag epsilon b, 8.25%)
    sys(negative tag epsilon c, 7.14%)
    sys(negative tag run period dependence,-16.19%)
    sys(d0 and z0 smearing,-1.22%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5445,0.0738)
    usys(MC statistics,16.32%)
    sys(jet energy scale,-5.89%)
    sys(jet energy resolution, 7.22%)
    sys(b-fractions, 3.96%)
    sys(c-fractions, 3.28%)
    sys(track multiplicity,12.87%)
    sys(long lived particles,-1.40%)
    sys(fake tracks, 0.83%)
    sys(photon conversions,10.58%)
    sys(trigger bias, 1.99%)
    sys(negative tag epsilon b, 8.28%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence,-14.94%)
    sys(d0 and z0 smearing,-9.06%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5037,0.0599)
    usys(MC statistics,15.12%)
    sys(jet energy scale,-15.43%)
    sys(jet energy resolution,-11.08%)
    sys(b-fractions, 2.24%)
    sys(c-fractions, 4.02%)
    sys(track multiplicity,17.57%)
    sys(long lived particles,-1.58%)
    sys(fake tracks, 2.26%)
    sys(photon conversions,10.56%)
    sys(trigger bias,-3.57%)
    sys(negative tag epsilon b, 5.75%)
    sys(negative tag epsilon c, 7.66%)
    sys(negative tag run period dependence,-6.22%)
    sys(d0 and z0 smearing,-9.06%)
  }
}
