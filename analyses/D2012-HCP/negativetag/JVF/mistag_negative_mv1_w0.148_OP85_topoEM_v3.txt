Analysis(negative tags,light,MV1, 0.148, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2265,0.0127)
    usys(MC statistics, 1.09%)
    sys(jet energy scale,-1.50%)
    sys(jet energy resolution,-1.16%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 0.71%)
    sys(track multiplicity, 6.09%)
    sys(long lived particles, 3.22%)
    sys(fake tracks,-0.35%)
    sys(photon conversions, 3.52%)
    sys(trigger bias, 2.11%)
    sys(negative tag epsilon b, 0.99%)
    sys(negative tag epsilon c, 3.09%)
    sys(negative tag run period dependence, 2.42%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1982,0.0071)
    usys(MC statistics, 0.68%)
    sys(jet energy scale,-0.96%)
    sys(jet energy resolution,-0.77%)
    sys(b-fractions, 0.29%)
    sys(c-fractions, 1.17%)
    sys(track multiplicity, 3.65%)
    sys(long lived particles,-0.83%)
    sys(fake tracks,-0.60%)
    sys(photon conversions, 5.69%)
    sys(trigger bias, 2.11%)
    sys(negative tag epsilon b, 1.07%)
    sys(negative tag epsilon c, 3.73%)
    sys(negative tag run period dependence, 1.14%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1698,0.0039)
    usys(MC statistics, 0.43%)
    sys(jet energy scale, 0.24%)
    sys(jet energy resolution,-0.09%)
    sys(b-fractions, 0.64%)
    sys(c-fractions, 2.35%)
    sys(track multiplicity, 4.73%)
    sys(long lived particles, 0.99%)
    sys(fake tracks,-0.86%)
    sys(photon conversions, 8.63%)
    sys(trigger bias, 5.49%)
    sys(negative tag epsilon b, 1.95%)
    sys(negative tag epsilon c, 5.65%)
    sys(negative tag run period dependence,-2.31%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2353,0.0026)
    usys(MC statistics, 0.59%)
    sys(jet energy scale, 1.00%)
    sys(jet energy resolution, 0.09%)
    sys(b-fractions, 0.90%)
    sys(c-fractions, 3.35%)
    sys(track multiplicity, 2.94%)
    sys(long lived particles, 1.30%)
    sys(fake tracks,-0.28%)
    sys(photon conversions,10.45%)
    sys(trigger bias, 6.09%)
    sys(negative tag epsilon b, 2.40%)
    sys(negative tag epsilon c, 6.98%)
    sys(negative tag run period dependence,-4.49%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.2816,0.0024)
    usys(MC statistics, 0.53%)
    sys(jet energy scale, 0.73%)
    sys(jet energy resolution, 0.33%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 3.14%)
    sys(track multiplicity, 3.05%)
    sys(long lived particles,-0.97%)
    sys(fake tracks, 0.86%)
    sys(photon conversions, 9.52%)
    sys(trigger bias, 2.68%)
    sys(negative tag epsilon b, 2.31%)
    sys(negative tag epsilon c, 6.88%)
    sys(negative tag run period dependence,-7.41%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2133,0.0017)
    usys(MC statistics, 0.65%)
    sys(jet energy scale,-0.23%)
    sys(jet energy resolution, 0.68%)
    sys(b-fractions, 0.70%)
    sys(c-fractions, 2.71%)
    sys(track multiplicity, 6.57%)
    sys(long lived particles,-0.77%)
    sys(fake tracks, 2.42%)
    sys(photon conversions, 8.93%)
    sys(trigger bias, 6.63%)
    sys(negative tag epsilon b, 2.28%)
    sys(negative tag epsilon c, 7.01%)
    sys(negative tag run period dependence,-0.99%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1673,0.0138)
    usys(MC statistics, 1.20%)
    sys(jet energy scale,-4.93%)
    sys(jet energy resolution,-2.13%)
    sys(b-fractions, 0.09%)
    sys(c-fractions, 0.22%)
    sys(track multiplicity, 5.41%)
    sys(long lived particles, 2.54%)
    sys(fake tracks,-0.32%)
    sys(photon conversions, 2.13%)
    sys(trigger bias, 3.29%)
    sys(negative tag epsilon b, 0.67%)
    sys(negative tag epsilon c, 2.40%)
    sys(negative tag run period dependence, 1.15%)
    sys(d0 and z0 smearing, 0.54%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2086,0.0087)
    usys(MC statistics, 0.76%)
    sys(jet energy scale,-1.29%)
    sys(jet energy resolution,-1.87%)
    sys(b-fractions, 0.15%)
    sys(c-fractions, 0.59%)
    sys(track multiplicity, 4.10%)
    sys(long lived particles,-0.64%)
    sys(fake tracks,-0.47%)
    sys(photon conversions, 3.64%)
    sys(trigger bias, 3.29%)
    sys(negative tag epsilon b, 0.76%)
    sys(negative tag epsilon c, 2.90%)
    sys(negative tag run period dependence, 2.02%)
    sys(d0 and z0 smearing,-0.08%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1723,0.0051)
    usys(MC statistics, 0.55%)
    sys(jet energy scale, 0.10%)
    sys(jet energy resolution, 0.45%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.30%)
    sys(track multiplicity, 5.58%)
    sys(long lived particles, 0.71%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 5.40%)
    sys(trigger bias, 6.20%)
    sys(negative tag epsilon b, 1.21%)
    sys(negative tag epsilon c, 4.01%)
    sys(negative tag run period dependence,-1.59%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2461,0.0041)
    usys(MC statistics, 0.92%)
    sys(jet energy scale, 0.76%)
    sys(jet energy resolution, 0.19%)
    sys(b-fractions, 0.49%)
    sys(c-fractions, 1.81%)
    sys(track multiplicity, 3.51%)
    sys(long lived particles, 0.98%)
    sys(fake tracks,-0.20%)
    sys(photon conversions, 6.60%)
    sys(trigger bias, 5.93%)
    sys(negative tag epsilon b, 1.41%)
    sys(negative tag epsilon c, 4.56%)
    sys(negative tag run period dependence,-4.28%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3442,0.0049)
    usys(MC statistics, 0.92%)
    sys(jet energy scale, 0.93%)
    sys(jet energy resolution, 0.22%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 1.99%)
    sys(track multiplicity, 3.86%)
    sys(long lived particles,-0.77%)
    sys(fake tracks, 0.58%)
    sys(photon conversions, 6.66%)
    sys(trigger bias, 2.37%)
    sys(negative tag epsilon b, 1.39%)
    sys(negative tag epsilon c, 4.80%)
    sys(negative tag run period dependence,-6.44%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2953,0.0038)
    usys(MC statistics, 1.48%)
    sys(jet energy scale, 2.31%)
    sys(jet energy resolution, 0.47%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.57%)
    sys(track multiplicity, 7.17%)
    sys(long lived particles,-0.67%)
    sys(fake tracks, 1.54%)
    sys(photon conversions, 6.17%)
    sys(trigger bias, 1.51%)
    sys(negative tag epsilon b, 1.19%)
    sys(negative tag epsilon c, 4.63%)
    sys(negative tag run period dependence,-3.41%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
