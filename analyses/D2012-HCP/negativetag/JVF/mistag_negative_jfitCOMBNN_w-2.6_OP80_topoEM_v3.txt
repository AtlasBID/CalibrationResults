Analysis(negative tags,light,JetFitterCOMBNN, -2.60, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2671,0.0173)
    usys(MC statistics, 1.41%)
    sys(jet energy scale,-3.33%)
    sys(jet energy resolution,-2.80%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 0.86%)
    sys(track multiplicity, 5.67%)
    sys(long lived particles, 3.88%)
    sys(fake tracks,-0.42%)
    sys(photon conversions, 3.81%)
    sys(trigger bias, 2.84%)
    sys(negative tag epsilon b, 1.16%)
    sys(negative tag epsilon c, 3.25%)
    sys(negative tag run period dependence, 1.88%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2359,0.0104)
    usys(MC statistics, 0.96%)
    sys(jet energy scale,-1.61%)
    sys(jet energy resolution,-2.07%)
    sys(b-fractions, 0.30%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity, 4.15%)
    sys(long lived particles,-0.92%)
    sys(fake tracks,-0.65%)
    sys(photon conversions, 5.29%)
    sys(trigger bias, 2.84%)
    sys(negative tag epsilon b, 1.09%)
    sys(negative tag epsilon c, 3.60%)
    sys(negative tag run period dependence, 1.88%)
    sys(d0 and z0 smearing, 1.39%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2124,0.0058)
    usys(MC statistics, 0.62%)
    sys(jet energy scale, 0.15%)
    sys(jet energy resolution, 0.54%)
    sys(b-fractions, 0.76%)
    sys(c-fractions, 2.70%)
    sys(track multiplicity, 7.84%)
    sys(long lived particles, 1.10%)
    sys(fake tracks,-0.97%)
    sys(photon conversions, 8.27%)
    sys(trigger bias, 7.11%)
    sys(negative tag epsilon b, 2.18%)
    sys(negative tag epsilon c, 6.06%)
    sys(negative tag run period dependence,-2.29%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3177,0.0040)
    usys(MC statistics, 0.86%)
    sys(jet energy scale, 1.56%)
    sys(jet energy resolution,-0.52%)
    sys(b-fractions, 1.26%)
    sys(c-fractions, 4.25%)
    sys(track multiplicity, 5.63%)
    sys(long lived particles, 1.63%)
    sys(fake tracks,-0.34%)
    sys(photon conversions,11.27%)
    sys(trigger bias, 7.85%)
    sys(negative tag epsilon b, 3.08%)
    sys(negative tag epsilon c, 7.97%)
    sys(negative tag run period dependence,-5.36%)
    sys(d0 and z0 smearing, 5.79%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3640,0.0036)
    usys(MC statistics, 0.75%)
    sys(jet energy scale, 0.84%)
    sys(jet energy resolution, 0.93%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 4.03%)
    sys(track multiplicity, 5.74%)
    sys(long lived particles,-1.12%)
    sys(fake tracks, 1.06%)
    sys(photon conversions,10.83%)
    sys(trigger bias, 3.41%)
    sys(negative tag epsilon b, 3.11%)
    sys(negative tag epsilon c, 7.82%)
    sys(negative tag run period dependence,-9.39%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2766,0.0024)
    usys(MC statistics, 0.89%)
    sys(jet energy scale,-0.53%)
    sys(jet energy resolution, 1.07%)
    sys(b-fractions, 1.02%)
    sys(c-fractions, 3.61%)
    sys(track multiplicity,10.64%)
    sys(long lived particles,-0.86%)
    sys(fake tracks, 2.84%)
    sys(photon conversions,10.15%)
    sys(trigger bias, 9.36%)
    sys(negative tag epsilon b, 3.02%)
    sys(negative tag epsilon c, 8.14%)
    sys(negative tag run period dependence,-0.82%)
    sys(d0 and z0 smearing,-0.26%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1966,0.0171)
    usys(MC statistics, 1.44%)
    sys(jet energy scale,-6.91%)
    sys(jet energy resolution,-1.98%)
    sys(b-fractions, 0.07%)
    sys(c-fractions, 0.19%)
    sys(track multiplicity, 6.04%)
    sys(long lived particles, 2.82%)
    sys(fake tracks,-0.31%)
    sys(photon conversions, 2.00%)
    sys(trigger bias, 2.63%)
    sys(negative tag epsilon b, 0.64%)
    sys(negative tag epsilon c, 2.37%)
    sys(negative tag run period dependence, 3.57%)
    sys(d0 and z0 smearing, 0.03%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2276,0.0115)
    usys(MC statistics, 0.99%)
    sys(jet energy scale,-1.79%)
    sys(jet energy resolution,-0.66%)
    sys(b-fractions, 0.11%)
    sys(c-fractions, 0.52%)
    sys(track multiplicity, 5.07%)
    sys(long lived particles,-0.64%)
    sys(fake tracks,-0.39%)
    sys(photon conversions, 3.03%)
    sys(trigger bias, 2.63%)
    sys(negative tag epsilon b, 0.68%)
    sys(negative tag epsilon c, 2.83%)
    sys(negative tag run period dependence,-1.79%)
    sys(d0 and z0 smearing,-0.89%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2097,0.0072)
    usys(MC statistics, 0.75%)
    sys(jet energy scale,-0.21%)
    sys(jet energy resolution, 0.15%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 1.41%)
    sys(track multiplicity, 8.32%)
    sys(long lived particles, 0.70%)
    sys(fake tracks,-0.68%)
    sys(photon conversions, 4.86%)
    sys(trigger bias, 6.10%)
    sys(negative tag epsilon b, 1.26%)
    sys(negative tag epsilon c, 4.15%)
    sys(negative tag run period dependence,-0.28%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.2857,0.0060)
    usys(MC statistics, 1.39%)
    sys(jet energy scale, 1.02%)
    sys(jet energy resolution, 1.69%)
    sys(b-fractions, 0.67%)
    sys(c-fractions, 2.20%)
    sys(track multiplicity, 5.77%)
    sys(long lived particles, 1.04%)
    sys(fake tracks,-0.22%)
    sys(photon conversions, 6.67%)
    sys(trigger bias, 5.86%)
    sys(negative tag epsilon b, 1.76%)
    sys(negative tag epsilon c, 5.02%)
    sys(negative tag run period dependence,-5.26%)
    sys(d0 and z0 smearing, 1.23%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3753,0.0070)
    usys(MC statistics, 1.31%)
    sys(jet energy scale, 1.43%)
    sys(jet energy resolution, 0.33%)
    sys(b-fractions, 0.71%)
    sys(c-fractions, 2.65%)
    sys(track multiplicity, 5.70%)
    sys(long lived particles,-0.86%)
    sys(fake tracks, 0.63%)
    sys(photon conversions, 7.20%)
    sys(trigger bias, 3.08%)
    sys(negative tag epsilon b, 1.80%)
    sys(negative tag epsilon c, 5.58%)
    sys(negative tag run period dependence,-9.78%)
    sys(d0 and z0 smearing, 1.89%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3233,0.0054)
    usys(MC statistics, 2.32%)
    sys(jet energy scale, 3.45%)
    sys(jet energy resolution, 3.13%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 2.01%)
    sys(track multiplicity, 9.64%)
    sys(long lived particles,-0.71%)
    sys(fake tracks, 1.63%)
    sys(photon conversions, 6.54%)
    sys(trigger bias,-1.44%)
    sys(negative tag epsilon b, 1.60%)
    sys(negative tag epsilon c, 5.22%)
    sys(negative tag run period dependence,-4.28%)
    sys(d0 and z0 smearing, 1.89%)
  }
}
