Analysis(negative tags,light,MV2, 0.010, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.5633,0.1009)
    usys(MC statistics, 6.68%)
    sys(jet energy scale, 5.89%)
    sys(jet energy resolution,-1.84%)
    sys(b-fractions, 0.53%)
    sys(c-fractions, 1.36%)
    sys(track multiplicity, 3.86%)
    sys(long lived particles, 5.01%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 4.85%)
    sys(trigger bias, 3.21%)
    sys(negative tag epsilon b, 1.69%)
    sys(negative tag epsilon c, 3.81%)
    sys(negative tag run period dependence, 5.85%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2743,0.0135)
    usys(MC statistics, 1.30%)
    sys(jet energy scale, 5.89%)
    sys(jet energy resolution,-1.84%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 1.41%)
    sys(track multiplicity, 2.43%)
    sys(long lived particles,-0.99%)
    sys(fake tracks,-0.74%)
    sys(photon conversions, 6.43%)
    sys(trigger bias, 3.21%)
    sys(negative tag epsilon b, 1.18%)
    sys(negative tag epsilon c, 3.99%)
    sys(negative tag run period dependence, 1.60%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1775,0.0052)
    usys(MC statistics, 0.57%)
    sys(jet energy scale,-1.98%)
    sys(jet energy resolution, 2.38%)
    sys(b-fractions, 0.73%)
    sys(c-fractions, 2.50%)
    sys(track multiplicity, 4.32%)
    sys(long lived particles, 1.01%)
    sys(fake tracks,-0.84%)
    sys(photon conversions, 8.80%)
    sys(trigger bias, 7.72%)
    sys(negative tag epsilon b, 2.12%)
    sys(negative tag epsilon c, 5.82%)
    sys(negative tag run period dependence,-0.10%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3805,0.0115)
    usys(MC statistics, 1.67%)
    sys(jet energy scale,-9.06%)
    sys(jet energy resolution,-9.81%)
    sys(b-fractions, 2.32%)
    sys(c-fractions, 5.21%)
    sys(track multiplicity, 4.39%)
    sys(long lived particles, 2.28%)
    sys(fake tracks,-0.31%)
    sys(photon conversions,15.06%)
    sys(trigger bias,15.23%)
    sys(negative tag epsilon b, 5.13%)
    sys(negative tag epsilon c, 8.81%)
    sys(negative tag run period dependence,10.59%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.5036,0.0183)
    usys(MC statistics, 3.42%)
    sys(jet energy scale,-0.89%)
    sys(jet energy resolution, 4.16%)
    sys(b-fractions, 3.88%)
    sys(c-fractions, 5.11%)
    sys(track multiplicity, 7.52%)
    sys(long lived particles,-1.53%)
    sys(fake tracks, 1.32%)
    sys(photon conversions,17.23%)
    sys(trigger bias, 6.84%)
    sys(negative tag epsilon b, 8.06%)
    sys(negative tag epsilon c, 8.29%)
    sys(negative tag run period dependence,-15.42%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2167,0.0112)
    usys(MC statistics, 4.83%)
    sys(jet energy scale, 6.08%)
    sys(jet energy resolution, 6.68%)
    sys(b-fractions, 3.03%)
    sys(c-fractions, 6.22%)
    sys(track multiplicity,14.68%)
    sys(long lived particles,-1.51%)
    sys(fake tracks, 3.65%)
    sys(photon conversions,14.93%)
    sys(trigger bias, 0.90%)
    sys(negative tag epsilon b, 7.66%)
    sys(negative tag epsilon c,10.95%)
    sys(negative tag run period dependence, 5.16%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3863,0.1055)
    usys(MC statistics,11.83%)
    sys(jet energy scale, 6.46%)
    sys(jet energy resolution, 1.73%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.15%)
    sys(track multiplicity, 0.68%)
    sys(long lived particles, 4.82%)
    sys(fake tracks,-0.51%)
    sys(photon conversions, 2.89%)
    sys(trigger bias, 5.60%)
    sys(negative tag epsilon b, 0.85%)
    sys(negative tag epsilon c, 2.30%)
    sys(negative tag run period dependence, 4.01%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3079,0.0179)
    usys(MC statistics, 1.61%)
    sys(jet energy scale, 6.46%)
    sys(jet energy resolution, 1.73%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.86%)
    sys(track multiplicity, 4.01%)
    sys(long lived particles,-0.78%)
    sys(fake tracks,-0.60%)
    sys(photon conversions, 4.39%)
    sys(trigger bias, 5.60%)
    sys(negative tag epsilon b, 0.83%)
    sys(negative tag epsilon c, 3.22%)
    sys(negative tag run period dependence, 4.01%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2307,0.0087)
    usys(MC statistics, 0.93%)
    sys(jet energy scale,-4.14%)
    sys(jet energy resolution, 1.88%)
    sys(b-fractions, 0.46%)
    sys(c-fractions, 1.58%)
    sys(track multiplicity, 5.35%)
    sys(long lived particles, 0.89%)
    sys(fake tracks,-0.66%)
    sys(photon conversions, 5.61%)
    sys(trigger bias, 9.23%)
    sys(negative tag epsilon b, 1.48%)
    sys(negative tag epsilon c, 4.33%)
    sys(negative tag run period dependence, 5.18%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5454,0.0233)
    usys(MC statistics, 4.79%)
    sys(jet energy scale,-10.90%)
    sys(jet energy resolution,-9.06%)
    sys(b-fractions, 1.51%)
    sys(c-fractions, 3.74%)
    sys(track multiplicity, 5.76%)
    sys(long lived particles, 2.03%)
    sys(fake tracks,-0.23%)
    sys(photon conversions, 8.69%)
    sys(trigger bias,12.12%)
    sys(negative tag epsilon b, 3.40%)
    sys(negative tag epsilon c, 6.78%)
    sys(negative tag run period dependence, 5.25%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.7833,0.0460)
    usys(MC statistics, 7.65%)
    sys(jet energy scale,-3.08%)
    sys(jet energy resolution,-18.32%)
    sys(b-fractions, 1.80%)
    sys(c-fractions, 2.59%)
    sys(track multiplicity, 5.93%)
    sys(long lived particles,-1.30%)
    sys(fake tracks, 0.72%)
    sys(photon conversions, 9.64%)
    sys(trigger bias, 4.78%)
    sys(negative tag epsilon b, 3.97%)
    sys(negative tag epsilon c, 5.25%)
    sys(negative tag run period dependence,-12.00%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.8430,0.0370)
    usys(MC statistics, 7.76%)
    sys(jet energy scale,-3.74%)
    sys(jet energy resolution,-18.32%)
    sys(b-fractions, 1.21%)
    sys(c-fractions, 3.63%)
    sys(track multiplicity,15.48%)
    sys(long lived particles,-1.22%)
    sys(fake tracks, 1.94%)
    sys(photon conversions, 9.37%)
    sys(trigger bias,12.45%)
    sys(negative tag epsilon b, 3.26%)
    sys(negative tag epsilon c, 7.38%)
    sys(negative tag run period dependence,-7.70%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
