Analysis(negative tags,light,MV2, -0.098, AntiKt4TopoEM){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3087,0.0331)
    usys(MC statistics, 2.61%)
    sys(jet energy scale,-19.03%)
    sys(jet energy resolution,-11.26%)
    sys(b-fractions, 0.26%)
    sys(c-fractions, 1.04%)
    sys(track multiplicity, 3.73%)
    sys(long lived particles, 4.51%)
    sys(fake tracks,-0.46%)
    sys(photon conversions, 4.21%)
    sys(trigger bias, 1.52%)
    sys(negative tag epsilon b, 1.11%)
    sys(negative tag epsilon c, 3.48%)
    sys(negative tag run period dependence,-4.06%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1427,0.0050)
    usys(MC statistics, 0.57%)
    sys(jet energy scale, 7.19%)
    sys(jet energy resolution,-11.26%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.72%)
    sys(track multiplicity, 4.53%)
    sys(long lived particles,-0.57%)
    sys(fake tracks,-0.44%)
    sys(photon conversions, 4.18%)
    sys(trigger bias, 1.52%)
    sys(negative tag epsilon b, 0.88%)
    sys(negative tag epsilon c, 3.24%)
    sys(negative tag run period dependence, 0.90%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(0.9761,0.0013)
    usys(MC statistics, 0.16%)
    sys(jet energy scale,-3.25%)
    sys(jet energy resolution, 2.38%)
    sys(b-fractions, 0.01%)
    sys(c-fractions, 0.13%)
    sys(track multiplicity,11.01%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.12%)
    sys(photon conversions, 1.82%)
    sys(trigger bias, 3.75%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 3.09%)
    sys(negative tag run period dependence, 4.75%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2861,0.0048)
    usys(MC statistics, 0.93%)
    sys(jet energy scale,-7.03%)
    sys(jet energy resolution,-8.56%)
    sys(b-fractions, 1.30%)
    sys(c-fractions, 4.11%)
    sys(track multiplicity, 3.63%)
    sys(long lived particles, 1.62%)
    sys(fake tracks,-0.28%)
    sys(photon conversions,11.79%)
    sys(trigger bias, 9.17%)
    sys(negative tag epsilon b, 3.16%)
    sys(negative tag epsilon c, 7.79%)
    sys(negative tag run period dependence, 7.98%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4456,0.0075)
    usys(MC statistics, 1.48%)
    sys(jet energy scale, 0.76%)
    sys(jet energy resolution, 1.80%)
    sys(b-fractions, 2.35%)
    sys(c-fractions, 5.50%)
    sys(track multiplicity, 6.65%)
    sys(long lived particles,-1.55%)
    sys(fake tracks, 1.12%)
    sys(photon conversions,14.29%)
    sys(trigger bias, 6.08%)
    sys(negative tag epsilon b, 5.09%)
    sys(negative tag epsilon c, 9.28%)
    sys(negative tag run period dependence,-11.57%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2873,0.0048)
    usys(MC statistics, 1.56%)
    sys(jet energy scale, 1.46%)
    sys(jet energy resolution, 1.31%)
    sys(b-fractions, 1.83%)
    sys(c-fractions, 4.78%)
    sys(track multiplicity,12.87%)
    sys(long lived particles,-1.12%)
    sys(fake tracks, 3.35%)
    sys(photon conversions,12.88%)
    sys(trigger bias,15.50%)
    sys(negative tag epsilon b, 4.89%)
    sys(negative tag epsilon c, 9.44%)
    sys(negative tag run period dependence,-0.26%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3204,0.0388)
    usys(MC statistics, 3.13%)
    sys(jet energy scale, 6.63%)
    sys(jet energy resolution,-2.46%)
    sys(b-fractions, 0.16%)
    sys(c-fractions, 0.16%)
    sys(track multiplicity, 4.55%)
    sys(long lived particles, 4.02%)
    sys(fake tracks,-0.34%)
    sys(photon conversions, 2.48%)
    sys(trigger bias, 2.41%)
    sys(negative tag epsilon b, 0.85%)
    sys(negative tag epsilon c, 2.31%)
    sys(negative tag run period dependence,-5.60%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1668,0.0056)
    usys(MC statistics, 0.54%)
    sys(jet energy scale, 6.63%)
    sys(jet energy resolution,-2.46%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.36%)
    sys(track multiplicity, 4.84%)
    sys(long lived particles,-0.44%)
    sys(fake tracks,-0.39%)
    sys(photon conversions, 2.80%)
    sys(trigger bias, 2.41%)
    sys(negative tag epsilon b, 0.63%)
    sys(negative tag epsilon c, 2.64%)
    sys(negative tag run period dependence, 1.68%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.0888,0.0027)
    usys(MC statistics, 0.32%)
    sys(jet energy scale,-4.34%)
    sys(jet energy resolution, 1.87%)
    sys(b-fractions, 0.11%)
    sys(c-fractions, 0.54%)
    sys(track multiplicity, 7.35%)
    sys(long lived particles, 0.41%)
    sys(fake tracks,-0.36%)
    sys(photon conversions, 2.93%)
    sys(trigger bias, 2.89%)
    sys(negative tag epsilon b, 0.77%)
    sys(negative tag epsilon c, 3.09%)
    sys(negative tag run period dependence, 3.64%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3716,0.0086)
    usys(MC statistics, 2.21%)
    sys(jet energy scale,-10.36%)
    sys(jet energy resolution,-6.72%)
    sys(b-fractions, 0.86%)
    sys(c-fractions, 2.68%)
    sys(track multiplicity, 3.99%)
    sys(long lived particles, 1.34%)
    sys(fake tracks,-0.19%)
    sys(photon conversions, 7.06%)
    sys(trigger bias, 5.94%)
    sys(negative tag epsilon b, 2.14%)
    sys(negative tag epsilon c, 5.58%)
    sys(negative tag run period dependence, 6.36%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6827,0.0169)
    usys(MC statistics, 2.79%)
    sys(jet energy scale, 0.61%)
    sys(jet energy resolution, 0.80%)
    sys(b-fractions, 1.36%)
    sys(c-fractions, 3.44%)
    sys(track multiplicity, 6.52%)
    sys(long lived particles,-1.10%)
    sys(fake tracks, 0.71%)
    sys(photon conversions, 8.86%)
    sys(trigger bias, 5.65%)
    sys(negative tag epsilon b, 3.09%)
    sys(negative tag epsilon c, 6.43%)
    sys(negative tag run period dependence,-15.97%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5099,0.0116)
    usys(MC statistics, 3.89%)
    sys(jet energy scale, 1.89%)
    sys(jet energy resolution,-0.78%)
    sys(b-fractions, 0.99%)
    sys(c-fractions, 2.43%)
    sys(track multiplicity,13.22%)
    sys(long lived particles,-1.04%)
    sys(fake tracks, 1.79%)
    sys(photon conversions, 8.01%)
    sys(trigger bias,-2.00%)
    sys(negative tag epsilon b, 2.77%)
    sys(negative tag epsilon c, 5.69%)
    sys(negative tag run period dependence,-3.49%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
