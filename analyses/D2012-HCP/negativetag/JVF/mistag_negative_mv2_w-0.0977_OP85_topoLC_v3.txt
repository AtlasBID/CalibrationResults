Analysis(negative tags,light,MV2, -0.098, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2682,0.0353)
    usys(MC statistics, 3.02%)
    sys(jet energy scale,-18.38%)
    sys(jet energy resolution,-10.11%)
    sys(b-fractions, 0.32%)
    sys(c-fractions, 0.94%)
    sys(track multiplicity, 1.41%)
    sys(long lived particles, 4.72%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 4.46%)
    sys(trigger bias, 1.00%)
    sys(negative tag epsilon b, 1.24%)
    sys(negative tag epsilon c, 3.37%)
    sys(negative tag run period dependence,-0.23%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1434,0.0053)
    usys(MC statistics, 0.51%)
    sys(jet energy scale, 8.59%)
    sys(jet energy resolution,-10.11%)
    sys(b-fractions, 0.17%)
    sys(c-fractions, 0.80%)
    sys(track multiplicity, 3.94%)
    sys(long lived particles,-0.55%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 4.21%)
    sys(trigger bias, 1.00%)
    sys(negative tag epsilon b, 0.87%)
    sys(negative tag epsilon c, 3.36%)
    sys(negative tag run period dependence, 1.33%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.0135,0.0013)
    usys(MC statistics, 0.16%)
    sys(jet energy scale,-3.57%)
    sys(jet energy resolution, 2.66%)
    sys(b-fractions, 0.04%)
    sys(c-fractions, 0.24%)
    sys(track multiplicity, 9.26%)
    sys(long lived particles, 0.25%)
    sys(fake tracks,-0.14%)
    sys(photon conversions, 2.13%)
    sys(trigger bias, 1.69%)
    sys(negative tag epsilon b, 0.79%)
    sys(negative tag epsilon c, 3.23%)
    sys(negative tag run period dependence, 4.90%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2990,0.0054)
    usys(MC statistics, 1.17%)
    sys(jet energy scale,-7.39%)
    sys(jet energy resolution,-10.47%)
    sys(b-fractions, 1.68%)
    sys(c-fractions, 4.84%)
    sys(track multiplicity, 3.60%)
    sys(long lived particles, 1.65%)
    sys(fake tracks,-0.27%)
    sys(photon conversions,11.85%)
    sys(trigger bias, 8.47%)
    sys(negative tag epsilon b, 3.93%)
    sys(negative tag epsilon c, 8.62%)
    sys(negative tag run period dependence, 6.08%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4447,0.0073)
    usys(MC statistics, 1.29%)
    sys(jet energy scale,-0.48%)
    sys(jet energy resolution,-1.07%)
    sys(b-fractions, 2.59%)
    sys(c-fractions, 5.62%)
    sys(track multiplicity, 6.82%)
    sys(long lived particles,-1.55%)
    sys(fake tracks, 1.17%)
    sys(photon conversions,13.89%)
    sys(trigger bias, 5.73%)
    sys(negative tag epsilon b, 5.55%)
    sys(negative tag epsilon c, 9.33%)
    sys(negative tag run period dependence,-12.31%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2948,0.0047)
    usys(MC statistics, 1.95%)
    sys(jet energy scale, 0.39%)
    sys(jet energy resolution, 1.13%)
    sys(b-fractions, 2.11%)
    sys(c-fractions, 5.21%)
    sys(track multiplicity,12.09%)
    sys(long lived particles,-1.24%)
    sys(fake tracks, 3.28%)
    sys(photon conversions,12.26%)
    sys(trigger bias, 8.52%)
    sys(negative tag epsilon b, 5.28%)
    sys(negative tag epsilon c, 9.66%)
    sys(negative tag run period dependence, 0.66%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2470,0.0421)
    usys(MC statistics, 3.45%)
    sys(jet energy scale, 6.03%)
    sys(jet energy resolution,-1.75%)
    sys(b-fractions, 0.15%)
    sys(c-fractions, 0.26%)
    sys(track multiplicity, 2.32%)
    sys(long lived particles, 4.10%)
    sys(fake tracks,-0.45%)
    sys(photon conversions, 2.64%)
    sys(trigger bias, 1.19%)
    sys(negative tag epsilon b, 0.80%)
    sys(negative tag epsilon c, 2.43%)
    sys(negative tag run period dependence,14.19%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1574,0.0059)
    usys(MC statistics, 0.59%)
    sys(jet energy scale, 6.03%)
    sys(jet energy resolution,-1.75%)
    sys(b-fractions, 0.09%)
    sys(c-fractions, 0.43%)
    sys(track multiplicity, 4.99%)
    sys(long lived particles,-0.44%)
    sys(fake tracks,-0.38%)
    sys(photon conversions, 2.67%)
    sys(trigger bias, 1.19%)
    sys(negative tag epsilon b, 0.64%)
    sys(negative tag epsilon c, 2.73%)
    sys(negative tag run period dependence, 1.15%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1165,0.0030)
    usys(MC statistics, 0.35%)
    sys(jet energy scale,-4.75%)
    sys(jet energy resolution, 1.55%)
    sys(b-fractions, 0.15%)
    sys(c-fractions, 0.67%)
    sys(track multiplicity, 4.43%)
    sys(long lived particles, 0.45%)
    sys(fake tracks,-0.41%)
    sys(photon conversions, 3.24%)
    sys(trigger bias, 2.24%)
    sys(negative tag epsilon b, 0.84%)
    sys(negative tag epsilon c, 3.23%)
    sys(negative tag run period dependence, 3.29%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4751,0.0117)
    usys(MC statistics, 1.75%)
    sys(jet energy scale,-12.63%)
    sys(jet energy resolution,-13.89%)
    sys(b-fractions, 1.19%)
    sys(c-fractions, 3.73%)
    sys(track multiplicity, 4.25%)
    sys(long lived particles, 1.42%)
    sys(fake tracks,-0.21%)
    sys(photon conversions, 7.54%)
    sys(trigger bias, 8.62%)
    sys(negative tag epsilon b, 2.83%)
    sys(negative tag epsilon c, 6.91%)
    sys(negative tag run period dependence, 2.28%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5775,0.0166)
    usys(MC statistics, 2.57%)
    sys(jet energy scale, 0.81%)
    sys(jet energy resolution, 1.29%)
    sys(b-fractions, 1.54%)
    sys(c-fractions, 3.56%)
    sys(track multiplicity, 7.08%)
    sys(long lived particles,-1.08%)
    sys(fake tracks, 0.71%)
    sys(photon conversions, 8.67%)
    sys(trigger bias, 4.13%)
    sys(negative tag epsilon b, 3.44%)
    sys(negative tag epsilon c, 6.48%)
    sys(negative tag run period dependence,-13.00%)
    sys(d0 and z0 smearing, 0.00%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5597,0.0125)
    usys(MC statistics, 3.51%)
    sys(jet energy scale,-2.19%)
    sys(jet energy resolution,-7.61%)
    sys(b-fractions, 1.22%)
    sys(c-fractions, 3.21%)
    sys(track multiplicity,10.81%)
    sys(long lived particles,-1.08%)
    sys(fake tracks, 1.81%)
    sys(photon conversions, 7.95%)
    sys(trigger bias,-0.48%)
    sys(negative tag epsilon b, 3.11%)
    sys(negative tag epsilon c, 6.59%)
    sys(negative tag run period dependence,-1.26%)
    sys(d0 and z0 smearing, 0.00%)
  }
}
