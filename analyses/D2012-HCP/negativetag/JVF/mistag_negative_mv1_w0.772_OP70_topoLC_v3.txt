Analysis(negative tags,light,MV1, 0.772, AntiKt4TopoLC){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2882,0.0638)
    usys(MC statistics, 5.13%)
    sys(jet energy scale,-3.19%)
    sys(jet energy resolution, 4.23%)
    sys(b-fractions, 0.45%)
    sys(c-fractions, 1.31%)
    sys(track multiplicity, 2.13%)
    sys(long lived particles, 5.55%)
    sys(fake tracks,-0.47%)
    sys(photon conversions, 5.14%)
    sys(trigger bias, 5.06%)
    sys(negative tag epsilon b, 1.52%)
    sys(negative tag epsilon c, 3.80%)
    sys(negative tag run period dependence, 4.65%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3036,0.0331)
    usys(MC statistics, 2.83%)
    sys(jet energy scale, 1.67%)
    sys(jet energy resolution, 1.57%)
    sys(b-fractions, 0.56%)
    sys(c-fractions, 1.87%)
    sys(track multiplicity, 2.35%)
    sys(long lived particles,-1.21%)
    sys(fake tracks,-0.78%)
    sys(photon conversions, 7.27%)
    sys(trigger bias, 5.06%)
    sys(negative tag epsilon b, 1.57%)
    sys(negative tag epsilon c, 4.53%)
    sys(negative tag run period dependence, 2.37%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2788,0.0155)
    usys(MC statistics, 1.62%)
    sys(jet energy scale, 0.44%)
    sys(jet energy resolution,-0.74%)
    sys(b-fractions, 1.64%)
    sys(c-fractions, 4.32%)
    sys(track multiplicity, 3.57%)
    sys(long lived particles, 1.54%)
    sys(fake tracks,-1.07%)
    sys(photon conversions,12.28%)
    sys(trigger bias, 8.09%)
    sys(negative tag epsilon b, 3.93%)
    sys(negative tag epsilon c, 7.86%)
    sys(negative tag run period dependence,-2.14%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3627,0.0100)
    usys(MC statistics, 1.92%)
    sys(jet energy scale,-0.60%)
    sys(jet energy resolution, 2.78%)
    sys(b-fractions, 2.67%)
    sys(c-fractions, 5.87%)
    sys(track multiplicity, 5.44%)
    sys(long lived particles, 2.08%)
    sys(fake tracks,-0.33%)
    sys(photon conversions,14.88%)
    sys(trigger bias, 7.47%)
    sys(negative tag epsilon b, 5.87%)
    sys(negative tag epsilon c, 9.58%)
    sys(negative tag run period dependence,-5.28%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4561,0.0094)
    usys(MC statistics, 1.64%)
    sys(jet energy scale,-0.16%)
    sys(jet energy resolution,-2.48%)
    sys(b-fractions, 2.87%)
    sys(c-fractions, 5.23%)
    sys(track multiplicity, 7.12%)
    sys(long lived particles,-1.59%)
    sys(fake tracks, 1.14%)
    sys(photon conversions,14.71%)
    sys(trigger bias, 6.34%)
    sys(negative tag epsilon b, 6.12%)
    sys(negative tag epsilon c, 8.76%)
    sys(negative tag run period dependence,-10.76%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2566,0.0059)
    usys(MC statistics, 2.87%)
    sys(jet energy scale,-0.61%)
    sys(jet energy resolution, 3.17%)
    sys(b-fractions, 2.33%)
    sys(c-fractions, 4.88%)
    sys(track multiplicity,12.01%)
    sys(long lived particles,-1.19%)
    sys(fake tracks, 3.17%)
    sys(photon conversions,12.65%)
    sys(trigger bias, 6.84%)
    sys(negative tag epsilon b, 5.79%)
    sys(negative tag epsilon c, 9.13%)
    sys(negative tag run period dependence, 0.55%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2321,0.0823)
    usys(MC statistics, 8.55%)
    sys(jet energy scale,-1.28%)
    sys(jet energy resolution,10.84%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 0.35%)
    sys(track multiplicity, 0.39%)
    sys(long lived particles, 5.80%)
    sys(fake tracks,-0.47%)
    sys(photon conversions, 3.95%)
    sys(trigger bias, 3.93%)
    sys(negative tag epsilon b, 0.95%)
    sys(negative tag epsilon c, 2.54%)
    sys(negative tag run period dependence, 9.33%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.4860,0.0481)
    usys(MC statistics, 3.91%)
    sys(jet energy scale,-1.75%)
    sys(jet energy resolution, 5.00%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.21%)
    sys(track multiplicity, 2.09%)
    sys(long lived particles,-0.95%)
    sys(fake tracks,-0.70%)
    sys(photon conversions, 5.38%)
    sys(trigger bias, 3.93%)
    sys(negative tag epsilon b, 1.00%)
    sys(negative tag epsilon c, 3.62%)
    sys(negative tag run period dependence,12.48%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3910,0.0244)
    usys(MC statistics, 2.39%)
    sys(jet energy scale, 1.40%)
    sys(jet energy resolution, 2.25%)
    sys(b-fractions, 1.04%)
    sys(c-fractions, 2.47%)
    sys(track multiplicity, 4.43%)
    sys(long lived particles, 1.23%)
    sys(fake tracks,-0.84%)
    sys(photon conversions, 8.13%)
    sys(trigger bias, 9.44%)
    sys(negative tag epsilon b, 2.68%)
    sys(negative tag epsilon c, 5.30%)
    sys(negative tag run period dependence, 8.52%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5954,0.0199)
    usys(MC statistics, 2.88%)
    sys(jet energy scale,-1.05%)
    sys(jet energy resolution, 1.18%)
    sys(b-fractions, 1.37%)
    sys(c-fractions, 3.64%)
    sys(track multiplicity, 5.02%)
    sys(long lived particles, 1.62%)
    sys(fake tracks,-0.25%)
    sys(photon conversions, 9.38%)
    sys(trigger bias,13.73%)
    sys(negative tag epsilon b, 3.18%)
    sys(negative tag epsilon c, 6.75%)
    sys(negative tag run period dependence,-6.89%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5866,0.0220)
    usys(MC statistics, 3.40%)
    sys(jet energy scale, 2.08%)
    sys(jet energy resolution, 2.06%)
    sys(b-fractions, 1.47%)
    sys(c-fractions, 2.75%)
    sys(track multiplicity, 6.55%)
    sys(long lived particles,-0.97%)
    sys(fake tracks, 0.74%)
    sys(photon conversions, 9.37%)
    sys(trigger bias, 2.64%)
    sys(negative tag epsilon b, 3.32%)
    sys(negative tag epsilon c, 5.48%)
    sys(negative tag run period dependence,-11.28%)
    sys(d0 and z0 smearing,-22.13%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5724,0.0174)
    usys(MC statistics, 5.28%)
    sys(jet energy scale,-3.24%)
    sys(jet energy resolution,-10.86%)
    sys(b-fractions, 1.11%)
    sys(c-fractions, 2.24%)
    sys(track multiplicity, 9.39%)
    sys(long lived particles,-1.22%)
    sys(fake tracks, 1.84%)
    sys(photon conversions, 8.88%)
    sys(trigger bias, 2.99%)
    sys(negative tag epsilon b, 2.88%)
    sys(negative tag epsilon c, 5.27%)
    sys(negative tag run period dependence,-1.05%)
    sys(d0 and z0 smearing,-22.13%)
  }
}
