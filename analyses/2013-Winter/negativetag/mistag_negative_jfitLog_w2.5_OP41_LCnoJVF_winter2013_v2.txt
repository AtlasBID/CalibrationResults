Analysis(negative tags,light,JetFitterLog, 2.5, AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.4375,0.3214)
    meta_data(N jets tagged,     20,  4.5)
    meta_data(N jets total,  82557,287.3)
    usys(MC statistics,18.33%)
    sys(jet vertex fraction,19.05%)
    sys(jet energy scale,-3.60%)
    sys(jet energy resolution, 1.34%)
    sys(b-fractions, 2.47%)
    sys(c-fractions, 0.25%)
    sys(track multiplicity,17.94%)
    sys(long lived particles, 0.22%)
    sys(fake tracks,-0.05%)
    sys(photon conversions, 0.52%)
    sys(material interactions, 0.10%)
    sys(trigger bias, 0.00%)
    sys(negative tag epsilon b, 6.23%)
    sys(negative tag epsilon c, 2.00%)
    sys(negative tag run period dependence,24.32%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.4928,0.1252)
    meta_data(N jets tagged,    142, 11.9)
    meta_data(N jets total, 316538,562.6)
    usys(MC statistics, 8.90%)
    sys(jet vertex fraction,-3.31%)
    sys(jet energy scale, 5.29%)
    sys(jet energy resolution,-3.55%)
    sys(b-fractions, 1.01%)
    sys(c-fractions, 2.50%)
    sys(track multiplicity, 4.52%)
    sys(long lived particles,-0.08%)
    sys(fake tracks,-1.34%)
    sys(photon conversions,-0.11%)
    sys(material interactions, 0.37%)
    sys(trigger bias, 1.13%)
    sys(negative tag epsilon b, 2.42%)
    sys(negative tag epsilon c, 5.15%)
    sys(negative tag run period dependence,10.52%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.6189,0.0674)
    meta_data(N jets tagged,    576, 24.0)
    meta_data(N jets total, 815694,903.2)
    usys(MC statistics, 6.08%)
    sys(jet vertex fraction, 2.18%)
    sys(jet energy scale,-1.27%)
    sys(jet energy resolution, 0.85%)
    sys(b-fractions, 2.37%)
    sys(c-fractions, 6.97%)
    sys(track multiplicity,-2.32%)
    sys(long lived particles,-0.91%)
    sys(fake tracks, 0.95%)
    sys(photon conversions, 0.14%)
    sys(material interactions, 1.94%)
    sys(trigger bias,-2.31%)
    sys(negative tag epsilon b, 5.37%)
    sys(negative tag epsilon c,10.95%)
    sys(negative tag run period dependence, 3.39%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.5260,0.0589)
    meta_data(N jets tagged,    672, 25.9)
    meta_data(N jets total,1692275,1300.9)
    usys(MC statistics,10.33%)
    sys(jet vertex fraction,-0.54%)
    sys(jet energy scale,-6.71%)
    sys(jet energy resolution,-0.57%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 5.80%)
    sys(track multiplicity,-0.22%)
    sys(long lived particles,-1.97%)
    sys(fake tracks, 2.36%)
    sys(photon conversions, 1.02%)
    sys(material interactions, 4.33%)
    sys(trigger bias,-4.24%)
    sys(negative tag epsilon b, 7.81%)
    sys(negative tag epsilon c, 9.07%)
    sys(negative tag run period dependence, 4.15%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.7124,0.0777)
    meta_data(N jets tagged,    485, 22.0)
    meta_data(N jets total,1719598,1311.3)
    usys(MC statistics,10.03%)
    sys(jet vertex fraction,-0.79%)
    sys(jet energy scale,-4.79%)
    sys(jet energy resolution, 1.15%)
    sys(b-fractions, 4.11%)
    sys(c-fractions, 5.34%)
    sys(track multiplicity, 2.47%)
    sys(long lived particles,-0.36%)
    sys(fake tracks,-0.75%)
    sys(photon conversions, 2.44%)
    sys(material interactions, 4.63%)
    sys(trigger bias, 2.80%)
    sys(negative tag epsilon b, 8.50%)
    sys(negative tag epsilon c, 8.48%)
    sys(negative tag run period dependence, 9.71%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3936,0.0453)
    meta_data(N jets tagged,    947, 30.8)
    meta_data(N jets total,3484602,1866.7)
    usys(MC statistics, 6.82%)
    sys(jet vertex fraction,-0.46%)
    sys(jet energy scale,-9.40%)
    sys(jet energy resolution,-7.69%)
    sys(b-fractions, 3.90%)
    sys(c-fractions, 2.93%)
    sys(track multiplicity,-1.38%)
    sys(long lived particles, 0.77%)
    sys(fake tracks, 2.73%)
    sys(photon conversions, 0.04%)
    sys(material interactions, 0.50%)
    sys(trigger bias, 2.95%)
    sys(negative tag epsilon b, 9.32%)
    sys(negative tag epsilon c, 5.91%)
    sys(negative tag run period dependence, 9.73%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.7157,0.4952)
    meta_data(N jets tagged,     12,  3.5)
    meta_data(N jets total,  64177,253.3)
    usys(MC statistics,21.54%)
    sys(jet vertex fraction,68.73%)
    sys(jet energy scale,30.08%)
    sys(jet energy resolution,123.08%)
    sys(b-fractions,-0.08%)
    sys(c-fractions,-0.68%)
    sys(track multiplicity, 5.18%)
    sys(long lived particles,-0.75%)
    sys(fake tracks,-0.77%)
    sys(photon conversions, 0.65%)
    sys(material interactions, 2.16%)
    sys(trigger bias, 0.00%)
    sys(negative tag epsilon b, 0.30%)
    sys(negative tag epsilon c, 1.30%)
    sys(negative tag run period dependence,70.10%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.5721,0.2223)
    meta_data(N jets tagged,     50,  7.1)
    meta_data(N jets total, 237900,487.7)
    usys(MC statistics,25.37%)
    sys(jet vertex fraction, 4.20%)
    sys(jet energy scale,-15.07%)
    sys(jet energy resolution,-7.27%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 3.24%)
    sys(track multiplicity, 8.58%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 0.93%)
    sys(photon conversions,-2.36%)
    sys(material interactions,-1.85%)
    sys(trigger bias, 0.17%)
    sys(negative tag epsilon b, 1.39%)
    sys(negative tag epsilon c, 6.00%)
    sys(negative tag run period dependence,12.67%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.7295,0.1264)
    meta_data(N jets tagged,    187, 13.7)
    meta_data(N jets total, 550039,741.6)
    usys(MC statistics, 7.93%)
    sys(jet vertex fraction,-1.95%)
    sys(jet energy scale, 4.47%)
    sys(jet energy resolution, 7.96%)
    sys(b-fractions, 2.33%)
    sys(c-fractions, 2.37%)
    sys(track multiplicity, 2.69%)
    sys(long lived particles,-0.27%)
    sys(fake tracks, 1.08%)
    sys(photon conversions, 0.36%)
    sys(material interactions, 1.71%)
    sys(trigger bias,-10.75%)
    sys(negative tag epsilon b, 5.38%)
    sys(negative tag epsilon c, 4.86%)
    sys(negative tag run period dependence,20.44%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.7679,0.1478)
    meta_data(N jets tagged,    143, 12.0)
    meta_data(N jets total, 880666,938.4)
    usys(MC statistics,12.83%)
    sys(jet vertex fraction,-0.30%)
    sys(jet energy scale,14.05%)
    sys(jet energy resolution,14.94%)
    sys(b-fractions, 1.93%)
    sys(c-fractions, 1.38%)
    sys(track multiplicity,-0.65%)
    sys(long lived particles,-0.07%)
    sys(fake tracks, 6.75%)
    sys(photon conversions, 2.79%)
    sys(material interactions,15.15%)
    sys(trigger bias,10.50%)
    sys(negative tag epsilon b, 4.30%)
    sys(negative tag epsilon c, 3.66%)
    sys(negative tag run period dependence,33.27%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.8981,0.1958)
    meta_data(N jets tagged,     94,  9.7)
    meta_data(N jets total, 551249,742.5)
    usys(MC statistics,18.98%)
    sys(jet vertex fraction,-1.21%)
    sys(jet energy scale,30.41%)
    sys(jet energy resolution,-22.32%)
    sys(b-fractions, 2.30%)
    sys(c-fractions, 2.31%)
    sys(track multiplicity,-0.80%)
    sys(long lived particles,-0.24%)
    sys(fake tracks,-6.73%)
    sys(photon conversions, 2.52%)
    sys(material interactions, 4.94%)
    sys(trigger bias,-13.68%)
    sys(negative tag epsilon b, 4.96%)
    sys(negative tag epsilon c, 4.72%)
    sys(negative tag run period dependence,10.25%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(4.8501,0.4484)
    meta_data(N jets tagged,    117, 10.8)
    meta_data(N jets total, 877985,937.0)
    usys(MC statistics,14.00%)
    sys(jet vertex fraction,-0.52%)
    sys(jet energy scale,-43.68%)
    sys(jet energy resolution,-24.26%)
    sys(b-fractions, 0.91%)
    sys(c-fractions, 2.05%)
    sys(track multiplicity,11.44%)
    sys(long lived particles,-2.32%)
    sys(fake tracks,-1.96%)
    sys(photon conversions,-2.03%)
    sys(material interactions,-1.39%)
    sys(trigger bias, 1.23%)
    sys(negative tag epsilon b, 2.42%)
    sys(negative tag epsilon c, 5.06%)
    sys(negative tag run period dependence, 8.40%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
