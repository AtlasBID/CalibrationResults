Analysis(negative tags,light,MV1c, 0.0822, AntiKt4TopoEMJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(0.9927,0.0021)
    meta_data(N jets tagged,  50756,225.3)
    meta_data(N jets total,  65054,255.1)
    usys(MC statistics, 0.30%)
    sys(jet vertex fraction,-4.22%)
    sys(jet energy scale,-1.61%)
    sys(jet energy resolution, 1.34%)
    sys(b-fractions,-0.06%)
    sys(c-fractions,-0.16%)
    sys(track multiplicity, 2.73%)
    sys(long lived particles,-0.13%)
    sys(fake tracks, 0.19%)
    sys(photon conversions, 0.15%)
    sys(material interactions, 0.32%)
    sys(trigger bias,-0.04%)
    sys(negative tag epsilon b, 0.42%)
    sys(negative tag epsilon c, 2.11%)
    sys(negative tag run period dependence, 0.19%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.0709,0.0023)
    meta_data(N jets tagged, 121145,348.1)
    meta_data(N jets total, 282714,531.7)
    usys(MC statistics, 0.44%)
    sys(jet vertex fraction,-0.50%)
    sys(jet energy scale,-4.20%)
    sys(jet energy resolution,-3.93%)
    sys(b-fractions, 0.04%)
    sys(c-fractions, 0.34%)
    sys(track multiplicity,-1.73%)
    sys(long lived particles,-0.26%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 1.47%)
    sys(material interactions, 2.87%)
    sys(trigger bias,-2.56%)
    sys(negative tag epsilon b, 0.63%)
    sys(negative tag epsilon c, 2.83%)
    sys(negative tag run period dependence, 0.92%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1665,0.0020)
    meta_data(N jets tagged, 240474,490.4)
    meta_data(N jets total, 841867,917.5)
    usys(MC statistics, 0.36%)
    sys(jet vertex fraction,-0.31%)
    sys(jet energy scale,-0.40%)
    sys(jet energy resolution, 0.05%)
    sys(b-fractions, 0.32%)
    sys(c-fractions, 1.28%)
    sys(track multiplicity,-2.49%)
    sys(long lived particles,-0.35%)
    sys(fake tracks, 0.46%)
    sys(photon conversions, 0.80%)
    sys(material interactions, 1.96%)
    sys(trigger bias,-0.54%)
    sys(negative tag epsilon b, 1.33%)
    sys(negative tag epsilon c, 4.43%)
    sys(negative tag run period dependence, 0.38%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1814,0.0014)
    meta_data(N jets tagged, 509192,713.6)
    meta_data(N jets total,1799372,1341.4)
    usys(MC statistics, 0.51%)
    sys(jet vertex fraction,-0.11%)
    sys(jet energy scale, 0.54%)
    sys(jet energy resolution,-0.40%)
    sys(b-fractions, 0.46%)
    sys(c-fractions, 1.85%)
    sys(track multiplicity,-0.30%)
    sys(long lived particles,-0.65%)
    sys(fake tracks, 1.17%)
    sys(photon conversions, 1.61%)
    sys(material interactions, 4.28%)
    sys(trigger bias,-0.95%)
    sys(negative tag epsilon b, 1.57%)
    sys(negative tag epsilon c, 5.29%)
    sys(negative tag run period dependence, 1.00%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.2081,0.0013)
    meta_data(N jets tagged, 563219,750.5)
    meta_data(N jets total,1864051,1365.3)
    usys(MC statistics, 0.47%)
    sys(jet vertex fraction,-0.18%)
    sys(jet energy scale, 0.43%)
    sys(jet energy resolution,-0.35%)
    sys(b-fractions, 0.48%)
    sys(c-fractions, 1.94%)
    sys(track multiplicity, 0.17%)
    sys(long lived particles,-0.45%)
    sys(fake tracks, 0.68%)
    sys(photon conversions, 0.83%)
    sys(material interactions, 2.38%)
    sys(trigger bias,-0.55%)
    sys(negative tag epsilon b, 1.57%)
    sys(negative tag epsilon c, 5.53%)
    sys(negative tag run period dependence, 2.29%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.1591,0.0009)
    meta_data(N jets tagged,1129070,1062.6)
    meta_data(N jets total,3600008,1897.4)
    usys(MC statistics, 0.51%)
    sys(jet vertex fraction,-0.22%)
    sys(jet energy scale, 0.17%)
    sys(jet energy resolution, 0.70%)
    sys(b-fractions, 0.37%)
    sys(c-fractions, 1.70%)
    sys(track multiplicity, 0.83%)
    sys(long lived particles,-0.34%)
    sys(fake tracks, 0.45%)
    sys(photon conversions, 0.38%)
    sys(material interactions, 1.01%)
    sys(trigger bias,-0.84%)
    sys(negative tag epsilon b, 1.54%)
    sys(negative tag epsilon c, 5.74%)
    sys(negative tag run period dependence, 0.61%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.0168,0.0045)
    meta_data(N jets tagged,  26555,163.0)
    meta_data(N jets total,  54818,234.1)
    usys(MC statistics, 0.50%)
    sys(jet vertex fraction,-2.88%)
    sys(jet energy scale,-14.58%)
    sys(jet energy resolution,-1.91%)
    sys(b-fractions,-0.04%)
    sys(c-fractions,-0.14%)
    sys(track multiplicity, 3.41%)
    sys(long lived particles,-0.13%)
    sys(fake tracks, 0.15%)
    sys(photon conversions, 0.23%)
    sys(material interactions, 0.28%)
    sys(trigger bias,-0.24%)
    sys(negative tag epsilon b, 0.38%)
    sys(negative tag epsilon c, 2.00%)
    sys(negative tag run period dependence, 0.25%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1705,0.0052)
    meta_data(N jets tagged,  40922,202.3)
    meta_data(N jets total, 219736,468.8)
    usys(MC statistics, 0.65%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale,-1.65%)
    sys(jet energy resolution,-3.80%)
    sys(b-fractions, 0.08%)
    sys(c-fractions, 0.36%)
    sys(track multiplicity, 3.30%)
    sys(long lived particles,-0.17%)
    sys(fake tracks,-0.22%)
    sys(photon conversions, 0.99%)
    sys(material interactions, 2.33%)
    sys(trigger bias,-1.46%)
    sys(negative tag epsilon b, 0.62%)
    sys(negative tag epsilon c, 2.64%)
    sys(negative tag run period dependence, 1.21%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1332,0.0031)
    meta_data(N jets tagged, 109545,331.0)
    meta_data(N jets total, 563471,750.6)
    usys(MC statistics, 0.40%)
    sys(jet vertex fraction,-0.45%)
    sys(jet energy scale, 0.26%)
    sys(jet energy resolution, 0.06%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.85%)
    sys(track multiplicity, 4.29%)
    sys(long lived particles,-0.17%)
    sys(fake tracks, 0.13%)
    sys(photon conversions, 0.63%)
    sys(material interactions, 1.72%)
    sys(trigger bias,-1.21%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 3.47%)
    sys(negative tag run period dependence, 1.19%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.1763,0.0023)
    meta_data(N jets tagged, 202794,450.3)
    meta_data(N jets total, 925092,961.8)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction,-0.16%)
    sys(jet energy scale, 0.82%)
    sys(jet energy resolution, 0.37%)
    sys(b-fractions, 0.27%)
    sys(c-fractions, 1.14%)
    sys(track multiplicity, 2.24%)
    sys(long lived particles,-0.29%)
    sys(fake tracks, 0.86%)
    sys(photon conversions, 1.50%)
    sys(material interactions, 4.45%)
    sys(trigger bias,-2.91%)
    sys(negative tag epsilon b, 0.98%)
    sys(negative tag epsilon c, 3.75%)
    sys(negative tag run period dependence, 1.59%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.2428,0.0027)
    meta_data(N jets tagged, 157669,397.1)
    meta_data(N jets total, 596955,772.6)
    usys(MC statistics, 0.66%)
    sys(jet vertex fraction,-0.14%)
    sys(jet energy scale, 0.64%)
    sys(jet energy resolution, 0.39%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.26%)
    sys(track multiplicity, 1.07%)
    sys(long lived particles,-0.25%)
    sys(fake tracks, 0.62%)
    sys(photon conversions, 1.19%)
    sys(material interactions, 3.25%)
    sys(trigger bias,-1.14%)
    sys(negative tag epsilon b, 0.96%)
    sys(negative tag epsilon c, 3.91%)
    sys(negative tag run period dependence, 2.71%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2118,0.0020)
    meta_data(N jets tagged, 253137,503.1)
    meta_data(N jets total, 905489,951.6)
    usys(MC statistics, 0.94%)
    sys(jet vertex fraction,-0.36%)
    sys(jet energy scale, 1.08%)
    sys(jet energy resolution, 0.45%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.99%)
    sys(track multiplicity, 1.86%)
    sys(long lived particles,-0.24%)
    sys(fake tracks, 0.62%)
    sys(photon conversions, 0.58%)
    sys(material interactions, 1.42%)
    sys(trigger bias,-1.93%)
    sys(negative tag epsilon b, 0.86%)
    sys(negative tag epsilon c, 3.83%)
    sys(negative tag run period dependence, 0.74%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
