Analysis(negative tags,light,JetFitterCOMBCharm, -1.0_-0.82, AntiKt4TopoEMJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2659,0.0519)
    meta_data(N jets tagged,    589, 24.3)
    meta_data(N jets total,  65054,255.1)
    usys(MC statistics, 5.82%)
    sys(jet vertex fraction,-3.92%)
    sys(jet energy scale,-4.83%)
    sys(jet energy resolution,-2.15%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 1.49%)
    sys(track multiplicity, 3.93%)
    sys(long lived particles, 0.04%)
    sys(fake tracks, 0.08%)
    sys(photon conversions, 0.08%)
    sys(material interactions, 0.43%)
    sys(trigger bias,-0.95%)
    sys(negative tag epsilon b, 1.13%)
    sys(negative tag epsilon c, 4.02%)
    sys(negative tag run period dependence, 3.99%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3078,0.0273)
    meta_data(N jets tagged,   2269, 47.6)
    meta_data(N jets total, 282714,531.7)
    usys(MC statistics, 2.77%)
    sys(jet vertex fraction,-1.45%)
    sys(jet energy scale, 1.81%)
    sys(jet energy resolution, 6.23%)
    sys(b-fractions, 0.50%)
    sys(c-fractions, 2.21%)
    sys(track multiplicity, 2.23%)
    sys(long lived particles,-0.24%)
    sys(fake tracks,-0.15%)
    sys(photon conversions,-0.04%)
    sys(material interactions, 0.37%)
    sys(trigger bias, 1.85%)
    sys(negative tag epsilon b, 1.43%)
    sys(negative tag epsilon c, 4.87%)
    sys(negative tag run period dependence, 3.48%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2985,0.0121)
    meta_data(N jets tagged,  11397,106.8)
    meta_data(N jets total, 841867,917.5)
    usys(MC statistics, 1.61%)
    sys(jet vertex fraction,-0.58%)
    sys(jet energy scale, 1.49%)
    sys(jet energy resolution, 1.02%)
    sys(b-fractions, 1.36%)
    sys(c-fractions, 4.13%)
    sys(track multiplicity, 0.40%)
    sys(long lived particles,-0.47%)
    sys(fake tracks, 0.53%)
    sys(photon conversions, 0.17%)
    sys(material interactions, 0.95%)
    sys(trigger bias,-1.65%)
    sys(negative tag epsilon b, 3.35%)
    sys(negative tag epsilon c, 7.65%)
    sys(negative tag run period dependence, 1.24%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3956,0.0083)
    meta_data(N jets tagged,  27530,165.9)
    meta_data(N jets total,1799372,1341.4)
    usys(MC statistics, 2.30%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale, 0.26%)
    sys(jet energy resolution,-1.14%)
    sys(b-fractions, 2.10%)
    sys(c-fractions, 6.00%)
    sys(track multiplicity, 1.66%)
    sys(long lived particles,-1.06%)
    sys(fake tracks, 2.06%)
    sys(photon conversions, 1.46%)
    sys(material interactions, 3.80%)
    sys(trigger bias,-3.58%)
    sys(negative tag epsilon b, 4.68%)
    sys(negative tag epsilon c, 9.83%)
    sys(negative tag run period dependence, 1.76%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4861,0.0080)
    meta_data(N jets tagged,  33816,183.9)
    meta_data(N jets total,1864051,1365.3)
    usys(MC statistics, 2.20%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale, 0.90%)
    sys(jet energy resolution, 2.97%)
    sys(b-fractions, 2.51%)
    sys(c-fractions, 6.14%)
    sys(track multiplicity, 2.15%)
    sys(long lived particles,-0.69%)
    sys(fake tracks, 2.53%)
    sys(photon conversions, 1.16%)
    sys(material interactions, 1.79%)
    sys(trigger bias,-1.27%)
    sys(negative tag epsilon b, 5.38%)
    sys(negative tag epsilon c,10.00%)
    sys(negative tag run period dependence, 7.11%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3353,0.0052)
    meta_data(N jets tagged,  64436,253.8)
    meta_data(N jets total,3600008,1897.4)
    usys(MC statistics, 1.63%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale,-0.77%)
    sys(jet energy resolution, 0.50%)
    sys(b-fractions, 1.95%)
    sys(c-fractions, 5.44%)
    sys(track multiplicity, 3.50%)
    sys(long lived particles,-0.48%)
    sys(fake tracks, 0.59%)
    sys(photon conversions, 0.56%)
    sys(material interactions, 0.66%)
    sys(trigger bias,-5.71%)
    sys(negative tag epsilon b, 5.13%)
    sys(negative tag epsilon c,10.28%)
    sys(negative tag run period dependence, 0.18%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2796,0.0731)
    meta_data(N jets tagged,    305, 17.5)
    meta_data(N jets total,  54818,234.1)
    usys(MC statistics, 5.95%)
    sys(jet vertex fraction,-7.63%)
    sys(jet energy scale,-7.07%)
    sys(jet energy resolution,-6.82%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.47%)
    sys(track multiplicity, 3.38%)
    sys(long lived particles,-0.24%)
    sys(fake tracks, 0.19%)
    sys(photon conversions,-0.27%)
    sys(material interactions,-0.49%)
    sys(trigger bias, 0.18%)
    sys(negative tag epsilon b, 0.89%)
    sys(negative tag epsilon c, 2.68%)
    sys(negative tag run period dependence,11.58%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2698,0.0383)
    meta_data(N jets tagged,   1095, 33.1)
    meta_data(N jets total, 219736,468.8)
    usys(MC statistics, 3.31%)
    sys(jet vertex fraction, 3.59%)
    sys(jet energy scale, 4.58%)
    sys(jet energy resolution, 1.91%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.46%)
    sys(track multiplicity, 4.12%)
    sys(long lived particles,-0.11%)
    sys(fake tracks, 0.26%)
    sys(photon conversions, 0.26%)
    sys(material interactions, 1.60%)
    sys(trigger bias,-2.91%)
    sys(negative tag epsilon b, 0.91%)
    sys(negative tag epsilon c, 3.91%)
    sys(negative tag run period dependence, 4.30%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3946,0.0213)
    meta_data(N jets tagged,   4239, 65.1)
    meta_data(N jets total, 563471,750.6)
    usys(MC statistics, 2.05%)
    sys(jet vertex fraction,-0.29%)
    sys(jet energy scale, 0.70%)
    sys(jet energy resolution, 0.20%)
    sys(b-fractions, 0.84%)
    sys(c-fractions, 2.65%)
    sys(track multiplicity, 2.10%)
    sys(long lived particles,-0.31%)
    sys(fake tracks, 0.30%)
    sys(photon conversions, 0.67%)
    sys(material interactions, 1.65%)
    sys(trigger bias,-5.66%)
    sys(negative tag epsilon b, 2.26%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence, 3.97%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4130,0.0180)
    meta_data(N jets tagged,   6095, 78.1)
    meta_data(N jets total, 925092,961.8)
    usys(MC statistics, 2.73%)
    sys(jet vertex fraction,-0.00%)
    sys(jet energy scale,-3.33%)
    sys(jet energy resolution,-1.75%)
    sys(b-fractions, 1.17%)
    sys(c-fractions, 3.38%)
    sys(track multiplicity, 3.00%)
    sys(long lived particles,-0.42%)
    sys(fake tracks, 1.68%)
    sys(photon conversions, 0.96%)
    sys(material interactions, 3.34%)
    sys(trigger bias,-9.87%)
    sys(negative tag epsilon b, 2.74%)
    sys(negative tag epsilon c, 6.39%)
    sys(negative tag run period dependence, 3.34%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6616,0.0238)
    meta_data(N jets tagged,   4840, 69.6)
    meta_data(N jets total, 596955,772.6)
    usys(MC statistics, 3.85%)
    sys(jet vertex fraction,-0.44%)
    sys(jet energy scale, 1.21%)
    sys(jet energy resolution, 0.14%)
    sys(b-fractions, 1.77%)
    sys(c-fractions, 3.99%)
    sys(track multiplicity, 3.97%)
    sys(long lived particles,-0.24%)
    sys(fake tracks, 0.60%)
    sys(photon conversions, 2.32%)
    sys(material interactions, 4.03%)
    sys(trigger bias,-5.26%)
    sys(negative tag epsilon b, 3.88%)
    sys(negative tag epsilon c, 7.02%)
    sys(negative tag run period dependence, 9.16%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.4409,0.0158)
    meta_data(N jets tagged,   8193, 90.5)
    meta_data(N jets total, 905489,951.6)
    usys(MC statistics, 3.85%)
    sys(jet vertex fraction,-0.74%)
    sys(jet energy scale,-3.15%)
    sys(jet energy resolution, 0.09%)
    sys(b-fractions, 1.46%)
    sys(c-fractions, 2.97%)
    sys(track multiplicity, 7.05%)
    sys(long lived particles,-0.29%)
    sys(fake tracks, 0.55%)
    sys(photon conversions, 0.36%)
    sys(material interactions, 1.08%)
    sys(trigger bias,-20.53%)
    sys(negative tag epsilon b, 3.89%)
    sys(negative tag epsilon c, 6.36%)
    sys(negative tag run period dependence, 5.30%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
