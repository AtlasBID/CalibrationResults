Analysis(negative tags,light,MV1, 0.3511, AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2345,0.0206)
    meta_data(N jets tagged,   3404, 58.3)
    meta_data(N jets total,  67495,259.8)
    usys(MC statistics, 2.02%)
    sys(jet vertex fraction,-22.64%)
    sys(jet energy scale,15.19%)
    sys(jet energy resolution,13.84%)
    sys(b-fractions, 0.43%)
    sys(c-fractions, 1.00%)
    sys(track multiplicity,37.03%)
    sys(long lived particles,-0.22%)
    sys(fake tracks,-0.01%)
    sys(photon conversions, 0.32%)
    sys(material interactions, 0.66%)
    sys(trigger bias,-0.10%)
    sys(negative tag epsilon b, 1.50%)
    sys(negative tag epsilon c, 3.42%)
    sys(negative tag run period dependence, 0.86%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2345,0.0115)
    meta_data(N jets tagged,  11023,105.0)
    meta_data(N jets total, 279474,528.7)
    usys(MC statistics, 1.13%)
    sys(jet vertex fraction,-7.51%)
    sys(jet energy scale, 6.68%)
    sys(jet energy resolution, 5.15%)
    sys(b-fractions, 0.44%)
    sys(c-fractions, 1.77%)
    sys(track multiplicity,16.14%)
    sys(long lived particles,-0.15%)
    sys(fake tracks,-0.65%)
    sys(photon conversions, 0.85%)
    sys(material interactions, 1.58%)
    sys(trigger bias,-2.70%)
    sys(negative tag epsilon b, 1.35%)
    sys(negative tag epsilon c, 4.44%)
    sys(negative tag run period dependence, 2.86%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1871,0.0058)
    meta_data(N jets tagged,  40297,200.7)
    meta_data(N jets total, 798382,893.5)
    usys(MC statistics, 0.69%)
    sys(jet vertex fraction,-0.85%)
    sys(jet energy scale, 0.39%)
    sys(jet energy resolution, 0.67%)
    sys(b-fractions, 1.01%)
    sys(c-fractions, 3.20%)
    sys(track multiplicity, 8.82%)
    sys(long lived particles,-0.29%)
    sys(fake tracks, 0.31%)
    sys(photon conversions, 0.55%)
    sys(material interactions, 1.43%)
    sys(trigger bias,-1.89%)
    sys(negative tag epsilon b, 2.69%)
    sys(negative tag epsilon c, 6.64%)
    sys(negative tag run period dependence, 2.04%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2626,0.0037)
    meta_data(N jets tagged, 111588,334.0)
    meta_data(N jets total,1680610,1296.4)
    usys(MC statistics, 0.84%)
    sys(jet vertex fraction,-0.26%)
    sys(jet energy scale,-0.12%)
    sys(jet energy resolution,-0.12%)
    sys(b-fractions, 1.45%)
    sys(c-fractions, 4.47%)
    sys(track multiplicity, 4.88%)
    sys(long lived particles,-0.64%)
    sys(fake tracks, 1.23%)
    sys(photon conversions, 1.63%)
    sys(material interactions, 4.23%)
    sys(trigger bias,-2.87%)
    sys(negative tag epsilon b, 3.43%)
    sys(negative tag epsilon c, 8.16%)
    sys(negative tag run period dependence, 4.03%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3256,0.0033)
    meta_data(N jets tagged, 144571,380.2)
    meta_data(N jets total,1712041,1308.4)
    usys(MC statistics, 0.79%)
    sys(jet vertex fraction,-0.18%)
    sys(jet energy scale, 0.43%)
    sys(jet energy resolution, 0.45%)
    sys(b-fractions, 1.39%)
    sys(c-fractions, 3.96%)
    sys(track multiplicity, 3.62%)
    sys(long lived particles,-0.54%)
    sys(fake tracks, 1.31%)
    sys(photon conversions, 1.08%)
    sys(material interactions, 2.63%)
    sys(trigger bias,-0.64%)
    sys(negative tag epsilon b, 3.27%)
    sys(negative tag epsilon c, 7.70%)
    sys(negative tag run period dependence, 4.53%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2109,0.0021)
    meta_data(N jets tagged, 295661,543.7)
    meta_data(N jets total,3472572,1863.5)
    usys(MC statistics, 1.05%)
    sys(jet vertex fraction,-0.17%)
    sys(jet energy scale, 1.10%)
    sys(jet energy resolution, 1.58%)
    sys(b-fractions, 1.16%)
    sys(c-fractions, 3.68%)
    sys(track multiplicity, 5.74%)
    sys(long lived particles,-0.43%)
    sys(fake tracks, 0.56%)
    sys(photon conversions, 0.49%)
    sys(material interactions, 1.07%)
    sys(trigger bias,-1.31%)
    sys(negative tag epsilon b, 3.20%)
    sys(negative tag epsilon c, 7.97%)
    sys(negative tag run period dependence, 0.46%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1865,0.0248)
    meta_data(N jets tagged,   2205, 47.0)
    meta_data(N jets total,  56373,237.4)
    usys(MC statistics, 2.33%)
    sys(jet vertex fraction,-16.66%)
    sys(jet energy scale,16.17%)
    sys(jet energy resolution,19.69%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.38%)
    sys(track multiplicity,38.49%)
    sys(long lived particles,-0.23%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 0.47%)
    sys(material interactions, 0.70%)
    sys(trigger bias,-0.43%)
    sys(negative tag epsilon b, 0.88%)
    sys(negative tag epsilon c, 2.57%)
    sys(negative tag run period dependence, 8.09%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3066,0.0158)
    meta_data(N jets tagged,   6654, 81.6)
    meta_data(N jets total, 213460,462.0)
    usys(MC statistics, 1.20%)
    sys(jet vertex fraction,-6.92%)
    sys(jet energy scale, 6.04%)
    sys(jet energy resolution, 6.28%)
    sys(b-fractions, 0.28%)
    sys(c-fractions, 0.92%)
    sys(track multiplicity,19.09%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.16%)
    sys(photon conversions, 1.47%)
    sys(material interactions, 2.70%)
    sys(trigger bias,-1.37%)
    sys(negative tag epsilon b, 1.01%)
    sys(negative tag epsilon c, 3.27%)
    sys(negative tag run period dependence, 2.25%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2837,0.0087)
    meta_data(N jets tagged,  20964,144.8)
    meta_data(N jets total, 536506,732.5)
    usys(MC statistics, 0.78%)
    sys(jet vertex fraction,-1.13%)
    sys(jet energy scale, 0.75%)
    sys(jet energy resolution, 0.78%)
    sys(b-fractions, 0.61%)
    sys(c-fractions, 1.86%)
    sys(track multiplicity,14.50%)
    sys(long lived particles,-0.23%)
    sys(fake tracks, 0.26%)
    sys(photon conversions, 1.31%)
    sys(material interactions, 2.79%)
    sys(trigger bias,-2.36%)
    sys(negative tag epsilon b, 1.78%)
    sys(negative tag epsilon c, 4.64%)
    sys(negative tag run period dependence, 2.46%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3636,0.0064)
    meta_data(N jets tagged,  42898,207.1)
    meta_data(N jets total, 872175,933.9)
    usys(MC statistics, 1.10%)
    sys(jet vertex fraction,-0.36%)
    sys(jet energy scale,-0.82%)
    sys(jet energy resolution, 0.21%)
    sys(b-fractions, 0.80%)
    sys(c-fractions, 2.61%)
    sys(track multiplicity, 8.39%)
    sys(long lived particles,-0.50%)
    sys(fake tracks, 0.94%)
    sys(photon conversions, 2.55%)
    sys(material interactions, 6.67%)
    sys(trigger bias,-7.08%)
    sys(negative tag epsilon b, 2.02%)
    sys(negative tag epsilon c, 5.51%)
    sys(negative tag run period dependence, 3.83%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4570,0.0073)
    meta_data(N jets tagged,  36716,191.6)
    meta_data(N jets total, 547574,740.0)
    usys(MC statistics, 1.15%)
    sys(jet vertex fraction,-0.27%)
    sys(jet energy scale, 0.61%)
    sys(jet energy resolution,-0.83%)
    sys(b-fractions, 0.83%)
    sys(c-fractions, 2.58%)
    sys(track multiplicity, 5.91%)
    sys(long lived particles,-0.22%)
    sys(fake tracks, 1.00%)
    sys(photon conversions, 1.90%)
    sys(material interactions, 4.43%)
    sys(trigger bias,-2.63%)
    sys(negative tag epsilon b, 2.03%)
    sys(negative tag epsilon c, 5.41%)
    sys(negative tag run period dependence, 4.57%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.4659,0.0057)
    meta_data(N jets tagged,  60894,246.8)
    meta_data(N jets total, 872061,933.8)
    usys(MC statistics, 1.43%)
    sys(jet vertex fraction,-0.37%)
    sys(jet energy scale,-2.82%)
    sys(jet energy resolution,-5.61%)
    sys(b-fractions, 0.66%)
    sys(c-fractions, 2.41%)
    sys(track multiplicity, 7.73%)
    sys(long lived particles,-0.20%)
    sys(fake tracks, 0.49%)
    sys(photon conversions, 0.63%)
    sys(material interactions, 1.81%)
    sys(trigger bias,-5.30%)
    sys(negative tag epsilon b, 1.86%)
    sys(negative tag epsilon c, 5.63%)
    sys(negative tag run period dependence, 2.43%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
