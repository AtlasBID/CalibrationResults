Analysis(negative tags,light,MV1, 0.39, AntiKt4TopoEMJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2808,0.0220)
    meta_data(N jets tagged,   3232, 56.9)
    meta_data(N jets total,  65054,255.1)
    usys(MC statistics, 2.06%)
    sys(jet vertex fraction,-6.62%)
    sys(jet energy scale,-3.01%)
    sys(jet energy resolution,-1.28%)
    sys(b-fractions, 0.37%)
    sys(c-fractions, 1.03%)
    sys(track multiplicity,10.12%)
    sys(long lived particles,-0.29%)
    sys(fake tracks,-0.02%)
    sys(photon conversions, 0.36%)
    sys(material interactions, 0.64%)
    sys(trigger bias, 0.21%)
    sys(negative tag epsilon b, 1.36%)
    sys(negative tag epsilon c, 3.44%)
    sys(negative tag run period dependence, 0.74%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2650,0.0119)
    meta_data(N jets tagged,  10826,104.0)
    meta_data(N jets total, 282714,531.7)
    usys(MC statistics, 1.35%)
    sys(jet vertex fraction,-0.88%)
    sys(jet energy scale,-0.73%)
    sys(jet energy resolution,-1.49%)
    sys(b-fractions, 0.44%)
    sys(c-fractions, 1.60%)
    sys(track multiplicity, 5.93%)
    sys(long lived particles,-0.16%)
    sys(fake tracks,-0.22%)
    sys(photon conversions, 0.61%)
    sys(material interactions, 1.39%)
    sys(trigger bias,-3.29%)
    sys(negative tag epsilon b, 1.35%)
    sys(negative tag epsilon c, 4.18%)
    sys(negative tag run period dependence, 0.80%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1880,0.0059)
    meta_data(N jets tagged,  39278,198.2)
    meta_data(N jets total, 841867,917.5)
    usys(MC statistics, 0.73%)
    sys(jet vertex fraction,-0.64%)
    sys(jet energy scale, 0.29%)
    sys(jet energy resolution, 1.03%)
    sys(b-fractions, 0.99%)
    sys(c-fractions, 3.10%)
    sys(track multiplicity, 8.28%)
    sys(long lived particles,-0.27%)
    sys(fake tracks, 0.33%)
    sys(photon conversions, 0.54%)
    sys(material interactions, 1.54%)
    sys(trigger bias,-2.95%)
    sys(negative tag epsilon b, 2.64%)
    sys(negative tag epsilon c, 6.49%)
    sys(negative tag run period dependence, 2.38%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2706,0.0038)
    meta_data(N jets tagged, 105916,325.4)
    meta_data(N jets total,1799372,1341.4)
    usys(MC statistics, 0.92%)
    sys(jet vertex fraction, 0.19%)
    sys(jet energy scale, 0.95%)
    sys(jet energy resolution, 0.64%)
    sys(b-fractions, 1.41%)
    sys(c-fractions, 4.46%)
    sys(track multiplicity, 4.76%)
    sys(long lived particles,-0.72%)
    sys(fake tracks, 1.81%)
    sys(photon conversions, 1.60%)
    sys(material interactions, 4.39%)
    sys(trigger bias,-5.04%)
    sys(negative tag epsilon b, 3.36%)
    sys(negative tag epsilon c, 8.17%)
    sys(negative tag run period dependence, 3.86%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3321,0.0034)
    meta_data(N jets tagged, 138734,372.5)
    meta_data(N jets total,1864051,1365.3)
    usys(MC statistics, 0.86%)
    sys(jet vertex fraction,-0.32%)
    sys(jet energy scale, 0.58%)
    sys(jet energy resolution, 1.94%)
    sys(b-fractions, 1.41%)
    sys(c-fractions, 4.11%)
    sys(track multiplicity, 2.49%)
    sys(long lived particles,-0.52%)
    sys(fake tracks, 1.29%)
    sys(photon conversions, 0.93%)
    sys(material interactions, 2.51%)
    sys(trigger bias,-2.69%)
    sys(negative tag epsilon b, 3.31%)
    sys(negative tag epsilon c, 7.89%)
    sys(negative tag run period dependence, 5.96%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2343,0.0023)
    meta_data(N jets tagged, 258231,508.2)
    meta_data(N jets total,3600008,1897.4)
    usys(MC statistics, 1.04%)
    sys(jet vertex fraction,-0.85%)
    sys(jet energy scale,-1.69%)
    sys(jet energy resolution,-0.74%)
    sys(b-fractions, 1.13%)
    sys(c-fractions, 3.80%)
    sys(track multiplicity, 4.10%)
    sys(long lived particles,-0.26%)
    sys(fake tracks, 0.51%)
    sys(photon conversions, 0.53%)
    sys(material interactions, 1.19%)
    sys(trigger bias, 0.51%)
    sys(negative tag epsilon b, 3.30%)
    sys(negative tag epsilon c, 8.39%)
    sys(negative tag run period dependence, 0.80%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2542,0.0273)
    meta_data(N jets tagged,   2037, 45.1)
    meta_data(N jets total,  54818,234.1)
    usys(MC statistics, 2.29%)
    sys(jet vertex fraction,-3.35%)
    sys(jet energy scale,-6.06%)
    sys(jet energy resolution,-2.04%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.40%)
    sys(track multiplicity,11.66%)
    sys(long lived particles,-0.26%)
    sys(fake tracks, 0.03%)
    sys(photon conversions, 0.38%)
    sys(material interactions, 0.68%)
    sys(trigger bias, 0.34%)
    sys(negative tag epsilon b, 0.90%)
    sys(negative tag epsilon c, 2.60%)
    sys(negative tag run period dependence, 3.20%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3354,0.0160)
    meta_data(N jets tagged,   6737, 82.1)
    meta_data(N jets total, 219736,468.8)
    usys(MC statistics, 1.29%)
    sys(jet vertex fraction,-0.74%)
    sys(jet energy scale,-0.64%)
    sys(jet energy resolution, 0.18%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 0.89%)
    sys(track multiplicity, 9.38%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 1.62%)
    sys(material interactions, 3.10%)
    sys(trigger bias,-1.93%)
    sys(negative tag epsilon b, 0.91%)
    sys(negative tag epsilon c, 3.24%)
    sys(negative tag run period dependence, 1.79%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2653,0.0085)
    meta_data(N jets tagged,  21155,145.4)
    meta_data(N jets total, 563471,750.6)
    usys(MC statistics, 0.81%)
    sys(jet vertex fraction,-0.92%)
    sys(jet energy scale, 0.58%)
    sys(jet energy resolution, 0.64%)
    sys(b-fractions, 0.55%)
    sys(c-fractions, 1.83%)
    sys(track multiplicity,13.02%)
    sys(long lived particles,-0.23%)
    sys(fake tracks, 0.21%)
    sys(photon conversions, 1.33%)
    sys(material interactions, 2.81%)
    sys(trigger bias,-2.50%)
    sys(negative tag epsilon b, 1.65%)
    sys(negative tag epsilon c, 4.63%)
    sys(negative tag run period dependence, 2.46%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3640,0.0065)
    meta_data(N jets tagged,  41909,204.7)
    meta_data(N jets total, 925092,961.8)
    usys(MC statistics, 0.92%)
    sys(jet vertex fraction, 1.05%)
    sys(jet energy scale, 1.58%)
    sys(jet energy resolution,-0.22%)
    sys(b-fractions, 0.77%)
    sys(c-fractions, 2.75%)
    sys(track multiplicity, 8.45%)
    sys(long lived particles,-0.46%)
    sys(fake tracks, 1.49%)
    sys(photon conversions, 3.29%)
    sys(material interactions, 7.61%)
    sys(trigger bias,-10.02%)
    sys(negative tag epsilon b, 1.94%)
    sys(negative tag epsilon c, 5.71%)
    sys(negative tag run period dependence, 3.89%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5336,0.0078)
    meta_data(N jets tagged,  36168,190.2)
    meta_data(N jets total, 596955,772.6)
    usys(MC statistics, 1.22%)
    sys(jet vertex fraction,-0.89%)
    sys(jet energy scale,-0.87%)
    sys(jet energy resolution,-0.58%)
    sys(b-fractions, 0.80%)
    sys(c-fractions, 2.62%)
    sys(track multiplicity, 3.24%)
    sys(long lived particles,-0.25%)
    sys(fake tracks, 1.01%)
    sys(photon conversions, 2.15%)
    sys(material interactions, 4.52%)
    sys(trigger bias,-4.33%)
    sys(negative tag epsilon b, 1.99%)
    sys(negative tag epsilon c, 5.53%)
    sys(negative tag run period dependence, 5.42%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(5.0320,0.0208)
    meta_data(N jets tagged,  54878,234.3)
    meta_data(N jets total, 905489,951.6)
    usys(MC statistics,61.78%)
    sys(jet vertex fraction,-70.99%)
    sys(jet energy scale,-71.18%)
    sys(jet energy resolution,-72.07%)
    sys(b-fractions, 0.75%)
    sys(c-fractions, 3.38%)
    sys(track multiplicity,-68.95%)
    sys(long lived particles, 0.09%)
    sys(fake tracks, 8.89%)
    sys(photon conversions, 0.84%)
    sys(material interactions, 0.89%)
    sys(trigger bias,168.14%)
    sys(negative tag epsilon b, 6.55%)
    sys(negative tag epsilon c,18.26%)
    sys(negative tag run period dependence, 1.74%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
