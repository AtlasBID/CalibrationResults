Analysis(negative tags,light,MV1, 0.3900, AntiKt4TopoEMJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2820,0.0220)
    meta_data(N jets tagged,   3232, 56.9)
    meta_data(N jets total,  65054,255.1)
    usys(MC statistics, 2.06%)
    sys(jet vertex fraction,-6.71%)
    sys(jet energy scale,-3.01%)
    sys(jet energy resolution,-1.37%)
    sys(b-fractions, 0.37%)
    sys(c-fractions, 1.03%)
    sys(track multiplicity,10.01%)
    sys(long lived particles,-0.30%)
    sys(fake tracks,-0.03%)
    sys(photon conversions, 0.35%)
    sys(material interactions, 0.64%)
    sys(trigger bias, 0.12%)
    sys(negative tag epsilon b, 1.36%)
    sys(negative tag epsilon c, 3.45%)
    sys(negative tag run period dependence, 0.74%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2630,0.0119)
    meta_data(N jets tagged,  10826,104.0)
    meta_data(N jets total, 282714,531.7)
    usys(MC statistics, 1.36%)
    sys(jet vertex fraction,-0.68%)
    sys(jet energy scale,-0.54%)
    sys(jet energy resolution,-1.30%)
    sys(b-fractions, 0.45%)
    sys(c-fractions, 1.61%)
    sys(track multiplicity, 6.14%)
    sys(long lived particles,-0.16%)
    sys(fake tracks,-0.23%)
    sys(photon conversions, 0.61%)
    sys(material interactions, 1.39%)
    sys(trigger bias,-3.18%)
    sys(negative tag epsilon b, 1.36%)
    sys(negative tag epsilon c, 4.20%)
    sys(negative tag run period dependence, 0.80%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1898,0.0059)
    meta_data(N jets tagged,  39278,198.2)
    meta_data(N jets total, 841867,917.5)
    usys(MC statistics, 0.72%)
    sys(jet vertex fraction,-0.75%)
    sys(jet energy scale, 0.29%)
    sys(jet energy resolution, 0.92%)
    sys(b-fractions, 0.99%)
    sys(c-fractions, 3.10%)
    sys(track multiplicity, 8.16%)
    sys(long lived particles,-0.27%)
    sys(fake tracks, 0.33%)
    sys(photon conversions, 0.55%)
    sys(material interactions, 1.54%)
    sys(trigger bias,-2.95%)
    sys(negative tag epsilon b, 2.64%)
    sys(negative tag epsilon c, 6.49%)
    sys(negative tag run period dependence, 2.38%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2752,0.0038)
    meta_data(N jets tagged, 105916,325.4)
    meta_data(N jets total,1799372,1341.4)
    usys(MC statistics, 0.96%)
    sys(jet vertex fraction,-0.14%)
    sys(jet energy scale, 0.94%)
    sys(jet energy resolution, 0.31%)
    sys(b-fractions, 1.39%)
    sys(c-fractions, 4.46%)
    sys(track multiplicity, 4.41%)
    sys(long lived particles,-0.69%)
    sys(fake tracks, 1.76%)
    sys(photon conversions, 1.54%)
    sys(material interactions, 4.31%)
    sys(trigger bias,-4.77%)
    sys(negative tag epsilon b, 3.32%)
    sys(negative tag epsilon c, 8.18%)
    sys(negative tag run period dependence, 3.86%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3308,0.0034)
    meta_data(N jets tagged, 138734,372.5)
    meta_data(N jets total,1864051,1365.3)
    usys(MC statistics, 0.88%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale, 0.61%)
    sys(jet energy resolution, 2.06%)
    sys(b-fractions, 1.41%)
    sys(c-fractions, 4.14%)
    sys(track multiplicity, 2.61%)
    sys(long lived particles,-0.52%)
    sys(fake tracks, 1.29%)
    sys(photon conversions, 0.92%)
    sys(material interactions, 2.50%)
    sys(trigger bias,-2.44%)
    sys(negative tag epsilon b, 3.31%)
    sys(negative tag epsilon c, 7.91%)
    sys(negative tag run period dependence, 5.96%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2272,0.0023)
    meta_data(N jets tagged, 258231,508.2)
    meta_data(N jets total,3600008,1897.4)
    usys(MC statistics, 0.95%)
    sys(jet vertex fraction,-0.18%)
    sys(jet energy scale,-1.71%)
    sys(jet energy resolution,-0.06%)
    sys(b-fractions, 1.11%)
    sys(c-fractions, 3.76%)
    sys(track multiplicity, 4.81%)
    sys(long lived particles,-0.27%)
    sys(fake tracks, 0.53%)
    sys(photon conversions, 0.54%)
    sys(material interactions, 1.21%)
    sys(trigger bias, 0.02%)
    sys(negative tag epsilon b, 3.23%)
    sys(negative tag epsilon c, 8.30%)
    sys(negative tag run period dependence, 0.80%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2540,0.0273)
    meta_data(N jets tagged,   2037, 45.1)
    meta_data(N jets total,  54818,234.1)
    usys(MC statistics, 2.29%)
    sys(jet vertex fraction,-3.33%)
    sys(jet energy scale,-6.06%)
    sys(jet energy resolution,-2.02%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.43%)
    sys(track multiplicity,11.68%)
    sys(long lived particles,-0.26%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.38%)
    sys(material interactions, 0.71%)
    sys(trigger bias, 0.28%)
    sys(negative tag epsilon b, 0.90%)
    sys(negative tag epsilon c, 2.63%)
    sys(negative tag run period dependence, 3.20%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3345,0.0160)
    meta_data(N jets tagged,   6737, 82.1)
    meta_data(N jets total, 219736,468.8)
    usys(MC statistics, 1.28%)
    sys(jet vertex fraction,-0.64%)
    sys(jet energy scale,-0.55%)
    sys(jet energy resolution, 0.28%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 0.89%)
    sys(track multiplicity, 9.49%)
    sys(long lived particles,-0.13%)
    sys(fake tracks, 0.06%)
    sys(photon conversions, 1.60%)
    sys(material interactions, 3.08%)
    sys(trigger bias,-1.88%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 3.23%)
    sys(negative tag run period dependence, 1.79%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2649,0.0085)
    meta_data(N jets tagged,  21155,145.4)
    meta_data(N jets total, 563471,750.6)
    usys(MC statistics, 0.81%)
    sys(jet vertex fraction,-0.87%)
    sys(jet energy scale, 0.58%)
    sys(jet energy resolution, 0.70%)
    sys(b-fractions, 0.55%)
    sys(c-fractions, 1.82%)
    sys(track multiplicity,13.09%)
    sys(long lived particles,-0.23%)
    sys(fake tracks, 0.21%)
    sys(photon conversions, 1.33%)
    sys(material interactions, 2.81%)
    sys(trigger bias,-2.34%)
    sys(negative tag epsilon b, 1.66%)
    sys(negative tag epsilon c, 4.62%)
    sys(negative tag run period dependence, 2.46%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3798,0.0066)
    meta_data(N jets tagged,  41909,204.7)
    meta_data(N jets total, 925092,961.8)
    usys(MC statistics, 0.99%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale, 0.43%)
    sys(jet energy resolution,-1.35%)
    sys(b-fractions, 0.77%)
    sys(c-fractions, 2.70%)
    sys(track multiplicity, 7.22%)
    sys(long lived particles,-0.45%)
    sys(fake tracks, 1.38%)
    sys(photon conversions, 3.06%)
    sys(material interactions, 7.26%)
    sys(trigger bias,-8.67%)
    sys(negative tag epsilon b, 1.95%)
    sys(negative tag epsilon c, 5.63%)
    sys(negative tag run period dependence, 3.89%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5236,0.0078)
    meta_data(N jets tagged,  36168,190.2)
    meta_data(N jets total, 596955,772.6)
    usys(MC statistics, 1.25%)
    sys(jet vertex fraction,-0.23%)
    sys(jet energy scale, 0.64%)
    sys(jet energy resolution, 0.08%)
    sys(b-fractions, 0.79%)
    sys(c-fractions, 2.66%)
    sys(track multiplicity, 3.93%)
    sys(long lived particles,-0.24%)
    sys(fake tracks, 1.03%)
    sys(photon conversions, 2.15%)
    sys(material interactions, 4.53%)
    sys(trigger bias,-3.81%)
    sys(negative tag epsilon b, 1.96%)
    sys(negative tag epsilon c, 5.56%)
    sys(negative tag run period dependence, 5.42%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.4663,0.0061)
    meta_data(N jets tagged,  54878,234.3)
    meta_data(N jets total, 905489,951.6)
    usys(MC statistics, 1.67%)
    sys(jet vertex fraction,-0.45%)
    sys(jet energy scale,-2.12%)
    sys(jet energy resolution,-4.14%)
    sys(b-fractions, 0.64%)
    sys(c-fractions, 2.09%)
    sys(track multiplicity, 6.55%)
    sys(long lived particles,-0.15%)
    sys(fake tracks, 0.44%)
    sys(photon conversions, 0.54%)
    sys(material interactions, 1.59%)
    sys(trigger bias,-9.26%)
    sys(negative tag epsilon b, 1.92%)
    sys(negative tag epsilon c, 5.30%)
    sys(negative tag run period dependence, 1.74%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
