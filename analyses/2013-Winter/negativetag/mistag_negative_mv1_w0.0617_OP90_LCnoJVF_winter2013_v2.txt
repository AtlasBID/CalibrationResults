Analysis(negative tags,light,MV1, 0.0617, AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.0006,0.0027)
    meta_data(N jets tagged,  45728,213.8)
    meta_data(N jets total,  67495,259.8)
    usys(MC statistics, 0.33%)
    sys(jet vertex fraction,-18.21%)
    sys(jet energy scale,12.40%)
    sys(jet energy resolution,13.30%)
    sys(b-fractions,-0.05%)
    sys(c-fractions,-0.10%)
    sys(track multiplicity,24.13%)
    sys(long lived particles,-0.11%)
    sys(fake tracks, 0.14%)
    sys(photon conversions, 0.12%)
    sys(material interactions, 0.24%)
    sys(trigger bias,-0.11%)
    sys(negative tag epsilon b, 0.42%)
    sys(negative tag epsilon c, 2.17%)
    sys(negative tag run period dependence, 0.56%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1200,0.0034)
    meta_data(N jets tagged,  77192,277.8)
    meta_data(N jets total, 279474,528.7)
    usys(MC statistics, 0.48%)
    sys(jet vertex fraction,-4.46%)
    sys(jet energy scale,-4.55%)
    sys(jet energy resolution,-2.89%)
    sys(b-fractions, 0.10%)
    sys(c-fractions, 0.57%)
    sys(track multiplicity, 3.05%)
    sys(long lived particles,-0.22%)
    sys(fake tracks,-0.50%)
    sys(photon conversions, 1.09%)
    sys(material interactions, 2.40%)
    sys(trigger bias,-4.73%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 3.12%)
    sys(negative tag run period dependence, 0.82%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1363,0.0023)
    meta_data(N jets tagged, 189774,435.6)
    meta_data(N jets total, 798382,893.5)
    usys(MC statistics, 0.35%)
    sys(jet vertex fraction,-0.69%)
    sys(jet energy scale, 0.56%)
    sys(jet energy resolution, 0.76%)
    sys(b-fractions, 0.34%)
    sys(c-fractions, 1.40%)
    sys(track multiplicity, 2.23%)
    sys(long lived particles,-0.28%)
    sys(fake tracks, 0.22%)
    sys(photon conversions, 0.42%)
    sys(material interactions, 1.31%)
    sys(trigger bias,-0.92%)
    sys(negative tag epsilon b, 1.38%)
    sys(negative tag epsilon c, 4.59%)
    sys(negative tag run period dependence, 0.94%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1677,0.0014)
    meta_data(N jets tagged, 496666,704.7)
    meta_data(N jets total,1680610,1296.4)
    usys(MC statistics, 0.46%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale, 0.68%)
    sys(jet energy resolution, 0.05%)
    sys(b-fractions, 0.44%)
    sys(c-fractions, 1.77%)
    sys(track multiplicity, 0.49%)
    sys(long lived particles,-0.63%)
    sys(fake tracks, 0.86%)
    sys(photon conversions, 1.24%)
    sys(material interactions, 3.79%)
    sys(trigger bias,-1.25%)
    sys(negative tag epsilon b, 1.51%)
    sys(negative tag epsilon c, 5.18%)
    sys(negative tag run period dependence, 1.67%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1881,0.0013)
    meta_data(N jets tagged, 589043,767.5)
    meta_data(N jets total,1712041,1308.4)
    usys(MC statistics, 0.41%)
    sys(jet vertex fraction,-0.13%)
    sys(jet energy scale, 0.52%)
    sys(jet energy resolution,-0.25%)
    sys(b-fractions, 0.42%)
    sys(c-fractions, 1.66%)
    sys(track multiplicity, 0.13%)
    sys(long lived particles,-0.47%)
    sys(fake tracks, 0.72%)
    sys(photon conversions, 0.80%)
    sys(material interactions, 2.48%)
    sys(trigger bias,-0.13%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 5.20%)
    sys(negative tag run period dependence, 1.55%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.1549,0.0008)
    meta_data(N jets tagged,1265924,1125.1)
    meta_data(N jets total,3472572,1863.5)
    usys(MC statistics, 0.49%)
    sys(jet vertex fraction,-0.14%)
    sys(jet energy scale, 0.58%)
    sys(jet energy resolution, 0.56%)
    sys(b-fractions, 0.35%)
    sys(c-fractions, 1.54%)
    sys(track multiplicity, 0.47%)
    sys(long lived particles,-0.42%)
    sys(fake tracks, 0.28%)
    sys(photon conversions, 0.36%)
    sys(material interactions, 1.05%)
    sys(trigger bias,-0.37%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 5.41%)
    sys(negative tag run period dependence, 0.73%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.0130,0.0037)
    meta_data(N jets tagged,  31940,178.7)
    meta_data(N jets total,  56373,237.4)
    usys(MC statistics, 0.46%)
    sys(jet vertex fraction,-15.63%)
    sys(jet energy scale,14.64%)
    sys(jet energy resolution,11.99%)
    sys(b-fractions,-0.05%)
    sys(c-fractions,-0.09%)
    sys(track multiplicity,25.35%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.15%)
    sys(photon conversions, 0.25%)
    sys(material interactions, 0.40%)
    sys(trigger bias,-0.15%)
    sys(negative tag epsilon b, 0.36%)
    sys(negative tag epsilon c, 2.06%)
    sys(negative tag run period dependence, 0.36%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1423,0.0044)
    meta_data(N jets tagged,  51063,226.0)
    meta_data(N jets total, 213460,462.0)
    usys(MC statistics, 0.52%)
    sys(jet vertex fraction,-4.02%)
    sys(jet energy scale, 3.38%)
    sys(jet energy resolution,-0.27%)
    sys(b-fractions, 0.05%)
    sys(c-fractions, 0.33%)
    sys(track multiplicity, 7.05%)
    sys(long lived particles,-0.16%)
    sys(fake tracks,-0.15%)
    sys(photon conversions, 1.07%)
    sys(material interactions, 2.22%)
    sys(trigger bias,-2.31%)
    sys(negative tag epsilon b, 0.56%)
    sys(negative tag epsilon c, 2.62%)
    sys(negative tag run period dependence, 1.75%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1255,0.0028)
    meta_data(N jets tagged, 122092,349.4)
    meta_data(N jets total, 536506,732.5)
    usys(MC statistics, 0.37%)
    sys(jet vertex fraction,-0.67%)
    sys(jet energy scale, 0.32%)
    sys(jet energy resolution, 0.80%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.81%)
    sys(track multiplicity, 3.93%)
    sys(long lived particles,-0.17%)
    sys(fake tracks, 0.16%)
    sys(photon conversions, 0.61%)
    sys(material interactions, 1.66%)
    sys(trigger bias,-0.63%)
    sys(negative tag epsilon b, 0.90%)
    sys(negative tag epsilon c, 3.41%)
    sys(negative tag run period dependence, 1.29%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.1733,0.0021)
    meta_data(N jets tagged, 234308,484.1)
    meta_data(N jets total, 872175,933.9)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction,-0.26%)
    sys(jet energy scale, 0.80%)
    sys(jet energy resolution, 0.38%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.01%)
    sys(track multiplicity, 1.75%)
    sys(long lived particles,-0.34%)
    sys(fake tracks, 0.92%)
    sys(photon conversions, 1.58%)
    sys(material interactions, 4.53%)
    sys(trigger bias,-1.81%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 3.59%)
    sys(negative tag run period dependence, 1.91%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.2224,0.0024)
    meta_data(N jets tagged, 177360,421.1)
    meta_data(N jets total, 547574,740.0)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction,-0.15%)
    sys(jet energy scale, 0.50%)
    sys(jet energy resolution,-0.34%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity, 0.95%)
    sys(long lived particles,-0.22%)
    sys(fake tracks, 0.88%)
    sys(photon conversions, 1.14%)
    sys(material interactions, 3.23%)
    sys(trigger bias,-0.54%)
    sys(negative tag epsilon b, 0.87%)
    sys(negative tag epsilon c, 3.66%)
    sys(negative tag run period dependence, 1.94%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2233,0.0018)
    meta_data(N jets tagged, 302329,549.8)
    meta_data(N jets total, 872061,933.8)
    usys(MC statistics, 0.75%)
    sys(jet vertex fraction,-0.21%)
    sys(jet energy scale,-1.56%)
    sys(jet energy resolution,-1.99%)
    sys(b-fractions, 0.20%)
    sys(c-fractions, 0.91%)
    sys(track multiplicity, 1.05%)
    sys(long lived particles,-0.30%)
    sys(fake tracks, 0.49%)
    sys(photon conversions, 0.70%)
    sys(material interactions, 1.54%)
    sys(trigger bias, 0.53%)
    sys(negative tag epsilon b, 0.85%)
    sys(negative tag epsilon c, 3.67%)
    sys(negative tag run period dependence, 1.26%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
