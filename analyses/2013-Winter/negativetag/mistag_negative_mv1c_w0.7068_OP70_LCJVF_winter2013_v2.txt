Analysis(negative tags,light,MV1c, 0.7068, AntiKt4TopoLCJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.3124,0.0515)
    meta_data(N jets tagged,    643, 25.4)
    meta_data(N jets total,  67495,259.8)
    usys(MC statistics, 4.71%)
    sys(jet vertex fraction,-9.69%)
    sys(jet energy scale,-3.57%)
    sys(jet energy resolution,-2.19%)
    sys(b-fractions, 0.45%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity,14.93%)
    sys(long lived particles,-0.18%)
    sys(fake tracks, 0.01%)
    sys(photon conversions, 0.19%)
    sys(material interactions, 0.48%)
    sys(trigger bias, 1.20%)
    sys(negative tag epsilon b, 1.54%)
    sys(negative tag epsilon c, 3.49%)
    sys(negative tag run period dependence, 5.90%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3262,0.0285)
    meta_data(N jets tagged,   2144, 46.3)
    meta_data(N jets total, 279474,528.7)
    usys(MC statistics, 2.57%)
    sys(jet vertex fraction,-1.44%)
    sys(jet energy scale,-1.37%)
    sys(jet energy resolution,-1.01%)
    sys(b-fractions, 0.58%)
    sys(c-fractions, 1.93%)
    sys(track multiplicity,10.48%)
    sys(long lived particles,-0.18%)
    sys(fake tracks,-1.13%)
    sys(photon conversions, 0.58%)
    sys(material interactions, 1.21%)
    sys(trigger bias,-3.10%)
    sys(negative tag epsilon b, 1.60%)
    sys(negative tag epsilon c, 4.59%)
    sys(negative tag run period dependence, 4.78%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2940,0.0151)
    meta_data(N jets tagged,   7312, 85.5)
    meta_data(N jets total, 798382,893.5)
    usys(MC statistics, 1.66%)
    sys(jet vertex fraction,-0.86%)
    sys(jet energy scale,-0.54%)
    sys(jet energy resolution,-0.84%)
    sys(b-fractions, 1.94%)
    sys(c-fractions, 4.53%)
    sys(track multiplicity,10.42%)
    sys(long lived particles,-0.39%)
    sys(fake tracks, 0.48%)
    sys(photon conversions, 0.49%)
    sys(material interactions, 1.61%)
    sys(trigger bias,-5.27%)
    sys(negative tag epsilon b, 4.56%)
    sys(negative tag epsilon c, 8.03%)
    sys(negative tag run period dependence, 2.10%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3598,0.0113)
    meta_data(N jets tagged,  14243,119.3)
    meta_data(N jets total,1680610,1296.4)
    usys(MC statistics, 2.10%)
    sys(jet vertex fraction,-0.03%)
    sys(jet energy scale,-0.80%)
    sys(jet energy resolution, 4.14%)
    sys(b-fractions, 3.72%)
    sys(c-fractions, 5.73%)
    sys(track multiplicity, 4.81%)
    sys(long lived particles,-1.13%)
    sys(fake tracks, 3.40%)
    sys(photon conversions, 2.50%)
    sys(material interactions, 7.41%)
    sys(trigger bias,-8.72%)
    sys(negative tag epsilon b, 7.82%)
    sys(negative tag epsilon c, 9.00%)
    sys(negative tag run period dependence, 2.69%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4386,0.0109)
    meta_data(N jets tagged,  17140,130.9)
    meta_data(N jets total,1712041,1308.4)
    usys(MC statistics, 2.07%)
    sys(jet vertex fraction,-0.24%)
    sys(jet energy scale, 0.67%)
    sys(jet energy resolution,-2.84%)
    sys(b-fractions, 3.92%)
    sys(c-fractions, 5.33%)
    sys(track multiplicity, 4.09%)
    sys(long lived particles,-0.96%)
    sys(fake tracks, 2.88%)
    sys(photon conversions, 1.19%)
    sys(material interactions, 3.74%)
    sys(trigger bias,-1.95%)
    sys(negative tag epsilon b, 8.12%)
    sys(negative tag epsilon c, 8.53%)
    sys(negative tag run period dependence, 8.83%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2529,0.0070)
    meta_data(N jets tagged,  32176,179.4)
    meta_data(N jets total,3472572,1863.5)
    usys(MC statistics, 1.89%)
    sys(jet vertex fraction,-0.08%)
    sys(jet energy scale, 1.17%)
    sys(jet energy resolution, 1.05%)
    sys(b-fractions, 3.21%)
    sys(c-fractions, 4.86%)
    sys(track multiplicity, 5.92%)
    sys(long lived particles,-0.84%)
    sys(fake tracks, 1.15%)
    sys(photon conversions, 0.85%)
    sys(material interactions, 1.60%)
    sys(trigger bias,-0.14%)
    sys(negative tag epsilon b, 7.75%)
    sys(negative tag epsilon c, 8.77%)
    sys(negative tag run period dependence, 1.28%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.3916,0.0739)
    meta_data(N jets tagged,    352, 18.8)
    meta_data(N jets total,  56373,237.4)
    usys(MC statistics, 6.37%)
    sys(jet vertex fraction, 0.40%)
    sys(jet energy scale,-0.26%)
    sys(jet energy resolution, 8.19%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 0.37%)
    sys(track multiplicity,20.18%)
    sys(long lived particles,-0.76%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.25%)
    sys(material interactions, 0.76%)
    sys(trigger bias, 0.75%)
    sys(negative tag epsilon b, 0.94%)
    sys(negative tag epsilon c, 2.56%)
    sys(negative tag run period dependence, 4.68%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.5033,0.0438)
    meta_data(N jets tagged,   1170, 34.2)
    meta_data(N jets total, 213460,462.0)
    usys(MC statistics, 2.74%)
    sys(jet vertex fraction,-2.62%)
    sys(jet energy scale,-0.60%)
    sys(jet energy resolution, 2.48%)
    sys(b-fractions, 0.33%)
    sys(c-fractions, 1.40%)
    sys(track multiplicity,14.56%)
    sys(long lived particles,-0.09%)
    sys(fake tracks,-0.57%)
    sys(photon conversions, 0.56%)
    sys(material interactions, 1.20%)
    sys(trigger bias,-1.59%)
    sys(negative tag epsilon b, 1.09%)
    sys(negative tag epsilon c, 3.83%)
    sys(negative tag run period dependence, 4.50%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3428,0.0245)
    meta_data(N jets tagged,   2985, 54.6)
    meta_data(N jets total, 536506,732.5)
    usys(MC statistics, 1.96%)
    sys(jet vertex fraction,-1.29%)
    sys(jet energy scale, 1.39%)
    sys(jet energy resolution, 2.41%)
    sys(b-fractions, 1.27%)
    sys(c-fractions, 2.56%)
    sys(track multiplicity,14.51%)
    sys(long lived particles,-0.25%)
    sys(fake tracks, 0.29%)
    sys(photon conversions, 0.86%)
    sys(material interactions, 2.41%)
    sys(trigger bias,-2.28%)
    sys(negative tag epsilon b, 3.17%)
    sys(negative tag epsilon c, 5.36%)
    sys(negative tag run period dependence, 5.61%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5447,0.0248)
    meta_data(N jets tagged,   3861, 62.1)
    meta_data(N jets total, 872175,933.9)
    usys(MC statistics, 2.29%)
    sys(jet vertex fraction,-0.84%)
    sys(jet energy scale, 0.35%)
    sys(jet energy resolution,-0.85%)
    sys(b-fractions, 2.25%)
    sys(c-fractions, 3.43%)
    sys(track multiplicity, 7.30%)
    sys(long lived particles,-0.78%)
    sys(fake tracks, 1.23%)
    sys(photon conversions, 2.24%)
    sys(material interactions, 7.11%)
    sys(trigger bias,-0.50%)
    sys(negative tag epsilon b, 4.92%)
    sys(negative tag epsilon c, 6.22%)
    sys(negative tag run period dependence, 5.85%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.4511,0.0262)
    meta_data(N jets tagged,   3058, 55.3)
    meta_data(N jets total, 547574,740.0)
    usys(MC statistics, 2.97%)
    sys(jet vertex fraction,-0.01%)
    sys(jet energy scale, 1.05%)
    sys(jet energy resolution, 1.26%)
    sys(b-fractions, 1.89%)
    sys(c-fractions, 2.43%)
    sys(track multiplicity, 6.58%)
    sys(long lived particles, 0.07%)
    sys(fake tracks, 1.29%)
    sys(photon conversions, 1.81%)
    sys(material interactions, 5.43%)
    sys(trigger bias,-3.66%)
    sys(negative tag epsilon b, 4.15%)
    sys(negative tag epsilon c, 4.98%)
    sys(negative tag run period dependence, 7.48%)
    sys(d0 and z0 smearing,-22.13%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5148,0.0212)
    meta_data(N jets tagged,   5080, 71.3)
    meta_data(N jets total, 872061,933.8)
    usys(MC statistics, 3.14%)
    sys(jet vertex fraction,-0.71%)
    sys(jet energy scale,-4.45%)
    sys(jet energy resolution,-15.52%)
    sys(b-fractions, 1.74%)
    sys(c-fractions, 2.28%)
    sys(track multiplicity, 8.53%)
    sys(long lived particles,-0.50%)
    sys(fake tracks, 0.13%)
    sys(photon conversions, 0.21%)
    sys(material interactions, 1.77%)
    sys(trigger bias,10.57%)
    sys(negative tag epsilon b, 4.29%)
    sys(negative tag epsilon c, 5.15%)
    sys(negative tag run period dependence, 1.13%)
    sys(d0 and z0 smearing,-22.13%)
  }
}
