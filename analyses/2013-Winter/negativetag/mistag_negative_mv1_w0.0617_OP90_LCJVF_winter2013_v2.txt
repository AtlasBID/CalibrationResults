Analysis(negative tags,light,MV1, 0.0617, AntiKt4TopoLCJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.0001,0.0026)
    meta_data(N jets tagged,  46716,216.1)
    meta_data(N jets total,  67495,259.8)
    usys(MC statistics, 0.33%)
    sys(jet vertex fraction,-8.22%)
    sys(jet energy scale,-4.24%)
    sys(jet energy resolution, 0.55%)
    sys(b-fractions,-0.05%)
    sys(c-fractions,-0.09%)
    sys(track multiplicity, 2.69%)
    sys(long lived particles,-0.11%)
    sys(fake tracks, 0.13%)
    sys(photon conversions, 0.12%)
    sys(material interactions, 0.24%)
    sys(trigger bias,-0.09%)
    sys(negative tag epsilon b, 0.42%)
    sys(negative tag epsilon c, 2.19%)
    sys(negative tag run period dependence, 0.57%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.1188,0.0034)
    meta_data(N jets tagged,  77816,279.0)
    meta_data(N jets total, 279474,528.7)
    usys(MC statistics, 0.48%)
    sys(jet vertex fraction,-1.71%)
    sys(jet energy scale,-4.70%)
    sys(jet energy resolution,-6.13%)
    sys(b-fractions, 0.10%)
    sys(c-fractions, 0.57%)
    sys(track multiplicity,-0.43%)
    sys(long lived particles,-0.22%)
    sys(fake tracks,-0.48%)
    sys(photon conversions, 1.09%)
    sys(material interactions, 2.39%)
    sys(trigger bias,-4.78%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 3.11%)
    sys(negative tag run period dependence, 0.74%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1367,0.0023)
    meta_data(N jets tagged, 190631,436.6)
    meta_data(N jets total, 798382,893.5)
    usys(MC statistics, 0.34%)
    sys(jet vertex fraction,-0.29%)
    sys(jet energy scale, 0.17%)
    sys(jet energy resolution, 0.32%)
    sys(b-fractions, 0.34%)
    sys(c-fractions, 1.40%)
    sys(track multiplicity, 1.55%)
    sys(long lived particles,-0.28%)
    sys(fake tracks, 0.22%)
    sys(photon conversions, 0.42%)
    sys(material interactions, 1.31%)
    sys(trigger bias,-0.93%)
    sys(negative tag epsilon b, 1.37%)
    sys(negative tag epsilon c, 4.59%)
    sys(negative tag run period dependence, 0.96%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.1676,0.0014)
    meta_data(N jets tagged, 499412,706.7)
    meta_data(N jets total,1680610,1296.4)
    usys(MC statistics, 0.46%)
    sys(jet vertex fraction,-0.09%)
    sys(jet energy scale, 0.67%)
    sys(jet energy resolution,-0.08%)
    sys(b-fractions, 0.43%)
    sys(c-fractions, 1.76%)
    sys(track multiplicity, 0.29%)
    sys(long lived particles,-0.63%)
    sys(fake tracks, 0.87%)
    sys(photon conversions, 1.25%)
    sys(material interactions, 3.80%)
    sys(trigger bias,-1.26%)
    sys(negative tag epsilon b, 1.50%)
    sys(negative tag epsilon c, 5.17%)
    sys(negative tag run period dependence, 1.68%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.1878,0.0012)
    meta_data(N jets tagged, 593778,770.6)
    meta_data(N jets total,1712041,1308.4)
    usys(MC statistics, 0.41%)
    sys(jet vertex fraction,-0.10%)
    sys(jet energy scale, 0.50%)
    sys(jet energy resolution,-0.28%)
    sys(b-fractions, 0.41%)
    sys(c-fractions, 1.65%)
    sys(track multiplicity,-0.02%)
    sys(long lived particles,-0.47%)
    sys(fake tracks, 0.73%)
    sys(photon conversions, 0.82%)
    sys(material interactions, 2.51%)
    sys(trigger bias,-0.15%)
    sys(negative tag epsilon b, 1.44%)
    sys(negative tag epsilon c, 5.19%)
    sys(negative tag run period dependence, 1.54%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.1556,0.0008)
    meta_data(N jets tagged,1272626,1128.1)
    meta_data(N jets total,3472572,1863.5)
    usys(MC statistics, 0.49%)
    sys(jet vertex fraction,-0.07%)
    sys(jet energy scale, 0.53%)
    sys(jet energy resolution, 0.50%)
    sys(b-fractions, 0.34%)
    sys(c-fractions, 1.53%)
    sys(track multiplicity, 0.24%)
    sys(long lived particles,-0.42%)
    sys(fake tracks, 0.28%)
    sys(photon conversions, 0.37%)
    sys(material interactions, 1.05%)
    sys(trigger bias,-0.33%)
    sys(negative tag epsilon b, 1.45%)
    sys(negative tag epsilon c, 5.41%)
    sys(negative tag run period dependence, 0.74%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.0118,0.0037)
    meta_data(N jets tagged,  32302,179.7)
    meta_data(N jets total,  56373,237.4)
    usys(MC statistics, 0.46%)
    sys(jet vertex fraction,-3.76%)
    sys(jet energy scale,-9.17%)
    sys(jet energy resolution,-1.56%)
    sys(b-fractions,-0.05%)
    sys(c-fractions,-0.09%)
    sys(track multiplicity, 3.31%)
    sys(long lived particles,-0.14%)
    sys(fake tracks, 0.15%)
    sys(photon conversions, 0.24%)
    sys(material interactions, 0.39%)
    sys(trigger bias,-0.15%)
    sys(negative tag epsilon b, 0.36%)
    sys(negative tag epsilon c, 2.06%)
    sys(negative tag run period dependence, 0.46%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1419,0.0044)
    meta_data(N jets tagged,  51440,226.8)
    meta_data(N jets total, 213460,462.0)
    usys(MC statistics, 0.51%)
    sys(jet vertex fraction, 0.22%)
    sys(jet energy scale,-2.98%)
    sys(jet energy resolution,-4.94%)
    sys(b-fractions, 0.05%)
    sys(c-fractions, 0.33%)
    sys(track multiplicity, 2.17%)
    sys(long lived particles,-0.16%)
    sys(fake tracks,-0.14%)
    sys(photon conversions, 1.07%)
    sys(material interactions, 2.22%)
    sys(trigger bias,-2.34%)
    sys(negative tag epsilon b, 0.56%)
    sys(negative tag epsilon c, 2.61%)
    sys(negative tag run period dependence, 1.72%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.1254,0.0028)
    meta_data(N jets tagged, 122669,350.2)
    meta_data(N jets total, 536506,732.5)
    usys(MC statistics, 0.37%)
    sys(jet vertex fraction,-0.20%)
    sys(jet energy scale, 0.29%)
    sys(jet energy resolution, 0.27%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.81%)
    sys(track multiplicity, 3.04%)
    sys(long lived particles,-0.17%)
    sys(fake tracks, 0.16%)
    sys(photon conversions, 0.60%)
    sys(material interactions, 1.66%)
    sys(trigger bias,-0.67%)
    sys(negative tag epsilon b, 0.90%)
    sys(negative tag epsilon c, 3.41%)
    sys(negative tag run period dependence, 1.31%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.1737,0.0021)
    meta_data(N jets tagged, 235398,485.2)
    meta_data(N jets total, 872175,933.9)
    usys(MC statistics, 0.57%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale, 0.75%)
    sys(jet energy resolution, 0.15%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.00%)
    sys(track multiplicity, 1.40%)
    sys(long lived particles,-0.34%)
    sys(fake tracks, 0.92%)
    sys(photon conversions, 1.56%)
    sys(material interactions, 4.51%)
    sys(trigger bias,-1.76%)
    sys(negative tag epsilon b, 0.92%)
    sys(negative tag epsilon c, 3.58%)
    sys(negative tag run period dependence, 1.88%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.2212,0.0024)
    meta_data(N jets tagged, 178080,422.0)
    meta_data(N jets total, 547574,740.0)
    usys(MC statistics, 0.56%)
    sys(jet vertex fraction,-0.08%)
    sys(jet energy scale, 0.50%)
    sys(jet energy resolution,-0.30%)
    sys(b-fractions, 0.23%)
    sys(c-fractions, 1.06%)
    sys(track multiplicity, 0.79%)
    sys(long lived particles,-0.21%)
    sys(fake tracks, 0.86%)
    sys(photon conversions, 1.12%)
    sys(material interactions, 3.21%)
    sys(trigger bias,-0.56%)
    sys(negative tag epsilon b, 0.86%)
    sys(negative tag epsilon c, 3.66%)
    sys(negative tag run period dependence, 1.93%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.2221,0.0018)
    meta_data(N jets tagged, 303636,551.0)
    meta_data(N jets total, 872061,933.8)
    usys(MC statistics, 0.75%)
    sys(jet vertex fraction,-0.24%)
    sys(jet energy scale,-1.59%)
    sys(jet energy resolution,-1.89%)
    sys(b-fractions, 0.19%)
    sys(c-fractions, 0.90%)
    sys(track multiplicity, 0.93%)
    sys(long lived particles,-0.30%)
    sys(fake tracks, 0.49%)
    sys(photon conversions, 0.69%)
    sys(material interactions, 1.53%)
    sys(trigger bias, 0.60%)
    sys(negative tag epsilon b, 0.85%)
    sys(negative tag epsilon c, 3.66%)
    sys(negative tag run period dependence, 1.26%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
