Analysis(negative tags,light,JetFitterCOMBCharm, -1.0_1.0, AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.0814,0.0382)
    meta_data(N jets tagged,    793, 28.2)
    meta_data(N jets total,  82557,287.3)
    usys(MC statistics, 4.98%)
    sys(jet vertex fraction, 9.82%)
    sys(jet energy scale,-8.31%)
    sys(jet energy resolution, 1.53%)
    sys(b-fractions, 0.29%)
    sys(c-fractions, 0.89%)
    sys(track multiplicity,12.24%)
    sys(long lived particles,-0.10%)
    sys(fake tracks,-0.04%)
    sys(photon conversions, 0.32%)
    sys(material interactions, 0.40%)
    sys(trigger bias, 0.00%)
    sys(negative tag epsilon b, 1.17%)
    sys(negative tag epsilon c, 3.29%)
    sys(negative tag run period dependence, 7.81%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2935,0.0251)
    meta_data(N jets tagged,   2625, 51.2)
    meta_data(N jets total, 316538,562.6)
    usys(MC statistics, 2.41%)
    sys(jet vertex fraction, 1.67%)
    sys(jet energy scale, 0.94%)
    sys(jet energy resolution,-1.85%)
    sys(b-fractions, 0.39%)
    sys(c-fractions, 2.24%)
    sys(track multiplicity, 3.70%)
    sys(long lived particles,-0.12%)
    sys(fake tracks,-0.94%)
    sys(photon conversions, 0.39%)
    sys(material interactions, 0.96%)
    sys(trigger bias, 1.27%)
    sys(negative tag epsilon b, 1.26%)
    sys(negative tag epsilon c, 4.99%)
    sys(negative tag run period dependence, 1.30%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3094,0.0126)
    meta_data(N jets tagged,  10728,103.6)
    meta_data(N jets total, 815694,903.2)
    usys(MC statistics, 1.60%)
    sys(jet vertex fraction, 0.46%)
    sys(jet energy scale, 0.35%)
    sys(jet energy resolution, 0.38%)
    sys(b-fractions, 1.16%)
    sys(c-fractions, 4.00%)
    sys(track multiplicity, 1.67%)
    sys(long lived particles,-0.35%)
    sys(fake tracks, 0.45%)
    sys(photon conversions, 0.17%)
    sys(material interactions, 0.83%)
    sys(trigger bias,-3.57%)
    sys(negative tag epsilon b, 2.98%)
    sys(negative tag epsilon c, 7.59%)
    sys(negative tag run period dependence, 2.63%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3945,0.0087)
    meta_data(N jets tagged,  25242,158.9)
    meta_data(N jets total,1692275,1300.9)
    usys(MC statistics, 1.82%)
    sys(jet vertex fraction, 0.10%)
    sys(jet energy scale, 0.06%)
    sys(jet energy resolution, 0.99%)
    sys(b-fractions, 2.23%)
    sys(c-fractions, 5.91%)
    sys(track multiplicity, 1.90%)
    sys(long lived particles,-1.13%)
    sys(fake tracks, 1.98%)
    sys(photon conversions, 1.34%)
    sys(material interactions, 3.14%)
    sys(trigger bias,-4.25%)
    sys(negative tag epsilon b, 4.91%)
    sys(negative tag epsilon c, 9.68%)
    sys(negative tag run period dependence, 3.43%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4800,0.0081)
    meta_data(N jets tagged,  32961,181.6)
    meta_data(N jets total,1719598,1311.3)
    usys(MC statistics, 2.02%)
    sys(jet vertex fraction, 0.05%)
    sys(jet energy scale, 0.47%)
    sys(jet energy resolution,-0.07%)
    sys(b-fractions, 2.62%)
    sys(c-fractions, 6.02%)
    sys(track multiplicity, 1.87%)
    sys(long lived particles,-0.76%)
    sys(fake tracks, 2.58%)
    sys(photon conversions, 1.32%)
    sys(material interactions, 1.72%)
    sys(trigger bias,-0.84%)
    sys(negative tag epsilon b, 5.60%)
    sys(negative tag epsilon c, 9.80%)
    sys(negative tag run period dependence, 6.16%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3450,0.0051)
    meta_data(N jets tagged,  68789,262.3)
    meta_data(N jets total,3484602,1866.7)
    usys(MC statistics, 1.94%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale, 1.47%)
    sys(jet energy resolution, 1.38%)
    sys(b-fractions, 2.26%)
    sys(c-fractions, 5.22%)
    sys(track multiplicity, 3.15%)
    sys(long lived particles,-0.45%)
    sys(fake tracks, 0.81%)
    sys(photon conversions, 0.54%)
    sys(material interactions, 0.65%)
    sys(trigger bias,-1.94%)
    sys(negative tag epsilon b, 5.61%)
    sys(negative tag epsilon c, 9.60%)
    sys(negative tag run period dependence, 1.44%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.0920,0.0491)
    meta_data(N jets tagged,    490, 22.1)
    meta_data(N jets total,  64177,253.3)
    usys(MC statistics, 5.23%)
    sys(jet vertex fraction, 3.33%)
    sys(jet energy scale,-6.65%)
    sys(jet energy resolution, 3.77%)
    sys(b-fractions, 0.14%)
    sys(c-fractions, 0.06%)
    sys(track multiplicity,14.70%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.06%)
    sys(photon conversions, 0.34%)
    sys(material interactions, 0.49%)
    sys(trigger bias, 0.00%)
    sys(negative tag epsilon b, 0.79%)
    sys(negative tag epsilon c, 2.17%)
    sys(negative tag run period dependence, 7.38%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2988,0.0356)
    meta_data(N jets tagged,   1321, 36.3)
    meta_data(N jets total, 237900,487.7)
    usys(MC statistics, 4.01%)
    sys(jet vertex fraction,-2.93%)
    sys(jet energy scale,-1.23%)
    sys(jet energy resolution, 4.28%)
    sys(b-fractions, 0.24%)
    sys(c-fractions, 1.45%)
    sys(track multiplicity, 5.23%)
    sys(long lived particles,-0.06%)
    sys(fake tracks, 1.00%)
    sys(photon conversions, 0.47%)
    sys(material interactions, 1.13%)
    sys(trigger bias,-4.71%)
    sys(negative tag epsilon b, 0.93%)
    sys(negative tag epsilon c, 3.90%)
    sys(negative tag run period dependence, 3.94%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3818,0.0219)
    meta_data(N jets tagged,   3947, 62.8)
    meta_data(N jets total, 550039,741.6)
    usys(MC statistics, 2.02%)
    sys(jet vertex fraction, 0.38%)
    sys(jet energy scale,-0.85%)
    sys(jet energy resolution, 1.21%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 2.44%)
    sys(track multiplicity, 4.00%)
    sys(long lived particles,-0.22%)
    sys(fake tracks, 0.18%)
    sys(photon conversions, 0.58%)
    sys(material interactions, 1.46%)
    sys(trigger bias,-6.15%)
    sys(negative tag epsilon b, 2.33%)
    sys(negative tag epsilon c, 5.30%)
    sys(negative tag run period dependence, 1.11%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5088,0.0190)
    meta_data(N jets tagged,   6268, 79.2)
    meta_data(N jets total, 880666,938.4)
    usys(MC statistics, 2.99%)
    sys(jet vertex fraction,-0.13%)
    sys(jet energy scale,-2.62%)
    sys(jet energy resolution,-2.33%)
    sys(b-fractions, 1.55%)
    sys(c-fractions, 3.84%)
    sys(track multiplicity, 4.40%)
    sys(long lived particles,-0.82%)
    sys(fake tracks, 0.96%)
    sys(photon conversions, 1.80%)
    sys(material interactions, 3.12%)
    sys(trigger bias,-3.85%)
    sys(negative tag epsilon b, 3.50%)
    sys(negative tag epsilon c, 6.91%)
    sys(negative tag run period dependence, 4.70%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6463,0.0219)
    meta_data(N jets tagged,   5610, 74.9)
    meta_data(N jets total, 551249,742.5)
    usys(MC statistics, 3.26%)
    sys(jet vertex fraction,-0.02%)
    sys(jet energy scale, 0.98%)
    sys(jet energy resolution, 0.14%)
    sys(b-fractions, 1.92%)
    sys(c-fractions, 3.76%)
    sys(track multiplicity, 4.27%)
    sys(long lived particles, 0.05%)
    sys(fake tracks, 2.04%)
    sys(photon conversions, 2.04%)
    sys(material interactions, 2.79%)
    sys(trigger bias,-2.35%)
    sys(negative tag epsilon b, 4.19%)
    sys(negative tag epsilon c, 6.63%)
    sys(negative tag run period dependence, 6.78%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.5974,0.0157)
    meta_data(N jets tagged,  10204,101.0)
    meta_data(N jets total, 877985,937.0)
    usys(MC statistics, 3.34%)
    sys(jet vertex fraction, 1.33%)
    sys(jet energy scale,-5.53%)
    sys(jet energy resolution,-9.35%)
    sys(b-fractions, 1.36%)
    sys(c-fractions, 3.55%)
    sys(track multiplicity, 5.23%)
    sys(long lived particles,-0.00%)
    sys(fake tracks, 1.42%)
    sys(photon conversions, 1.23%)
    sys(material interactions, 1.63%)
    sys(trigger bias,-3.96%)
    sys(negative tag epsilon b, 3.40%)
    sys(negative tag epsilon c, 7.03%)
    sys(negative tag run period dependence, 3.45%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
