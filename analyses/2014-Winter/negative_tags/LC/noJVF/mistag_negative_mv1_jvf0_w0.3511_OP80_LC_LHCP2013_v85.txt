Analysis(negative_tags,light,MV1, 0.3511, AntiKt4TopoLCnoJVF){ 
  meta_data_s (Hadronization, Pythia8) 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1203,0.0177)
    meta_data(N jets tagged,   3829, 61.9)
    meta_data(N jets total,  98387,313.7)
    usys(MC statistics, 2.68%)
    sys(jet energy scale, 1.02%)
    sys(jet energy resolution, 2.73%)
    sys(b-fractions, 0.36%)
    sys(c-fractions, 0.94%)
    sys(track multiplicity,19.98%)
    sys(long lived particles,-0.32%)
    sys(fake tracks,-0.19%)
    sys(photon conversions, 0.18%)
    sys(material interactions, 0.63%)
    sys(trigger bias,-3.06%)
    sys(negative tag epsilon b, 1.24%)
    sys(negative tag epsilon c, 3.25%)
    sys(negative tag run period dependence, 1.59%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.2041,0.0102)
    meta_data(N jets tagged,  13522,116.3)
    meta_data(N jets total, 373590,611.2)
    usys(MC statistics, 1.51%)
    sys(jet energy scale, 0.39%)
    sys(jet energy resolution, 0.53%)
    sys(b-fractions, 0.48%)
    sys(c-fractions, 1.45%)
    sys(track multiplicity, 7.35%)
    sys(long lived particles,-0.15%)
    sys(fake tracks,-0.27%)
    sys(photon conversions, 0.65%)
    sys(material interactions, 1.19%)
    sys(trigger bias,-3.06%)
    sys(negative tag epsilon b, 1.44%)
    sys(negative tag epsilon c, 4.01%)
    sys(negative tag run period dependence, 0.84%)
    sys(d0 and z0 smearing,-2.04%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.1688,0.0052)
    meta_data(N jets tagged,  48246,219.6)
    meta_data(N jets total, 971738,985.8)
    usys(MC statistics, 0.41%)
    sys(jet energy scale, 0.43%)
    sys(jet energy resolution, 0.17%)
    sys(b-fractions, 1.00%)
    sys(c-fractions, 3.21%)
    sys(track multiplicity, 8.25%)
    sys(long lived particles,-0.28%)
    sys(fake tracks, 0.35%)
    sys(photon conversions, 0.62%)
    sys(material interactions, 1.57%)
    sys(trigger bias,-2.12%)
    sys(negative tag epsilon b, 2.64%)
    sys(negative tag epsilon c, 6.56%)
    sys(negative tag run period dependence, 0.44%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.2706,0.0033)
    meta_data(N jets tagged, 136621,369.6)
    meta_data(N jets total,2062213,1436.0)
    usys(MC statistics, 0.49%)
    sys(jet energy scale,-0.58%)
    sys(jet energy resolution, 0.08%)
    sys(b-fractions, 1.46%)
    sys(c-fractions, 4.35%)
    sys(track multiplicity, 4.28%)
    sys(long lived particles,-0.64%)
    sys(fake tracks, 1.44%)
    sys(photon conversions, 1.40%)
    sys(material interactions, 3.90%)
    sys(trigger bias,-2.55%)
    sys(negative tag epsilon b, 3.58%)
    sys(negative tag epsilon c, 8.14%)
    sys(negative tag run period dependence, 0.26%)
    sys(d0 and z0 smearing, 5.59%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.3136,0.0030)
    meta_data(N jets tagged, 178117,422.0)
    meta_data(N jets total,2121080,1456.4)
    usys(MC statistics, 0.39%)
    sys(jet energy scale, 0.47%)
    sys(jet energy resolution, 0.24%)
    sys(b-fractions, 1.41%)
    sys(c-fractions, 4.12%)
    sys(track multiplicity, 3.13%)
    sys(long lived particles,-0.52%)
    sys(fake tracks, 1.26%)
    sys(photon conversions, 0.98%)
    sys(material interactions, 2.46%)
    sys(trigger bias,-0.87%)
    sys(negative tag epsilon b, 3.37%)
    sys(negative tag epsilon c, 7.92%)
    sys(negative tag run period dependence, 0.23%)
    sys(d0 and z0 smearing,-3.34%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2008,0.0018)
    meta_data(N jets tagged, 392335,626.4)
    meta_data(N jets total,4671649,2161.4)
    usys(MC statistics, 0.45%)
    sys(jet energy scale, 0.53%)
    sys(jet energy resolution, 0.38%)
    sys(b-fractions, 1.15%)
    sys(c-fractions, 3.58%)
    sys(track multiplicity, 5.42%)
    sys(long lived particles,-0.36%)
    sys(fake tracks, 0.71%)
    sys(photon conversions, 0.49%)
    sys(material interactions, 1.09%)
    sys(trigger bias,-0.76%)
    sys(negative tag epsilon b, 3.18%)
    sys(negative tag epsilon c, 7.72%)
    sys(negative tag run period dependence, 0.15%)
    sys(d0 and z0 smearing,-3.34%)
  }
}
Analysis(negative_tags,light,MV1, 0.3511, AntiKt4TopoLCnoJVF){ 
  meta_data_s (Hadronization, Pythia8) 
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.2387,0.0236)
    meta_data(N jets tagged,   2673, 51.7)
    meta_data(N jets total,  79624,282.2)
    usys(MC statistics, 2.87%)
    sys(jet energy scale, 0.90%)
    sys(jet energy resolution, 2.90%)
    sys(b-fractions, 0.14%)
    sys(c-fractions, 0.61%)
    sys(track multiplicity,20.26%)
    sys(long lived particles,-0.29%)
    sys(fake tracks,-0.12%)
    sys(photon conversions, 0.27%)
    sys(material interactions, 0.51%)
    sys(trigger bias,-5.46%)
    sys(negative tag epsilon b, 0.73%)
    sys(negative tag epsilon c, 2.79%)
    sys(negative tag run period dependence, 1.91%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.1607,0.0129)
    meta_data(N jets tagged,   7922, 89.0)
    meta_data(N jets total, 283835,532.8)
    usys(MC statistics, 1.40%)
    sys(jet energy scale, 1.32%)
    sys(jet energy resolution, 1.92%)
    sys(b-fractions, 0.18%)
    sys(c-fractions, 0.83%)
    sys(track multiplicity, 9.30%)
    sys(long lived particles,-0.16%)
    sys(fake tracks,-0.34%)
    sys(photon conversions, 1.27%)
    sys(material interactions, 2.35%)
    sys(trigger bias,-5.46%)
    sys(negative tag epsilon b, 0.82%)
    sys(negative tag epsilon c, 3.15%)
    sys(negative tag run period dependence, 1.11%)
    sys(d0 and z0 smearing,-6.09%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.2005,0.0076)
    meta_data(N jets tagged,  24225,155.6)
    meta_data(N jets total, 657288,810.7)
    usys(MC statistics, 0.47%)
    sys(jet energy scale, 0.23%)
    sys(jet energy resolution, 0.28%)
    sys(b-fractions, 0.58%)
    sys(c-fractions, 1.98%)
    sys(track multiplicity,13.66%)
    sys(long lived particles,-0.23%)
    sys(fake tracks, 0.27%)
    sys(photon conversions, 1.33%)
    sys(material interactions, 2.86%)
    sys(trigger bias,-3.12%)
    sys(negative tag epsilon b, 1.69%)
    sys(negative tag epsilon c, 4.75%)
    sys(negative tag run period dependence, 0.63%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.3202,0.0058)
    meta_data(N jets tagged,  49765,223.1)
    meta_data(N jets total,1069126,1034.0)
    usys(MC statistics, 0.57%)
    sys(jet energy scale,-0.21%)
    sys(jet energy resolution,-1.47%)
    sys(b-fractions, 0.84%)
    sys(c-fractions, 2.65%)
    sys(track multiplicity, 7.82%)
    sys(long lived particles,-0.43%)
    sys(fake tracks, 1.01%)
    sys(photon conversions, 2.69%)
    sys(material interactions, 6.80%)
    sys(trigger bias,-3.98%)
    sys(negative tag epsilon b, 2.19%)
    sys(negative tag epsilon c, 5.60%)
    sys(negative tag run period dependence, 0.44%)
    sys(d0 and z0 smearing,-7.97%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.3989,0.0065)
    meta_data(N jets tagged,  43842,209.4)
    meta_data(N jets total, 683805,826.9)
    usys(MC statistics, 0.56%)
    sys(jet energy scale, 0.54%)
    sys(jet energy resolution,-0.40%)
    sys(b-fractions, 0.85%)
    sys(c-fractions, 2.57%)
    sys(track multiplicity, 5.19%)
    sys(long lived particles,-0.34%)
    sys(fake tracks, 1.17%)
    sys(photon conversions, 1.85%)
    sys(material interactions, 4.48%)
    sys(trigger bias,-2.73%)
    sys(negative tag epsilon b, 2.10%)
    sys(negative tag epsilon c, 5.47%)
    sys(negative tag run period dependence, 0.46%)
    sys(d0 and z0 smearing,-8.19%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3156,0.0046)
    meta_data(N jets tagged,  75773,275.3)
    meta_data(N jets total,1137598,1066.6)
    usys(MC statistics, 0.94%)
    sys(jet energy scale, 1.05%)
    sys(jet energy resolution, 0.17%)
    sys(b-fractions, 0.60%)
    sys(c-fractions, 2.08%)
    sys(track multiplicity, 7.56%)
    sys(long lived particles,-0.21%)
    sys(fake tracks, 0.61%)
    sys(photon conversions, 0.71%)
    sys(material interactions, 1.67%)
    sys(trigger bias,-3.51%)
    sys(negative tag epsilon b, 1.79%)
    sys(negative tag epsilon c, 5.12%)
    sys(negative tag run period dependence, 0.35%)
    sys(d0 and z0 smearing,-8.19%)
  }
}
