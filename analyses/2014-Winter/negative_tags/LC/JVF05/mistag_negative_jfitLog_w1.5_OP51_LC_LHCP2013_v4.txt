Analysis(negative_tags,light,JetFitterCharm,-0.9_0.95,AntiKt4TopoLCJVF0_5){
  meta_data_s (Hadronization, Pythia8EvtGen)
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.1876,0.0401)
    meta_data(N jets tagged,    816, 27.7)
    meta_data(N jets total,  67495,259.8)
    usys(MC statistics, 4.25%)
    sys(jet vertex fraction,-8.94%)
    sys(jet energy scale,-8.27%)
    sys(jet energy resolution,-0.19%)
    sys(b-fractions, 0.31%)
    sys(c-fractions, 0.80%)
    sys(track multiplicity, 3.80%)
    sys(long lived particles,-0.03%)
    sys(fake tracks, 0.04%)
    sys(photon conversions, 0.18%)
    sys(material interactions, 0.37%)
    sys(trigger bias,-1.27%)
    sys(negative tag epsilon b, 1.21%)
    sys(negative tag epsilon c, 3.20%)
    sys(negative tag run period dependence, 3.29%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3151,0.0261)
    meta_data(N jets tagged,   2707, 54.1)
    meta_data(N jets total, 279474,528.7)
    usys(MC statistics, 2.52%)
    sys(jet vertex fraction,-1.64%)
    sys(jet energy scale,-0.45%)
    sys(jet energy resolution,-3.30%)
    sys(b-fractions, 0.41%)
    sys(c-fractions, 2.24%)
    sys(track multiplicity, 2.14%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.85%)
    sys(photon conversions, 0.54%)
    sys(material interactions, 1.08%)
    sys(trigger bias, 1.75%)
    sys(negative tag epsilon b, 1.28%)
    sys(negative tag epsilon c, 4.99%)
    sys(negative tag run period dependence, 1.47%)
    sys(d0 and z0 smearing, 1.20%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.3154,0.0127)
    meta_data(N jets tagged,   8073, 78.5)
    meta_data(N jets total, 798382,893.5)
    usys(MC statistics, 1.61%)
    sys(jet vertex fraction,-0.46%)
    sys(jet energy scale, 0.33%)
    sys(jet energy resolution, 0.15%)
    sys(b-fractions, 1.17%)
    sys(c-fractions, 4.04%)
    sys(track multiplicity, 1.36%)
    sys(long lived particles,-0.35%)
    sys(fake tracks, 0.47%)
    sys(photon conversions, 0.19%)
    sys(material interactions, 0.87%)
    sys(trigger bias,-3.41%)
    sys(negative tag epsilon b, 3.00%)
    sys(negative tag epsilon c, 7.64%)
    sys(negative tag run period dependence, 2.20%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3959,0.0087)
    meta_data(N jets tagged,  17058,107.6)
    meta_data(N jets total,1680610,1296.4)
    usys(MC statistics, 1.83%)
    sys(jet vertex fraction,-0.10%)
    sys(jet energy scale, 0.12%)
    sys(jet energy resolution, 0.90%)
    sys(b-fractions, 2.23%)
    sys(c-fractions, 5.90%)
    sys(track multiplicity, 1.81%)
    sys(long lived particles,-1.14%)
    sys(fake tracks, 1.96%)
    sys(photon conversions, 1.36%)
    sys(material interactions, 3.16%)
    sys(trigger bias,-4.27%)
    sys(negative tag epsilon b, 4.92%)
    sys(negative tag epsilon c, 9.66%)
    sys(negative tag run period dependence, 3.46%)
    sys(d0 and z0 smearing, 2.73%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4807,0.0081)
    meta_data(N jets tagged,  25576,141.0)
    meta_data(N jets total,1712041,1308.4)
    usys(MC statistics, 2.03%)
    sys(jet vertex fraction,-0.05%)
    sys(jet energy scale, 0.43%)
    sys(jet energy resolution,-0.12%)
    sys(b-fractions, 2.62%)
    sys(c-fractions, 6.04%)
    sys(track multiplicity, 1.79%)
    sys(long lived particles,-0.76%)
    sys(fake tracks, 2.62%)
    sys(photon conversions, 1.29%)
    sys(material interactions, 1.68%)
    sys(trigger bias,-0.85%)
    sys(negative tag epsilon b, 5.60%)
    sys(negative tag epsilon c, 9.83%)
    sys(negative tag run period dependence, 6.17%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.3447,0.0051)
    meta_data(N jets tagged,  65870,251.4)
    meta_data(N jets total,3472572,1863.5)
    usys(MC statistics, 1.95%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale, 1.46%)
    sys(jet energy resolution, 1.43%)
    sys(b-fractions, 2.25%)
    sys(c-fractions, 5.22%)
    sys(track multiplicity, 3.02%)
    sys(long lived particles,-0.45%)
    sys(fake tracks, 0.82%)
    sys(photon conversions, 0.55%)
    sys(material interactions, 0.66%)
    sys(trigger bias,-1.97%)
    sys(negative tag epsilon b, 5.59%)
    sys(negative tag epsilon c, 9.61%)
    sys(negative tag run period dependence, 1.46%)
    sys(d0 and z0 smearing, 0.99%)
  }
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1284,0.0494)
    meta_data(N jets tagged,    681, 30.0)
    meta_data(N jets total,  56373,237.4)
    usys(MC statistics, 4.74%)
    sys(jet vertex fraction,-3.22%)
    sys(jet energy scale,-5.92%)
    sys(jet energy resolution,-3.18%)
    sys(b-fractions, 0.11%)
    sys(c-fractions,-0.03%)
    sys(track multiplicity, 5.67%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.13%)
    sys(photon conversions, 0.40%)
    sys(material interactions, 0.57%)
    sys(trigger bias,-0.41%)
    sys(negative tag epsilon b, 0.71%)
    sys(negative tag epsilon c, 2.10%)
    sys(negative tag run period dependence, 5.43%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.2608,0.0357)
    meta_data(N jets tagged,   1752, 49.8)
    meta_data(N jets total, 213460,462.0)
    usys(MC statistics, 3.15%)
    sys(jet vertex fraction, 3.02%)
    sys(jet energy scale,-1.67%)
    sys(jet energy resolution, 5.16%)
    sys(b-fractions, 0.26%)
    sys(c-fractions, 1.34%)
    sys(track multiplicity, 3.68%)
    sys(long lived particles,-0.06%)
    sys(fake tracks, 0.62%)
    sys(photon conversions, 0.79%)
    sys(material interactions, 1.60%)
    sys(trigger bias,-3.35%)
    sys(negative tag epsilon b, 0.97%)
    sys(negative tag epsilon c, 3.78%)
    sys(negative tag run period dependence, 3.15%)
    sys(d0 and z0 smearing, 0.07%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3871,0.0222)
    meta_data(N jets tagged,   4548, 73.0)
    meta_data(N jets total, 536506,732.5)
    usys(MC statistics, 2.05%)
    sys(jet vertex fraction,-0.38%)
    sys(jet energy scale,-0.74%)
    sys(jet energy resolution, 1.42%)
    sys(b-fractions, 0.87%)
    sys(c-fractions, 2.41%)
    sys(track multiplicity, 3.29%)
    sys(long lived particles,-0.22%)
    sys(fake tracks, 0.13%)
    sys(photon conversions, 0.61%)
    sys(material interactions, 1.48%)
    sys(trigger bias,-5.84%)
    sys(negative tag epsilon b, 2.32%)
    sys(negative tag epsilon c, 5.27%)
    sys(negative tag run period dependence, 1.19%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.5069,0.0190)
    meta_data(N jets tagged,   8411,106.7)
    meta_data(N jets total, 872175,933.9)
    usys(MC statistics, 3.00%)
    sys(jet vertex fraction, 0.13%)
    sys(jet energy scale,-2.58%)
    sys(jet energy resolution,-2.65%)
    sys(b-fractions, 1.57%)
    sys(c-fractions, 3.88%)
    sys(track multiplicity, 4.28%)
    sys(long lived particles,-0.82%)
    sys(fake tracks, 0.97%)
    sys(photon conversions, 1.87%)
    sys(material interactions, 3.13%)
    sys(trigger bias,-3.87%)
    sys(negative tag epsilon b, 3.53%)
    sys(negative tag epsilon c, 6.96%)
    sys(negative tag run period dependence, 4.38%)
    sys(d0 and z0 smearing,-1.64%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.6460,0.0219)
    meta_data(N jets tagged,   8753,117.1)
    meta_data(N jets total, 547574,740.0)
    usys(MC statistics, 3.27%)
    sys(jet vertex fraction, 0.02%)
    sys(jet energy scale, 0.88%)
    sys(jet energy resolution, 0.21%)
    sys(b-fractions, 1.93%)
    sys(c-fractions, 3.75%)
    sys(track multiplicity, 4.18%)
    sys(long lived particles, 0.05%)
    sys(fake tracks, 2.03%)
    sys(photon conversions, 2.04%)
    sys(material interactions, 2.87%)
    sys(trigger bias,-2.59%)
    sys(negative tag epsilon b, 4.22%)
    sys(negative tag epsilon c, 6.61%)
    sys(negative tag run period dependence, 6.78%)
    sys(d0 and z0 smearing,-5.53%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.6186,0.0160)
    meta_data(N jets tagged,  19477,193.2)
    meta_data(N jets total, 872061,933.8)
    usys(MC statistics, 3.38%)
    sys(jet vertex fraction,-1.31%)
    sys(jet energy scale,-6.32%)
    sys(jet energy resolution,-10.31%)
    sys(b-fractions, 1.34%)
    sys(c-fractions, 3.58%)
    sys(track multiplicity, 5.09%)
    sys(long lived particles, 0.03%)
    sys(fake tracks, 1.30%)
    sys(photon conversions, 1.17%)
    sys(material interactions, 1.70%)
    sys(trigger bias,-0.63%)
    sys(negative tag epsilon b, 3.38%)
    sys(negative tag epsilon c, 7.07%)
    sys(negative tag run period dependence, 3.43%)
    sys(d0 and z0 smearing,-5.53%)
  }
}
