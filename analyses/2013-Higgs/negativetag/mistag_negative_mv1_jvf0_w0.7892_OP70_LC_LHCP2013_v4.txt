Analysis(negative_tags,light,MV1,0.7892,AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 0.0<abseta<1.2)
  {
    central_value(1.2044,0.0526)
    meta_data(N jets tagged,    522, 22.8)
    meta_data(N jets total,  98387,313.7)
    usys(MC statistics, 5.62%)
    sys(jet energy scale, 1.40%)
    sys(jet energy resolution,-0.74%)
    sys(b-fractions, 0.88%)
    sys(c-fractions, 0.71%)
    sys(track multiplicity,28.66%)
    sys(long lived particles,-0.20%)
    sys(fake tracks, 0.02%)
    sys(photon conversions, 0.06%)
    sys(material interactions, 0.29%)
    sys(trigger bias,-4.72%)
    sys(negative tag epsilon b, 2.48%)
    sys(negative tag epsilon c, 2.87%)
    sys(negative tag run period dependence, 4.38%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 30<pt< 60, 0.0<abseta<1.2)
  {
    central_value(1.3160,0.0276)
    meta_data(N jets tagged,   2261, 47.5)
    meta_data(N jets total, 373590,611.2)
    usys(MC statistics, 2.66%)
    sys(jet energy scale, 3.92%)
    sys(jet energy resolution,-0.34%)
    sys(b-fractions, 0.47%)
    sys(c-fractions, 2.20%)
    sys(track multiplicity,11.11%)
    sys(long lived particles,-0.21%)
    sys(fake tracks,-0.89%)
    sys(photon conversions, 0.56%)
    sys(material interactions, 1.21%)
    sys(trigger bias,-4.72%)
    sys(negative tag epsilon b, 1.43%)
    sys(negative tag epsilon c, 4.90%)
    sys(negative tag run period dependence, 2.10%)
    sys(d0 and z0 smearing,-2.89%)
  }
  bin( 60<pt<140, 0.0<abseta<1.2)
  {
    central_value(1.2248,0.0126)
    meta_data(N jets tagged,   9378, 96.8)
    meta_data(N jets total, 971738,985.8)
    usys(MC statistics, 1.52%)
    sys(jet energy scale,-0.19%)
    sys(jet energy resolution, 2.23%)
    sys(b-fractions, 1.67%)
    sys(c-fractions, 4.40%)
    sys(track multiplicity,10.98%)
    sys(long lived particles,-0.41%)
    sys(fake tracks, 0.57%)
    sys(photon conversions, 0.57%)
    sys(material interactions, 1.79%)
    sys(trigger bias,-2.92%)
    sys(negative tag epsilon b, 4.01%)
    sys(negative tag epsilon c, 7.98%)
    sys(negative tag run period dependence, 1.03%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(140<pt<300, 0.0<abseta<1.2)
  {
    central_value(1.3881,0.0083)
    meta_data(N jets tagged,  27739,166.6)
    meta_data(N jets total,2062213,1436.0)
    usys(MC statistics, 1.78%)
    sys(jet energy scale, 1.00%)
    sys(jet energy resolution, 1.39%)
    sys(b-fractions, 2.67%)
    sys(c-fractions, 5.73%)
    sys(track multiplicity, 5.26%)
    sys(long lived particles,-1.13%)
    sys(fake tracks, 2.82%)
    sys(photon conversions, 1.75%)
    sys(material interactions, 5.76%)
    sys(trigger bias,-6.50%)
    sys(negative tag epsilon b, 5.82%)
    sys(negative tag epsilon c, 9.33%)
    sys(negative tag run period dependence, 0.60%)
    sys(d0 and z0 smearing, 5.80%)
  }
  bin(300<pt<450, 0.0<abseta<1.2)
  {
    central_value(1.4735,0.0076)
    meta_data(N jets tagged,  36726,191.6)
    meta_data(N jets total,2121080,1456.4)
    usys(MC statistics, 1.81%)
    sys(jet energy scale, 0.51%)
    sys(jet energy resolution,-0.64%)
    sys(b-fractions, 2.94%)
    sys(c-fractions, 5.47%)
    sys(track multiplicity, 3.79%)
    sys(long lived particles,-1.04%)
    sys(fake tracks, 2.53%)
    sys(photon conversions, 1.60%)
    sys(material interactions, 4.18%)
    sys(trigger bias,-1.00%)
    sys(negative tag epsilon b, 6.24%)
    sys(negative tag epsilon c, 9.02%)
    sys(negative tag run period dependence, 0.52%)
    sys(d0 and z0 smearing,-3.57%)
  }
  bin(450<pt<750, 0.0<abseta<1.2)
  {
    central_value(1.2521,0.0045)
    meta_data(N jets tagged,  75734,275.2)
    meta_data(N jets total,4671649,2161.4)
    usys(MC statistics, 1.65%)
    sys(jet energy scale, 0.61%)
    sys(jet energy resolution,-2.88%)
    sys(b-fractions, 2.49%)
    sys(c-fractions, 5.07%)
    sys(track multiplicity, 6.05%)
    sys(long lived particles,-0.62%)
    sys(fake tracks, 1.08%)
    sys(photon conversions, 1.02%)
    sys(material interactions, 1.72%)
    sys(trigger bias, 0.44%)
    sys(negative tag epsilon b, 6.20%)
    sys(negative tag epsilon c, 9.31%)
    sys(negative tag run period dependence, 0.36%)
    sys(d0 and z0 smearing,-3.57%)
  }
}
Analysis(negative_tags,light,MV1,0.7892,AntiKt4TopoLCnoJVF){ 
  bin( 20<pt< 30, 1.2<abseta<2.5)
  {
    central_value(1.1182,0.0655)
    meta_data(N jets tagged,    290, 17.0)
    meta_data(N jets total,  79624,282.2)
    usys(MC statistics, 6.99%)
    sys(jet energy scale, 1.98%)
    sys(jet energy resolution,11.45%)
    sys(b-fractions, 0.12%)
    sys(c-fractions, 0.64%)
    sys(track multiplicity,37.70%)
    sys(long lived particles,-0.44%)
    sys(fake tracks, 0.37%)
    sys(photon conversions, 0.03%)
    sys(material interactions,-0.10%)
    sys(trigger bias,-4.64%)
    sys(negative tag epsilon b, 0.71%)
    sys(negative tag epsilon c, 2.84%)
    sys(negative tag run period dependence, 5.96%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 30<pt< 60, 1.2<abseta<2.5)
  {
    central_value(1.3453,0.0369)
    meta_data(N jets tagged,   1320, 36.3)
    meta_data(N jets total, 283835,532.8)
    usys(MC statistics, 3.21%)
    sys(jet energy scale,-2.08%)
    sys(jet energy resolution, 1.72%)
    sys(b-fractions, 0.21%)
    sys(c-fractions, 1.37%)
    sys(track multiplicity,14.25%)
    sys(long lived particles,-0.14%)
    sys(fake tracks,-0.38%)
    sys(photon conversions, 1.29%)
    sys(material interactions, 1.91%)
    sys(trigger bias,-4.64%)
    sys(negative tag epsilon b, 0.87%)
    sys(negative tag epsilon c, 3.80%)
    sys(negative tag run period dependence, 2.75%)
    sys(d0 and z0 smearing,-10.63%)
  }
  bin( 60<pt<140, 1.2<abseta<2.5)
  {
    central_value(1.3319,0.0201)
    meta_data(N jets tagged,   4370, 66.1)
    meta_data(N jets total, 657288,810.7)
    usys(MC statistics, 1.79%)
    sys(jet energy scale,-0.49%)
    sys(jet energy resolution,-0.62%)
    sys(b-fractions, 0.94%)
    sys(c-fractions, 2.73%)
    sys(track multiplicity,16.15%)
    sys(long lived particles,-0.27%)
    sys(fake tracks, 0.05%)
    sys(photon conversions, 1.17%)
    sys(material interactions, 2.69%)
    sys(trigger bias,-4.38%)
    sys(negative tag epsilon b, 2.46%)
    sys(negative tag epsilon c, 5.67%)
    sys(negative tag run period dependence, 1.51%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(140<pt<300, 1.2<abseta<2.5)
  {
    central_value(1.4795,0.0159)
    meta_data(N jets tagged,   8625, 92.9)
    meta_data(N jets total,1069126,1034.0)
    usys(MC statistics, 1.79%)
    sys(jet energy scale,-1.03%)
    sys(jet energy resolution, 2.91%)
    sys(b-fractions, 1.44%)
    sys(c-fractions, 3.14%)
    sys(track multiplicity, 8.26%)
    sys(long lived particles,-0.80%)
    sys(fake tracks, 0.70%)
    sys(photon conversions, 3.56%)
    sys(material interactions, 7.95%)
    sys(trigger bias,-2.97%)
    sys(negative tag epsilon b, 3.21%)
    sys(negative tag epsilon c, 6.00%)
    sys(negative tag run period dependence, 1.07%)
    sys(d0 and z0 smearing,-9.99%)
  }
  bin(300<pt<450, 1.2<abseta<2.5)
  {
    central_value(1.5315,0.0176)
    meta_data(N jets tagged,   7506, 86.6)
    meta_data(N jets total, 683805,826.9)
    usys(MC statistics, 2.63%)
    sys(jet energy scale, 1.11%)
    sys(jet energy resolution, 0.57%)
    sys(b-fractions, 1.48%)
    sys(c-fractions, 2.95%)
    sys(track multiplicity, 4.93%)
    sys(long lived particles,-0.19%)
    sys(fake tracks, 1.95%)
    sys(photon conversions, 3.05%)
    sys(material interactions, 6.65%)
    sys(trigger bias,-1.56%)
    sys(negative tag epsilon b, 3.35%)
    sys(negative tag epsilon c, 5.75%)
    sys(negative tag run period dependence, 1.15%)
    sys(d0 and z0 smearing,-22.13%)
  }
  bin(450<pt<750, 1.2<abseta<2.5)
  {
    central_value(1.3969,0.0122)
    meta_data(N jets tagged,  12906,113.6)
    meta_data(N jets total,1137598,1066.6)
    usys(MC statistics, 4.87%)
    sys(jet energy scale,-1.67%)
    sys(jet energy resolution,-5.57%)
    sys(b-fractions, 1.40%)
    sys(c-fractions, 2.16%)
    sys(track multiplicity, 8.86%)
    sys(long lived particles,-0.53%)
    sys(fake tracks, 0.49%)
    sys(photon conversions, 0.53%)
    sys(material interactions, 2.89%)
    sys(trigger bias,-5.92%)
    sys(negative tag epsilon b, 3.42%)
    sys(negative tag epsilon c, 5.03%)
    sys(negative tag run period dependence, 0.88%)
    sys(d0 and z0 smearing,-22.13%)
  }
}
